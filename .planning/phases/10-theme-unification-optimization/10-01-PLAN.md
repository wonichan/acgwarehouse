---
phase: 10-theme-unification-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/lib/theme/app_colors.dart
  - flutter_app/lib/theme/app_theme.dart
  - flutter_app/lib/providers/theme_provider.dart
autonomous: true
requirements:
  - CROSS-01
  - CROSS-02
must_haves:
  truths:
    - 集中式颜色定义存在于 app_colors.dart
    - ThemeProvider 管理主题模式状态（浅色/深色/跟随系统）
    - 主题配置可被 Windows 和 Android 共享
    - 主题变更通过 Provider 通知机制实时生效
  artifacts:
    - path: flutter_app/lib/theme/app_colors.dart
      provides: 统一颜色定义（seedColor 0xFFED79B5 及衍生色）
      exports: [AppColors]
    - path: flutter_app/lib/theme/app_theme.dart
      provides: 主题数据生成（FluentThemeData 和 ThemeData）
      exports: [AppTheme]
    - path: flutter_app/lib/providers/theme_provider.dart
      provides: 主题状态管理
      exports: [ThemeProvider, ThemeMode]
  key_links:
    - from: ThemeProvider
      to: app_theme.dart
      via: 读取当前主题模式生成对应主题数据
    - from: app_theme.dart
      to: app_colors.dart
      via: 使用统一颜色定义生成主题
---

<objective>
创建统一的主题基础设施：颜色定义、主题数据生成和状态管理 Provider。
这是 Phase 10 的基础，所有后续计划都依赖此计划创建的文件。

Purpose: 建立集中式主题配置，确保双平台颜色一致，支持主题切换
Output: app_colors.dart, app_theme.dart, theme_provider.dart
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-theme-unification-optimization/10-CONTEXT.md
@flutter_app/lib/main.dart
@flutter_app/lib/providers/navigation_provider.dart

## 设计决策（来自 CONTEXT.md）

**主色调**: 0xFFED79B5 (柔和粉紫/桃红色)
**配色生成**: Material 3 自动生成完整颜色系统
**Fluent UI 配色**: 映射 Material 3 颜色到 FluentThemeData
**暗色主题**: 深色粉紫变体，更低饱和度减少夜间疲劳
**代码组织**: 集中式主题配置（lib/theme/app_theme.dart）
**状态管理**: Provider + ChangeNotifier 模式（参照 NavigationProvider）
</context>

<tasks>

<task type="auto">
  <name>task 1: 创建统一颜色定义 app_colors.dart</name>
  <files>flutter_app/lib/theme/app_colors.dart</files>
  <action>
创建 lib/theme/app_colors.dart，定义统一的颜色系统：

1. **主色常量**:
   - seedColor = Color(0xFFED79B5) // 柔和粉紫
   - 基于 seedColor 预计算的主要颜色（避免运行时计算）

2. **浅色主题颜色**:
   - primary, primaryContainer, secondary, secondaryContainer
   - surface, surfaceVariant, background
   - onPrimary, onSurface 等对比色
   - 使用 Material 3 配色算法预计算的值

3. **深色主题颜色**:
   - 对应浅色主题的深色版本
   - 使用更深沉、饱和度略低的粉紫色调
   - 确保夜间阅读舒适度

4. **工具方法**:
   - 根据 Brightness 获取对应颜色集的方法

注意：不要直接使用 Material 的 ColorScheme.fromSeed（在 app_theme.dart 中使用），
这里只定义常量供两个平台引用。
  </action>
  <verify>
    <automated>test -f flutter_app/lib/theme/app_colors.dart && grep -q "0xFFED79B5" flutter_app/lib/theme/app_colors.dart</automated>
  </verify>
  <done>文件存在且包含 seedColor 定义，包含浅色和深色两套颜色常量</done>
</task>

<task type="auto">
  <name>task 2: 创建主题数据生成 app_theme.dart</name>
  <files>flutter_app/lib/theme/app_theme.dart</files>
  <action>
创建 lib/theme/app_theme.dart，提供双平台的主题数据生成：

1. **导入**:
   - material.dart, fluent_ui.dart
   - app_colors.dart

2. **Material 主题生成**:
   - `ThemeData getMaterialTheme(Brightness brightness)`:
     - 使用 ColorScheme.fromSeed(seedColor: AppColors.seedColor)
     - brightness 参数控制明暗
     - useMaterial3: true
     - 配置 cardTheme, appBarTheme 等组件主题

3. **Fluent 主题生成**:
   - `FluentThemeData getFluentTheme(Brightness brightness)`:
     - accentColor: 映射 Material 3 primary 到 Fluent accent
     - 根据 brightness 设置 brightness 属性
     - 配置 scaffoldBackgroundColor 等 Fluent 特有属性
     - 确保颜色与 Material 主题协调一致

4. **亮度工具**:
   - `Brightness getBrightness(ThemeMode mode, BuildContext context)`:
     - 处理 ThemeMode.system -> 实际 Brightness 转换
     - 使用 MediaQuery.platformBrightnessOf(context)

确保两个平台使用从同一 seedColor 生成的颜色系统，视觉一致。
  </action>
  <verify>
    <automated>test -f flutter_app/lib/theme/app_theme.dart && grep -q "getMaterialTheme" flutter_app/lib/theme/app_theme.dart && grep -q "getFluentTheme" flutter_app/lib/theme/app_theme.dart</automated>
  </verify>
  <done>文件存在且包含 Material 和 Fluent 主题生成方法，使用 AppColors.seedColor</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 创建 ThemeProvider 状态管理</name>
  <files>flutter_app/lib/providers/theme_provider.dart</files>
  <behavior>
    - Test 1: ThemeProvider 初始化时默认模式为 ThemeMode.system
    - Test 2: setThemeMode(ThemeMode.light) 会更新 _themeMode 并通知监听器
    - Test 3: setThemeMode(ThemeMode.dark) 会更新 _themeMode 并通知监听器
    - Test 4: 连续设置相同模式不会重复通知
    - Test 5: themeMode getter 返回当前模式
  </behavior>
  <action>
创建 lib/providers/theme_provider.dart，管理主题模式状态：

1. **导入**:
   - flutter/foundation.dart (ChangeNotifier)
   - flutter/material.dart (ThemeMode)

2. **ThemeProvider 类**:
   ```dart
   class ThemeProvider extends ChangeNotifier {
     ThemeMode _themeMode = ThemeMode.system;
     
     ThemeMode get themeMode => _themeMode;
     
     void setThemeMode(ThemeMode mode) {
       if (_themeMode != mode) {
         _themeMode = mode;
         notifyListeners();
       }
     }
     
     bool get isSystemMode => _themeMode == ThemeMode.system;
     bool get isLightMode => _themeMode == ThemeMode.light;
     bool get isDarkMode => _themeMode == ThemeMode.dark;
   }
   ```

3. **持久化准备（注释说明）**:
   - 添加注释说明 Phase 10-04 将实现 shared_preferences 持久化
   - 当前只实现内存状态管理

参照 NavigationProvider 的模式（ChangeNotifier + notifyListeners），
确保与现有 Provider 体系一致。
  </action>
  <verify>
    <automated>cd flutter_app && flutter test test/providers/theme_provider_test.dart 2>/dev/null || echo "Test file will be created in 10-04"</automated>
  </verify>
  <done>ThemeProvider 类存在，继承 ChangeNotifier，包含 themeMode 状态和 setThemeMode 方法</done>
</task>

</tasks>

<verification>
- [ ] lib/theme/app_colors.dart 存在且定义 seedColor
- [ ] lib/theme/app_theme.dart 存在且提供 getMaterialTheme 和 getFluentTheme
- [ ] lib/providers/theme_provider.dart 存在且继承 ChangeNotifier
- [ ] 三个文件可以互相导入无循环依赖
</verification>

<success_criteria>
1. 统一颜色定义文件可被 Material 和 Fluent 主题引用
2. ThemeProvider 可注入到 main.dart 的 MultiProvider
3. 主题切换逻辑可在 Provider 层工作（UI 在 10-04 实现）
</success_criteria>

<output>
After completion, create `.planning/phases/10-theme-unification-optimization/10-01-SUMMARY.md`
</output>
