---
phase: 10-theme-unification-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - flutter_app/lib/app/material_app_shell.dart
  - flutter_app/lib/main.dart
autonomous: true
requirements:
  - ANDROID-04
  - CROSS-01
must_haves:
  truths:
    - MaterialApp 使用粉紫配色方案（ColorScheme.fromSeed 0xFFED79B5）
    - Android 状态栏、导航栏、AppBar 显示粉紫主题色
    - 所有 Material 3 组件（卡片、按钮、FAB）使用统一配色
    - 暗色模式下使用深色粉紫变体，减少视觉疲劳
  artifacts:
    - path: flutter_app/lib/app/material_app_shell.dart
      provides: 应用 Material 主题到 Scaffold 和导航组件
      modifies: [背景色, 导航栏颜色]
    - path: flutter_app/lib/main.dart
      provides: MaterialApp 主题绑定到 ThemeProvider
      modifies: [_buildMaterialApp 函数]
  key_links:
    - from: main.dart _buildMaterialApp
      to: ThemeProvider
      via: Consumer<ThemeProvider> 监听主题变化
    - from: ThemeProvider
      to: app_theme.dart getMaterialTheme
      via: 读取当前模式获取 ThemeData
    - from: material_app_shell.dart
      to: Theme
      via: Scaffold 使用主题背景色和组件颜色
---

<objective>
将粉紫配色应用到 Android Material 3 UI，确保与 Windows 端视觉一致。

Purpose: 实现 Android 端的二次元粉紫主题，支持 Material 3 动态配色
Output: 更新的 material_app_shell.dart 和 main.dart，粉紫配色的 Material UI
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-theme-unification-optimization/10-CONTEXT.md
@flutter_app/lib/main.dart
@flutter_app/lib/app/material_app_shell.dart
@flutter_app/lib/theme/app_theme.dart
@flutter_app/lib/providers/theme_provider.dart

## 当前状态

main.dart _buildMaterialApp() 当前使用硬编码蓝色种子色：
```dart
theme: ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
  useMaterial3: true,
),
```

需要改为使用 app_theme.dart 生成的粉紫主题，并支持暗色模式。

## Material 3 特性

- ColorScheme.fromSeed 自动生成完整配色系统
- useMaterial3: true 启用 Material 3 组件样式
- 支持动态计算 onPrimary, surface 等对比色
</context>

<tasks>

<task type="auto">
  <name>task 1: 更新 main.dart MaterialApp 主题绑定</name>
  <files>flutter_app/lib/main.dart</files>
  <action>
更新 main.dart 中的 _buildMaterialApp，使用 ThemeProvider 和粉紫主题：

1. **修改 _buildMaterialApp**（已在 10-02 中修改，此处补充 Material 特定配置）：
   ```dart
   Widget _buildMaterialApp() {
     return Consumer<ThemeProvider>(
       builder: (context, themeProvider, child) {
         final brightness = getBrightness(themeProvider.themeMode, context);
         final themeData = getMaterialTheme(brightness);
         
         return MaterialApp(
           title: 'ACGWarehouse',
           theme: themeData,
           // MaterialApp 不直接支持 darkTheme 因为我们使用 brightness 控制
           // 主题数据已通过 getMaterialTheme 根据 brightness 生成
           home: const MaterialAppShell(),
         );
       },
     );
   }
   ```

2. **确保导入**:
   ```dart
   import 'providers/theme_provider.dart';
   import 'theme/app_theme.dart';
   ```

3. **与 10-02 的修改协同**:
   - 如果 10-02 已完成，确保 _buildMaterialApp 也使用 Consumer 模式
   - 两个构建函数保持一致的 Consumer<ThemeProvider> 模式

注意：MaterialApp 的 theme 属性会根据 brightness 自动应用，
不需要单独的 darkTheme（因为我们通过 getMaterialTheme 传入 brightness 参数）。
  </action>
  <verify>
    <automated>grep -q "Consumer<ThemeProvider>" flutter_app/lib/main.dart && grep -q "getMaterialTheme" flutter_app/lib/main.dart</automated>
  </verify>
  <done>_buildMaterialApp 使用 Consumer<ThemeProvider> 和 getMaterialTheme，与 _buildFluentApp 一致</done>
</task>

<task type="auto">
  <name>task 2: 优化 MaterialAppShell 主题应用</name>
  <files>flutter_app/lib/app/material_app_shell.dart</files>
  <action>
更新 material_app_shell.dart 确保正确应用 Material 主题：

1. **检查 Scaffold 背景色**:
   - 当前 Scaffold 使用默认背景色，会自动响应 Theme
   - 确保没有硬编码颜色

2. **添加 Material 3 组件主题配置**（可选）:
   ```dart
   // 如果需要自定义特定组件
   // 例如 NavigationBar 的背景色、选中色
   ```

3. **确保 NavigationBar/Rail 响应主题**:
   - AdaptiveNavigationBar 和 AdaptiveNavigationRail 会自动使用 Theme
   - 选中项会显示主题色（粉紫色）

4. **添加暗色模式支持标记**:
   ```dart
   /// MaterialApp Shell - Android/Web 平台
   /// 
   /// Supports light/dark theme switching via ThemeProvider.
   /// Theme is automatically applied from getMaterialTheme().
   ```

主要修改是确保所有组件继承主题，不需要大幅重构。
  </action>
  <verify>
    <automated>grep -q "MaterialApp Shell" flutter_app/lib/app/material_app_shell.dart</automated>
  </verify>
  <done>MaterialAppShell 文档更新，组件正确响应 Material 3 主题</done>
</task>

<task type="auto">
  <name>task 3: 完善 Material 3 主题配置</name>
  <files>flutter_app/lib/theme/app_theme.dart</files>
  <action>
确保 getMaterialTheme 生成完整的 Material 3 主题：

1. **检查现有实现**:
   - ColorScheme.fromSeed(seedColor: AppColors.seedColor, brightness: brightness)
   - useMaterial3: true

2. **添加组件主题配置**:
   ```dart
   ThemeData getMaterialTheme(Brightness brightness) {
     final colorScheme = ColorScheme.fromSeed(
       seedColor: AppColors.seedColor,
       brightness: brightness,
     );
     
     return ThemeData(
       colorScheme: colorScheme,
       useMaterial3: true,
       // AppBar 主题
       appBarTheme: AppBarTheme(
         backgroundColor: colorScheme.surface,
         foregroundColor: colorScheme.onSurface,
       ),
       // 卡片主题
       cardTheme: CardTheme(
         color: colorScheme.surfaceContainerHighest,
         elevation: 0,
       ),
       // 导航栏主题
       navigationBarTheme: NavigationBarThemeData(
         backgroundColor: colorScheme.surface,
         indicatorColor: colorScheme.primaryContainer,
       ),
       // FAB 主题
       floatingActionButtonTheme: FloatingActionButtonThemeData(
         backgroundColor: colorScheme.primaryContainer,
         foregroundColor: colorScheme.onPrimaryContainer,
       ),
       // 输入框主题
       inputDecorationTheme: InputDecorationTheme(
         filled: true,
         fillColor: colorScheme.surfaceContainerHighest,
       ),
     );
   }
   ```

3. **确保与 AppColors 一致**:
   - seedColor 使用 AppColors.seedColor
   - 暗色模式亮度正确传递

4. **验证 ColorScheme 生成**:
   - primary 和 secondary 应基于 seedColor 自动生成
   - surface 和 background 明暗适配

完整的主题配置确保 Android UI 的视觉一致性和 Material 3 规范。
  </action>
  <verify>
    <automated>grep -q "ColorScheme.fromSeed" flutter_app/lib/theme/app_theme.dart && grep -q "useMaterial3: true" flutter_app/lib/theme/app_theme.dart && grep -q "AppBarTheme\|cardTheme\|navigationBarTheme" flutter_app/lib/theme/app_theme.dart</automated>
  </verify>
  <done>Material 3 主题配置完整，包含 ColorScheme.fromSeed 和组件主题</done>
</task>

</tasks>

<verification>
- [ ] main.dart _buildMaterialApp 使用 Consumer<ThemeProvider>
- [ ] getMaterialTheme 生成完整 Material 3 主题
- [ ] MaterialAppShell 组件正确响应主题
- [ ] AppBar、NavigationBar、卡片显示粉紫配色
</verification>

<success_criteria>
1. Android 应用显示粉紫色 AppBar 和导航栏
2. 选中项、按钮、FAB 使用统一的粉紫配色
3. 暗色模式下颜色深沉柔和，不刺眼
4. 代码无分析警告或错误
</success_criteria>

<output>
After completion, create `.planning/phases/10-theme-unification-optimization/10-03-SUMMARY.md`
</output>
