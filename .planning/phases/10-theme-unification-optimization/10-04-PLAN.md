---
phase: 10-theme-unification-optimization
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
files_modified:
  - flutter_app/lib/providers/theme_provider.dart
  - flutter_app/lib/widgets/fluent_settings_page.dart
  - flutter_app/lib/screens/settings_screen.dart
autonomous: true
requirements:
  - CROSS-02
  - ENH-01
must_haves:
  truths:
    - 用户可以在设置页面选择浅色/深色/跟随系统三种模式
    - 主题选择持久化存储（shared_preferences）
    - 主题切换实时生效，无需重启应用
    - Windows 和 Android 设置页面都提供主题切换控件
  artifacts:
    - path: flutter_app/lib/providers/theme_provider.dart
      provides: 添加 shared_preferences 持久化
      modifies: [初始化逻辑, setThemeMode]
    - path: flutter_app/lib/widgets/fluent_settings_page.dart
      provides: Fluent 设置页面主题切换 UI
      contains: [主题选择控件, 当前模式显示]
    - path: flutter_app/lib/screens/settings_screen.dart
      provides: Material 设置页面主题切换 UI
      creates: [新文件]
  key_links:
    - from: ThemeProvider
      to: shared_preferences
      via: 读取和写入主题偏好
    - from: FluentSettingsPage / SettingsScreen
      to: ThemeProvider
      via: 调用 setThemeMode 切换主题
    - from: main.dart
      to: ThemeProvider
      via: 监听通知重建主题
---

<objective>
实现明暗主题切换功能：持久化存储、设置页面 UI、实时切换。

Purpose: 让用户可以自定义主题偏好，支持跟随系统、浅色、深色三种模式
Output: 持久化的主题切换功能，双平台设置页面
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-theme-unification-optimization/10-CONTEXT.md
@flutter_app/lib/providers/theme_provider.dart
@flutter_app/lib/widgets/fluent_settings_page.dart
@flutter_app/lib/providers/navigation_provider.dart

## 设计决策

**切换方式**: 默认跟随系统，用户可手动选择"浅色"/"深色"/"跟随系统"
**存储方式**: shared_preferences 持久化
**生效时机**: 实时切换，Provider 通知重建
**UI 入口**: FluentSettingsPage (Windows) + SettingsScreen (Android)

## 当前状态

- theme_provider.dart: 只有内存状态管理，无持久化
- fluent_settings_page.dart: 占位页面，无实际功能
- settings_screen.dart: 不存在，需要新建（Android）
</context>

<tasks>

<task type="auto">
  <name>task 1: 为主题 Provider 添加 shared_preferences 持久化</name>
  <files>flutter_app/lib/providers/theme_provider.dart</files>
  <action>
更新 ThemeProvider，添加 shared_preferences 持久化支持：

1. **添加依赖**（pubspec.yaml 已存在，确认即可）:
   ```yaml
   dependencies:
     shared_preferences: ^2.2.2
   ```

2. **更新 ThemeProvider**:
   ```dart
   import 'package:shared_preferences/shared_preferences.dart';
   
   class ThemeProvider extends ChangeNotifier {
     static const String _themeModeKey = 'theme_mode';
     
     ThemeMode _themeMode = ThemeMode.system;
     SharedPreferences? _prefs;
     bool _initialized = false;
     
     ThemeMode get themeMode => _themeMode;
     bool get isInitialized => _initialized;
     
     // 初始化时加载保存的偏好
     Future<void> initialize() async {
       if (_initialized) return;
       
       _prefs = await SharedPreferences.getInstance();
       final savedMode = _prefs?.getString(_themeModeKey);
       
       if (savedMode != null) {
         _themeMode = _parseThemeMode(savedMode);
       }
       
       _initialized = true;
       notifyListeners();
     }
     
     void setThemeMode(ThemeMode mode) async {
       if (_themeMode == mode) return;
       
       _themeMode = mode;
       await _prefs?.setString(_themeModeKey, _themeModeToString(mode));
       notifyListeners();
     }
     
     ThemeMode _parseThemeMode(String value) {
       switch (value) {
         case 'light': return ThemeMode.light;
         case 'dark': return ThemeMode.dark;
         default: return ThemeMode.system;
       }
     }
     
     String _themeModeToString(ThemeMode mode) {
       switch (mode) {
         case ThemeMode.light: return 'light';
         case ThemeMode.dark: return 'dark';
         default: return 'system';
       }
     }
     
     bool get isSystemMode => _themeMode == ThemeMode.system;
     bool get isLightMode => _themeMode == ThemeMode.light;
     bool get isDarkMode => _themeMode == ThemeMode.dark;
   }
   ```

3. **在 main.dart 中初始化**:
   在 MultiProvider 之后，AdaptiveApp 之前添加初始化逻辑。
  </action>
  <verify>
    <automated>grep -q "shared_preferences" flutter_app/lib/providers/theme_provider.dart && grep -q "initialize()" flutter_app/lib/providers/theme_provider.dart && grep -q "_themeModeKey" flutter_app/lib/providers/theme_provider.dart</automated>
  </verify>
  <done>ThemeProvider 包含 shared_preferences 持久化，支持加载和保存主题偏好</done>
</task>

<task type="auto">
  <name>task 2: 实现 FluentSettingsPage 主题切换 UI</name>
  <files>flutter_app/lib/widgets/fluent_settings_page.dart</files>
  <action>
将占位页面替换为完整的 Fluent 风格设置页面：

1. **页面结构**:
   ```dart
   import 'package:fluent_ui/fluent_ui.dart';
   import 'package:provider/provider.dart';
   import '../providers/theme_provider.dart';
   
   class FluentSettingsPage extends StatelessWidget {
     const FluentSettingsPage({super.key});
     
     @override
     Widget build(BuildContext context) {
       return Consumer<ThemeProvider>(
         builder: (context, themeProvider, child) {
           return ScaffoldPage(
             header: const PageHeader(
               title: Text('设置'),
             ),
             content: SingleChildScrollView(
               padding: const EdgeInsets.all(16.0),
               child: Column(
                 crossAxisAlignment: CrossAxisAlignment.start,
                 children: [
                   _buildThemeSection(context, themeProvider),
                   const SizedBox(height: 24),
                   // 预留其他设置项位置
                 ],
               ),
             ),
           );
         },
       );
     }
     
     Widget _buildThemeSection(BuildContext context, ThemeProvider provider) {
       return Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
           Text(
             '外观',
             style: FluentTheme.of(context).typography.subtitle,
           ),
           const SizedBox(height: 12),
           _buildThemeOption(
             context,
             provider,
             ThemeMode.system,
             '跟随系统',
             FluentIcons.devices,
           ),
           _buildThemeOption(
             context,
             provider,
             ThemeMode.light,
             '浅色',
             FluentIcons.light,
           ),
           _buildThemeOption(
             context,
             provider,
             ThemeMode.dark,
             '深色',
             FluentIcons.lightbulb,
           ),
         ],
       );
     }
     
     Widget _buildThemeOption(
       BuildContext context,
       ThemeProvider provider,
       ThemeMode mode,
       String label,
       IconData icon,
     ) {
       final isSelected = provider.themeMode == mode;
       
       return ListTile.selectable(
         selected: isSelected,
         leading: Icon(icon),
         title: Text(label),
         onPressed: () => provider.setThemeMode(mode),
       );
     }
   }
   ```

2. **确保使用 Fluent UI 组件**:
   - ListTile.selectable 用于选项列表
   - 遵循 Fluent Design 视觉风格

3. **实时反馈**:
   - 选中项显示高亮
   - 切换后 Provider 通知重建
  </action>
  <verify>
    <automated>grep -q "ThemeProvider" flutter_app/lib/widgets/fluent_settings_page.dart && grep -q "setThemeMode" flutter_app/lib/widgets/fluent_settings_page.dart && grep -q "ThemeMode.system\|ThemeMode.light\|ThemeMode.dark" flutter_app/lib/widgets/fluent_settings_page.dart</automated>
  </verify>
  <done>FluentSettingsPage 包含主题切换 UI，使用 ListTile.selectable 展示三个选项</done>
</task>

<task type="auto">
  <name>task 3: 创建 Material 设置页面 SettingsScreen</name>
  <files>flutter_app/lib/screens/settings_screen.dart</files>
  <action>
创建 lib/screens/settings_screen.dart，为 Android 提供设置页面：

1. **文件结构**:
   ```dart
   import 'package:flutter/material.dart';
   import 'package:provider/provider.dart';
   import '../providers/theme_provider.dart';
   
   class SettingsScreen extends StatelessWidget {
     const SettingsScreen({super.key});
     
     @override
     Widget build(BuildContext context) {
       return Consumer<ThemeProvider>(
         builder: (context, themeProvider, child) {
           return Scaffold(
             appBar: AppBar(
               title: const Text('设置'),
             ),
             body: ListView(
               children: [
                 _buildThemeSection(context, themeProvider),
                 // 预留其他设置项位置
               ],
             ),
           );
         },
       );
     }
     
     Widget _buildThemeSection(BuildContext context, ThemeProvider provider) {
       return Column(
         children: [
           const ListTile(
             title: Text(
               '外观',
               style: TextStyle(fontWeight: FontWeight.bold),
             ),
           ),
           RadioListTile<ThemeMode>(
             title: const Text('跟随系统'),
             secondary: const Icon(Icons.brightness_auto),
             value: ThemeMode.system,
             groupValue: provider.themeMode,
             onChanged: (value) => provider.setThemeMode(value!),
           ),
           RadioListTile<ThemeMode>(
             title: const Text('浅色'),
             secondary: const Icon(Icons.light_mode),
             value: ThemeMode.light,
             groupValue: provider.themeMode,
             onChanged: (value) => provider.setThemeMode(value!),
           ),
           RadioListTile<ThemeMode>(
             title: const Text('深色'),
             secondary: const Icon(Icons.dark_mode),
             value: ThemeMode.dark,
             groupValue: provider.themeMode,
             onChanged: (value) => provider.setThemeMode(value!),
           ),
           const Divider(),
         ],
       );
     }
   }
   ```

2. **添加到导航**:
   - 在 material_app_shell.dart 的 screens 列表中添加 SettingsScreen
   - 确保导航栏可以跳转到设置页面

3. **使用 Material 3 组件**:
   - RadioListTile 用于单选列表
   - 遵循 Material Design 3 视觉风格

4. **更新 navigation_provider.dart**（如果需要）:
   - 如果 Android 导航不包含设置，添加设置项
   - 当前有 Gallery、Search、Tag Management，可能需要添加 Settings
  </action>
  <verify>
    <automated>test -f flutter_app/lib/screens/settings_screen.dart && grep -q "ThemeProvider" flutter_app/lib/screens/settings_screen.dart && grep -q "RadioListTile" flutter_app/lib/screens/settings_screen.dart</automated>
  </verify>
  <done>SettingsScreen 创建完成，包含三个 RadioListTile 主题选项，添加到导航</done>
</task>

<task type="auto">
  <name>task 4: 在 main.dart 初始化 ThemeProvider</name>
  <files>flutter_app/lib/main.dart</files>
  <action>
更新 main.dart 在应用启动时初始化 ThemeProvider：

1. **修改 MyApp 为有状态组件或 FutureBuilder**：
   ```dart
   class MyApp extends StatefulWidget {
     const MyApp({super.key});
     
     @override
     State<MyApp> createState() => _MyAppState();
   }
   
   class _MyAppState extends State<MyApp> {
     late final Future<void> _initFuture;
     
     @override
     void initState() {
       super.initState();
       _initFuture = _initializeApp();
     }
     
     Future<void> _initializeApp() async {
       // 获取 ThemeProvider 并初始化
       final context = navigatorKey.currentContext;
       if (context != null) {
         final themeProvider = context.read<ThemeProvider>();
         await themeProvider.initialize();
       }
     }
     
     @override
     Widget build(BuildContext context) {
       return FutureBuilder(
         future: _initFuture,
         builder: (context, snapshot) {
           if (snapshot.connectionState == ConnectionState.waiting) {
             return const MaterialApp(
               home: Scaffold(
                 body: Center(child: CircularProgressIndicator()),
               ),
             );
           }
           
           return MultiProvider(
             // ... 现有 providers
             child: const AdaptiveApp(...),
           );
         },
       );
     }
   }
   ```

2. **简化方案**（推荐）：使用 Builder 在运行时初始化：
   ```dart
   class MyApp extends StatelessWidget {
     const MyApp({super.key});
     
     @override
     Widget build(BuildContext context) {
       return MultiProvider(
         providers: [
           // ... 其他 providers
           ChangeNotifierProvider(create: (_) => ThemeProvider()),
         ],
         child: Builder(
           builder: (context) {
             // 延迟初始化 ThemeProvider
             WidgetsBinding.instance.addPostFrameCallback((_) {
               context.read<ThemeProvider>().initialize();
             });
             
             return const AdaptiveApp(...);
           },
         ),
       );
     }
   }
   ```

3. **确保初始化只执行一次**：
   - ThemeProvider.initialize() 内部有 _initialized 标记
   - 多次调用不会重复执行

选择简化方案，避免复杂的 FutureBuilder 嵌套。
  </action>
  <verify>
    <automated>grep -q "ThemeProvider().initialize()" flutter_app/lib/main.dart || grep -q "addPostFrameCallback" flutter_app/lib/main.dart</automated>
  </verify>
  <done>main.dart 在应用启动时初始化 ThemeProvider，加载保存的主题偏好</done>
</task>

</tasks>

<verification>
- [ ] ThemeProvider 包含 shared_preferences 持久化
- [ ] FluentSettingsPage 有主题切换 UI
- [ ] SettingsScreen (Android) 有主题切换 UI
- [ ] 主题切换实时生效
- [ ] 重启后主题偏好保持
</verification>

<success_criteria>
1. Windows 设置页面显示三个主题选项，选中项高亮
2. Android 设置页面显示三个 RadioListTile 选项
3. 切换主题后立即生效，无需重启
4. 重启应用后恢复上次选择的主题
5. 代码无分析警告或错误
</success_criteria>

<output>
After completion, create `.planning/phases/10-theme-unification-optimization/10-04-SUMMARY.md`
</output>
