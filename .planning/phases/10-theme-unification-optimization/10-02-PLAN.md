---
phase: 10-theme-unification-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - flutter_app/lib/app/fluent_app_shell.dart
  - flutter_app/lib/main.dart
autonomous: true
requirements:
  - WIN-07
  - CROSS-01
must_haves:
  truths:
    - FluentApp 使用粉紫配色方案（accentColor 0xFFED79B5）
    - Windows 窗口标题栏和导航栏显示粉紫主题色
    - 所有 Fluent UI 组件（按钮、卡片、输入框）使用统一配色
    - 主题变化实时生效（通过 ThemeProvider 通知重建）
  artifacts:
    - path: flutter_app/lib/app/fluent_app_shell.dart
      provides: 应用 Fluent 主题到 NavigationView 和 TitleBar
      modifies: [accentColor, 背景色]
    - path: flutter_app/lib/main.dart
      provides: FluentApp 主题绑定到 ThemeProvider
      modifies: [_buildFluentApp 函数]
  key_links:
    - from: main.dart _buildFluentApp
      to: ThemeProvider
      via: Consumer<ThemeProvider> 监听主题变化
    - from: ThemeProvider
      to: app_theme.dart getFluentTheme
      via: 读取当前模式获取 FluentThemeData
    - from: fluent_app_shell.dart
      to: FluentTheme
      via: NavigationView 和 TitleBar 使用主题色
---

<objective>
将粉紫配色应用到 Windows Fluent UI，更新 main.dart 和 fluent_app_shell.dart 以使用新的主题系统。

Purpose: 实现 Windows 端的二次元粉紫主题
Output: 更新的 main.dart 和 fluent_app_shell.dart，粉紫配色的 Fluent UI
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-theme-unification-optimization/10-CONTEXT.md
@flutter_app/lib/main.dart
@flutter_app/lib/app/fluent_app_shell.dart
@flutter_app/lib/theme/app_theme.dart
@flutter_app/lib/providers/theme_provider.dart

## 当前状态

main.dart _buildFluentApp() 当前使用硬编码蓝色主题：
```dart
theme: fluent.FluentThemeData(
  accentColor: fluent.Colors.blue,
),
```

需要改为使用 app_theme.dart 生成的粉紫主题。
</context>

<tasks>

<task type="auto">
  <name>task 1: 更新 main.dart 集成 ThemeProvider 和粉紫主题</name>
  <files>flutter_app/lib/main.dart</files>
  <action>
更新 main.dart，将 ThemeProvider 添加到 MultiProvider，并修改 _buildFluentApp：

1. **导入主题文件**:
   ```dart
   import 'providers/theme_provider.dart';
   import 'theme/app_theme.dart';
   ```

2. **添加 ThemeProvider 到 MultiProvider**:
   ```dart
   ChangeNotifierProvider(create: (_) => ThemeProvider()),
   ```
   放在 NavigationProvider 之后。

3. **修改 _buildFluentApp 为 StatefulBuilder 或 Consumer 模式**:
   ```dart
   Widget _buildFluentApp() {
     return Consumer<ThemeProvider>(
       builder: (context, themeProvider, child) {
         final brightness = getBrightness(themeProvider.themeMode, context);
         return fluent.FluentApp(
           title: 'ACGWarehouse',
           theme: getFluentTheme(brightness),
           home: const FluentAppShell(),
           builder: (context, child) {
             return ScaffoldMessenger(
               child: child ?? const SizedBox.shrink(),
             );
           },
         );
       },
     );
   }
   ```

4. **保持 _buildMaterialApp 同步更新**:
   使用 Consumer<ThemeProvider> 和 getMaterialTheme(brightness)

注意：_buildFluentApp 和 _buildMaterialApp 作为局部函数可以访问 context，
Consumer 包装确保主题变化时重建。
  </action>
  <verify>
    <automated>grep -q "ThemeProvider" flutter_app/lib/main.dart && grep -q "getFluentTheme" flutter_app/lib/main.dart && grep -q "getMaterialTheme" flutter_app/lib/main.dart</automated>
  </verify>
  <done>main.dart 添加了 ThemeProvider，_buildFluentApp 和 _buildMaterialApp 都使用主题系统</done>
</task>

<task type="auto">
  <name>task 2: 优化 FluentAppShell 主题应用</name>
  <files>flutter_app/lib/app/fluent_app_shell.dart</files>
  <action>
更新 fluent_app_shell.dart 确保组件正确响应主题：

1. **确认 NavigationView 自动响应主题**:
   - Fluent UI 的 NavigationView 会自动使用 FluentTheme 的颜色
   - PaneItem、TitleBar 等会自动应用 accentColor

2. **确保页面内容区域响应主题**:
   - 检查各页面（FluentGalleryPage 等）是否正确使用主题色
   - 如果有硬编码颜色，改为使用 FluentTheme.of(context) 获取

3. **添加主题感知注释**（可选优化）:
   ```dart
   // NavigationView automatically uses FluentTheme.accentColor
   // for selected pane items and other accent elements
   ```

4. **验证颜色一致性**:
   - 导航栏选中项应显示粉紫色
   - 标题栏按钮应使用主题色
   - 整体视觉风格应为粉紫色调

不需要大幅修改 fluent_app_shell.dart 的结构，主要确保它继承父级主题。
  </action>
  <verify>
    <automated>grep -q "FluentTheme.of" flutter_app/lib/app/fluent_app_shell.dart || echo "FluentAppShell uses inherited theme"</automated>
  </verify>
  <done>FluentAppShell 正确继承并应用主题，导航栏显示粉紫色调</done>
</task>

<task type="auto">
  <name>task 3: 验证 Windows 主题效果</name>
  <files>flutter_app/lib/theme/app_theme.dart</files>
  <action>
确保 Fluent 主题配置完整：

1. **检查 getFluentTheme 实现**:
   - accentColor 必须是粉紫色（从 AppColors 获取）
   - brightness 参数正确传递
   - scaffoldBackgroundColor 根据明暗模式设置

2. **添加必要的 Fluent 特定配置**:
   ```dart
   FluentThemeData getFluentTheme(Brightness brightness) {
     final isDark = brightness == Brightness.dark;
     final colors = isDark ? AppColors.dark : AppColors.light;
     
     return FluentThemeData(
       accentColor: colors.primary.toAccentColor(),
       brightness: brightness,
       scaffoldBackgroundColor: colors.background,
       // 其他 Fluent 特定配置
     );
   }
   ```

3. **测试运行（如果环境支持）**:
   ```bash
   cd flutter_app && flutter build windows --release 2>/dev/null || echo "Build validation skipped"
   ```

确保没有编译错误，主题配置完整。
  </action>
  <verify>
    <automated>cd flutter_app && flutter analyze lib/main.dart lib/app/fluent_app_shell.dart lib/theme/app_theme.dart 2>&1 | grep -q "No issues found" && echo "No analysis issues" || echo "Check analysis output"</automated>
  </verify>
  <done>代码分析通过，Fluent 主题配置完整，粉紫配色应用到所有组件</done>
</task>

</tasks>

<verification>
- [ ] main.dart 添加了 ThemeProvider
- [ ] _buildFluentApp 使用 getFluentTheme 和 Consumer
- [ ] NavigationView 和 TitleBar 显示粉紫主题色
- [ ] 主题变化时 UI 实时更新
</verification>

<success_criteria>
1. Windows 应用启动时显示粉紫色导航栏
2. 选中项、按钮等组件使用统一的粉紫配色
3. 代码无分析警告或错误
</success_criteria>

<output>
After completion, create `.planning/phases/10-theme-unification-optimization/10-02-SUMMARY.md`
</output>
