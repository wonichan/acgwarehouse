---
phase: 07-architecture-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - flutter_app/lib/main.dart
  - flutter_app/test/integration_test.dart
autonomous: true
requirements:
  - ARCH-04
user_setup: []

must_haves:
  truths:
    - "所有 Provider 在双 UI 框架中正常工作"
    - "ImageListProvider 可以加载图片列表"
    - "TagProvider 可以管理标签"
    - "SearchProvider 可以执行搜索"
    - "DuplicateProvider 可以检测重复"
    - "NavigationProvider 状态在导航切换时保持"
  artifacts:
    - path: "flutter_app/test/integration_test.dart"
      provides: "集成测试验证共享 Provider"
      contains: "testWidgets"
  key_links:
    - from: "FluentAppShell"
      to: "ImageListProvider"
      via: "GalleryScreen"
      pattern: "ImageListProvider"
    - from: "MaterialAppShell"
      to: "ImageListProvider"
      via: "GalleryScreen"
      pattern: "ImageListProvider"
---

<objective>
验证共享业务逻辑层 (Provider/Services/Models) 与双 UI 框架 (Fluent/Material) 的兼容性，确保所有 Provider 在两个 UI 框架中正常工作。

Purpose: 确认架构设计正确，共享层可以在双 UI 框架中无缝使用
Output: 集成测试、验证报告
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-architecture-foundation/07-CONTEXT.md

<interfaces>
<!-- Plan 01-03 创建的接口 -->

From flutter_app/lib/providers/:
```dart
class ImageListProvider extends ChangeNotifier { ... }
class TagProvider extends ChangeNotifier { ... }
class SearchProvider extends ChangeNotifier { ... }
class DuplicateProvider extends ChangeNotifier { ... }
class NavigationProvider extends ChangeNotifier { ... }
```

From flutter_app/lib/app/:
```dart
class AdaptiveApp extends StatelessWidget { ... }
class FluentAppShell extends StatelessWidget { ... }
class MaterialAppShell extends StatelessWidget { ... }
```

From flutter_app/lib/screens/:
```dart
class GalleryScreen extends StatelessWidget { ... }
class SearchScreen extends StatelessWidget { ... }
class DuplicateScreen extends StatelessWidget { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: 创建共享 Provider 集成测试</name>
  <files>flutter_app/test/integration/provider_integration_test.dart</files>
  <action>
    创建测试目录和文件 flutter_app/test/integration/provider_integration_test.dart:
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:provider/provider.dart';
    import 'package:fluent_ui/fluent_ui.dart' as fluent;
    
    import 'package:gallery/providers/navigation_provider.dart';
    import 'package:gallery/providers/image_provider.dart';
    import 'package:gallery/providers/tag_provider.dart';
    import 'package:gallery/providers/search_provider.dart';
    import 'package:gallery/providers/duplicate_provider.dart';
    import 'package:gallery/services/api_service.dart';
    import 'package:gallery/services/tag_service.dart';
    import 'package:gallery/services/search_service.dart';
    import 'package:gallery/services/duplicate_service.dart';
    import 'package:gallery/app/adaptive_app.dart';
    import 'package:gallery/app/fluent_app_shell.dart';
    import 'package:gallery/app/material_app_shell.dart';
    
    void main() {
      group('Provider Integration Tests', () {
        testWidgets('NavigationProvider 状态在 MaterialApp 中正常工作', (tester) async {
          await tester.pumpWidget(
            MultiProvider(
              providers: [
                ChangeNotifierProvider(create: (_) => NavigationProvider()),
              ],
              child: const MaterialApp(
                home: MaterialAppShell(),
              ),
            ),
          );
          
          // 验证初始状态
          final navProvider = tester.element<NavigationProvider>(
            find.byType(MaterialAppShell),
          ).read<NavigationProvider>();
          
          expect(navProvider.selectedIndex, 0);
          
          // 点击第二个导航项
          await tester.tap(find.text('搜索'));
          await tester.pumpAndSettle();
          
          expect(navProvider.selectedIndex, 1);
        });
        
        testWidgets('NavigationProvider 状态在 FluentApp 中正常工作', (tester) async {
          await tester.pumpWidget(
            MultiProvider(
              providers: [
                ChangeNotifierProvider(create: (_) => NavigationProvider()),
              ],
              child: const fluent.FluentApp(
                home: FluentAppShell(),
              ),
            ),
          );
          
          // 验证初始状态
          final navProvider = tester.element<NavigationProvider>(
            find.byType(FluentAppShell),
          ).read<NavigationProvider>();
          
          expect(navProvider.selectedIndex, 0);
        });
        
        testWidgets('ImageListProvider 可被创建和访问', (tester) async {
          await tester.pumpWidget(
            MultiProvider(
              providers: [
                Provider(create: (_) => ApiService()),
                ChangeNotifierProvider(
                  create: (context) => ImageListProvider(context.read<ApiService>()),
                ),
              ],
              child: MaterialApp(
                home: Builder(
                  builder: (context) {
                    final provider = context.watch<ImageListProvider>();
                    return Text('Images: ${provider.images.length}');
                  },
                ),
              ),
            ),
          );
          
          expect(find.textContaining('Images:'), findsOneWidget);
        });
        
        testWidgets('所有 Provider 可同时初始化', (tester) async {
          await tester.pumpWidget(
            MultiProvider(
              providers: [
                Provider(create: (_) => ApiService()),
                Provider(create: (_) => TagService()),
                Provider(create: (_) => DuplicateService()),
                Provider(create: (_) => SearchService()),
                ChangeNotifierProvider(
                  create: (context) => ImageListProvider(context.read<ApiService>()),
                ),
                ChangeNotifierProvider(
                  create: (context) => TagProvider(context.read<TagService>()),
                ),
                ChangeNotifierProvider(
                  create: (context) => DuplicateProvider(service: context.read<DuplicateService>()),
                ),
                ChangeNotifierProvider(
                  create: (context) => SearchProvider(service: context.read<SearchService>()),
                ),
                ChangeNotifierProvider(create: (_) => NavigationProvider()),
              ],
              child: const MaterialApp(
                home: Scaffold(body: Text('All Providers Ready')),
              ),
            ),
          );
          
          expect(find.text('All Providers Ready'), findsOneWidget);
        });
      });
    }
    ```
    
    注意：
    - 测试覆盖所有 6 个 Provider
    - 测试 NavigationProvider 在双 UI 框架中的行为
    - 测试 Provider 初始化链
  </action>
  <verify>
    <automated>cd flutter_app && flutter test test/integration/provider_integration_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>集成测试创建完成，覆盖所有 Provider 的兼容性验证</done>
</task>

<task type="auto">
  <name>task 2: 验证 AdaptiveApp 平台检测逻辑</name>
  <files>flutter_app/test/widget/adaptive_app_test.dart</files>
  <action>
    创建测试文件 flutter_app/test/widget/adaptive_app_test.dart:
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:flutter/foundation.dart';
    import 'package:fluent_ui/fluent_ui.dart' as fluent;
    
    import 'package:gallery/app/adaptive_app.dart';
    
    void main() {
      group('AdaptiveApp Tests', () {
        testWidgets('AdaptiveApp 根据 TargetPlatform 选择正确的 App', (tester) async {
          // 测试 Windows 平台
          await tester.pumpWidget(
            const AdaptiveApp(
              fluentAppBuilder: () => fluent.FluentApp(home: Text('Fluent App')),
              materialAppBuilder: () => MaterialApp(home: Text('Material App')),
            ),
          );
          
          // 验证根据 defaultTargetPlatform 显示正确的 App
          // 在测试环境中，默认是 TargetPlatform.android
          // 所以应该显示 Material App
          expect(find.text('Material App'), findsOneWidget);
        });
        
        testWidgets('fluentAppBuilder 返回正确的 Widget', (tester) async {
          await tester.pumpWidget(
            const AdaptiveApp(
              fluentAppBuilder: () => fluent.FluentApp(home: Text('Fluent Content')),
              materialAppBuilder: () => MaterialApp(home: Text('Material Content')),
            ),
          );
          
          // 验证 builder 函数被调用
          expect(find.byType(AdaptiveApp), findsOneWidget);
        });
      });
    }
    ```
    
    注意：
    - 测试 AdaptiveApp 的平台检测逻辑
    - 验证 builder 函数被正确调用
  </action>
  <verify>
    <automated>cd flutter_app && flutter test test/widget/adaptive_app_test.dart 2>&1 | tail -10</automated>
  </verify>
  <done>AdaptiveApp 测试创建完成，平台检测逻辑验证通过</done>
</task>

<task type="auto">
  <name>task 3: 运行完整测试套件</name>
  <files>flutter_app/</files>
  <action>
    运行所有测试，验证共享 Provider 与双 UI 框架的兼容性:
    
    ```bash
    cd flutter_app
    flutter test --reporter=compact
    ```
    
    检查测试输出:
    - 所有现有测试应该通过
    - 新增的集成测试应该通过
    - 不应该有编译错误或警告
    
    如果有测试失败，检查:
    - Provider 是否正确初始化
    - NavigationProvider 是否被正确使用
    - Screen Widget 是否正确引用 Provider
  </action>
  <verify>
    <automated>cd flutter_app && flutter test --reporter=compact 2>&1 | grep -E "(passed|failed|All tests)"</automated>
  </verify>
  <done>所有测试通过，共享业务逻辑层与双 UI 框架兼容性验证完成</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>共享业务逻辑层验证 (Provider 集成测试 + AdaptiveApp 测试)</what-built>
  <how-to-verify>
    1. 运行 `cd flutter_app && flutter test --reporter=compact`
    2. 确认所有测试通过
    3. 运行 `cd flutter_app && flutter run -d chrome`
    4. 点击图库导航项，确认图片列表加载正常
    5. 点击搜索导航项，确认搜索功能正常
    6. 点击重复检测导航项，确认重复检测功能正常
    7. 运行 `cd flutter_app && flutter run -d windows`（如果支持）
    8. 确认 Fluent UI 界面显示正常，导航切换正常
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
**Goal-Backward 验证:**

1. ✓ Provider 双框架兼容: 集成测试覆盖所有 6 个 Provider
2. ✓ ImageListProvider 工作: 测试验证可创建和访问
3. ✓ TagProvider 工作: 包含在 Provider 初始化链中
4. ✓ SearchProvider 工作: 包含在 Provider 初始化链中
5. ✓ DuplicateProvider 工作: 包含在 Provider 初始化链中
6. ✓ NavigationProvider 状态保持: 测试验证状态在双 UI 中同步

**文件完整性:**
- flutter_app/test/integration/provider_integration_test.dart: Provider 集成测试
- flutter_app/test/widget/adaptive_app_test.dart: AdaptiveApp 测试
</verification>

<success_criteria>
- [ ] Provider 集成测试创建完成
- [ ] AdaptiveApp 测试创建完成
- [ ] 所有测试通过
- [ ] Android/Web 运行正常
- [ ] Windows 运行正常（如果支持）
- [ ] 共享 Provider 在双 UI 框架中工作正常
</success_criteria>

<output>
完成后创建 `.planning/phases/07-architecture-foundation/07-04-SUMMARY.md`
</output>