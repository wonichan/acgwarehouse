---
phase: 07-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/pubspec.yaml
  - flutter_app/lib/providers/navigation_provider.dart
  - flutter_app/lib/app/adaptive_app.dart
  - flutter_app/lib/main.dart
autonomous: true
requirements:
  - ARCH-01
user_setup: []

must_haves:
  truths:
    - "应用启动时能够检测当前运行平台"
    - "Windows 平台返回 Fluent UI 应用"
    - "Android/Web 平台返回 Material UI 应用"
    - "导航状态在 NavigationProvider 中集中管理"
  artifacts:
    - path: "flutter_app/pubspec.yaml"
      provides: "fluent_ui 和 window_manager 依赖"
      contains: "fluent_ui:"
    - path: "flutter_app/lib/providers/navigation_provider.dart"
      provides: "全局导航状态管理"
      exports: ["NavigationProvider"]
    - path: "flutter_app/lib/app/adaptive_app.dart"
      provides: "平台自适应应用入口"
      exports: ["AdaptiveApp"]
    - path: "flutter_app/lib/main.dart"
      provides: "应用启动入口"
      contains: "AdaptiveApp"
  key_links:
    - from: "flutter_app/lib/main.dart"
      to: "AdaptiveApp"
      via: "runApp"
      pattern: "AdaptiveApp"
    - from: "AdaptiveApp"
      to: "NavigationProvider"
      via: "Provider.read"
      pattern: "NavigationProvider"
---

<objective>
创建平台感知的应用入口，实现根据运行平台自动选择 Fluent UI 或 Material UI，同时建立全局导航状态管理。

Purpose: 为双 UI 框架提供统一的应用入口和导航状态管理基础
Output: AdaptiveApp widget、NavigationProvider、更新的依赖配置
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-architecture-foundation/07-CONTEXT.md
@flutter_app/lib/main.dart
@flutter_app/pubspec.yaml

<interfaces>
<!-- 现有 Provider 接口，新 NavigationProvider 需要遵循的模式 -->

From flutter_app/lib/providers/image_provider.dart:
```dart
class ImageListProvider extends ChangeNotifier {
  // 使用 ChangeNotifier 模式
  // 包含私有状态字段 + getter + 状态更新方法
}
```

现有 main.dart 结构:
```dart
void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  // MultiProvider 包裹 MaterialApp
  // Provider 初始化在这里完成
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: 添加 fluent_ui 和 window_manager 依赖</name>
  <files>flutter_app/pubspec.yaml</files>
  <action>
    在 pubspec.yaml 的 dependencies 部分添加:
    ```yaml
    fluent_ui: ^4.9.1
    window_manager: ^0.4.3
    ```
    
    注意：
    - fluent_ui 版本使用最新稳定版 4.9.1
    - window_manager 用于 Windows 窗口控制（Phase 8 完整实现）
    - 不要修改其他依赖
  </action>
  <verify>
    <automated>cd flutter_app && flutter pub get 2>&1 | grep -E "(fluent_ui|window_manager)"</automated>
  </verify>
  <done>pubspec.yaml 包含 fluent_ui 和 window_manager 依赖，flutter pub get 成功</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 创建 NavigationProvider</name>
  <files>flutter_app/lib/providers/navigation_provider.dart, flutter_app/lib/providers/navigation_provider_test.dart</files>
  <behavior>
    - Test 1: 初始 selectedIndex 为 0
    - Test 2: setSelectedIndex(2) 后 selectedIndex 变为 2
    - Test 3: notifyListeners 在 setSelectedIndex 时被调用
  </behavior>
  <action>
    创建 flutter_app/lib/providers/navigation_provider.dart:
    
    ```dart
    import 'package:flutter/foundation.dart';
    
    /// 全局导航状态管理
    /// 管理 FluentApp 和 MaterialApp 共享的导航索引
    class NavigationProvider extends ChangeNotifier {
      int _selectedIndex = 0;
      
      int get selectedIndex => _selectedIndex;
      
      void setSelectedIndex(int index) {
        if (_selectedIndex != index) {
          _selectedIndex = index;
          notifyListeners();
        }
      }
    }
    ```
    
    创建测试文件 flutter_app/lib/providers/navigation_provider_test.dart:
    - 测试初始值为 0
    - 测试 setSelectedIndex 更新状态
    - 测试相同索引不触发 notifyListeners
  </action>
  <verify>
    <automated>cd flutter_app && flutter test lib/providers/navigation_provider_test.dart</automated>
  </verify>
  <done>NavigationProvider 类创建完成，测试通过，遵循现有 Provider 模式</done>
</task>

<task type="auto">
  <name>task 3: 创建 AdaptiveApp widget</name>
  <files>flutter_app/lib/app/adaptive_app.dart</files>
  <action>
    创建新目录 flutter_app/lib/app/ 和文件 adaptive_app.dart:
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:fluent_ui/fluent_ui.dart' as fluent;
    import 'package:provider/provider.dart';
    import 'package:flutter/foundation.dart' show defaultTargetPlatform, TargetPlatform;
    
    import '../providers/navigation_provider.dart';
    
    /// 平台自适应应用入口
    /// Windows 使用 Fluent UI，其他平台使用 Material 3
    class AdaptiveApp extends StatelessWidget {
      final Widget Function() fluentAppBuilder;
      final Widget Function() materialAppBuilder;
      
      const AdaptiveApp({
        super.key,
        required this.fluentAppBuilder,
        required this.materialAppBuilder,
      });
      
      @override
      Widget build(BuildContext context) {
        // 平台检测：Windows 使用 Fluent UI
        final isWindows = defaultTargetPlatform == TargetPlatform.windows;
        
        if (isWindows) {
          return fluentAppBuilder();
        } else {
          return materialAppBuilder();
        }
      }
    }
    ```
    
    注意：
    - 使用 defaultTargetPlatform 进行平台检测（遵循用户决策）
    - 使用 fluent 命名空间避免与 Material 组件冲突
    - 通过 Builder 函数允许外部构建具体 App
  </action>
  <verify>
    <automated>cd flutter_app && flutter analyze lib/app/adaptive_app.dart 2>&1 | grep -E "(error|warning)" || echo "No errors"</automated>
  </verify>
  <done>AdaptiveApp widget 创建完成，无编译错误，平台检测逻辑正确</done>
</task>

<task type="auto">
  <name>task 4: 重构 main.dart 使用 AdaptiveApp</name>
  <files>flutter_app/lib/main.dart</files>
  <action>
    重构 main.dart，保留现有 Provider 初始化逻辑，使用 AdaptiveApp 入口:
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:provider/provider.dart';
    import 'package:fluent_ui/fluent_ui.dart' as fluent;
    
    import 'providers/image_provider.dart';
    import 'providers/tag_provider.dart';
    import 'providers/duplicate_provider.dart';
    import 'providers/search_provider.dart';
    import 'providers/navigation_provider.dart';
    import 'services/api_service.dart';
    import 'services/tag_service.dart';
    import 'services/duplicate_service.dart';
    import 'services/search_service.dart';
    import 'app/adaptive_app.dart';
    import 'screens/gallery_screen.dart';
    import 'screens/duplicate_screen.dart';
    import 'screens/search_screen.dart';
    
    void main() {
      runApp(const MyApp());
    }
    
    class MyApp extends StatelessWidget {
      const MyApp({super.key});
    
      @override
      Widget build(BuildContext context) {
        return MultiProvider(
          providers: [
            Provider(create: (_) => ApiService()),
            Provider(create: (_) => TagService()),
            Provider(create: (_) => DuplicateService()),
            Provider(create: (_) => SearchService()),
            ChangeNotifierProvider(create: (context) => ImageListProvider(context.read<ApiService>())..loadImages()),
            ChangeNotifierProvider(create: (context) => TagProvider(context.read<TagService>())),
            ChangeNotifierProvider(create: (context) => DuplicateProvider(service: context.read<DuplicateService>())),
            ChangeNotifierProvider(create: (context) => SearchProvider(service: context.read<SearchService>())),
            ChangeNotifierProvider(create: (_) => NavigationProvider()),
          ],
          child: AdaptiveApp(
            fluentAppBuilder: () => const PlaceholderFluentApp(),
            materialAppBuilder: () => const PlaceholderMaterialApp(),
          ),
        );
      }
    }
    
    /// Placeholder FluentApp - Phase 8 完整实现
    class PlaceholderFluentApp extends StatelessWidget {
      const PlaceholderFluentApp({super.key});
      
      @override
      Widget build(BuildContext context) {
        return fluent.FluentApp(
          title: 'ACGWarehouse',
          home: fluent.ScaffoldPage(
            content: Center(
              child: Text('Fluent UI - Windows (Phase 8)'),
            ),
          ),
        );
      }
    }
    
    /// Placeholder MaterialApp - 保持现有 MainScreen 逻辑
    class PlaceholderMaterialApp extends StatelessWidget {
      const PlaceholderMaterialApp({super.key});
      
      @override
      Widget build(BuildContext context) {
        return MaterialApp(
          title: 'ACGWarehouse',
          theme: ThemeData(
            colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
            useMaterial3: true,
          ),
          home: const MainScreen(),
        );
      }
    }
    
    /// 现有 MainScreen - 保持不变
    class MainScreen extends StatefulWidget {
      const MainScreen({super.key});
      
      @override
      State<MainScreen> createState() => _MainScreenState();
    }
    
    class _MainScreenState extends State<MainScreen> {
      int _selectedIndex = 0;
    
      static const List<Widget> _screens = [
        GalleryScreen(),
        SearchScreen(),
        DuplicateScreen(),
      ];
    
      @override
      Widget build(BuildContext context) {
        return Scaffold(
          body: _screens[_selectedIndex],
          bottomNavigationBar: NavigationBar(
            selectedIndex: _selectedIndex,
            onDestinationSelected: (index) {
              setState(() {
                _selectedIndex = index;
              });
            },
            destinations: const [
              NavigationDestination(
                icon: Icon(Icons.photo_library_outlined),
                selectedIcon: Icon(Icons.photo_library),
                label: '图库',
              ),
              NavigationDestination(
                icon: Icon(Icons.search_outlined),
                selectedIcon: Icon(Icons.search),
                label: '搜索',
              ),
              NavigationDestination(
                icon: Icon(Icons.content_copy_outlined),
                selectedIcon: Icon(Icons.content_copy),
                label: '重复检测',
              ),
            ],
          ),
        );
      }
    }
    ```
    
    注意：
    - NavigationProvider 已添加到 MultiProvider
    - 保留现有的 MainScreen 逻辑（Plan 03 会重构）
    - PlaceholderFluentApp 是占位符，Plan 02 会完整实现
    - 不删除任何现有的 import 或 Provider
  </action>
  <verify>
    <automated>cd flutter_app && flutter analyze lib/main.dart 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <done>main.dart 重构完成，应用使用 AdaptiveApp 入口，Provider 包含 NavigationProvider</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>平台检测入口 (AdaptiveApp + NavigationProvider)</what-built>
  <how-to-verify>
    1. 打开终端，运行 `cd flutter_app && flutter run -d windows`（如果支持）
    2. 应该看到 "Fluent UI - Windows (Phase 8)" 文本
    3. 运行 `cd flutter_app && flutter run -d chrome`
    4. 应该看到现有的 Material 界面（图库、搜索、重复检测）
    5. 确认没有编译错误或警告
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
**Goal-Backward 验证:**

1. ✓ 平台检测: AdaptiveApp 使用 defaultTargetPlatform 检测平台
2. ✓ Windows 使用 Fluent UI: 条件分支返回 fluentAppBuilder
3. ✓ 其他平台使用 Material: else 分支返回 materialAppBuilder
4. ✓ 导航状态管理: NavigationProvider 已创建并注册

**文件完整性:**
- flutter_app/pubspec.yaml: 包含 fluent_ui, window_manager
- flutter_app/lib/providers/navigation_provider.dart: NavigationProvider 类
- flutter_app/lib/app/adaptive_app.dart: AdaptiveApp widget
- flutter_app/lib/main.dart: 使用 AdaptiveApp 入口
</verification>

<success_criteria>
- [ ] fluent_ui 和 window_manager 依赖添加成功
- [ ] NavigationProvider 创建并通过测试
- [ ] AdaptiveApp widget 创建完成
- [ ] main.dart 重构使用 AdaptiveApp
- [ ] Windows 运行显示 Fluent UI 占位符
- [ ] Chrome/Android 运行显示现有 Material 界面
</success_criteria>

<output>
完成后创建 `.planning/phases/07-architecture-foundation/07-01-SUMMARY.md`
</output>