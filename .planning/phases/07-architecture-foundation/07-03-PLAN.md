---
phase: 07-architecture-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - flutter_app/lib/main.dart
  - flutter_app/lib/app/material_app_shell.dart
autonomous: true
requirements:
  - ARCH-03
user_setup: []

must_haves:
  truths:
    - "Android/Web 平台启动时显示 NavigationBar 底部导航栏"
    - "用户可以在导航项之间切换"
    - "导航状态从 NavigationProvider 读取"
    - "现有 Material UI 功能保持正常工作"
  artifacts:
    - path: "flutter_app/lib/app/material_app_shell.dart"
      provides: "MaterialApp shell (Android/Web)"
      exports: ["MaterialAppShell"]
      min_lines: 60
  key_links:
    - from: "MaterialAppShell"
      to: "NavigationProvider"
      via: "Consumer"
      pattern: "NavigationProvider"
    - from: "MaterialAppShell"
      to: "NavigationBar"
      via: "Material 3 NavigationBar"
      pattern: "NavigationBar"
---

<objective>
创建 MaterialApp Shell (Android/Web)，使用 NavigationProvider 管理导航状态，保持现有 Material UI 功能正常工作。

Purpose: 为 Android/Web 平台提供 Material 3 导航框架，验证共享 Provider 与双 UI 框架兼容
Output: MaterialAppShell、更新的 main.dart
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-architecture-foundation/07-CONTEXT.md

<interfaces>
<!-- Plan 01 创建的接口 -->

From flutter_app/lib/providers/navigation_provider.dart:
```dart
class NavigationProvider extends ChangeNotifier {
  int get selectedIndex;
  void setSelectedIndex(int index);
}
```

From flutter_app/lib/main.dart:
```dart
class MainScreen extends StatefulWidget {
  // 当前使用本地 _selectedIndex 状态
  // 需要重构为使用 NavigationProvider
}

class PlaceholderMaterialApp extends StatelessWidget {
  // 当前使用 MainScreen
  // 需要替换为 MaterialAppShell
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: 创建 MaterialAppShell</name>
  <files>flutter_app/lib/app/material_app_shell.dart</files>
  <action>
    创建 flutter_app/lib/app/material_app_shell.dart:
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:provider/provider.dart';
    
    import '../providers/navigation_provider.dart';
    import '../screens/gallery_screen.dart';
    import '../screens/search_screen.dart';
    import '../screens/duplicate_screen.dart';
    
    /// MaterialApp Shell - Android/Web 平台
    /// 使用 NavigationProvider 管理导航状态
    class MaterialAppShell extends StatelessWidget {
      const MaterialAppShell({super.key});
      
      @override
      Widget build(BuildContext context) {
        return Consumer<NavigationProvider>(
          builder: (context, navProvider, child) {
            final screens = <Widget>[
              const GalleryScreen(),
              const SearchScreen(),
              const DuplicateScreen(),
            ];
            
            return Scaffold(
              body: screens[navProvider.selectedIndex],
              bottomNavigationBar: NavigationBar(
                selectedIndex: navProvider.selectedIndex,
                onDestinationSelected: (index) {
                  navProvider.setSelectedIndex(index);
                },
                destinations: const [
                  NavigationDestination(
                    icon: Icon(Icons.photo_library_outlined),
                    selectedIcon: Icon(Icons.photo_library),
                    label: '图库',
                  ),
                  NavigationDestination(
                    icon: Icon(Icons.search_outlined),
                    selectedIcon: Icon(Icons.search),
                    label: '搜索',
                  ),
                  NavigationDestination(
                    icon: Icon(Icons.content_copy_outlined),
                    selectedIcon: Icon(Icons.content_copy),
                    label: '重复检测',
                  ),
                ],
              ),
            );
          },
        );
      }
    }
    ```
    
    注意：
    - 使用 Consumer<NavigationProvider> 监听导航状态
    - NavigationBar 是 Material 3 风格
    - 导航项与 FluentAppShell 保持一致
    - 这是 StatelessWidget，状态由 NavigationProvider 管理
  </action>
  <verify>
    <automated>cd flutter_app && flutter analyze lib/app/material_app_shell.dart 2>&1 | grep -E "(error|warning)" || echo "No errors"</automated>
  </verify>
  <done>MaterialAppShell 创建完成，无编译错误，使用 NavigationProvider</done>
</task>

<task type="auto">
  <name>task 2: 更新 main.dart 使用 MaterialAppShell</name>
  <files>flutter_app/lib/main.dart</files>
  <action>
    修改 main.dart:
    
    1. 添加 import:
    ```dart
    import 'app/material_app_shell.dart';
    ```
    
    2. 替换 PlaceholderMaterialApp:
    
    找到:
    ```dart
    /// Placeholder MaterialApp - 保持现有 MainScreen 逻辑
    class PlaceholderMaterialApp extends StatelessWidget {
      const PlaceholderMaterialApp({super.key});
      
      @override
      Widget build(BuildContext context) {
        return MaterialApp(
          title: 'ACGWarehouse',
          theme: ThemeData(
            colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
            useMaterial3: true,
          ),
          home: const MainScreen(),
        );
      }
    }
    ```
    
    替换为:
    ```dart
    /// MaterialApp - Android/Web 平台
    class MaterialAppWidget extends StatelessWidget {
      const MaterialAppWidget({super.key});
      
      @override
      Widget build(BuildContext context) {
        return MaterialApp(
          title: 'ACGWarehouse',
          theme: ThemeData(
            colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
            useMaterial3: true,
          ),
          home: const MaterialAppShell(),
        );
      }
    }
    ```
    
    3. 更新 AdaptiveApp 的 materialAppBuilder:
    
    找到:
    ```dart
    materialAppBuilder: () => const PlaceholderMaterialApp(),
    ```
    
    替换为:
    ```dart
    materialAppBuilder: () => const MaterialAppWidget(),
    ```
    
    4. 删除旧的 MainScreen 类（已由 MaterialAppShell 替代）:
    
    删除以下代码:
    ```dart
    /// 现有 MainScreen - 保持不变
    class MainScreen extends StatefulWidget {
      const MainScreen({super.key});
      
      @override
      State<MainScreen> createState() => _MainScreenState();
    }
    
    class _MainScreenState extends State<MainScreen> {
      int _selectedIndex = 0;
    
      static const List<Widget> _screens = [
        GalleryScreen(),
        SearchScreen(),
        DuplicateScreen(),
      ];
    
      @override
      Widget build(BuildContext context) {
        return Scaffold(
          body: _screens[_selectedIndex],
          bottomNavigationBar: NavigationBar(
            selectedIndex: _selectedIndex,
            onDestinationSelected: (index) {
              setState(() {
                _selectedIndex = index;
              });
            },
            destinations: const [
              NavigationDestination(
                icon: Icon(Icons.photo_library_outlined),
                selectedIcon: Icon(Icons.photo_library),
                label: '图库',
              ),
              NavigationDestination(
                icon: Icon(Icons.search_outlined),
                selectedIcon: Icon(Icons.search),
                label: '搜索',
              ),
              NavigationDestination(
                icon: Icon(Icons.content_copy_outlined),
                selectedIcon: Icon(Icons.content_copy),
                label: '重复检测',
              ),
            ],
          ),
        );
      }
    }
    ```
    
    注意：
    - MainScreen 已被 MaterialAppShell 替代
    - 不再需要 StatefulWidget，状态由 NavigationProvider 管理
    - 保持 Theme 配置不变
  </action>
  <verify>
    <automated>cd flutter_app && flutter analyze lib/main.dart 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <done>main.dart 更新完成，MaterialApp 使用 MaterialAppShell</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MaterialApp Shell (NavigationBar + 共享 Provider)</what-built>
  <how-to-verify>
    1. 运行 `cd flutter_app && flutter run -d chrome`
    2. 应该看到底部 NavigationBar
    3. 点击"图库"、"搜索"、"重复检测"导航项
    4. 页面应该切换，显示现有的 GalleryScreen、SearchScreen、DuplicateScreen
    5. 确认页面切换正常，与 v1.0 功能一致
    6. 确认没有编译错误或警告
  </how-to-verify>
  <resume-signal>输入 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
**Goal-Backward 验证:**

1. ✓ NavigationBar 显示: MaterialAppShell 使用 Material 3 NavigationBar
2. ✓ 导航项切换: onDestinationSelected 调用 NavigationProvider.setSelectedIndex
3. ✓ 导航状态管理: Consumer<NavigationProvider> 监听状态变化
4. ✓ 现有功能保持: 复用 GalleryScreen、SearchScreen、DuplicateScreen

**文件完整性:**
- flutter_app/lib/app/material_app_shell.dart: MaterialAppShell 类
- flutter_app/lib/main.dart: MaterialAppWidget 替换 PlaceholderMaterialApp，删除旧 MainScreen
</verification>

<success_criteria>
- [ ] MaterialAppShell 创建完成
- [ ] NavigationBar 显示底部导航栏
- [ ] 导航项点击切换页面
- [ ] 现有 Screen Widget 正常显示
- [ ] NavigationProvider 状态同步
- [ ] 旧 MainScreen 代码已删除
</success_criteria>

<output>
完成后创建 `.planning/phases/07-architecture-foundation/07-03-SUMMARY.md`
</output>