---
phase: 12-import-task-auto-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/domain/image_tag.go
  - internal/repository/image_tag_repository.go
  - internal/repository/image_tag_repository_test.go
  - internal/repository/schema.go
  - migrations/003_add_image_tag_source.up.sql
  - migrations/003_add_image_tag_source.down.sql
autonomous: true
requirements:
  - AIQ-02
user_setup: []

must_haves:
  truths:
    - "系统可以区分 AI 生成的标签和手动添加的标签"
    - "查询接口可以找到缩略图完成但无 AI 标签的图片"
    - "数据库迁移可幂等执行"
  artifacts:
    - path: "internal/domain/image_tag.go"
      provides: "ImageTag 模型含 source 字段"
      contains: "Source string"
    - path: "internal/repository/image_tag_repository.go"
      provides: "查询无 AI 标签图片的方法"
      exports: ["FindImagesWithoutAITags"]
    - path: "migrations/003_add_image_tag_source.up.sql"
      provides: "添加 image_tag.source 字段的迁移"
  key_links:
    - from: "internal/repository/image_tag_repository.go"
      to: "images 表"
      via: "JOIN 查询缩略图状态"
      pattern: "thumbnail.*NOT NULL.*NOT EXISTS.*ai_tag"
---

# Phase 12 Plan 01: 数据模型变更与查询基础

<objective>
为 image_tag 表添加 source 字段区分 AI 生成与手动添加，并实现查询"缩略图完成但无 AI 标签关联"图片的仓库方法。

Purpose: 为自动入队判定提供数据基础，确保系统能准确识别哪些图片需要 AI 标签。
Output: 数据库迁移、领域模型更新、查询方法实现。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-import-task-auto-scheduling/12-CONTEXT.md
@.planning/phases/12-import-task-auto-scheduling/12-RESEARCH.md
@.planning/phases/12-import-task-auto-scheduling/12-VALIDATION.md

<interfaces>
<!-- 现有相关接口 -->

From internal/domain/image_tag.go:
```go
type ImageTag struct {
    ImageID             int64
    TagID               int64
    SourceObservationID *int64
    Confidence          float64
    ReviewState         string
}
```

From internal/repository/image_tag_repository.go:
```go
type ImageTagRepository interface {
    Save(ctx context.Context, imageTag *domain.ImageTag) error
    FindByImageID(ctx context.Context, imageID int64) ([]*domain.ImageTag, error)
    // ... 其他方法
}
```

From internal/domain/tag_observations.go:
```go
const (
    EvidenceTypeAIGenerated = "ai_generated"
)
```

From internal/repository/schema.go:
```go
// EnsureScanSchema 确保数据库 schema 最新
func EnsureScanSchema(db *sql.DB) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 添加 image_tag.source 字段迁移</name>
  <files>
    migrations/003_add_image_tag_source.up.sql
    migrations/003_add_image_tag_source.down.sql
    internal/domain/image_tag.go
    internal/repository/schema.go
  </files>
  <behavior>
    - Test 1: 迁移执行后 image_tags 表存在 source 列
    - Test 2: ImageTag 模型可读写 Source 字段
    - Test 3: 迁移幂等执行不报错
  </behavior>
  <action>
    1. 创建迁移文件 003_add_image_tag_source.up.sql：
       - ALTER TABLE image_tags ADD COLUMN source TEXT DEFAULT 'manual'
       - 基于 source_observation_id IS NOT NULL 更新现有数据为 'ai'
    
    2. 创建迁移文件 003_add_image_tag_source.down.sql：
       - ALTER TABLE image_tags DROP COLUMN source
    
    3. 更新 internal/domain/image_tag.go：
       - 添加 Source 字段，类型 string
       - 定义常量：ImageTagSourceAI = "ai", ImageTagSourceManual = "manual"
    
    4. 更新 internal/repository/schema.go 的 EnsureScanSchema：
       - 在 schema 检查逻辑中添加 source 列的兼容处理
       - 使用 INSERT OR IGNORE 风格确保幂等
    
    注意：默认值 'manual'，AI 生成的标签在保存时设为 'ai'。
    参考 CONTEXT.md：判定口径检查当前 image_tag 表是否有 AI 来源的标签关联。
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run "ImageTagSource|Schema" -count=1</automated>
  </verify>
  <done>
    - 迁移文件存在且可执行
    - ImageTag 模型包含 Source 字段
    - 测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 实现查询无 AI 标签图片的方法</name>
  <files>
    internal/repository/image_repository.go
    internal/repository/image_repository_test.go
  </files>
  <behavior>
    - Test 1: FindImagesWithoutAITags 返回缩略图完成但无 AI 标签的图片
    - Test 2: 已有 AI 标签的图片不在结果中
    - Test 3: limit 参数限制返回数量
    - Test 4: 无缩略图的图片不在结果中
  </behavior>
  <action>
    1. 在 ImageRepository 接口添加方法：
       ```go
       FindImagesWithoutAITags(ctx context.Context, limit int) ([]domain.Image, error)
       ```
    
    2. 在 sqliteImageRepository 实现该方法：
       - JOIN images 与 image_tags
       - WHERE 条件：
         - images.thumbnail_small_url IS NOT NULL (缩略图已完成)
         - NOT EXISTS (image_tags.source = 'ai' AND image_tags.image_id = images.id)
       - ORDER BY images.id ASC (FIFO 入队)
       - LIMIT ?
    
    3. 添加测试用例覆盖各种场景
    
    参考 CONTEXT.md：
    - 扫描条件：缩略图已完成 + 无 AI 标签关联
    - 每批最多 100 张入队
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run "FindImagesWithoutAITags" -count=1</automated>
  </verify>
  <done>
    - ImageRepository 接口包含 FindImagesWithoutAITags 方法
    - 测试覆盖 4 种场景
    - 查询正确返回缩略图完成但无 AI 标签的图片
  </done>
</task>

<task type="auto">
  <name>task 3: 更新 ImageTagRepository.Save 支持 source 字段</name>
  <files>
    internal/repository/image_tag_repository.go
    internal/repository/image_tag_repository_test.go
  </files>
  <action>
    1. 更新 ImageTagRepository.Save 方法的 SQL：
       - INSERT OR REPLACE 语句添加 source 列
    
    2. 更新 scanImageTags 函数：
       - 扫描 source 字段到 ImageTag 结构
    
    3. 添加测试验证 source 字段正确持久化
    
    注意：保持向后兼容，source 为空时使用默认值 'manual'。
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run "ImageTag.*Save|ImageTag.*Source" -count=1</automated>
  </verify>
  <done>
    - Save 方法正确持久化 source 字段
    - FindByImageID 返回的 ImageTag 包含 source 值
    - 测试通过
  </done>
</task>

</tasks>

<verification>
- [ ] 迁移文件存在：`migrations/003_add_image_tag_source.up.sql`
- [ ] 领域模型更新：`ImageTag.Source` 字段存在
- [ ] 查询方法实现：`FindImagesWithoutAITags` 可用
- [ ] 所有测试通过：`go test ./internal/repository/... -count=1`
</verification>

<success_criteria>
1. 数据库迁移可幂等执行，添加 source 列
2. ImageTag 模型包含 Source 字段，默认值 'manual'
3. FindImagesWithoutAITags 方法正确返回缩略图完成但无 AI 标签的图片
4. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/12-import-task-auto-scheduling/12-01-SUMMARY.md`
</output>