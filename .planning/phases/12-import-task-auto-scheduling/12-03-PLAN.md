---
phase: 12-import-task-auto-scheduling
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - internal/app/app.go
  - internal/app/bootstrap.go
autonomous: true
requirements:
  - AIQ-01
  - AIQ-02
user_setup: []

must_haves:
  truths:
    - "应用启动时定时扫描服务自动启动"
    - "应用关闭时定时扫描服务正确停止"
    - "配置关闭时定时扫描服务不启动"
  artifacts:
    - path: "internal/app/app.go"
      provides: "App 结构包含 autoScheduler 字段"
      contains: "autoScheduler *service.AITagAutoScheduler"
    - path: "internal/app/bootstrap.go"
      provides: "应用启动时初始化调度服务"
      exports: ["initAutoScheduler"]
  key_links:
    - from: "internal/app/app.go"
      to: "AITagAutoScheduler"
      via: "New + Start"
      pattern: "NewAITagAutoScheduler.*Start"
---

# Phase 12 Plan 03: 集成调度服务到应用启动流程

<objective>
将 AITagAutoScheduler 集成到应用启动和关闭流程，确保服务随应用生命周期正确运行。

Purpose: 完成自动入队的端到端集成，使功能在生产环境中可用。
Output: 应用启动逻辑更新、生命周期管理。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-import-task-auto-scheduling/12-CONTEXT.md
@.planning/phases/12-import-task-auto-scheduling/12-RESEARCH.md
@.planning/phases/12-import-task-auto-scheduling/12-VALIDATION.md

<interfaces>
<!-- 现有相关接口 -->

From internal/app/app.go:
```go
type App struct {
    config      *config.Config
    cfgReloader *config.Reloader
    db          *sql.DB
    httpServer  *http.Server
    jobManager  *worker.Manager
    // Repositories...
    // Services...
    
    // Background task control
    refillStopMu sync.Mutex
    refillStopCh chan struct{}
}

func (a *App) Run() error
func (a *App) Shutdown(ctx context.Context) error
```

From internal/service/ai_tag_auto_scheduler.go:
```go
type AITagAutoScheduler struct { ... }
func NewAITagAutoScheduler(...) *AITagAutoScheduler
func (s *AITagAutoScheduler) Start(ctx context.Context)
func (s *AITagAutoScheduler) Stop()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: 在 App 结构添加 autoScheduler 字段</name>
  <files>
    internal/app/app.go
  </files>
  <action>
    1. 在 App 结构体添加字段：
       ```go
       autoScheduler *service.AITagAutoScheduler
       ```
    
    2. 在 New 函数中初始化：
       - 创建 AITagAutoScheduler 实例
       - 注入 imageRepo, taskPlatformSvc, config
       
    参考 app.go 中现有的 service 初始化模式
  </action>
  <verify>
    <automated>go build ./cmd/server/...</automated>
  </verify>
  <done>
    - App 结构包含 autoScheduler 字段
    - 编译通过
  </done>
</task>

<task type="auto">
  <name>task 2: 在 Run 方法中启动调度服务</name>
  <files>
    internal/app/app.go
  </files>
  <action>
    1. 在 Run 方法中添加启动逻辑：
       ```go
       // Start AI tag auto scheduler
       if a.config.AutoAITag.Enabled {
           go a.autoScheduler.Start(context.Background())
           log.Printf("AI 标签自动调度服务已启动，扫描间隔: %d 分钟", a.config.AutoAITag.ScanIntervalMin)
       }
       ```
    
    2. 添加配置变更回调：
       - 当 AutoAITag.Enabled 变化时重启服务
       
    参考 Run 方法中现有的 refill loop 和 job recovery 模式
  </action>
  <verify>
    <automated>go test ./internal/app/... -run "Run|Scheduler" -count=1</automated>
  </verify>
  <done>
    - 配置启用时调度服务自动启动
    - 启动日志正确输出
  </done>
</task>

<task type="auto">
  <name>task 3: 在 Shutdown 方法中停止调度服务</name>
  <files>
    internal/app/app.go
  </files>
  <action>
    1. 在 Shutdown 方法中添加停止逻辑：
       ```go
       // Stop AI tag auto scheduler
       if a.autoScheduler != nil {
           a.autoScheduler.Stop()
       }
       ```
    
    2. 确保停止顺序正确：
       - 先停止调度服务
       - 再停止 jobManager
       - 最后关闭数据库连接
       
    参考 Shutdown 方法中现有的资源释放顺序
  </action>
  <verify>
    <automated>go test ./internal/app/... -run "Shutdown" -count=1</automated>
  </verify>
  <done>
    - 应用关闭时调度服务正确停止
    - 资源释放顺序正确
  </done>
</task>

<task type="auto">
  <name>task 4: 添加配置热重载支持</name>
  <files>
    internal/app/app.go
  </files>
  <action>
    1. 在 Run 方法中添加配置变更回调：
       ```go
       a.cfgReloader.OnChange(func(old, new *config.Config) {
           if old.AutoAITag.Enabled != new.AutoAITag.Enabled {
               if new.AutoAITag.Enabled {
                   go a.autoScheduler.Start(context.Background())
                   log.Printf("AI 标签自动调度服务已启动")
               } else {
                   a.autoScheduler.Stop()
                   log.Printf("AI 标签自动调度服务已停止")
               }
           }
           if old.AutoAITag.ScanIntervalMin != new.AutoAITag.ScanIntervalMin {
               // 重建 scheduler 以应用新间隔
               a.autoScheduler.Stop()
               a.autoScheduler = service.NewAITagAutoScheduler(...)
               go a.autoScheduler.Start(context.Background())
               log.Printf("AI 标签自动调度服务已重启，新间隔: %d 分钟", new.AutoAITag.ScanIntervalMin)
           }
       })
       ```
  </action>
  <verify>
    <automated>go test ./internal/app/... -run "ConfigReload|OnChange" -count=1</automated>
  </verify>
  <done>
    - 配置变更时调度服务正确响应
    - 热重载生效
  </done>
</task>

</tasks>

<verification>
- [ ] App 结构包含 autoScheduler
- [ ] Run 方法启动调度服务（配置启用时）
- [ ] Shutdown 方法停止调度服务
- [ ] 配置热重载支持
- [ ] 编译通过：`go build ./cmd/server/...`
</verification>

<success_criteria>
1. 应用启动时，配置启用则自动调度服务启动
2. 应用关闭时，调度服务正确停止
3. 配置热重载可动态启用/禁用调度服务
4. 日志正确输出启动和停止信息
</success_criteria>

<output>
After completion, create `.planning/phases/12-import-task-auto-scheduling/12-03-SUMMARY.md`
</output>