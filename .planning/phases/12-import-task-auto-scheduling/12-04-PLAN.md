---
phase: 12-import-task-auto-scheduling
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - internal/service/ai_tag_auto_scheduler_test.go
  - internal/app/integration_test.go
autonomous: true
requirements:
  - AIQ-01
  - AIQ-02
user_setup: []

must_haves:
  truths:
    - "导入新图片后，定时扫描会自动为无 AI 标签的图片创建任务"
    - "已有 AI 标签的图片不会重复入队"
    - "配置关闭时不会自动入队"
  artifacts:
    - path: "internal/service/ai_tag_auto_scheduler_test.go"
      provides: "集成测试验证完整链路"
      contains: "TestAutoSchedulingE2E"
  key_links:
    - from: "定时扫描"
      to: "platform_tasks 表"
      via: "ScanAndEnqueue"
      pattern: "INSERT.*platform_tasks.*ai_tag_generation"
---

# Phase 12 Plan 04: 端到端验证与真实批次测试

<objective>
验证导入后自动调度链路在真实场景中的完整行为，确保所有组件正确协作。

Purpose: 确保 Phase 12 实现满足所有成功标准。
Output: 集成测试、验证报告。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-import-task-auto-scheduling/12-CONTEXT.md
@.planning/phases/12-import-task-auto-scheduling/12-RESEARCH.md
@.planning/phases/12-import-task-auto-scheduling/12-VALIDATION.md

<interfaces>
<!-- 现有相关接口 -->

From internal/service/ai_tag_auto_scheduler.go:
```go
func (s *AITagAutoScheduler) ScanAndEnqueue(ctx context.Context) (int, error)
```

From internal/repository/image_repository.go:
```go
func (r *sqliteImageRepository) FindImagesWithoutAITags(ctx context.Context, limit int) ([]domain.Image, error)
```

From internal/service/task_platform_service.go:
```go
func (s *TaskPlatformService) PlanBatch(ctx context.Context, req TaskPlatformPlanRequest) (*TaskPlatformPlanResult, error)
func (s *TaskPlatformService) QueueTask(ctx context.Context, task *domain.PlatformTask, jobType, payload string) (*domain.AsyncJob, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 添加集成测试验证自动入队条件</name>
  <files>
    internal/service/ai_tag_auto_scheduler_test.go
  </files>
  <behavior>
    - Test 1: 缩略图完成 + 无 AI 标签的图片会被入队
    - Test 2: 缩略图未完成的图片不会被入队
    - Test 3: 已有 AI 标签的图片不会被入队
    - Test 4: 配置关闭时不会执行扫描
    - Test 5: 已有 pending/queued 任务时去重跳过
  </behavior>
  <action>
    1. 在 ai_tag_auto_scheduler_test.go 添加集成测试：
       ```go
       func TestAutoEnqueueCondition(t *testing.T) {
           // Setup: 创建测试数据库、图片、缩略图
           // Case 1: 图片有缩略图，无 AI 标签 → 入队
           // Case 2: 图片无缩略图 → 不入队
           // Case 3: 图片有 AI 标签 → 不入队
           // Case 4: 配置关闭 → 不扫描
           // Case 5: 已有任务 → 去重
       }
       ```
    
    2. 使用真实的 SQLite 内存数据库
    3. 验证 platform_tasks 表的任务创建
    
    参考 RESEARCH.md 的验证命令和测试场景
  </action>
  <verify>
    <automated>go test ./internal/service/... -run "AutoEnqueueCondition" -count=1</automated>
  </verify>
  <done>
    - 集成测试覆盖 5 种场景
    - 测试全部通过
  </done>
</task>

<task type="auto">
  <name>task 2: 验证批量入队行为</name>
  <files>
    internal/service/ai_tag_auto_scheduler_test.go
  </files>
  <action>
    1. 添加批量测试：
       ```go
       func TestAutoEnqueueBatchLimit(t *testing.T) {
           // Setup: 创建 150 张符合条件的图片
           // Action: 调用 ScanAndEnqueue
           // Assert: 只入队 100 张（配置的 BatchSize）
           // Action: 再次调用 ScanAndEnqueue
           // Assert: 剩余 50 张入队
       }
       ```
    
    2. 验证分批处理逻辑正确
  </action>
  <verify>
    <automated>go test ./internal/service/... -run "BatchLimit" -count=1</automated>
  </verify>
  <done>
    - 批量限制正确生效
    - 分批处理逻辑正确
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 12 完整的自动调度功能：
    - 数据库迁移：image_tag.source 字段
    - 查询方法：FindImagesWithoutAITags
    - 定时服务：AITagAutoScheduler
    - 应用集成：启动/关闭/热重载
  </what-built>
  <how-to-verify>
    1. 启动服务：`go run cmd/server/main.go`
    2. 检查启动日志：应看到 "AI 标签自动调度服务已启动"
    3. 导入测试图片（或使用已有图片）
    4. 等待 5 分钟后检查数据库：
       ```sql
       SELECT COUNT(*) FROM platform_tasks 
       WHERE task_type='ai_tag_generation' 
       AND source_type='import_scan';
       ```
    5. 验证日志中有扫描执行记录
  </how-to-verify>
  <resume-signal>验证通过或描述问题</resume-signal>
</task>

</tasks>

<verification>
- [ ] 集成测试覆盖自动入队条件
- [ ] 批量限制测试通过
- [ ] 人工验证成功
- [ ] 所有测试通过：`go test ./internal/... -count=1`
</verification>

<success_criteria>
1. 用户导入图片后，符合条件的图片会自动加入 AI 打标签队列（验证：platform_tasks 表有新记录）
2. 默认只有没有 AI 标签的图片会自动进入 AI 打标签队列（验证：有 AI 标签的图片不入队）
3. 导入后的处理不再依赖逐图人工触发（验证：无需手动触发，任务自动入队）
</success_criteria>

<output>
After completion, create `.planning/phases/12-import-task-auto-scheduling/12-04-SUMMARY.md`
</output>