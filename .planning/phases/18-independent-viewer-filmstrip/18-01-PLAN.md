---
phase: 18-independent-viewer-filmstrip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/pubspec.yaml
  - flutter_app/lib/main.dart
  - flutter_app/lib/utils/window_manager.dart
  - flutter_app/lib/services/viewer_window_service.dart
  - flutter_app/lib/models/viewer_session.dart
  - flutter_app/lib/app/viewer_window_app.dart
  - flutter_app/windows/runner/main.cpp
  - flutter_app/test/services/viewer_window_service_test.dart
  - flutter_app/test/models/viewer_session_test.dart
autonomous: true
requirements: [VIEW-01]

must_haves:
  truths:
    - "Double-click launches a true non-modal secondary viewer window instead of route-only detail navigation"
    - "Multiple viewer windows can exist concurrently while the main shell remains interactive"
    - "Every new viewer window opens with the same default size and centered placement without restoring prior memory"
    - "Viewer launch logic is isolated behind a service/session boundary rather than leaking plugin calls into gallery widgets"
  artifacts:
    - path: "flutter_app/lib/services/viewer_window_service.dart"
      provides: "True multi-window viewer launch coordinator for Windows desktop"
      exports: ["ViewerWindowService"]
    - path: "flutter_app/lib/models/viewer_session.dart"
      provides: "Serializable result-set-scoped viewer session payload"
      exports: ["ViewerSession", "ViewerSessionItem"]
    - path: "flutter_app/lib/app/viewer_window_app.dart"
      provides: "Dedicated app/bootstrap entry for spawned viewer windows"
      exports: ["ViewerWindowApp"]
    - path: "flutter_app/test/services/viewer_window_service_test.dart"
      provides: "Automated contract coverage for secondary-window launch translation"
  key_links:
    - from: "flutter_app/lib/main.dart"
      to: "flutter_app/lib/app/viewer_window_app.dart"
      via: "desktop bootstrap branch for spawned viewer windows"
      pattern: "viewer-window"
    - from: "flutter_app/lib/services/viewer_window_service.dart"
      to: "flutter_app/lib/models/viewer_session.dart"
      via: "serialized session payload"
      pattern: "toJson"
    - from: "flutter_app/lib/utils/window_manager.dart"
      to: "flutter_app/windows/runner/main.cpp"
      via: "shared default viewer window sizing/title policy"
      pattern: "viewer"
---

<objective>
Establish the true Windows secondary-window host required for Phase 18 before any gallery/search entry-point rewiring.

Purpose: This plan implements the architectural prerequisite for `VIEW-01` and locks in D-01, D-02, and the desktop portion of the UI spec so later plans can target real multi-window delivery instead of route simulation.

Output: A tested viewer-session payload model, a Windows-capable viewer launch service, and desktop bootstrap paths that can open multiple independent viewer windows with stable default sizing/title behavior.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md
@.planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md
@.planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-03-SUMMARY.md

<interfaces>
From `flutter_app/lib/main.dart`:
```dart
void main() async
class MyApp extends StatelessWidget
```

From `flutter_app/lib/utils/window_manager.dart`:
```dart
class AppWindowManager {
  static Future<void> ensureInitialized() async
  static Future<void> setTitle(String title) async
  static Future<void> close() async
}
```

From `18-CONTEXT.md` locked decisions:
```text
D-01 non-modal independent viewer window, multiple windows allowed
D-02 no window size/position memory in Phase 18
D-03 result-set-scoped session payload
```

From `18-UI-SPEC.md`:
```text
Viewer window title pattern: ACGWarehouse Viewer — {filename}
Phase 18 targets true multi-window Windows desktop behavior
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Red-green the viewer session payload and launch coordinator seam</name>
  <files>
    flutter_app/test/models/viewer_session_test.dart,
    flutter_app/test/services/viewer_window_service_test.dart,
    flutter_app/lib/models/viewer_session.dart,
    flutter_app/lib/services/viewer_window_service.dart,
    flutter_app/lib/utils/window_manager.dart
  </files>
  <read_first>
    flutter_app/lib/models/image.dart,
    flutter_app/lib/utils/window_manager.dart,
    flutter_app/lib/main.dart,
    flutter_app/test/services/import_service_test.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md,
    .planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a viewer session built from a result set preserves launch source, ordered item snapshot, and initial selected index for the clicked image
    - Test 2: window title formatting follows `ACGWarehouse Viewer — {filename}` exactly
    - Test 3: viewer launch requests map to a default centered viewer window policy and do not read or write persisted window memory
    - Test 4: the coordinator can issue distinct launch requests for two different sessions without collapsing to a singleton viewer
  </behavior>
  <action>
    1. Add failing tests in `flutter_app/test/models/viewer_session_test.dart` that define the serializable session contract around result-set scope: `gallery`/`search` source, ordered `ViewerSessionItem` snapshot, selected index resolution from clicked image id, and JSON round-trip safety for spawned windows.
    2. Add failing tests in `flutter_app/test/services/viewer_window_service_test.dart` using a fake multi-window adapter interface. Assert `ViewerWindowService` produces:
       - window titles in the exact Phase 18 copy format,
       - default viewer size/center policy separate from the main shell,
       - one launch call per request so multiple viewer windows remain possible,
       - no persistence API usage for D-02.
    3. Create `flutter_app/lib/models/viewer_session.dart` with plain, serializable session types built from `ImageModel` data only. Do not depend on live provider instances inside spawned windows.
    4. Create `flutter_app/lib/services/viewer_window_service.dart` with a thin adapter boundary for the chosen true multi-window package. Keep plugin-specific calls isolated here and keep route/page widgets unaware of native window APIs.
    5. Refactor `flutter_app/lib/utils/window_manager.dart` into explicit main-window and viewer-window policy helpers (`mainWindowOptions`, `viewerWindowOptions`, title builders, default dimensions) while preserving current main-shell behavior.
    6. After green, stage and commit the RED/GREEN seam as:
       - `test(18-01): add failing viewer session and launch coordinator coverage`
       - `feat(18-01): add viewer session model and launch coordinator seam`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/models/viewer_session_test.dart test/services/viewer_window_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/models/viewer_session.dart` defines serializable viewer session types
    - `flutter_app/lib/services/viewer_window_service.dart` contains title formatting `ACGWarehouse Viewer —`
    - `flutter_app/lib/utils/window_manager.dart` contains distinct viewer window defaults
    - `flutter_app/test/models/viewer_session_test.dart` exits `0`
    - `flutter_app/test/services/viewer_window_service_test.dart` exits `0`
  </acceptance_criteria>
  <done>The viewer can be launched through a test-covered service/session boundary that honors D-01, D-02, and result-set-scoped payload rules before native bootstrap wiring begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap Windows secondary-window hosting and viewer app entry</name>
  <files>
    flutter_app/pubspec.yaml,
    flutter_app/lib/main.dart,
    flutter_app/lib/app/viewer_window_app.dart,
    flutter_app/lib/services/viewer_window_service.dart,
    flutter_app/lib/utils/window_manager.dart,
    flutter_app/windows/runner/main.cpp,
    flutter_app/test/services/viewer_window_service_test.dart
  </files>
  <read_first>
    flutter_app/pubspec.yaml,
    flutter_app/lib/main.dart,
    flutter_app/windows/runner/main.cpp,
    flutter_app/lib/utils/window_manager.dart,
    flutter_app/lib/services/viewer_window_service.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md,
    .planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: desktop bootstrap can distinguish main-shell startup from spawned viewer-window startup
    - Test 2: spawned viewer windows initialize only the viewer app shell needed for session rendering
    - Test 3: Windows bootstrap remains compatible with the main shell while enabling true secondary windows
  </behavior>
  <action>
    1. Add one proven true multi-window dependency to `flutter_app/pubspec.yaml`; do not introduce unrelated state-management or viewer-framework churn.
    2. Extend `flutter_app/lib/main.dart` so desktop startup branches cleanly between:
       - the existing main-shell `MyApp`, and
       - a new `ViewerWindowApp` entry that reads serialized launch arguments/session payload and applies viewer window defaults.
    3. Create `flutter_app/lib/app/viewer_window_app.dart` as a focused desktop-only app shell for secondary windows. It should set viewer-specific title behavior and host a placeholder/test harness surface until Plan 02 supplies the full workspace.
    4. Update `flutter_app/windows/runner/main.cpp` only as required by the chosen plugin/bootstrap path so secondary windows can be created on Windows. Keep packaging concerns out of scope.
    5. Re-run and extend `flutter_app/test/services/viewer_window_service_test.dart` with any new bootstrap-translation assertions needed after integrating the real plugin boundary.
    6. Stage and commit the bootstrap slice as:
       - `test(18-01): extend failing desktop bootstrap coverage for viewer windows`
       - `feat(18-01): add Windows secondary-window bootstrap for viewer host`
       - `refactor(18-01): extract shared desktop window helpers`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/models/viewer_session_test.dart test/services/viewer_window_service_test.dart</automated>
    <automated>cd flutter_app; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/pubspec.yaml` includes exactly one true multi-window dependency in addition to existing stack
    - `flutter_app/lib/main.dart` contains a viewer-window bootstrap branch
    - `flutter_app/lib/app/viewer_window_app.dart` exists and hosts the spawned-window app entry
    - `flutter_app/windows/runner/main.cpp` is updated for the chosen secondary-window bootstrap path
    - `flutter test test/models/viewer_session_test.dart test/services/viewer_window_service_test.dart` exits `0`
    - `flutter test` exits `0`
  </acceptance_criteria>
  <done>Windows desktop can spawn independent viewer windows through a dedicated viewer app entry without regressing the main shell bootstrap.</done>
</task>

</tasks>

<verification>
- Confirm all viewer-host tests pass before any gallery/search widget integration begins.
- Manually smoke-check that two spawned viewer windows can coexist while the main shell stays open.
- Confirm no persisted window-memory behavior was introduced.
</verification>

<success_criteria>
- `VIEW-01` has a real Windows multi-window host path, not a route-only stand-in.
- Later plans can plug a reusable viewer workspace into a spawned window without reworking bootstrap.
</success_criteria>

<commit_strategy>
- Commit 1: `test(18-01): add failing viewer session and launch coordinator coverage`
- Commit 2: `feat(18-01): add viewer session model and launch coordinator seam`
- Commit 3: `test(18-01): extend failing desktop bootstrap coverage for viewer windows`
- Commit 4: `feat(18-01): add Windows secondary-window bootstrap for viewer host`
- Commit 5: `refactor(18-01): extract shared desktop window helpers`
</commit_strategy>

<output>
After completion, create `.planning/phases/18-independent-viewer-filmstrip/18-01-SUMMARY.md`
</output>
