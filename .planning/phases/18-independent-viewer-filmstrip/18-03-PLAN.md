---
phase: 18-independent-viewer-filmstrip
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - flutter_app/lib/app/fluent_screens.dart
  - flutter_app/lib/widgets/fluent_gallery_content.dart
  - flutter_app/lib/widgets/fluent_search_content.dart
  - flutter_app/lib/widgets/fluent_image_card.dart
  - flutter_app/lib/providers/image_provider.dart
  - flutter_app/lib/providers/search_provider.dart
  - flutter_app/lib/services/viewer_window_service.dart
  - flutter_app/lib/app/viewer_window_app.dart
  - flutter_app/test/app/fluent_screens_test.dart
  - flutter_app/test/widgets/fluent_gallery_content_test.dart
  - flutter_app/test/widgets/fluent_search_content_test.dart
  - flutter_app/test/widgets/fluent_image_card_test.dart
  - flutter_app/test/services/viewer_window_service_test.dart
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, VIEW-04]

must_haves:
  truths:
    - "Gallery and search both launch independent viewer windows on desktop double-click using the current result set snapshot"
    - "The viewer window title and filmstrip session reflect the originating image and result context"
    - "Main-shell browsing, search, and filtering remain usable while one or more viewer windows are open"
    - "Phase 18 ships as real multi-window desktop behavior with automated regression coverage plus repeatable desktop smoke verification"
  artifacts:
    - path: "flutter_app/lib/app/fluent_screens.dart"
      provides: "Desktop gallery/search entry-point orchestration for viewer launch"
      exports: ["FluentGalleryPage", "FluentSearchPage"]
    - path: "flutter_app/lib/widgets/fluent_image_card.dart"
      provides: "Desktop double-click affordance for independent viewer launch"
      exports: ["FluentImageCard"]
    - path: "flutter_app/test/app/fluent_screens_test.dart"
      provides: "Regression coverage for desktop viewer launch orchestration"
    - path: "flutter_app/test/widgets/fluent_image_card_test.dart"
      provides: "Gesture-level coverage for single-click vs double-click semantics"
  key_links:
    - from: "flutter_app/lib/app/fluent_screens.dart"
      to: "flutter_app/lib/services/viewer_window_service.dart"
      via: "desktop viewer launch requests"
      pattern: "ViewerWindowService"
    - from: "flutter_app/lib/app/fluent_screens.dart"
      to: "flutter_app/lib/providers/image_provider.dart"
      via: "gallery result-set snapshot"
      pattern: "images"
    - from: "flutter_app/lib/app/fluent_screens.dart"
      to: "flutter_app/lib/providers/search_provider.dart"
      via: "search result-set snapshot"
      pattern: "results"
---

<objective>
Replace route-push detail behavior with true desktop viewer launch orchestration from gallery and search, then close the phase with verification.

Purpose: This plan delivers the end-user Phase 18 workflow by wiring the already-built host and workspace into the real shell entry points while preserving Phase 17 shell behavior and avoiding out-of-scope tag/ops/performance work.

Output: Gallery/search double-click launches independent viewer windows with result-set filmstrip context, automated regressions, and repeatable desktop smoke checks.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md
@.planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md
@.planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-03-SUMMARY.md

<interfaces>
From `flutter_app/lib/providers/image_provider.dart`:
```dart
List<ImageModel> get images
```

From `flutter_app/lib/providers/search_provider.dart`:
```dart
List<ImageModel> get results
String get currentQuery
```

From `flutter_app/lib/app/fluent_screens.dart`:
```dart
void _showImageDetail(BuildContext context, ImageModel image)
```

From `18-UI-SPEC.md`:
```text
Double-clicking an image from gallery or search opens a separate non-blocking viewer window
Keyboard navigation and close should function without requiring focus inside sidebar or filmstrip first
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Red-green gallery and search launch orchestration</name>
  <files>
    flutter_app/test/app/fluent_screens_test.dart,
    flutter_app/test/widgets/fluent_gallery_content_test.dart,
    flutter_app/test/widgets/fluent_search_content_test.dart,
    flutter_app/test/widgets/fluent_image_card_test.dart,
    flutter_app/test/services/viewer_window_service_test.dart,
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/widgets/fluent_gallery_content.dart,
    flutter_app/lib/widgets/fluent_search_content.dart,
    flutter_app/lib/widgets/fluent_image_card.dart,
    flutter_app/lib/services/viewer_window_service.dart
  </files>
  <read_first>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/widgets/fluent_gallery_content.dart,
    flutter_app/lib/widgets/fluent_search_content.dart,
    flutter_app/lib/widgets/fluent_image_card.dart,
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/lib/providers/search_provider.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: double-clicking a gallery image launches `ViewerWindowService` with `ImageListProvider.images` as the session result set and the clicked image index
    - Test 2: double-clicking a search result launches `ViewerWindowService` with `SearchProvider.results` as the session result set and the clicked image index
    - Test 3: single-click remains non-launching so gallery/search browsing does not open windows accidentally
    - Test 4: the legacy `Navigator.push(ImageDetailScreen(...))` desktop path is removed from Fluent gallery/search entry points
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/widgets/fluent_image_card_test.dart` for desktop double-click versus single-click semantics.
    2. Extend `flutter_app/test/widgets/fluent_gallery_content_test.dart` and `flutter_app/test/widgets/fluent_search_content_test.dart` so result-card gestures are proven to funnel through a viewer-launch callback instead of route push behavior.
    3. Extend `flutter_app/test/app/fluent_screens_test.dart` with fake providers and a fake `ViewerWindowService` to prove gallery and search entry points build correct result-set-scoped viewer sessions.
    4. Refactor `flutter_app/lib/widgets/fluent_image_card.dart` to expose an explicit double-click/open callback for desktop use while preserving current hover visuals.
    5. Refactor `flutter_app/lib/widgets/fluent_gallery_content.dart`, `flutter_app/lib/widgets/fluent_search_content.dart`, and `flutter_app/lib/app/fluent_screens.dart` so desktop launches flow through `ViewerWindowService` and no longer use page-level `Navigator.push(ImageDetailScreen(...))` in Phase 18 paths.
    6. Keep route-based detail behavior out of the shipped desktop path; if a compatibility path is needed for non-Windows targets, isolate it behind platform gating rather than mixing it into the desktop launch code.
    7. Stage and commit integration coverage/wiring as:
       - `test(18-03): add failing gallery and search viewer launch coverage`
       - `feat(18-03): wire independent viewer launches from gallery and search`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/app/fluent_screens_test.dart test/widgets/fluent_gallery_content_test.dart test/widgets/fluent_search_content_test.dart test/widgets/fluent_image_card_test.dart test/services/viewer_window_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/app/fluent_screens.dart` references `ViewerWindowService` for desktop viewer launches
    - `flutter_app/lib/widgets/fluent_image_card.dart` exposes desktop double-click launch behavior
    - Fluent desktop gallery/search code no longer uses `Navigator.push(ImageDetailScreen(...))` for the Phase 18 shipped path
    - `flutter test test/app/fluent_screens_test.dart test/widgets/fluent_gallery_content_test.dart test/widgets/fluent_search_content_test.dart test/widgets/fluent_image_card_test.dart test/services/viewer_window_service_test.dart` exits `0`
  </acceptance_criteria>
  <done>Desktop gallery and search launch real viewer windows from result-set context using deliberate double-click semantics instead of page navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount the viewer workspace in spawned windows and close the phase with verification</name>
  <files>
    flutter_app/lib/app/viewer_window_app.dart,
    flutter_app/lib/services/viewer_window_service.dart,
    flutter_app/test/app/fluent_screens_test.dart,
    flutter_app/test/services/viewer_window_service_test.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-VALIDATION.md
  </files>
  <read_first>
    flutter_app/lib/app/viewer_window_app.dart,
    flutter_app/lib/screens/viewer/viewer_workspace.dart,
    flutter_app/lib/services/viewer_window_service.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md,
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Test 1: spawned viewer windows mount `ViewerWorkspace` using only the serialized viewer session payload
    - Test 2: title updates follow the currently selected image as the viewer session changes
    - Test 3: verification artifacts capture multi-window smoke evidence, keyboard close/navigation evidence, and out-of-scope guardrails
  </behavior>
  <action>
    1. Update `flutter_app/lib/app/viewer_window_app.dart` so the spawned window renders the real `ViewerWorkspace` from Plan 02 instead of a placeholder host.
    2. Finalize `flutter_app/lib/services/viewer_window_service.dart` title-update hooks so changing the active image updates the current viewer window title without affecting sibling windows.
    3. Extend `flutter_app/test/services/viewer_window_service_test.dart` and, if needed, `flutter_app/test/app/fluent_screens_test.dart` with assertions around active-title updates and session isolation between concurrent viewers.
    4. Create `.planning/phases/18-independent-viewer-filmstrip/18-VALIDATION.md` with repeatable evidence requirements and execution commands for:
       - opening at least two viewer windows concurrently,
       - proving the main shell remains interactive,
       - proving `Esc` closes only the current viewer,
       - proving `←/→` stays inside the current viewer session,
       - proving no edit/share/delete/slideshow/tag-governance actions were added.
    5. Run the focused automated suites first, then a full `flutter test`, then one repeatable Windows desktop smoke pass. Stop after the first fully green verification pass.
    6. Stage and commit the final integration/verification slice as:
       - `test(18-03): extend viewer host coverage for mounted workspace and title updates`
       - `feat(18-03): mount viewer workspace in spawned windows`
       - `docs(18-03): add phase validation procedure for independent viewer windows`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/app/fluent_screens_test.dart test/widgets/fluent_gallery_content_test.dart test/widgets/fluent_search_content_test.dart test/widgets/fluent_image_card_test.dart test/services/viewer_window_service_test.dart test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_stage_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart</automated>
    <automated>cd flutter_app; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/app/viewer_window_app.dart` mounts `ViewerWorkspace`
    - `flutter_app/lib/services/viewer_window_service.dart` updates titles per active image without collapsing multi-window behavior
    - `.planning/phases/18-independent-viewer-filmstrip/18-VALIDATION.md` exists with multi-window smoke procedures and out-of-scope checks
    - `flutter test test/app/fluent_screens_test.dart test/widgets/fluent_gallery_content_test.dart test/widgets/fluent_search_content_test.dart test/widgets/fluent_image_card_test.dart test/services/viewer_window_service_test.dart test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_stage_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart` exits `0`
    - `flutter test` exits `0`
  </acceptance_criteria>
  <done>The shipped Phase 18 path is real Windows multi-window viewer delivery with automated regressions and a repeatable validation playbook.</done>
</task>

</tasks>

<verification>
- Confirm focused integration suites pass before full `flutter test`.
- Capture one successful Windows smoke verification pass and stop there.
- Confirm the final desktop path is independent-window delivery, not route-only simulation.
</verification>

<success_criteria>
- All Phase 18 requirements (`VIEW-01` through `VIEW-04`) are closed by real desktop viewer behavior.
- Phase 17 browsing/search shell remains intact while independent viewers open and operate concurrently.
</success_criteria>

<commit_strategy>
- Commit 1: `test(18-03): add failing gallery and search viewer launch coverage`
- Commit 2: `feat(18-03): wire independent viewer launches from gallery and search`
- Commit 3: `test(18-03): extend viewer host coverage for mounted workspace and title updates`
- Commit 4: `feat(18-03): mount viewer workspace in spawned windows`
- Commit 5: `docs(18-03): add phase validation procedure for independent viewer windows`
</commit_strategy>

<output>
After completion, create `.planning/phases/18-independent-viewer-filmstrip/18-03-SUMMARY.md`
</output>
