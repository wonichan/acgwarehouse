---
phase: 18-independent-viewer-filmstrip
plan: 02
type: tdd
wave: 2
depends_on: [18-01]
files_modified:
  - flutter_app/lib/screens/viewer/viewer_workspace.dart
  - flutter_app/lib/screens/viewer/viewer_stage.dart
  - flutter_app/lib/screens/viewer/viewer_filmstrip.dart
  - flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart
  - flutter_app/lib/screens/viewer/viewer_keyboard_scope.dart
  - flutter_app/lib/models/viewer_session.dart
  - flutter_app/lib/screens/image_detail_screen.dart
  - flutter_app/lib/screens/image_gallery_viewer.dart
  - flutter_app/test/screens/viewer/viewer_workspace_test.dart
  - flutter_app/test/screens/viewer/viewer_stage_test.dart
  - flutter_app/test/screens/viewer/viewer_metadata_sidebar_test.dart
autonomous: true
requirements: [VIEW-02, VIEW-03, VIEW-04]

must_haves:
  truths:
    - "Viewer content renders as a three-region desktop workspace: dominant image stage, fixed right metadata sidebar, bottom filmstrip"
    - "Filmstrip follows the launch result set with medium-density thumbnails and a selected state stronger than color alone"
    - "Double-click toggles fit-to-window ↔ 2x, pan works while zoomed, and switching images resets state to fit"
    - "Sidebar shows filename, format, resolution, size, path, imported time, and tags without introducing editing/governance actions"
  artifacts:
    - path: "flutter_app/lib/screens/viewer/viewer_workspace.dart"
      provides: "Container-agnostic desktop viewer composition"
      exports: ["ViewerWorkspace"]
    - path: "flutter_app/lib/screens/viewer/viewer_stage.dart"
      provides: "ExtendedImage-based fit/zoom/pan interaction surface"
      exports: ["ViewerStage"]
    - path: "flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart"
      provides: "Reusable Phase 18 metadata and tags sidebar"
      exports: ["ViewerMetadataSidebar"]
    - path: "flutter_app/lib/screens/viewer/viewer_filmstrip.dart"
      provides: "Result-set-scoped filmstrip with selected-item context"
      exports: ["ViewerFilmstrip"]
  key_links:
    - from: "flutter_app/lib/screens/viewer/viewer_workspace.dart"
      to: "flutter_app/lib/screens/viewer/viewer_stage.dart"
      via: "current image stage composition"
      pattern: "ViewerStage"
    - from: "flutter_app/lib/screens/viewer/viewer_workspace.dart"
      to: "flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart"
      via: "fixed 320px details rail"
      pattern: "ViewerMetadataSidebar"
    - from: "flutter_app/lib/screens/viewer/viewer_workspace.dart"
      to: "flutter_app/lib/screens/viewer/viewer_filmstrip.dart"
      via: "result-set filmstrip composition"
      pattern: "ViewerFilmstrip"
---

<objective>
Build the reusable viewer workspace surface that will run both under widget tests and inside spawned viewer windows.

Purpose: This plan delivers the behavior-heavy Phase 18 UI contract for `VIEW-02`, `VIEW-03`, and `VIEW-04` while keeping it container-agnostic enough to slot into the real multi-window host created by Plan 01.

Output: A test-covered desktop viewer workspace with stage, filmstrip, metadata sidebar, keyboard scope, and reset-on-switch semantics.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md
@.planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md
@.planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-03-SUMMARY.md

<interfaces>
From `flutter_app/lib/models/image.dart`:
```dart
class ImageModel {
  final int id;
  final String path;
  final String filename;
  final int fileSize;
  final int width;
  final int height;
  final String format;
  final DateTime createdAt;
  String get displayFileSize;
}
```

From `flutter_app/lib/screens/image_gallery_viewer.dart`:
```dart
ExtendedImage.network(... mode: ExtendedImageMode.gesture ...)
```

From `18-CONTEXT.md` locked decisions:
```text
D-04 medium-density filmstrip with current-item highlight
D-05 main image + fixed right metadata sidebar + bottom filmstrip
D-06 extended metadata set including path and imported time
D-07 double-click zoom toggles fit ↔ 2x
D-08 image switch resets viewer state to fit
D-09 keyboard scope is ←/→ and Esc only
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Red-green the composed viewer workspace contract</name>
  <files>
    flutter_app/test/screens/viewer/viewer_workspace_test.dart,
    flutter_app/test/screens/viewer/viewer_metadata_sidebar_test.dart,
    flutter_app/lib/screens/viewer/viewer_workspace.dart,
    flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart,
    flutter_app/lib/screens/image_detail_screen.dart
  </files>
  <read_first>
    flutter_app/lib/screens/image_detail_screen.dart,
    flutter_app/lib/models/image.dart,
    flutter_app/lib/models/viewer_session.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md,
    .planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the viewer workspace renders top chrome, dominant image region, fixed right sidebar, and bottom filmstrip in one desktop composition
    - Test 2: the sidebar heading is `Image Details` and metadata labels render `Filename`, `Format`, `Resolution`, `Size`, `Path`, `Imported`, `Tags`
    - Test 3: loading and failure states keep the three-region shell visible instead of collapsing to a blank page
    - Test 4: the extracted sidebar omits AI-tag triggers, add-tag dialogs, edit-tag flows, and other Phase 19 actions
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/screens/viewer/viewer_workspace_test.dart` for the desktop three-region layout, copy contract, and shell-preserving loading/failure states.
    2. Add failing widget tests in `flutter_app/test/screens/viewer/viewer_metadata_sidebar_test.dart` for the full metadata field set and tag rendering behavior.
    3. Create `flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart` by extracting only the reusable metadata/tags presentation logic from `flutter_app/lib/screens/image_detail_screen.dart`. Leave all AI/tag-management actions behind.
    4. Create `flutter_app/lib/screens/viewer/viewer_workspace.dart` as the container-agnostic viewer composition with:
       - lightweight title/chrome band,
       - central workspace row,
       - `320px` default right sidebar,
       - fixed bottom filmstrip surface.
    5. Trim `flutter_app/lib/screens/image_detail_screen.dart` only as needed to consume shared formatting helpers/widgets without changing its current Phase 17 behavior.
    6. Stage and commit this extraction/composition slice as:
       - `test(18-02): add failing viewer workspace and metadata sidebar coverage`
       - `feat(18-02): add reusable viewer workspace shell and metadata sidebar`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/screens/viewer/viewer_workspace.dart` contains the three-region viewer composition
    - `flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart` contains `Image Details`
    - `flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart` contains `Filename`, `Format`, `Resolution`, `Size`, `Path`, `Imported`, and `Tags`
    - `flutter_app/lib/screens/viewer/viewer_metadata_sidebar.dart` does not expose AI-tag mutation controls
    - `flutter test test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart` exits `0`
  </acceptance_criteria>
  <done>The viewer shell and reusable metadata sidebar exist as independent, test-covered widgets aligned with D-05 and D-06.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Red-green viewer stage, filmstrip, and keyboard behavior</name>
  <files>
    flutter_app/test/screens/viewer/viewer_stage_test.dart,
    flutter_app/test/screens/viewer/viewer_workspace_test.dart,
    flutter_app/lib/screens/viewer/viewer_stage.dart,
    flutter_app/lib/screens/viewer/viewer_filmstrip.dart,
    flutter_app/lib/screens/viewer/viewer_keyboard_scope.dart,
    flutter_app/lib/screens/viewer/viewer_workspace.dart,
    flutter_app/lib/screens/image_gallery_viewer.dart
  </files>
  <read_first>
    flutter_app/lib/screens/image_gallery_viewer.dart,
    flutter_app/lib/models/viewer_session.dart,
    flutter_app/lib/screens/viewer/viewer_workspace.dart,
    .planning/phases/18-independent-viewer-filmstrip/18-CONTEXT.md,
    .planning/phases/18-independent-viewer-filmstrip/18-UI-SPEC.md,
    .planning/phases/18-independent-viewer-filmstrip/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: initial image state is fit-to-window and double-click toggles to 2x then back to fit
    - Test 2: switching images via filmstrip or keyboard resets the stage state to fit rather than inheriting prior zoom/pan
    - Test 3: the selected filmstrip thumbnail has border/contrast emphasis and nearby context remains visible
    - Test 4: `←` and `→` navigate within the current result set only; `Esc` closes the active viewer surface callback only
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/screens/viewer/viewer_stage_test.dart` around `ExtendedImage` gesture configuration, fit ↔ 2x toggle semantics, and reset-on-switch behavior for D-07/D-08.
    2. Extend `flutter_app/test/screens/viewer/viewer_workspace_test.dart` so filmstrip selection state is verified by border/contrast semantics, not color only, and keyboard shortcuts work without manually focusing the sidebar first.
    3. Create `flutter_app/lib/screens/viewer/viewer_stage.dart` around the existing `ExtendedImage` stack mined from `flutter_app/lib/screens/image_gallery_viewer.dart`. Preserve reuse; do not rewrite the viewer framework.
    4. Create `flutter_app/lib/screens/viewer/viewer_filmstrip.dart` with medium-density thumbnails, position label `{current} of {total}`, auto-centering of the current item, and callback-based image switching.
    5. Create `flutter_app/lib/screens/viewer/viewer_keyboard_scope.dart` (or equivalent focused widget) so the viewer handles only `←`, `→`, and `Esc` in Phase 18.
    6. Reduce `flutter_app/lib/screens/image_gallery_viewer.dart` to a reusable reference/helper role or compatibility wrapper if needed, but do not leave it as the final Phase 18 desktop shell.
    7. Stage and commit this interaction slice as:
       - `test(18-02): add failing viewer stage, filmstrip, and keyboard coverage`
       - `feat(18-02): add viewer stage, filmstrip, and keyboard scope`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/screens/viewer/viewer_stage_test.dart test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/screens/viewer/viewer_stage.dart` contains `ExtendedImage` gesture-mode viewer logic
    - `flutter_app/lib/screens/viewer/viewer_stage.dart` implements fit-to-window ↔ `2x` double-click semantics
    - `flutter_app/lib/screens/viewer/viewer_filmstrip.dart` contains current-item position copy and selected-item emphasis stronger than color alone
    - `flutter_app/lib/screens/viewer/viewer_keyboard_scope.dart` handles `←`, `→`, and `Esc` only
    - `flutter test test/screens/viewer/viewer_stage_test.dart test/screens/viewer/viewer_workspace_test.dart test/screens/viewer/viewer_metadata_sidebar_test.dart` exits `0`
  </acceptance_criteria>
  <done>The reusable viewer workspace fully expresses the Phase 18 image/filmstrip/sidebar/keyboard contract and can now be embedded into real viewer windows.</done>
</task>

</tasks>

<verification>
- Confirm widget tests cover D-04 through D-09 behaviors before entry-point integration.
- Manually inspect that the sidebar remains visually quieter than the image stage and the filmstrip remains more glanceable than the sidebar.
</verification>

<success_criteria>
- `VIEW-02`, `VIEW-03`, and `VIEW-04` are implemented in a reusable workspace with automated evidence.
- The workspace is container-agnostic and ready to mount inside real spawned windows from Plan 01.
</success_criteria>

<commit_strategy>
- Commit 1: `test(18-02): add failing viewer workspace and metadata sidebar coverage`
- Commit 2: `feat(18-02): add reusable viewer workspace shell and metadata sidebar`
- Commit 3: `test(18-02): add failing viewer stage, filmstrip, and keyboard coverage`
- Commit 4: `feat(18-02): add viewer stage, filmstrip, and keyboard scope`
</commit_strategy>

<output>
After completion, create `.planning/phases/18-independent-viewer-filmstrip/18-02-SUMMARY.md`
</output>
