---
phase: 01-foundation-scan-tag-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/server/main.go
  - internal/config/config.go
  - internal/domain/image.go
  - internal/domain/tag.go
  - internal/domain/tag_alias.go
  - internal/domain/tag_observation.go
  - internal/domain/collection.go
  - internal/domain/async_job.go
  - migrations/001_initial_schema.up.sql
  - migrations/001_initial_schema.down.sql
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-04
  - AIRE-02
  - AIRE-04

must_haves:
  truths:
    - "Go 项目可以成功编译运行"
    - "配置文件可以被正确加载"
    - "SQLite 数据库可以被创建和连接"
    - "所有核心数据表已创建并包含正确的字段和索引"
  artifacts:
    - path: "go.mod"
      provides: "Go 模块定义"
      min_lines: 20
    - path: "cmd/server/main.go"
      provides: "应用入口"
      contains: "func main()"
    - path: "internal/config/config.go"
      provides: "配置管理"
      contains: "type Config struct"
    - path: "migrations/001_initial_schema.up.sql"
      provides: "数据库 Schema"
      contains: "CREATE TABLE images"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/config/config.go"
      via: "import and LoadConfig()"
      pattern: "config\\.LoadConfig"
    - from: "migrations/001_initial_schema.up.sql"
      to: "SQLite database file"
      via: "migration execution"
      pattern: "CREATE TABLE"
---

<objective>
建立 Go 后端项目骨架、配置管理基础设施和完整的数据库 Schema。

Purpose: 为后续 API 开发、图片扫描和标签治理提供稳定的项目基础和数据存储层。
Output: 可编译运行的 Go 项目 + 完整的数据库 Schema + 配置加载能力。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-scan-tag-base/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/PHASE1_PATTERNS.md
</context>

<interfaces>

<!-- 技术栈约束 (来自 STACK.md) -->
**Go 版本:** 1.24.x+
**Web 框架:** Gin (后续计划使用)
**数据库驱动:** ncruces/go-sqlite3 (纯 Go，无 CGO)
**配置格式:** YAML (推荐)

<!-- 目录结构 (来自 ARCHITECTURE.md) -->
```
acgwarehouse-backend/
├── cmd/
│   └── server/main.go
├── internal/
│   ├── config/config.go
│   ├── domain/
│   │   ├── image.go
│   │   ├── tag.go
│   │   ├── tag_alias.go
│   │   ├── tag_observation.go
│   │   ├── collection.go
│   │   └── async_job.go
│   ├── handler/
│   ├── service/
│   ├── repository/
│   └── worker/
├── migrations/
└── go.mod
```

<!-- 数据库 Schema (来自 STACK.md + CONTEXT.md) -->
**核心表:**
- `images`: 图片元数据（不存 BLOB，只存路径引用）
- `tags`: 标准标签
- `tag_aliases`: 标签别名（独立表模式）
- `tag_observations`: AI 原始观测结果
- `image_tags`: 图片-标签关联
- `collections`: 收藏夹
- `async_jobs`: 异步任务状态

</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Go 项目结构初始化</name>
  <files>
    go.mod,
    go.sum,
    cmd/server/main.go,
    Makefile
  </files>
  <action>
    创建 Go 1.24+ 后端项目骨架：

    1. 初始化 Go 模块：
       - `go mod init github.com/yourusername/acgwarehouse-backend`
       - Go 版本设置为 1.24+

    2. 创建目录结构（按 ARCHITECTURE.md）：
       ```
       cmd/server/main.go
       internal/config/
       internal/domain/
       internal/handler/
       internal/service/
       internal/repository/
       internal/worker/
       migrations/
       ```

    3. 创建 cmd/server/main.go：
       - 简单的入口文件，先只打印 "ACGWarehouse starting..."
       - 后续计划会扩展此文件

    4. 创建 Makefile：
       - `build`: 编译项目
       - `run`: 运行项目
       - `test`: 运行测试
       - `migrate-up`: 执行数据库迁移
       - `migrate-down`: 回滚数据库迁移

    **约束：**
    - 不要使用 CGO（确保跨平台编译兼容）
    - 项目名称使用小写和连字符
  </action>
  <verify>
    <automated>go build -o bin/server ./cmd/server && ./bin/server | grep "ACGWarehouse"</automated>
  </verify>
  <done>
    - go.mod 文件存在且包含正确模块名
    - `go build ./...` 成功无错误
    - 运行 `./bin/server` 输出启动消息
    - Makefile 包含 build、run、test 目标
  </done>
</task>

<task type="auto">
  <name>task 2: 配置文件管理</name>
  <files>
    internal/config/config.go,
    config.yaml,
    .gitignore
  </files>
  <action>
    实现配置文件加载功能：

    1. 使用 gopkg.in/yaml.v3 解析 YAML 配置文件

    2. 创建 internal/config/config.go：
       - 定义 Config 结构体，包含：
         - Server: Host, Port
         - Database: Type (sqlite/postgres), Path (SQLite) 或 ConnectionString (PostgreSQL)
         - Storage: ScanRoots []string (监控根目录列表)
         - AI: Provider, ApiKey, Model (预留 AI 配置)
       - 实现 LoadConfig() 函数，从 config.yaml 加载
       - 支持环境变量覆盖 (如 DATABASE_PATH)

    3. 创建 config.yaml 示例文件：
       ```yaml
       server:
         host: "0.0.0.0"
         port: 8080
       database:
         type: "sqlite"
         path: "./data/acgwarehouse.db"
       storage:
         scan_roots:
           - "/path/to/your/images"
       ai:
         provider: "qwen"
         api_key: ""
         model: "qwen-vl-max"
       ```

    4. 更新 .gitignore 排除敏感配置

    **决策依据（来自 CONTEXT.md）：**
    - 监控对象采用显式配置的多个根目录
    - SQLite/PostgreSQL 双模式支持
  </action>
  <verify>
    <automated>go test -v ./internal/config/... -run TestLoadConfig</automated>
  </verify>
  <done>
    - config.go 定义完整的 Config 结构体
    - LoadConfig() 可从 config.yaml 加载配置
    - 环境变量可覆盖配置项
    - config.yaml 示例文件存在
  </done>
</task>

<task type="auto">
  <name>task 3: 数据库 Schema 创建</name>
  <files>
    internal/domain/image.go,
    internal/domain/tag.go,
    internal/domain/tag_alias.go,
    internal/domain/tag_observation.go,
    internal/domain/collection.go,
    internal/domain/async_job.go,
    migrations/001_initial_schema.up.sql,
    migrations/001_initial_schema.down.sql
  </files>
  <action>
    创建完整的数据库 Schema 和 Go domain 模型：

    **Part A: Go Domain 模型 (internal/domain/)**

    1. image.go - 图片实体：
       ```go
       type Image struct {
           ID          int64     `json:"id"`
           Path        string    `json:"path"`          // 原始路径引用
           Filename    string    `json:"filename"`
           SourceRoot  string    `json:"source_root"`   // 来源根目录
           FileSize    int64     `json:"file_size"`
           Width       int       `json:"width"`
           Height      int       `json:"height"`
           Format      string    `json:"format"`        // jpg, png, webp, gif
           PHash       int64     `json:"phash"`         // 感知哈希 (Phase 2)
           CreatedAt   time.Time `json:"created_at"`
           UpdatedAt   time.Time `json:"updated_at"`
       }
       ```

    2. tag.go - 标准标签：
       ```go
       type Tag struct {
           ID              int64     `json:"id"`
           PreferredLabel  string    `json:"preferred_label"`
           Slug            string    `json:"slug"`
           PrimaryCategory string    `json:"primary_category"` // 人物、服装、场景等
           ReviewState     string    `json:"review_state"`     // pending, approved, rejected
           TrustScore      float64   `json:"trust_score"`
           UsageCount      int       `json:"usage_count"`
           CreatedAt       time.Time `json:"created_at"`
       }
       ```

    3. tag_alias.go - 标签别名 (参考 PHASE1_PATTERNS.md):
       ```go
       type TagAlias struct {
           ID              int64   `json:"id"`
           TagID           int64   `json:"tag_id"`
           Label           string  `json:"label"`
           NormalizedLabel string  `json:"normalized_label"`
           Locale          string  `json:"locale"`
           AliasType       string  `json:"alias_type"` // synonym, variant, locale
           IsPreferred     bool    `json:"is_preferred"`
       }
       ```

    4. tag_observation.go - AI 原始观测 (AIRE-02, AIRE-04):
       ```go
       type TagObservation struct {
           ID            int64     `json:"id"`
           ImageID       int64     `json:"image_id"`
           RawText       string    `json:"raw_text"`      // AI 原始输出
           Confidence    float64   `json:"confidence"`    // 置信度分数
           EvidenceType  string    `json:"evidence_type"` // ai_generated, manual
           Provider      string    `json:"provider"`      // qwen, doubao
           ModelName     string    `json:"model_name"`
           PromptVersion string    `json:"prompt_version"`
           CreatedAt     time.Time `json:"created_at"`
       }
       ```

    5. collection.go - 收藏夹：
       ```go
       type Collection struct {
           ID          int64     `json:"id"`
           Name        string    `json:"name"`
           Description string    `json:"description"`
           CoverImageID *int64   `json:"cover_image_id"`
           ImageCount  int       `json:"image_count"`
           CreatedAt   time.Time `json:"created_at"`
           UpdatedAt   time.Time `json:"updated_at"`
       }
       ```

    6. async_job.go - 异步任务状态：
       ```go
       type AsyncJob struct {
           ID          int64      `json:"id"`
           Type        string     `json:"type"`        // image_imported, ai_tagging
           Status      string     `json:"status"`      // ready, running, finished, failed, cancelled
           Payload     string     `json:"payload"`     // JSON payload
           Progress    float64    `json:"progress"`    // 0.0 - 1.0
           Error       *string    `json:"error"`
           CreatedAt   time.Time  `json:"created_at"`
           StartedAt   *time.Time `json:"started_at"`
           FinishedAt  *time.Time `json:"finished_at"`
       }
       ```

    **Part B: SQL Migration 文件**

    创建 migrations/001_initial_schema.up.sql：
    ```sql
    -- Images table (不存 BLOB，只存路径引用)
    CREATE TABLE images (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        path TEXT UNIQUE NOT NULL,
        filename TEXT NOT NULL,
        source_root TEXT NOT NULL,
        file_size INTEGER,
        width INTEGER,
        height INTEGER,
        format TEXT,
        phash INTEGER,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    CREATE INDEX idx_images_path ON images(path);
    CREATE INDEX idx_images_source_root ON images(source_root);
    CREATE INDEX idx_images_phash ON images(phash);

    -- Standard tags (治理后的标准标签)
    CREATE TABLE tags (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        preferred_label TEXT UNIQUE NOT NULL,
        slug TEXT UNIQUE NOT NULL,
        primary_category TEXT,
        review_state TEXT DEFAULT 'pending',
        trust_score REAL DEFAULT 0.0,
        usage_count INTEGER DEFAULT 0,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );

    CREATE INDEX idx_tags_slug ON tags(slug);
    CREATE INDEX idx_tags_category ON tags(primary_category);

    -- Tag aliases (独立表模式，参考 PHASE1_PATTERNS.md)
    CREATE TABLE tag_aliases (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        tag_id INTEGER NOT NULL,
        label TEXT NOT NULL,
        normalized_label TEXT NOT NULL,
        locale TEXT,
        alias_type TEXT DEFAULT 'synonym',
        is_preferred INTEGER DEFAULT 0,
        FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
    );

    CREATE UNIQUE INDEX idx_tag_aliases_label ON tag_aliases(label);
    CREATE INDEX idx_tag_aliases_normalized ON tag_aliases(normalized_label);

    -- AI raw observations (原始观测，不直接用于搜索)
    CREATE TABLE tag_observations (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        image_id INTEGER NOT NULL,
        raw_text TEXT NOT NULL,
        confidence REAL,
        evidence_type TEXT DEFAULT 'ai_generated',
        provider TEXT,
        model_name TEXT,
        prompt_version TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE
    );

    CREATE INDEX idx_tag_observations_image ON tag_observations(image_id);
    CREATE INDEX idx_tag_observations_provider ON tag_observations(provider);

    -- Image-tag associations (治理后的关联)
    CREATE TABLE image_tags (
        image_id INTEGER NOT NULL,
        tag_id INTEGER NOT NULL,
        source_observation_id INTEGER,
        confidence REAL,
        review_state TEXT DEFAULT 'pending',
        PRIMARY KEY (image_id, tag_id),
        FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE,
        FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE,
        FOREIGN KEY (source_observation_id) REFERENCES tag_observations(id) ON DELETE SET NULL
    );

    CREATE INDEX idx_image_tags_tag ON image_tags(tag_id);

    -- Collections
    CREATE TABLE collections (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        description TEXT,
        cover_image_id INTEGER,
        image_count INTEGER DEFAULT 0,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        FOREIGN KEY (cover_image_id) REFERENCES images(id) ON DELETE SET NULL
    );

    -- Collection-image junction
    CREATE TABLE collection_images (
        collection_id INTEGER NOT NULL,
        image_id INTEGER NOT NULL,
        added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        PRIMARY KEY (collection_id, image_id),
        FOREIGN KEY (collection_id) REFERENCES collections(id) ON DELETE CASCADE,
        FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE
    );

    -- Async jobs queue
    CREATE TABLE async_jobs (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        type TEXT NOT NULL,
        status TEXT DEFAULT 'ready',
        payload TEXT,
        progress REAL DEFAULT 0.0,
        error TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        started_at TIMESTAMP,
        finished_at TIMESTAMP
    );

    CREATE INDEX idx_async_jobs_status ON async_jobs(status);
    CREATE INDEX idx_async_jobs_type ON async_jobs(type);
    ```

    创建 migrations/001_initial_schema.down.sql：
    ```sql
    DROP TABLE IF EXISTS collection_images;
    DROP TABLE IF EXISTS collections;
    DROP TABLE IF EXISTS image_tags;
    DROP TABLE IF EXISTS tag_observations;
    DROP TABLE IF EXISTS tag_aliases;
    DROP TABLE IF EXISTS tags;
    DROP TABLE IF EXISTS images;
    DROP TABLE IF EXISTS async_jobs;
    ```

    **约束（来自 CONTEXT.md）：**
    - 图片不存 BLOB，只存路径引用
    - 标签采用"原始观测 -> 标准标签 -> 别名"三层结构
    - tag_aliases 使用独立表模式
  </action>
  <verify>
    <automated>sqlite3 ./data/test.db < migrations/001_initial_schema.up.sql && sqlite3 ./data/test.db ".tables" | grep -E "images|tags|tag_aliases|tag_observations|async_jobs"</automated>
  </verify>
  <done>
    - 所有 domain 文件存在且定义正确的结构体
    - SQL migration 文件存在且语法正确
    - 在测试数据库执行迁移后所有表存在
    - 所有必要的索引已创建
  </done>
</task>

</tasks>

<verification>
**编译验证：**
```bash
go build ./...
```

**Schema 验证：**
```bash
# 创建测试数据库
mkdir -p ./data
sqlite3 ./data/test.db < migrations/001_initial_schema.up.sql

# 验证表存在
sqlite3 ./data/test.db ".tables"

# 验证表结构
sqlite3 ./data/test.db ".schema images"
sqlite3 ./data/test.db ".schema tags"
sqlite3 ./data/test.db ".schema tag_aliases"
sqlite3 ./data/test.db ".schema tag_observations"
sqlite3 ./data/test.db ".schema async_jobs"
```
</verification>

<success_criteria>
- [ ] Go 项目可以 `go build ./...` 编译成功
- [ ] 配置文件可以被 LoadConfig() 正确加载
- [ ] 数据库 migration 可以成功执行
- [ ] 所有 7 个核心表已创建（images, tags, tag_aliases, tag_observations, image_tags, collections, async_jobs）
- [ ] 所有表都有必要的索引
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-scan-tag-base/01-01-SUMMARY.md`
</output>