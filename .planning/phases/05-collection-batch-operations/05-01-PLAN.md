---
wave: 1
depends_on: []
files_modified:
  - internal/domain/collection.go
  - internal/domain/collection_image.go
  - internal/repository/schema.go
  - internal/repository/collection_repository.go
  - internal/repository/collection_repository_test.go
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
phase: 05-collection-batch-operations
plan: 01
type: execute
must_haves:
  truths:
    - "收藏夹数据表存在于数据库中"
    - "收藏夹-图片关联表存在于数据库中"
    - "可以通过 Repository 创建和查询收藏夹"
    - "可以通过 Repository 添加和移除收藏夹中的图片"
  artifacts:
    - path: "internal/repository/schema.go"
      provides: "collections 和 collection_images 表定义"
      contains: "CREATE TABLE.*collections"
    - path: "internal/repository/collection_repository.go"
      provides: "收藏夹数据访问层"
      exports: ["CollectionRepository", "Create", "GetByID", "List", "Update", "Delete"]
  key_links:
    - from: "collection_repository.go"
      to: "schema.go"
      via: "SQL DDL 执行"
---

# Plan 05-01: 数据模型与 Repository 层

> 收藏夹数据模型定义、数据库扩展和 Repository 实现

---

## 目标

实现收藏夹核心数据模型、数据库 schema 扩展和 Repository 层，为上层业务提供数据访问能力。

## 背景分析

### 已有资产
- internal/domain/collection.go: Collection 结构体已定义（ID, Name, Description, CoverImageID, ImageCount, CreatedAt, UpdatedAt）
- Repository 实现模式：使用 EnsureScanSchema(db) 初始化数据库

### 需要创建
- CollectionImage 领域模型（collection_id, image_id, added_at）
- CollectionRepository 接口和 SQLite 实现
- Schema 扩展：在 schema.go 添加 collections 表定义

## 任务

<task id="05-01-01" type="auto">
<name>扩展 Collection 领域模型</name>
<files>
  - internal/domain/collection.go
</files>
<action>
  扩展现有 Collection 结构体，确保字段完整：
  - 验证必需字段：ID, Name, Description, CoverImageID, ImageCount, CreatedAt, UpdatedAt
  - 确保 JSON 标签和数据库标签
  - 确认 TableName() 方法返回表名
</action>
<verify>
  <automated>go build ./internal/domain/...</automated>
</verify>
<done>
  Collection 结构体完整定义，编译通过
</done>
</task>

<task id="05-01-02" type="auto">
<name>创建 CollectionImage 领域模型</name>
<files>
  - internal/domain/collection_image.go
</files>
<action>
  创建 CollectionImage 结构体，包含 CollectionID, ImageID, AddedAt 字段
</action>
<verify>
  <automated>go build ./internal/domain/...</automated>
</verify>
<done>
  CollectionImage 结构体定义完成，编译通过
</done>
</task>

<task id="05-01-03" type="auto">
<name>扩展数据库 Schema</name>
<files>
  - internal/repository/schema.go
</files>
<action>
  在 scanSchemaSQL 字符串中添加 collections 和 collection_images 表定义，包含索引
</action>
<verify>
  <automated>go build ./internal/repository/...</automated>
</verify>
<done>
  Schema 包含 collections 和 collection_images 表定义
</done>
</task>

<task id="05-01-04" type="auto">
<name>定义 CollectionRepository 接口</name>
<files>
  - internal/repository/collection_repository.go
</files>
<action>
  定义 CollectionRepository 接口，包含：
  - Collection CRUD: Save, FindByID, FindAll, FindByName, Update, Delete
  - Collection Image: AddImage, RemoveImage, FindImagesByCollection, CountImages
  - Cover: UpdateCover, GetLatestImageID
  - Statistics: Count
</action>
<verify>
  <automated>go build ./internal/repository/...</automated>
</verify>
<done>
  CollectionRepository 接口定义完成
</done>
</task>

<task id="05-01-05" type="auto">
<name>实现 SQLite CollectionRepository</name>
<files>
  - internal/repository/collection_repository.go
</files>
<action>
  实现 sqliteCollectionRepository 结构体和所有接口方法
</action>
<verify>
  <automated>go build ./internal/repository/...</automated>
</verify>
<done>
  CollectionRepository 实现完成，编译通过
</done>
</task>

<task id="05-01-06" type="auto">
<name>编写 Repository 单元测试</name>
<files>
  - internal/repository/collection_repository_test.go
</files>
<action>
  编写完整的单元测试，覆盖：
  - Collection CRUD 操作
  - Collection Image 添加/移除/查询
  - 封面更新
  - 统计功能
</action>
<verify>
  <automated>go test ./internal/repository/... -run Collection -v -count=1</automated>
</verify>
<done>
  所有测试通过，覆盖率 > 80%
</done>
</task>

<task id="05-01-07" type="auto">
<name>添加测试辅助函数和错误处理</name>
<files>
  - internal/repository/collection_repository_test.go
</files>
<action>
  添加测试辅助函数 newCollectionRepositoryForTest 和 mustSaveCollection
</action>
<verify>
  <automated>go test ./internal/repository/... -run Collection -v -count=1</automated>
</verify>
<done>
  测试辅助函数创建完成，测试通过
</done>
</task>

---

## 验证标准

### 自动化验证
testing: warning: no tests to run
PASS
ok  	github.com/wonichan/acgwarehouse-backend/internal/repository	0.779s [no tests to run]

### 手动验证
- [ ] Collection 结构体字段完整
- [ ] CollectionRepository 接口覆盖所有操作
- [ ] 封面自动更新逻辑正确实现
- [ ] 测试覆盖边界情况

---

## must_haves

| 需求 ID | 验证标准 |
|---------|----------|
| COLL-01 | CollectionRepository.Save() 可创建收藏夹 |
| COLL-02 | AddImage/RemoveImage 可添加/移除收藏夹中的图片 |
| COLL-03 | Update/Delete 可修改/删除收藏夹 |
| COLL-04 | UpdateCover/GetLatestImageID 支持封面更新 |
| COLL-05 | CountImages 返回正确的图片数量 |

---

*计划创建时间：2026-03-17*
*预计执行时间：2-3 小时*