---
phase: 03-ai
plan: 06
type: execute
wave: 5
depends_on: [03-03, 03-05]
files_modified:
  - internal/repository/image_repository.go
  - internal/repository/image_repository_test.go
  - internal/repository/image_tag_repository.go
  - internal/repository/image_tag_repository_test.go
  - internal/handler/image_handler.go
  - internal/handler/image_handler_test.go
  - internal/handler/image_tag_handler.go
  - internal/handler/image_tag_handler_test.go
  - internal/handler/tag_handler.go
  - internal/handler/tag_handler_test.go
  - internal/handler/routes.go
autonomous: true
requirements: [AIRE-05, TAGS-03, TAGS-05]
gap_closure: true

must_haves:
  truths:
    - "The backend image list supports standard-tag AND filtering so governed AI tags affect gallery results"
    - "The backend exposes an image-level merge endpoint for pending AI review items"
    - "The backend exposes tag statistics with usage, source, and pending-review counts"
  artifacts:
    - path: "internal/handler/image_handler.go"
      provides: "Real image list endpoint with governed tag filtering"
      exports: ["ListImages", "GetImage"]
    - path: "internal/handler/image_tag_handler.go"
      provides: "Image-level merge endpoint for reviewable AI tags"
      exports: ["MergeImageTag"]
    - path: "internal/handler/tag_handler.go"
      provides: "Tag statistics API"
      exports: ["GetTagStats"]
  key_links:
    - from: "internal/handler/image_handler.go"
      to: "internal/repository/image_repository.go"
      via: "tag_ids query translated into repository filter"
      pattern: "tag_ids"
    - from: "internal/handler/image_tag_handler.go"
      to: "internal/service/tag_governance_service.go"
      via: "pending image-tag merge operation"
      pattern: "MergeImageTag|target_tag_id|target_label"
    - from: "internal/handler/tag_handler.go"
      to: "internal/repository/image_tag_repository.go"
      via: "tag statistics aggregation"
      pattern: "pending_count|ai_count|manual_count"
---

<objective>
Close the remaining backend verification gaps by adding governed image filtering plus the missing merge/statistics APIs that the Flutter gap-closure plan will consume.

Purpose: The verifier still reports no real image filtering, no merge API for AI review items, and no governance statistics surface.
Output: Filterable image list API, image-tag merge endpoint, tag statistics endpoint, and route/repository coverage for those flows.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-VERIFICATION.md
@.planning/phases/03-ai/03-03-SUMMARY.md
@.planning/phases/03-ai/03-05-PLAN.md
@internal/handler/image_tag_handler.go
@internal/handler/tag_handler.go
@internal/handler/routes.go
@internal/repository/image_repository.go

<interfaces>
From `internal/handler/routes.go`:
```go
images := api.Group("/images")
images.GET("", placeholderHandler)
images.GET("/:id", placeholderHandler)

api.GET("/images/:id/tags", imageTagGet)
api.POST("/images/:id/tags/:tag_id/review", imageTagReview)
```

From `internal/handler/image_tag_handler.go`:
```go
type ImageTagHandler struct {
    imageTagRepo  repository.ImageTagRepository
    tagRepo       repository.TagRepository
    imageRepo     repository.ImageRepository
    governanceSvc *service.TagGovernanceService
}
```

From `internal/repository/image_repository.go`:
```go
type ImageRepository interface {
    SaveImage(image *domain.Image) error
    FindByID(id int64) (*domain.Image, error)
    FindAll(limit, offset int) ([]domain.Image, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add real image list filtering by governed standard tags</name>
  <files>internal/repository/image_repository.go, internal/repository/image_repository_test.go, internal/handler/image_handler.go, internal/handler/image_handler_test.go, internal/handler/routes.go</files>
  <behavior>
    - Test 1: `GET /api/v1/images?tag_ids=1,2` returns only images linked to both tag IDs.
    - Test 2: the unfiltered image list keeps the existing response contract (`images`, `next_cursor`, `has_more`).
    - Test 3: filtering uses `image_tags.tag_id`, so tags produced by the AI merge flow immediately participate in gallery filtering.
  </behavior>
  <action>
    Add the smallest backend extension that makes the existing Flutter drawer issue real governed-tag queries.

    Requirements:
    1. Create `internal/handler/image_handler.go` for `GET /api/v1/images` and `GET /api/v1/images/:id` instead of leaving those routes on `placeholderHandler`.
    2. Extend `ImageRepository` with a filtered list method that accepts `tagIDs []int64` and uses AND semantics by requiring the count of matched governed tags to equal the number requested.
    3. Keep the current Flutter-facing payload shape stable; do not redesign pagination or add search scope.

    Avoid OR filtering, local-only filtering, and any Phase 4 search work.
  </action>
  <verify>
    <automated>go test ./internal/repository/... ./internal/handler/... -run "Test(ImageRepository|ImageHandler)"</automated>
  </verify>
  <done>`/api/v1/images` supports governed tag filtering and remains compatible with the current Flutter image list contract.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: expose review merge and governance statistics APIs</name>
  <files>internal/repository/image_tag_repository.go, internal/repository/image_tag_repository_test.go, internal/handler/image_tag_handler.go, internal/handler/image_tag_handler_test.go, internal/handler/tag_handler.go, internal/handler/tag_handler_test.go, internal/handler/routes.go</files>
  <behavior>
    - Test 1: `POST /api/v1/images/:id/tags/:tag_id/merge` reassigns a pending AI tag on that image to an existing target tag and removes the old image-tag link.
    - Test 2: `GET /api/v1/tags/stats` returns per-tag `usage_count`, `pending_count`, `ai_count`, and `manual_count`.
    - Test 3: merge and stats routes are registered and keep `GET /api/v1/tags` stable for existing consumers.
  </behavior>
  <action>
    Finish the remaining backend review/governance surface with no extra scope beyond the verification report.

    Requirements:
    1. Extend `ImageTagRepository` with only the helpers needed for image-level merge and tag statistics aggregation.
    2. Add `POST /api/v1/images/:id/tags/:tag_id/merge` to `ImageTagHandler`, accepting `target_tag_id` or `target_label`, resolving the target through the existing governed tag rules, and leaving an alias trail so future AI labels can reuse it.
    3. Add `GET /api/v1/tags/stats` to `TagHandler` returning governance rows with usage/source/pending counts.
    4. Register the new routes in `internal/handler/routes.go` without altering unrelated endpoints.

    Avoid a global library-wide merge feature, client-side stats reconstruction, or any non-gap analytics work.
  </action>
  <verify>
    <automated>go test ./internal/repository/... ./internal/handler/... -run "Test(ImageTagRepository|ImageTagHandler|TagHandler)"</automated>
  </verify>
  <done>The backend exposes the filter, merge, and statistics APIs needed to close TAGS-03/TAGS-05 and the merge portion of AIRE-05.</done>
</task>

</tasks>

<commit_strategy>
- `test(03-06): add failing image filter API tests`
- `feat(03-06): add governed tag filtering to image list endpoints`
- `test(03-06): add failing merge and tag stats API tests`
- `feat(03-06): expose review merge and governance statistics endpoints`
</commit_strategy>

<verification>
- `go test ./internal/repository/... ./internal/handler/...`
- `go build ./...`
- Manual gap mapping check: `/api/v1/images?tag_ids=...` filters by governed tags, `/api/v1/images/:id/tags/:tag_id/merge` returns 200 for a pending AI tag merge, and `/api/v1/tags/stats` returns usage/source/pending counts.
</verification>

<success_criteria>
- TAGS-03 backend support is closed: the image list API filters by standard tag IDs with AND semantics.
- AIRE-05 merge support is closed backend-side: a dedicated image-tag merge endpoint exists.
- TAGS-05 backend support is closed: a dedicated governance statistics endpoint exists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai/03-06-SUMMARY.md`
</output>
