---
phase: 03-ai
plan: 07
type: execute
wave: 6
depends_on: [03-04, 03-05, 03-06]
files_modified:
  - flutter_app/lib/services/api_service.dart
  - flutter_app/lib/providers/image_provider.dart
  - flutter_app/lib/screens/gallery_screen.dart
  - flutter_app/test/services/api_service_test.dart
  - flutter_app/test/providers/image_provider_test.dart
  - flutter_app/lib/services/tag_service.dart
  - flutter_app/lib/screens/image_detail_screen.dart
  - flutter_app/test/services/tag_service_test.dart
  - flutter_app/test/screens/image_detail_screen_test.dart
  - flutter_app/lib/providers/tag_provider.dart
  - flutter_app/lib/screens/tag_management_screen.dart
  - flutter_app/test/providers/tag_provider_test.dart
  - flutter_app/test/screens/tag_management_screen_test.dart
autonomous: true
requirements: [AIRE-05, TAGS-03, TAGS-05]
gap_closure: true

must_haves:
  truths:
    - "The gallery drawer changes the real image query instead of only local selection state"
    - "The image detail screen shows AI job progress and refreshes reviewable tags when the job completes"
    - "Users can merge a pending AI tag into an existing governed tag from the review UI"
    - "Users can open a tag governance screen that shows usage, source, and pending-review statistics"
  artifacts:
    - path: "flutter_app/lib/services/api_service.dart"
      provides: "Image list API client with tag filter support"
      contains: "tagIds"
    - path: "flutter_app/lib/screens/image_detail_screen.dart"
      provides: "AI job polling, refresh, and merge review UI"
      contains: "getAITagStatus"
    - path: "flutter_app/lib/screens/tag_management_screen.dart"
      provides: "Governance statistics UI"
      contains: "usageCount"
  key_links:
    - from: "flutter_app/lib/screens/gallery_screen.dart"
      to: "flutter_app/lib/providers/image_provider.dart"
      via: "setTagFilter"
      pattern: "setTagFilter"
    - from: "flutter_app/lib/providers/image_provider.dart"
      to: "flutter_app/lib/services/api_service.dart"
      via: "fetchImages(tagIds: ...)"
      pattern: "fetchImages\(.*tagIds"
    - from: "flutter_app/lib/screens/image_detail_screen.dart"
      to: "flutter_app/lib/services/tag_service.dart"
      via: "AI status polling and merge action"
      pattern: "getAITagStatus|mergeImageTag"
    - from: "flutter_app/lib/screens/tag_management_screen.dart"
      to: "flutter_app/lib/providers/tag_provider.dart"
      via: "governance stats loader"
      pattern: "loadTagStatistics"
---

<objective>
Close the Phase 03 Flutter blockers by consuming the revised backend gap-closure APIs for governed image filtering, AI review refresh, merge actions, and governance statistics.

Purpose: Verification found the frontend stuck in partial-wiring mode even though the drawer, detail page, and tag provider already exist.
Output: Real gallery filter wiring, AI status polling with refreshed tags, image-level merge UI, and a dedicated governance/statistics screen.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-VERIFICATION.md
@.planning/phases/03-ai/03-04-SUMMARY.md
@.planning/phases/03-ai/03-06-PLAN.md
@flutter_app/lib/services/api_service.dart
@flutter_app/lib/providers/image_provider.dart
@flutter_app/lib/screens/gallery_screen.dart
@flutter_app/lib/services/tag_service.dart
@flutter_app/lib/screens/image_detail_screen.dart

<interfaces>
From `flutter_app/lib/services/api_service.dart`:
```dart
Future<PaginationResponse<ImageModel>> fetchImages({
  String? cursor,
  int limit = 20,
  String sortBy = 'created_at',
  String sortDir = 'desc',
})
```

From `flutter_app/lib/providers/image_provider.dart`:
```dart
class ImageListProvider extends ChangeNotifier {
  Future<void> loadImages({bool refresh = false}) async
  void setSort(SortField field, bool asc)
}
```

From `flutter_app/lib/services/tag_service.dart`:
```dart
Future<Map<String, List<Tag>>> getImageTags(int imageId)
Future<int> triggerAITags(int imageId)
Future<Map<String, dynamic>> getAITagStatus(int imageId)
Future<List<Tag>> searchTags(String query)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: connect the gallery drawer to real tag-filtered image queries</name>
  <files>flutter_app/lib/services/api_service.dart, flutter_app/lib/providers/image_provider.dart, flutter_app/lib/screens/gallery_screen.dart, flutter_app/test/services/api_service_test.dart, flutter_app/test/providers/image_provider_test.dart</files>
  <behavior>
    - Test 1: `ApiService.fetchImages(tagIds: [1, 2])` serializes the selected IDs into the backend query contract.
    - Test 2: `ImageListProvider.setTagFilter` refreshes the list and keeps sort/pagination state coherent.
    - Test 3: selecting tags in `GalleryScreen` triggers a real provider reload instead of only updating the local chip count.
  </behavior>
  <action>
    Replace the current placeholder filter behavior with a real backend-driven query flow.

    Requirements:
    1. Extend `ApiService.fetchImages` with an optional `tagIds` argument matching the Plan 03-06 backend API contract.
    2. Extend `ImageListProvider` with stored selected tag IDs plus a `setTagFilter(List<int>)` method that resets pagination and reloads using the active filter.
    3. Update `GalleryScreen._onTagFilterChanged` to call `context.read<ImageListProvider>().setTagFilter(tagIds)` while keeping the selected-count chip in sync.
    4. Add a reachable entry point from `GalleryScreen` into `TagManagementScreen` so the governance screen is not orphaned.

    Avoid local-only filtering or UX redesign beyond the current drawer-based pattern.
  </action>
  <verify>
    <automated>flutter test test/services/api_service_test.dart test/providers/image_provider_test.dart</automated>
  </verify>
  <done>The gallery drawer now drives real governed-tag image queries and still works with refresh/sort.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: show AI job progress and merge review actions in image detail</name>
  <files>flutter_app/lib/services/tag_service.dart, flutter_app/lib/screens/image_detail_screen.dart, flutter_app/test/services/tag_service_test.dart, flutter_app/test/screens/image_detail_screen_test.dart</files>
  <behavior>
    - Test 1: after `triggerAITags`, the screen polls `getAITagStatus` until completion and then refreshes `getImageTags`.
    - Test 2: pending AI tags render a merge action that searches governed tags and calls the backend merge endpoint.
    - Test 3: completed AI jobs surface newly created pending `image_tags` without requiring the user to reopen the screen.
  </behavior>
  <action>
    Turn the existing detail view from fire-and-forget into a real review surface for AI output.

    Requirements:
    1. Add typed service helpers in `TagService` for AI status polling and image-tag merge.
    2. Update `ImageDetailScreen` so triggering AI tags starts a polling loop that surfaces queued/running/failed/completed states and stops cleanly on dispose.
    3. Refresh grouped tag lists automatically when the AI job completes.
    4. Add a merge affordance on pending AI tags that lets the user search governed tags, select a target, call the merge API, and refresh the tags list.
    5. Keep confirm/reject/add/remove behavior intact.

    Avoid a separate review page or raw-job-json debug UI.
  </action>
  <verify>
    <automated>flutter test test/services/tag_service_test.dart test/screens/image_detail_screen_test.dart</automated>
  </verify>
  <done>The detail screen shows AI progress, refreshes new pending review tags, and supports merge actions inline.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: add a dedicated tag governance statistics screen</name>
  <files>flutter_app/lib/providers/tag_provider.dart, flutter_app/lib/screens/tag_management_screen.dart, flutter_app/test/providers/tag_provider_test.dart, flutter_app/test/screens/tag_management_screen_test.dart</files>
  <behavior>
    - Test 1: `TagProvider` loads governance statistics rows and exposes loading/error state for the screen.
    - Test 2: `TagManagementScreen` renders usage, AI/manual source counts, and pending-review counts for each tag.
    - Test 3: the screen supports searching/filtering the governance list without breaking the existing drawer-oriented tag loading behavior.
  </behavior>
  <action>
    Fill the final missing artifact called out by verification: a real tag governance/statistics screen.

    Requirements:
    1. Extend `TagProvider` with a dedicated governance-stats state path such as `loadTagStatistics` that consumes the new backend stats endpoint.
    2. Create `flutter_app/lib/screens/tag_management_screen.dart` as the central governance page for Phase 03 with searchable governed-tag rows and at least total usage, pending review count, AI count, and manual count.
    3. Keep the UI utilitarian and aligned with the current Flutter app rather than redesigning the product.

    Avoid advanced analytics or unrelated CRUD expansion.
  </action>
  <verify>
    <automated>flutter test test/providers/tag_provider_test.dart test/screens/tag_management_screen_test.dart</automated>
  </verify>
  <done>A dedicated governance screen exists, is reachable from the gallery, and exposes the missing tag statistics.</done>
</task>

</tasks>

<commit_strategy>
- `test(03-07): add failing gallery filter query tests`
- `feat(03-07): wire gallery tag filter to backend image queries`
- `test(03-07): add failing AI review polling and merge tests`
- `feat(03-07): refresh image detail with AI status and merge actions`
- `test(03-07): add failing governance screen tests`
- `feat(03-07): add tag management statistics screen`
</commit_strategy>

<verification>
- `flutter test test/services/api_service_test.dart test/providers/image_provider_test.dart test/services/tag_service_test.dart test/providers/tag_provider_test.dart test/screens/image_detail_screen_test.dart test/screens/tag_management_screen_test.dart`
- `flutter analyze`
- Manual gap mapping check: selecting tags in the drawer changes gallery results, triggering AI shows status and later pending tags on the same detail screen, merge action succeeds, and the governance screen shows usage/source/pending metrics.
</verification>

<success_criteria>
- TAGS-03 closes end-to-end: gallery filtering uses the real backend tag filter instead of local-only state.
- AIRE-05 closes in the UI: users can see AI progress, review resulting pending tags, and merge them from the detail screen.
- TAGS-05 closes in the UI: a dedicated governance/statistics screen exists and displays usage/source/pending counts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai/03-07-SUMMARY.md`
</output>
