---
phase: 04-duplicate-detection-search
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/service/search_service.go
  - internal/service/search_service_test.go
  - internal/repository/search_repository.go
  - internal/repository/search_repository_test.go
  - internal/repository/schema.go
  - internal/handler/search_handler.go
  - internal/handler/search_handler_test.go
  - go.mod
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-05
must_haves:
  truths:
    - 用户可以按文件名搜索图片
    - 用户可以按标签（包括别名）搜索图片
    - 用户可以组合搜索词和标签筛选
    - 搜索结果支持排序和分页
    - 搜索支持中文关键词
  artifacts:
    - path: internal/service/search_service.go
      provides: 全文搜索逻辑
      exports: SearchService, Search, SearchByTags, SearchCombined
    - path: internal/handler/search_handler.go
      provides: 搜索 API 端点
      exports: SearchHandler, SetupSearchRoutes
    - path: internal/repository/schema.go
      contains: images_fts 虚拟表
  key_links:
    - from: internal/service/search_service.go
      to: images_fts 虚拟表
      via: FTS5 全文搜索
      pattern: SELECT.*FROM images_fts
    - from: internal/service/search_service.go
      to: internal/repository/tag_repository.go
      via: 标签搜索
      pattern: FindByLabelLike|FindByAlias
    - from: internal/handler/search_handler.go
      to: internal/service/search_service.go
      via: API 调用服务
      pattern: searchService\.Search
---

<objective>
实现搜索后端层：SQLite FTS5 全文索引、文件名搜索、标签搜索（包括别名）、组合搜索、排序分页。

Purpose: 让用户能够快速找到想要的图片，支持多种搜索方式。
Output: 可工作的搜索服务、FTS5 索引、API 端点。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-duplicate-detection-search/04-CONTEXT.md
@.planning/phases/04-duplicate-detection-search/04-RESEARCH.md

<interfaces>
<!-- 现有代码接口 -->

From internal/repository/image_repository.go:
```go
type ImageRepository interface {
    FindByTagIDs(ctx context.Context, tagIDs []int64, limit, offset int) ([]domain.Image, error)
    CountByTagIDs(ctx context.Context, tagIDs []int64) (int64, error)
    FindAll(limit, offset int) ([]domain.Image, error)
}
```

From internal/repository/tag_repository.go:
```go
type TagRepository interface {
    FindByLabelLike(label string) ([]domain.Tag, error)
    FindByAliasLabel(aliasLabel string) (*domain.Tag, error)
}
```

From internal/handler/image_handler.go:
```go
// API 响应格式参考
c.JSON(http.StatusOK, gin.H{
    "images":      images,
    "next_cursor": nextCursor,
    "has_more":    hasMore,
    "total":       total,
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 创建 FTS5 全文索引表和同步机制</name>
  <files>internal/repository/schema.go, internal/repository/fts_sync.go, internal/repository/fts_sync_test.go</files>
  <behavior>
    - Test 1: FTS5 虚拟表创建成功
    - Test 2: 插入图片后 FTS 索引自动更新
    - Test 3: 删除图片后 FTS 索引自动清理
    - Test 4: 搜索关键词返回匹配的图片 ID
  </behavior>
  <action>
    1. 在 `internal/repository/schema.go` 添加 FTS5 虚拟表定义：
       ```sql
       -- 全文索引虚拟表
       CREATE VIRTUAL TABLE IF NOT EXISTS images_fts USING fts5(
           image_id UNINDEXED,  -- 关联图片 ID（不参与搜索）
           filename,            -- 文件名（可搜索）
           tags,                -- 标签文本（可搜索）
           tokenize = 'unicode61'  -- 支持中文简单分词
       );
       
       -- 同步触发器
       CREATE TRIGGER IF NOT EXISTS images_ai AFTER INSERT ON images BEGIN
           INSERT INTO images_fts(image_id, filename, tags)
           VALUES (new.id, new.filename, '');
       END;
       
       CREATE TRIGGER IF NOT EXISTS images_ad AFTER DELETE ON images BEGIN
           DELETE FROM images_fts WHERE image_id = old.id;
       END;
       
       CREATE TRIGGER IF NOT EXISTS images_au AFTER UPDATE ON images BEGIN
           UPDATE images_fts SET filename = new.filename WHERE image_id = old.id;
       END;
       ```
    
    2. 创建 `internal/repository/fts_sync.go`：
       ```go
       // UpdateImageTagsInFTS 更新图片的标签文本到 FTS 索引
       // 在图片标签变更时调用
       func UpdateImageTagsInFTS(db *sql.DB, imageID int64, tagsText string) error
       
       // RebuildFTSIndex 重建全文索引（维护用途）
       func RebuildFTSIndex(db *sql.DB) error
       ```
    
    3. 编写测试验证：
       - 创建测试图片记录
       - 验证 FTS 搜索返回正确结果
       - 验证删除后搜索不返回已删除记录
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run TestFTS -v</automated>
  </verify>
  <done>FTS5 全文索引表创建完成，支持自动同步和搜索</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 实现搜索服务和仓储</name>
  <files>internal/repository/search_repository.go, internal/repository/search_repository_test.go, internal/service/search_service.go, internal/service/search_service_test.go</files>
  <behavior>
    - Test 1: SearchByFilename 按文件名搜索返回匹配图片
    - Test 2: SearchByTags 按标签搜索返回匹配图片
    - Test 3: SearchCombined 同时使用关键词和标签筛选
    - Test 4: 搜索结果支持排序（相关度、时间、名称）
    - Test 5: 搜索结果支持分页
    - Test 6: 中文关键词搜索返回正确结果
  </behavior>
  <action>
    1. 创建 `internal/repository/search_repository.go`：
       ```go
       type SearchRepository interface {
           // FTSFullTextSearch 全文搜索
           FTSFullTextSearch(query string, limit, offset int) ([]int64, error)
           
           // SearchByFilenames 按文件名模糊搜索
           SearchByFilenames(pattern string, limit, offset int) ([]domain.Image, error)
       }
       ```
    
    2. 实现 SQLite 仓储：
       - 使用 `SELECT image_id FROM images_fts WHERE images_fts MATCH ?` 进行全文搜索
       - 支持 `*` 通配符：用户输入 `cat*` 转换为 `cat*` 匹配前缀
       - 支持 AND 逻辑：多个关键词用空格分隔，转换为 `"word1" AND "word2"`
    
    3. 创建 `internal/service/search_service.go`：
       ```go
       type SearchService struct {
           imageRepo   repository.ImageRepository
           tagRepo     repository.TagRepository
           searchRepo  repository.SearchRepository
       }
       
       type SearchOptions struct {
           Query       string   // 搜索关键词
           TagIDs      []int64  // 标签 ID 列表
           SortBy      string   // relevance, created_at, filename, file_size
           SortOrder   string   // asc, desc
           Limit       int
           Offset      int
       }
       
       type SearchResult struct {
           Images   []domain.Image
           Total    int64
           HasMore  bool
       }
       
       // Search 执行搜索
       func (s *SearchService) Search(ctx context.Context, opts SearchOptions) (*SearchResult, error)
       ```
    
    4. 搜索逻辑实现：
       - 如果只有关键词：FTS 全文搜索
       - 如果只有标签：FindByTagIDs（复用现有）
       - 如果同时有：先 FTS 搜索获取候选，再过滤标签
    
    5. 编写测试验证各种搜索场景
  </action>
  <verify>
    <automated>go test ./internal/service/... -run TestSearchService -v</automated>
  </verify>
  <done>搜索服务完成，支持文件名搜索、标签搜索、组合搜索、排序分页</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 实现搜索 API 端点</name>
  <files>internal/handler/search_handler.go, internal/handler/search_handler_test.go</files>
  <behavior>
    - Test 1: GET /api/v1/search?q=keyword 返回匹配图片
    - Test 2: GET /api/v1/search?tag_ids=1,2 返回标签匹配图片
    - Test 3: GET /api/v1/search?q=cat&tag_ids=1 返回组合搜索结果
    - Test 4: GET /api/v1/search?q=test&sort_by=created_at&sort_order=desc 返回按时间降序结果
    - Test 5: GET /api/v1/search?q=test&limit=10&offset=0 返回分页结果
    - Test 6: 搜索历史记录保存和查询
  </behavior>
  <action>
    创建 `internal/handler/search_handler.go`：
    
    ```go
    type SearchHandler struct {
        searchService *service.SearchService
    }
    
    func NewSearchHandler(ss *service.SearchService) *SearchHandler
    
    // Search GET /api/v1/search
    // Query params:
    //   - q: 搜索关键词（可选）
    //   - tag_ids: 标签 ID 列表，逗号分隔（可选）
    //   - sort_by: relevance | created_at | filename | file_size（默认 relevance）
    //   - sort_order: asc | desc（默认 desc）
    //   - limit: 每页数量（默认 20）
    //   - offset: 偏移量（默认 0）
    // Response: {"images": [...], "total": 100, "has_more": true}
    
    // GetSearchHistory GET /api/v1/search/history
    // 返回最近 10 条搜索记录
    // Response: {"history": ["cat", "dog", "anime"]}
    
    // ClearSearchHistory DELETE /api/v1/search/history
    // 清空搜索历史
    ```
    
    搜索历史存储：
    - 使用 SQLite 表存储搜索历史
    - 每个用户（当前为单用户）保存最近 10 条
    - 去重，新的搜索排在前面
    
    在 `cmd/server/main.go` 中注册路由。
    
    编写 API 测试验证端点行为。
  </action>
  <verify>
    <automated>go test ./internal/handler/... -run TestSearchHandler -v</automated>
  </verify>
  <done>搜索 API 端点完成，支持关键词搜索、标签搜索、组合搜索、排序分页、搜索历史</done>
</task>

</tasks>

<verification>
### 构建验证
```bash
go build ./...
```

### 单元测试
```bash
go test ./internal/service/... -v
go test ./internal/repository/... -v
go test ./internal/handler/... -v
```

### 集成验证
```bash
# 启动服务器
go run ./cmd/server

# 测试搜索 API
curl "http://localhost:8080/api/v1/search?q=test"
curl "http://localhost:8080/api/v1/search?tag_ids=1,2"
curl "http://localhost:8080/api/v1/search?q=cat&sort_by=created_at"
```
</verification>

<success_criteria>
1. FTS5 全文索引表创建成功
2. 文件名搜索返回正确结果
3. 标签搜索返回正确结果
4. 组合搜索正确过滤
5. 排序和分页功能正常
6. 中文搜索可用
7. 搜索历史记录功能正常
8. API 端点通过测试
9. 所有代码通过 `go build ./...`
</success_criteria>

<output>
After completion, create `.planning/phases/04-duplicate-detection-search/04-02-SUMMARY.md`
</output>