---
phase: 04-duplicate-detection-search
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - flutter_app/lib/screens/duplicate_screen.dart
  - flutter_app/lib/widgets/duplicate_group_card.dart
  - flutter_app/lib/widgets/detection_progress_indicator.dart
  - flutter_app/lib/providers/duplicate_provider.dart
  - flutter_app/lib/services/duplicate_service.dart
  - flutter_app/test/screens/duplicate_screen_test.dart
  - flutter_app/test/widgets/duplicate_group_card_test.dart
  - flutter_app/pubspec.yaml
autonomous: true
requirements:
  - DUPD-04
  - DUPD-05
must_haves:
  truths:
    - 用户可以查看重复图片组列表
    - 每组显示推荐保留的图片（分辨率最高）
    - 用户可以选择要删除的图片
    - 用户可以确认删除操作
    - 删除进度实时显示
    - 删除操作不可逆，需要二次确认
  artifacts:
    - path: flutter_app/lib/screens/duplicate_screen.dart
      provides: 重复管理界面
      min_lines: 100
    - path: flutter_app/lib/widgets/duplicate_group_card.dart
      provides: 重复组卡片组件
      exports: DuplicateGroupCard
    - path: flutter_app/lib/providers/duplicate_provider.dart
      provides: 重复检测状态管理
      exports: DuplicateProvider
    - path: flutter_app/lib/services/duplicate_service.dart
      provides: 重复检测 API 服务
      exports: DuplicateService
  key_links:
    - from: flutter_app/lib/screens/duplicate_screen.dart
      to: flutter_app/lib/providers/duplicate_provider.dart
      via: Provider 模式
      pattern: Provider\.of<DuplicateProvider>
    - from: flutter_app/lib/providers/duplicate_provider.dart
      to: flutter_app/lib/services/duplicate_service.dart
      via: API 调用
      pattern: duplicateService\.getGroups|detectDuplicates
    - from: flutter_app/lib/services/duplicate_service.dart
      to: /api/v1/duplicates
      via: HTTP 请求
      pattern: http\.get|http\.post
---

<objective>
实现重复检测前端界面：重复组列表、批量选择删除、进度显示、二次确认对话框。

Purpose: 让用户能够直观地管理和删除重复图片。
Output: 完整的重复管理 Flutter 界面。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-duplicate-detection-search/04-CONTEXT.md
@.planning/phases/04-duplicate-detection-search/04-RESEARCH.md

<interfaces>
<!-- 现有代码接口 -->

From flutter_app/lib/services/api_service.dart:
```dart
class ApiService {
  Future<Map<String, dynamic>> fetchImages({int limit = 20, int offset = 0});
  Future<Map<String, dynamic>> fetchImage(int id);
}
```

From flutter_app/lib/providers/image_provider.dart:
```dart
class ImageListProvider with ChangeNotifier {
  List<ImageModel> images = [];
  bool isLoading = false;
  Future<void> loadImages();
}
```

From flutter_app/lib/widgets/image_grid.dart:
```dart
class ImageGrid extends StatelessWidget {
  final List<ImageModel> images;
  final Function(ImageModel)? onTap;
}
```

From flutter_app/lib/screens/gallery_screen.dart:
```dart
// Gallery 页面模式参考
// 包含 AppBar、视图切换、排序、分页等
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 实现重复检测 API 服务</name>
  <files>flutter_app/lib/services/duplicate_service.dart, flutter_app/test/services/duplicate_service_test.dart</files>
  <behavior>
    - Test 1: detectDuplicates 返回检测状态
    - Test 2: getDuplicateGroups 返回重复组列表
    - Test 3: getDuplicateGroupDetail 返回单个组详情
    - Test 4: deleteImages 发送删除请求
  </behavior>
  <action>
    创建 `flutter_app/lib/services/duplicate_service.dart`：
    
    ```dart
    import 'package:http/http.dart' as http;
    import 'dart:convert';
    import '../config/api_config.dart';
    import '../models/image.dart';
    
    class DuplicateGroup {
      final int id;
      final int recommendedImageId;
      final int similarityThreshold;
      final DateTime createdAt;
      final List<DuplicateRelation> relations;
      
      DuplicateGroup.fromJson(Map<String, dynamic> json);
    }
    
    class DuplicateRelation {
      final int imageId;
      final bool isRecommended;
      final String? fileHash;
      final int? pHashDistance;
      final ImageModel? image;  // 包含图片详情
      
      DuplicateRelation.fromJson(Map<String, dynamic> json);
    }
    
    class DuplicateService {
      // 触发重复检测
      Future<Map<String, dynamic>> detectDuplicates({int threshold = 10});
      
      // 获取重复组列表
      Future<List<DuplicateGroup>> getDuplicateGroups({int limit = 20, int offset = 0});
      
      // 获取单个组详情
      Future<DuplicateGroup> getDuplicateGroupDetail(int groupId);
      
      // 删除指定图片（实际删除文件）
      Future<void> deleteImages(List<int> imageIds);
      
      // 删除重复组记录（不删除图片）
      Future<void> deleteGroup(int groupId);
    }
    ```
    
    编写测试使用 mockito 模拟 HTTP 响应。
  </action>
  <verify>
    <automated>flutter test test/services/duplicate_service_test.dart</automated>
  </verify>
  <done>重复检测 API 服务完成，支持触发检测、查询列表、删除图片</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 实现重复检测状态管理</name>
  <files>flutter_app/lib/providers/duplicate_provider.dart, flutter_app/test/providers/duplicate_provider_test.dart</files>
  <behavior>
    - Test 1: loadGroups 加载重复组列表
    - Test 2: startDetection 触发检测并更新状态
    - Test 3: selectImage 选择/取消选择图片
    - Test 4: deleteSelected 删除选中的图片
    - Test 5: 计算选中图片的总大小
  </behavior>
  <action>
    创建 `flutter_app/lib/providers/duplicate_provider.dart`：
    
    ```dart
    import 'package:flutter/foundation.dart';
    import '../services/duplicate_service.dart';
    import '../models/image.dart';
    
    enum DetectionStatus { idle, detecting, completed, error }
    
    class DuplicateProvider with ChangeNotifier {
      final DuplicateService _service;
      
      List<DuplicateGroup> _groups = [];
      DetectionStatus _status = DetectionStatus.idle;
      Set<int> _selectedImageIds = {};
      bool _isLoading = false;
      String? _errorMessage;
      int? _detectionProgress;  // 0-100
      
      // Getters
      List<DuplicateGroup> get groups => _groups;
      DetectionStatus get status => _status;
      Set<int> get selectedImageIds => _selectedImageIds;
      bool get isLoading => _isLoading;
      String? get errorMessage => _errorMessage;
      int get selectedCount => _selectedImageIds.length;
      
      // 计算选中图片的总大小（用于显示将释放的空间）
      int get totalSelectedSize;
      
      // 加载重复组列表
      Future<void> loadGroups();
      
      // 触发检测
      Future<void> startDetection({int threshold = 10});
      
      // 选择/取消选择图片
      void toggleImageSelection(int imageId);
      void selectAllInGroup(int groupId);
      void deselectAllInGroup(int groupId);
      void clearSelection();
      
      // 删除选中的图片
      Future<bool> deleteSelected();
      
      // 删除重复组记录
      Future<void> deleteGroup(int groupId);
    }
    ```
    
    编写测试验证状态管理逻辑。
  </action>
  <verify>
    <automated>flutter test test/providers/duplicate_provider_test.dart</automated>
  </verify>
  <done>重复检测状态管理完成，支持列表加载、选择、删除</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 实现重复组卡片组件</name>
  <files>flutter_app/lib/widgets/duplicate_group_card.dart, flutter_app/lib/widgets/detection_progress_indicator.dart, flutter_app/test/widgets/duplicate_group_card_test.dart</files>
  <behavior>
    - Test 1: 卡片显示推荐图片缩略图
    - Test 2: 卡片显示重复数量
    - Test 3: 展开后显示所有图片
    - Test 4: 点击图片可选择/取消选择
    - Test 5: 推荐图片有特殊标记
    - Test 6: 显示每张图片的分辨率和文件大小
  </behavior>
  <action>
    1. 创建 `flutter_app/lib/widgets/duplicate_group_card.dart`：
       ```dart
       import 'package:flutter/material.dart';
       import 'package:cached_network_image/cached_network_image.dart';
       import '../providers/duplicate_provider.dart';
       
       class DuplicateGroupCard extends StatefulWidget {
         final DuplicateGroup group;
         final VoidCallback? onDelete;
         
         @override
         State<DuplicateGroupCard> createState() => _DuplicateGroupCardState();
       }
       
       class _DuplicateGroupCardState extends State<DuplicateGroupCard> {
         bool _isExpanded = false;
         
         @override
         Widget build(BuildContext context) {
           return Card(
             child: Column(
               children: [
                 // 头部：推荐图片 + 统计信息
                 _buildHeader(),
                 // 展开后：所有图片网格
                 if (_isExpanded) _buildImageGrid(),
                 // 操作按钮
                 _buildActions(),
               ],
             ),
           );
         }
         
         Widget _buildHeader() {
           // 显示推荐图片缩略图
           // 显示 "找到 N 张相似图片"
           // 点击展开/收起
         }
         
         Widget _buildImageGrid() {
           // 显示所有图片，可勾选
           // 推荐图片有 "推荐" 标签
           // 显示分辨率、文件大小
         }
         
         Widget _buildActions() {
           // "删除选中" 按钮
           // "忽略此组" 按钮
         }
       }
       ```
    
    2. 创建 `flutter_app/lib/widgets/detection_progress_indicator.dart`：
       ```dart
       // 检测进度指示器
       class DetectionProgressIndicator extends StatelessWidget {
         final int progress;  // 0-100
         final VoidCallback? onCancel;
         
         @override
         Widget build(BuildContext context) {
           return Column(
             children: [
               LinearProgressIndicator(value: progress / 100),
               Text('正在检测重复图片... $progress%'),
               if (onCancel != null)
                 TextButton(onPressed: onCancel, child: Text('取消')),
             ],
           );
         }
       }
       ```
    
    3. 编写 widget 测试
  </action>
  <verify>
    <automated>flutter test test/widgets/duplicate_group_card_test.dart</automated>
  </verify>
  <done>重复组卡片组件完成，支持展开查看、选择删除</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: 实现重复管理界面</name>
  <files>flutter_app/lib/screens/duplicate_screen.dart, flutter_app/test/screens/duplicate_screen_test.dart, flutter_app/lib/main.dart</files>
  <behavior>
    - Test 1: 页面显示重复组列表
    - Test 2: 点击"开始检测"触发检测
    - Test 3: 检测过程中显示进度
    - Test 4: 选择图片后显示"删除选中"按钮
    - Test 5: 删除前弹出确认对话框
    - Test 6: 空状态显示提示
  </behavior>
  <action>
    创建 `flutter_app/lib/screens/duplicate_screen.dart`：
    
    ```dart
    import 'package:flutter/material.dart';
    import 'package:provider/provider.dart';
    import '../providers/duplicate_provider.dart';
    import '../widgets/duplicate_group_card.dart';
    import '../widgets/detection_progress_indicator.dart';
    
    class DuplicateScreen extends StatefulWidget {
      @override
      State<DuplicateScreen> createState() => _DuplicateScreenState();
    }
    
    class _DuplicateScreenState extends State<DuplicateScreen> {
      @override
      void initState() {
        super.initState();
        // 初始加载重复组
        context.read<DuplicateProvider>().loadGroups();
      }
      
      @override
      Widget build(BuildContext context) {
        return Scaffold(
          appBar: AppBar(
            title: Text('重复图片管理'),
            actions: [
              // 阈值设置
              IconButton(
                icon: Icon(Icons.settings),
                onPressed: _showThresholdSettings,
              ),
            ],
          ),
          body: Consumer<DuplicateProvider>(
            builder: (context, provider, child) {
              if (provider.status == DetectionStatus.detecting) {
                return DetectionProgressIndicator(
                  progress: provider._detectionProgress ?? 0,
                );
              }
              
              if (provider.isLoading) {
                return Center(child: CircularProgressIndicator());
              }
              
              if (provider.groups.isEmpty) {
                return _buildEmptyState();
              }
              
              return Column(
                children: [
                  // 选中统计
                  if (provider.selectedCount > 0) _buildSelectionBar(provider),
                  // 重复组列表
                  Expanded(
                    child: ListView.builder(
                      itemCount: provider.groups.length,
                      itemBuilder: (context, index) {
                        return DuplicateGroupCard(
                          group: provider.groups[index],
                        );
                      },
                    ),
                  ),
                ],
              );
            },
          ),
          floatingActionButton: FloatingActionButton(
            onPressed: () => _startDetection(context),
            child: Icon(Icons.search),
            tooltip: '开始检测',
          ),
        );
      }
      
      Widget _buildEmptyState() {
        return Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Icon(Icons.check_circle, size: 64, color: Colors.green),
              SizedBox(height: 16),
              Text('没有发现重复图片', style: TextStyle(fontSize: 18)),
              SizedBox(height: 8),
              Text('点击右下角按钮开始检测'),
            ],
          ),
        );
      }
      
      Widget _buildSelectionBar(DuplicateProvider provider) {
        return Container(
          padding: EdgeInsets.all(16),
          color: Theme.of(context).primaryColorLight,
          child: Row(
            children: [
              Text('已选择 ${provider.selectedCount} 张图片'),
              SizedBox(width: 8),
              Text('(${_formatSize(provider.totalSelectedSize)})'),
              Spacer(),
              TextButton(
                onPressed: () => provider.clearSelection(),
                child: Text('取消选择'),
              ),
              ElevatedButton(
                onPressed: () => _confirmDelete(context, provider),
                child: Text('删除选中'),
                style: ElevatedButton.styleFrom(backgroundColor: Colors.red),
              ),
            ],
          ),
        );
      }
      
      void _confirmDelete(BuildContext context, DuplicateProvider provider) {
        showDialog(
          context: context,
          builder: (context) => AlertDialog(
            title: Text('确认删除'),
            content: Text('确定要删除 ${provider.selectedCount} 张图片吗？\n'
                '此操作不可撤销，将释放 ${_formatSize(provider.totalSelectedSize)}。'),
            actions: [
              TextButton(
                onPressed: () => Navigator.pop(context),
                child: Text('取消'),
              ),
              ElevatedButton(
                onPressed: () async {
                  Navigator.pop(context);
                  final success = await provider.deleteSelected();
                  if (success && mounted) {
                    ScaffoldMessenger.of(context).showSnackBar(
                      SnackBar(content: Text('已删除 ${provider.selectedCount} 张图片')),
                    );
                  }
                },
                style: ElevatedButton.styleFrom(backgroundColor: Colors.red),
                child: Text('确认删除'),
              ),
            ],
          ),
        );
      }
      
      void _showThresholdSettings() {
        // 显示阈值设置对话框
      }
      
      String _formatSize(int bytes) {
        if (bytes < 1024) return '$bytes B';
        if (bytes < 1024 * 1024) return '${(bytes / 1024).toStringAsFixed(1)} KB';
        return '${(bytes / 1024 / 1024).toStringAsFixed(1)} MB';
      }
    }
    ```
    
    在 `main.dart` 中添加导航到重复管理页面的入口。
    
    编写 widget 测试。
  </action>
  <verify>
    <automated>flutter test test/screens/duplicate_screen_test.dart</automated>
  </verify>
  <done>重复管理界面完成，支持查看、选择、删除重复图片</done>
</task>

</tasks>

<verification>
### Flutter 分析
```bash
cd flutter_app && flutter analyze
```

### Flutter 测试
```bash
cd flutter_app && flutter test
```

### 手动验证
1. 启动后端：`go run ./cmd/server`
2. 启动 Flutter：`cd flutter_app && flutter run -d windows`
3. 导航到重复管理页面
4. 点击"开始检测"，验证进度显示
5. 展开重复组，选择图片
6. 确认删除，验证对话框
</verification>

<success_criteria>
1. DuplicateService API 调用正常
2. DuplicateProvider 状态管理正确
3. 重复组卡片显示推荐图片和统计信息
4. 可展开查看所有图片
5. 可选择和取消选择图片
6. 删除确认对话框正常工作
7. 所有测试通过
8. Flutter analyze 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/04-duplicate-detection-search/04-03-SUMMARY.md`
</output>