---
phase: 11-task-platform-batch-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/domain/task_batch.go
  - internal/domain/platform_task.go
  - internal/domain/async_job.go
  - internal/repository/schema.go
  - internal/repository/task_batch_repository.go
  - internal/repository/platform_task_repository.go
  - internal/repository/task_batch_repository_test.go
  - internal/repository/platform_task_repository_test.go
autonomous: true
requirements: [PIPE-01, PIPE-03]
must_haves:
  truths:
    - "系统中存在可持久化的导入后批次模型，而不是只有裸 async job。"
    - "平台任务有独立于 async_jobs 的业务语义字段，后续可按图片、任务类型、状态追踪。"
    - "底层 async job 可以关联到平台任务，后续调度和读模型不会丢失链路。"
  artifacts:
    - path: "internal/domain/task_batch.go"
      provides: "批次领域模型与状态常量"
      min_lines: 40
    - path: "internal/domain/platform_task.go"
      provides: "平台任务领域模型与去重相关字段"
      min_lines: 45
    - path: "internal/repository/schema.go"
      provides: "task_batches、task_batch_sources、platform_tasks 与 async_jobs 关联字段"
      contains: "CREATE TABLE IF NOT EXISTS task_batches"
    - path: "internal/repository/task_batch_repository.go"
      provides: "批次仓储接口契约"
      contains: "type TaskBatchRepository interface"
    - path: "internal/repository/platform_task_repository.go"
      provides: "平台任务仓储接口契约"
      contains: "type PlatformTaskRepository interface"
  key_links:
    - from: "internal/domain/task_batch.go"
      to: "internal/repository/schema.go"
      via: "字段与状态常量映射"
      pattern: "task_batches"
    - from: "internal/domain/platform_task.go"
      to: "internal/domain/async_job.go"
      via: "platform_task_id 关联"
      pattern: "PlatformTaskID"
---

<objective>
固化任务平台的基础契约：批次、平台任务、与 async job 的关联字段，以及 SQLite schema 基线。

Purpose: 先把 Phase 11 的核心数据边界锁定，避免后续持久化、调度、后台查询在不同语义上各自生长。
Output: 可编译的领域模型、repository 契约、schema 迁移与首批测试骨架。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-task-platform-batch-model/11-CONTEXT.md
@.planning/phases/11-task-platform-batch-model/11-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-VALIDATION.md
@internal/domain/async_job.go
@internal/repository/schema.go
@internal/repository/job_repository.go

<interfaces>
From `internal/domain/async_job.go`:
```go
type AsyncJob struct {
    ID         int64
    Type       string
    Status     string
    Payload    string
    Progress   float64
    Error      *string
    CreatedAt  time.Time
    StartedAt  *time.Time
    FinishedAt *time.Time
}
```

From `internal/repository/job_repository.go`:
```go
type JobRepository interface {
    Save(job *domain.AsyncJob) error
    FindByID(id int64) (*domain.AsyncJob, error)
    FindByStatus(status string) ([]domain.AsyncJob, error)
    Update(job *domain.AsyncJob) error
}
```

Use these as the existing execution-layer baseline. Phase 11 must add platform semantics above this layer, not replace it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为批次与平台任务契约写失败测试骨架</name>
  <files>internal/repository/task_batch_repository_test.go, internal/repository/platform_task_repository_test.go</files>
  <behavior>
    - Test 1: schema 初始化后存在 `task_batches`、`task_batch_sources`、`platform_tasks` 三张表。
    - Test 2: `platform_tasks` 能保存 `batch_id + image_id + task_type + image_version_key + dedupe_key`。
    - Test 3: `async_jobs` 新增的 `platform_task_id` 关联字段在 schema 中存在且可为空。
  </behavior>
  <action>新增 repository 级测试文件，沿用 `job_repository_test.go` 的 SQLite 内存库模式。先把 Phase 11 需要的 schema/字段断言写出来并运行到失败；不要先写实现。测试命名要直接反映批次表、平台任务表、async_jobs 关联字段的存在性。</action>
  <verify>
    <automated>go test ./internal/repository/... -run "TaskBatch|PlatformTask|Schema" -count=1</automated>
  </verify>
  <done>测试明确表达 Phase 11 的表结构与关键字段要求，并在实现前失败。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 定义领域模型、状态常量与 repository 契约</name>
  <files>internal/domain/task_batch.go, internal/domain/platform_task.go, internal/domain/async_job.go, internal/repository/schema.go, internal/repository/task_batch_repository.go, internal/repository/platform_task_repository.go</files>
  <behavior>
    - Test 1: 批次模型能表达 source type、summary、partial_failed/cancelled 等状态语义。
    - Test 2: 平台任务模型能表达 task type、dedupe key、image version key、skip reason、latest async job 关联。
    - Test 3: schema 应包含 `task_batches`、`task_batch_sources`、`platform_tasks`，并为 `async_jobs` 增加 `platform_task_id` 可空关联列和必要索引。
  </behavior>
  <action>创建批次与平台任务领域模型文件，补齐状态常量和 JSON/db 字段；扩展 `AsyncJob` 增加 `PlatformTaskID *int64` 字段。更新 `schema.go`，以 SQLite `CREATE TABLE IF NOT EXISTS` / `ALTER TABLE` 兼容方式加入三张新表与索引，不要删除或重命名现有 `async_jobs`。在 repository 文件中先定义接口、构造函数、必要的查询方法签名（Create/Find/List/Aggregate/Dedupe 相关），实现可以保留到 11-02，但文件必须可编译。</action>
  <verify>
    <automated>go test ./internal/repository/... -run "TaskBatch|PlatformTask|Schema" -count=1</automated>
  </verify>
  <done>核心 schema 和领域契约落地，repository 测试转绿，后续计划无需再猜 Phase 11 的主模型边界。</done>
</task>

</tasks>

<verification>
- `go test ./internal/repository/... -run "TaskBatch|PlatformTask|Schema" -count=1`
- 确认 `EnsureScanSchema` 在新库上可创建新增表与索引，不破坏现有 `images/tags/async_jobs`。
</verification>

<success_criteria>
- 批次、平台任务、async job 关联字段契约全部落盘。
- Schema 初始化后存在 Phase 11 所需主表与索引。
- 后续计划可以直接基于这些契约实现持久化与调度，无需再重定模型。
</success_criteria>

<output>
After completion, create `.planning/phases/11-task-platform-batch-model/11-01-SUMMARY.md`
</output>
