---
phase: 11-task-platform-batch-model
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - internal/repository/task_batch_repository.go
  - internal/repository/platform_task_repository.go
  - internal/repository/job_repository.go
  - internal/repository/job_repository_test.go
  - internal/service/task_platform_service.go
  - internal/service/task_platform_service_test.go
  - internal/repository/task_batch_repository_test.go
  - internal/repository/platform_task_repository_test.go
autonomous: true
requirements: [PIPE-03, SAFE-03]
must_haves:
  truths:
    - "平台任务状态可以稳定从 pending/queued/running 流转到 completed/failed/skipped。"
    - "批次总状态由任务终态聚合，而不是导入动作结束即提前完成。"
    - "同一图片、同一任务类型、同一版本不会被重复创建执行任务。"
  artifacts:
    - path: "internal/service/task_platform_service.go"
      provides: "批次/任务创建、状态流转与重复保护规则"
      min_lines: 120
    - path: "internal/repository/platform_task_repository.go"
      provides: "平台任务持久化、去重查询与状态更新"
      contains: "FindActiveByDedupeKey"
    - path: "internal/repository/task_batch_repository.go"
      provides: "批次聚合状态更新与来源记录"
      contains: "RefreshStatus"
  key_links:
    - from: "internal/service/task_platform_service.go"
      to: "internal/repository/platform_task_repository.go"
      via: "去重查询与任务创建"
      pattern: "Find.*Dedupe|Create"
    - from: "internal/service/task_platform_service.go"
      to: "internal/repository/task_batch_repository.go"
      via: "批次状态聚合"
      pattern: "RefreshStatus|UpdateSummary"
---

<objective>
实现任务平台的持久化规则、状态机与重复保护，让 Phase 11 的主模型真正可用。

Purpose: 没有 repository/service 级规则，批次与平台任务只是空壳，无法支撑后续导入接入与后台查询。
Output: 可测试的平台任务 repository 与 service，支持状态流转、批次聚合、去重跳过。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-task-platform-batch-model/11-CONTEXT.md
@.planning/phases/11-task-platform-batch-model/11-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-VALIDATION.md
@.planning/phases/11-task-platform-batch-model/11-01-PLAN.md
@internal/repository/task_batch_repository.go
@internal/repository/platform_task_repository.go
@internal/repository/job_repository.go

<interfaces>
This plan inherits the contracts from 11-01. Keep these semantics exact:

```go
// batch states must support partial failure and cancellation
const (
    TaskBatchStatusPending = "pending"
    TaskBatchStatusRunning = "running"
    TaskBatchStatusCompleted = "completed"
    TaskBatchStatusPartialFailed = "partial_failed"
    TaskBatchStatusFailed = "failed"
    TaskBatchStatusCancelled = "cancelled"
)
```

```go
// platform task states must keep queued/running/completed/failed/skipped
// dedupe is based on image version/content, not file path equality alone
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为生命周期聚合与重复保护写失败测试</name>
  <files>internal/service/task_platform_service_test.go, internal/repository/task_batch_repository_test.go, internal/repository/platform_task_repository_test.go, internal/repository/job_repository_test.go</files>
  <behavior>
    - Test 1: 同一 `dedupe_key` 已存在 active/completed 平台任务时，不再重复创建执行任务。
    - Test 2: 若同图缩略图已完成而 AI 任务不存在，只补缺失任务类型，不整图跳过。
    - Test 3: 批次在所有任务进入终态前不能标记 completed；有失败时聚合为 `partial_failed`。
    - Test 4: `async_jobs.platform_task_id` 保存后可通过 repository 查回关联。
  </behavior>
  <action>先为 service/repository 写失败测试，覆盖 dedupe、状态流转、批次聚合和 async job 关联。测试场景要直接对应 CONTEXT.md 的锁定决策，不要退化成只测简单 CRUD。</action>
  <verify>
    <automated>go test ./internal/repository/... -run "TaskBatch|PlatformTask|Job" -count=1 && go test ./internal/service/... -run "TaskPlatform|Duplicate|Lifecycle" -count=1</automated>
  </verify>
  <done>失败测试明确锁定去重规则、状态机和批次聚合口径。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 实现 repository 持久化与 async job 关联</name>
  <files>internal/repository/task_batch_repository.go, internal/repository/platform_task_repository.go, internal/repository/job_repository.go, internal/repository/job_repository_test.go, internal/repository/task_batch_repository_test.go, internal/repository/platform_task_repository_test.go</files>
  <behavior>
    - Test 1: repository 能创建批次、来源记录、平台任务，并按批次/任务类型/状态查询。
    - Test 2: repository 能按 `dedupe_key` 查找 active/completed 任务。
    - Test 3: job repository 保存/更新时保留 `platform_task_id`，并能按该关联读回。
  </behavior>
  <action>把 11-01 的 repository 契约补成真实 SQLite 实现：创建批次、批次来源、平台任务、状态更新、按 dedupe key 查询、批次状态刷新、错误摘要回写。同步扩展 `job_repository.go` 的 INSERT/SELECT/UPDATE，使 `platform_task_id` 能被完整保存和取回。不要引入 ORM，也不要新建第二套 async job 表。</action>
  <verify>
    <automated>go test ./internal/repository/... -run "TaskBatch|PlatformTask|Job" -count=1</automated>
  </verify>
  <done>SQLite 层已经支持平台任务与 async job 关联、去重查询、批次聚合刷新。</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 实现任务平台 service 的规划、去重与状态聚合规则</name>
  <files>internal/service/task_platform_service.go, internal/service/task_platform_service_test.go</files>
  <behavior>
    - Test 1: 给定一次触发动作，service 能创建批次并仅为需要的任务类型建立平台任务。
    - Test 2: 遇到重复或未变更时，service 不重复入队，并把原因汇总回批次统计。
    - Test 3: service 能根据平台任务终态刷新批次状态为 running/completed/partial_failed。
  </behavior>
  <action>新增 `TaskPlatformService`，负责：创建批次、记录来源、按任务类型规划平台任务、生成 dedupe key、执行重复保护、回写跳过统计、刷新批次状态。显式遵守用户锁定决策：去重按内容/版本，不按路径；同图不同任务类型只补缺失类型；失败默认隔离，不整批停下。</action>
  <verify>
    <automated>go test ./internal/service/... -run "TaskPlatform|Duplicate|Lifecycle" -count=1</automated>
  </verify>
  <done>任务平台 service 已能支撑后续导入入口与手动触发入口，不再依赖零散条件判断。</done>
</task>

</tasks>

<verification>
- `go test ./internal/repository/... -run "TaskBatch|PlatformTask|Job" -count=1`
- `go test ./internal/service/... -run "TaskPlatform|Duplicate|Lifecycle" -count=1`
</verification>

<success_criteria>
- 平台任务持久化与去重逻辑可独立通过自动化测试。
- 批次状态由任务状态聚合得到，包含 `partial_failed` 语义。
- SAFE-03 的“未变更不重复入队、只补缺失任务类型”已经体现在 service 规则里。
</success_criteria>

<output>
After completion, create `.planning/phases/11-task-platform-batch-model/11-02-SUMMARY.md`
</output>
