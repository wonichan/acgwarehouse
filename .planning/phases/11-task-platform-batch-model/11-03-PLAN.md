---
phase: 11-task-platform-batch-model
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - internal/service/scanner_service.go
  - internal/service/scanner_service_test.go
  - internal/app/bootstrap.go
  - internal/handler/ai_tag_handler.go
  - internal/handler/ai_tag_handler_test.go
  - internal/service/task_platform_service.go
  - internal/repository/job_repository.go
autonomous: true
requirements: [PIPE-01, PIPE-03, SAFE-03]
must_haves:
  truths:
    - "一次导入会创建可追踪批次，而不是只留下零散 image_imported 任务。"
    - "单图手动 AI 触发进入同一任务平台，并形成自己的 manual_single 小批次。"
    - "同源重复触发时可以新建批次保留历史，但不会对未变更且已完成/进行中的任务重复入队。"
  artifacts:
    - path: "internal/service/scanner_service.go"
      provides: "导入触发 -> 批次创建 -> 平台任务规划入口"
      min_lines: 180
    - path: "internal/handler/ai_tag_handler.go"
      provides: "手动单图/批量 AI 入口接入平台批次"
      min_lines: 120
    - path: "internal/app/bootstrap.go"
      provides: "统一调度/处理器包装骨架"
      contains: "RegisterHandler"
  key_links:
    - from: "internal/service/scanner_service.go"
      to: "internal/service/task_platform_service.go"
      via: "导入完成后创建 import_scan 批次和任务"
      pattern: "TaskPlatformService|CreateBatch|Plan"
    - from: "internal/handler/ai_tag_handler.go"
      to: "internal/service/task_platform_service.go"
      via: "manual_single/manual_batch 批次创建"
      pattern: "manual_single|manual_batch"
    - from: "internal/app/bootstrap.go"
      to: "internal/repository/job_repository.go"
      via: "async job 与 platform task 关联回写"
      pattern: "platform_task_id|PlatformTask"
---

<objective>
把导入与手动触发入口真正接入统一任务平台，并建立可持续扩展的调度骨架。

Purpose: 如果入口仍直接写裸 async job，Phase 11 的模型就不会进入真实业务路径，Phase 12/13 也无法复用。
Output: 导入批次创建、手动 AI 小批次创建、统一 dispatcher / handler 包装骨架。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-task-platform-batch-model/11-CONTEXT.md
@.planning/phases/11-task-platform-batch-model/11-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-02-PLAN.md
@internal/service/scanner_service.go
@internal/app/bootstrap.go
@internal/handler/ai_tag_handler.go
@internal/worker/job_manager.go

<interfaces>
From `internal/worker/job_manager.go`:
```go
type JobFunc func(ctx context.Context, id int64, payload string) error

func (m *Manager) RegisterHandler(jobType string, handler JobFunc)
func (m *Manager) AddJob(ctx context.Context, jobType, payload string) (int64, error)
```

From `internal/service/scanner_service.go` today:
```go
// current behavior: only new images create image_imported jobs
if !isNew {
    return nil
}
```

From `internal/handler/ai_tag_handler.go` today:
```go
jobID, err := h.jobManager.AddJob(c.Request.Context(), "ai_tag_generation", string(payload))
```

Replace these direct raw-job entrypoints with platform-batch creation while preserving existing worker manager as the execution engine.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 让导入扫描入口创建 import_scan 批次与平台任务</name>
  <files>internal/service/scanner_service.go, internal/service/scanner_service_test.go, internal/service/task_platform_service.go</files>
  <behavior>
    - Test 1: 一次扫描会创建一个 `import_scan` 批次，并记录本次 roots/source summary。
    - Test 2: 仅新图片生成平台任务；未变更图片只增加批次跳过统计，不逐条重复入队。
    - Test 3: 对同图同任务类型重复触发时，新批次可存在，但平台任务不会重复进入执行队列。
  </behavior>
  <action>改造 `ScannerService`：在一次 `Scan()` 内建立统一批次上下文，调用 `TaskPlatformService` 规划平台任务，而不是在 `importFile()` 中直接裸写 `image_imported`。保持现有“只对新图片处理”的基础行为，但把它升级为批次摘要 + 平台任务语义。不要在这里实现 Phase 12 的“仅无 AI 标签自动入队”规则，只先接入缩略图等基础任务与内部调度起点。</action>
  <verify>
    <automated>go test ./internal/service/... -run "Scanner|TaskPlatform" -count=1</automated>
  </verify>
  <done>导入动作已生成批次与平台任务，重复/未变更行为进入统一平台规则。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 让手动 AI 入口改为创建 manual_single / manual_batch 批次</name>
  <files>internal/handler/ai_tag_handler.go, internal/handler/ai_tag_handler_test.go, internal/service/task_platform_service.go</files>
  <behavior>
    - Test 1: 单图 AI 触发返回的是平台批次/任务入队结果，而非纯裸 job id。
    - Test 2: 单图触发形成 `manual_single` 小批次，即使只有 1 张图也可追踪。
    - Test 3: 批量 AI 触发形成 `manual_batch` 批次，并对已完成/执行中的未变更任务跳过而非重复入队。
  </behavior>
  <action>改造 `TriggerAITags` 与 `BatchTriggerAITags`，不再直接 `AddJob("ai_tag_generation")`。改为委托 `TaskPlatformService` 创建 `manual_single` / `manual_batch` 批次并规划 AI 平台任务；响应体至少返回 `batch_id`、创建/跳过数量，以及必要时返回 task/job 概览。必须遵守用户锁定决策：单图手动触发也进入统一平台，不保留长期平台外特例。</action>
  <verify>
    <automated>go test ./internal/handler/... -run "AITag" -count=1</automated>
  </verify>
  <done>手动 AI 已进入统一平台模型，单图与批量都可按批次追踪。</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 在 bootstrap 建立统一调度与状态回写骨架</name>
  <files>internal/app/bootstrap.go, internal/service/task_platform_service.go, internal/repository/job_repository.go</files>
  <behavior>
    - Test 1: 平台任务入队时会创建带 `platform_task_id` 的 async job。
    - Test 2: 任务处理器执行前后，平台任务状态能同步更新为 queued/running/completed/failed。
    - Test 3: `image_imported` 仍可作为内部事件/调度起点，但不再作为对外主任务主体依赖。
  </behavior>
  <action>在 `bootstrap.go` 中把现有处理器注册改造成“平台任务包装器”：创建 async job 时写入 `platform_task_id`，执行前/后通过 service 回写平台任务与批次状态。可以保留 `image_imported` 作为内部事件或过渡 job type，但不要继续让后台读模型依赖它作为主任务实体。避免大改 `worker.Manager` 公共 API，优先在 bootstrap/service 层包一层，降低对既有行为的破坏面。</action>
  <verify>
    <automated>go test ./internal/service/... -run "TaskPlatform|Scanner" -count=1 && go test ./internal/handler/... -run "AITag" -count=1</automated>
  </verify>
  <done>统一调度骨架成型：入口、执行、状态回写都围绕平台任务运转。</done>
</task>

</tasks>

<verification>
- `go test ./internal/service/... -run "Scanner|TaskPlatform" -count=1`
- `go test ./internal/handler/... -run "AITag" -count=1`
</verification>

<success_criteria>
- 导入与手动 AI 入口都进入统一平台批次语义。
- async job 只是执行层，不再是业务入口的唯一记录。
- 新批次可保留历史，但不会对未变更同任务无限重复入队。
</success_criteria>

<output>
After completion, create `.planning/phases/11-task-platform-batch-model/11-03-SUMMARY.md`
</output>
