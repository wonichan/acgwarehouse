---
phase: 11-task-platform-batch-model
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - internal/repository/task_batch_read_repository.go
  - internal/service/task_read_service.go
  - internal/service/task_read_service_test.go
  - internal/service/admin_service.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
autonomous: true
requirements: [PIPE-01, PIPE-03]
must_haves:
  truths:
    - "后台可以查看批次列表，而不是只能看裸 async_jobs 列表。"
    - "后台可以查看某批次下的平台任务明细与状态统计。"
    - "批次读模型能体现来源摘要、跳过数量与关键失败摘要。"
  artifacts:
    - path: "internal/service/task_read_service.go"
      provides: "批次/任务读模型聚合服务"
      min_lines: 100
    - path: "internal/handler/admin_handler.go"
      provides: "task-batches/tasks 管理后台接口"
      contains: "GetTaskBatches"
    - path: "internal/handler/routes.go"
      provides: "新的 /admin/api/task-batches 和 /admin/api/tasks 路由"
      contains: "/admin/api"
  key_links:
    - from: "internal/service/admin_service.go"
      to: "internal/service/task_read_service.go"
      via: "后台聚合查询"
      pattern: "TaskRead"
    - from: "internal/handler/admin_handler.go"
      to: "internal/service/admin_service.go"
      via: "批次/任务查询 HTTP 入口"
      pattern: "GetTaskBatches|GetTasks"
---

<objective>
为后台管理提供可查询的批次 / 任务读模型接口，完成 Phase 11 的“可追踪入口”闭环。

Purpose: 没有读模型，前 3 个计划建立的批次与平台任务仍无法被后台消费，也无法证明平台化真的成立。
Output: 批次列表、批次详情/任务列表、状态统计的后台查询 API。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-task-platform-batch-model/11-CONTEXT.md
@.planning/phases/11-task-platform-batch-model/11-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-03-PLAN.md
@internal/service/admin_service.go
@internal/handler/admin_handler.go
@internal/handler/routes.go

<interfaces>
From `internal/service/admin_service.go` today:
```go
type AdminService struct {
    jobRepo repository.JobRepository
    jobManager JobManagerControl
}

func (s *AdminService) GetSummary(ctx context.Context) (*Summary, error)
func (s *AdminService) GetJobs(ctx context.Context, limit int) ([]interface{}, error)
```

From `internal/handler/admin_handler.go` today:
```go
type AdminServiceInterface interface {
    GetSummary(ctx context.Context) (*service.Summary, error)
    GetJobs(ctx context.Context, limit int) ([]interface{}, error)
}
```

Extend this admin pattern; do not invent a parallel router style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为批次 / 任务读模型聚合写失败测试并实现查询服务</name>
  <files>internal/repository/task_batch_read_repository.go, internal/service/task_read_service.go, internal/service/task_read_service_test.go</files>
  <behavior>
    - Test 1: 读模型能按批次返回来源摘要、状态统计、跳过摘要、失败摘要。
    - Test 2: 读模型能按批次返回平台任务列表，并带 task type、status、skip reason、latest async job id。
    - Test 3: 查询结果按创建时间倒序，支持 limit / batch_id 等基础过滤。
  </behavior>
  <action>新增 read repository / service，专门做批次与任务聚合查询。若单一 repository 文件会过大，可将复杂 SQL 放在 `task_batch_read_repository.go`；但不要把后台查询 SQL 塞回 `job_repository.go`。优先直接从 `task_batches + task_batch_sources + platform_tasks + async_jobs` 生成后台需要的 DTO，而不是让 handler 自己拼装。</action>
  <verify>
    <automated>go test ./internal/service/... -run "TaskRead|Admin" -count=1</automated>
  </verify>
  <done>后台可依赖的批次/任务读模型已经存在，字段足以支撑 Phase 13 UI 接入。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 扩展 admin service/handler/routes 暴露批次与任务查询接口</name>
  <files>internal/service/admin_service.go, internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go</files>
  <behavior>
    - Test 1: `GET /admin/api/task-batches` 返回批次列表 JSON。
    - Test 2: `GET /admin/api/task-batches/:id` 或 `GET /admin/api/tasks?batch_id=` 能返回某批次任务明细。
    - Test 3: 响应延续现有 admin JSON 风格，认证和错误处理保持一致。
  </behavior>
  <action>扩展 `AdminServiceInterface`、`AdminService`、`AdminHandler` 和 `routes.go`，加入批次/任务查询接口。沿用现有 `/admin/api/*` 路由和 JSON 响应风格；不要在 Phase 11 做完整监控 UI，也不要提前实现暂停/继续/取消/清空/重试的新动作。接口至少包含批次列表和按批次查看任务明细两类读取能力。</action>
  <verify>
    <automated>go test ./internal/handler/... -run "Admin" -count=1</automated>
  </verify>
  <done>后台 API 已能查询批次与平台任务，为 Phase 13 监控页直接提供数据来源。</done>
</task>

</tasks>

<verification>
- `go test ./internal/service/... -run "TaskRead|Admin" -count=1`
- `go test ./internal/handler/... -run "Admin" -count=1`
</verification>

<success_criteria>
- 管理后台有批次列表与任务明细读接口。
- 查询结果体现来源、状态、跳过摘要、失败摘要，而非仅返回裸 async_jobs。
- Phase 11 的“可追踪批次 + 统一任务平台入口”可通过 API 直接证明成立。
</success_criteria>

<output>
After completion, create `.planning/phases/11-task-platform-batch-model/11-04-SUMMARY.md`
</output>
