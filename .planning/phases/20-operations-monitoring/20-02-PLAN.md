---
phase: 20-operations-monitoring
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - flutter_app/lib/models/monitoring_models.dart
  - flutter_app/lib/services/monitoring_service.dart
  - flutter_app/lib/providers/monitoring_provider.dart
  - flutter_app/test/services/monitoring_service_test.dart
  - flutter_app/test/providers/monitoring_provider_test.dart
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Desktop state loads monitoring overview, batch list, task details, sidecar status, and Go runtime metrics from the backend via typed models"
    - "MonitoringProvider manages WebSocket lifecycle: connects on page enter, disconnects on page leave, reconnects with exponential backoff on failure"
    - "MonitoringProvider exposes sidecar restart action with impact count for the confirmation dialog"
  artifacts:
    - path: "flutter_app/lib/models/monitoring_models.dart"
      provides: "typed models for monitoring overview, batch rows, task details, sidecar status, Go runtime metrics, and WebSocket events"
      exports: ["MonitoringOverview", "BatchRow", "TaskDetail", "SidecarStatusCard", "GoRuntimeMetrics", "MonitoringWsEvent"]
    - path: "flutter_app/lib/services/monitoring_service.dart"
      provides: "HTTP client methods for monitoring REST endpoints and sidecar restart"
      exports: ["MonitoringService"]
    - path: "flutter_app/lib/providers/monitoring_provider.dart"
      provides: "provider-owned monitoring state with WebSocket lifecycle, reconnection, and restart orchestration"
      exports: ["MonitoringProvider"]
  key_links:
    - from: "flutter_app/lib/services/monitoring_service.dart"
      to: "/admin/api/task-platform/overview"
      via: "overview fetch"
      pattern: "task-platform/overview"
    - from: "flutter_app/lib/services/monitoring_service.dart"
      to: "/admin/api/task-batches"
      via: "batch list fetch"
      pattern: "task-batches"
    - from: "flutter_app/lib/services/monitoring_service.dart"
      to: "/admin/api/actions/sidecar/restart"
      via: "sidecar restart"
      pattern: "sidecar/restart"
    - from: "flutter_app/lib/providers/monitoring_provider.dart"
      to: "flutter_app/lib/models/monitoring_models.dart"
      via: "typed desktop state"
      pattern: "MonitoringOverview|BatchRow|SidecarStatusCard"
    - from: "flutter_app/lib/providers/monitoring_provider.dart"
      to: "flutter_app/lib/services/monitoring_service.dart"
      via: "REST calls and WebSocket connection"
      pattern: "MonitoringService"
---

<objective>
Build the Flutter data contracts and state orchestration required to drive Phase 20's desktop monitoring workspace.

Purpose: This plan translates the backend WebSocket and REST contracts from Plan 01 into typed Flutter models and a provider-driven workflow that satisfies D-04 through D-16, especially real-time WebSocket updates, sidecar restart with impact, and resilient connection management.

Output: Typed monitoring models, service methods for monitoring endpoints, and a MonitoringProvider that drives the monitoring workspace with real-time updates and restart capability.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-operations-monitoring/20-CONTEXT.md
@.planning/phases/20-operations-monitoring/20-01-PLAN.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/19-tag-management/19-02-SUMMARY.md

<interfaces>
From `flutter_app/lib/config/api_config.dart`:
```dart
class ApiConfig {
  static String get hostUrl => _hostUrl;
  static String get baseUrl => '$hostUrl/api/v1';
  static void updateBaseUrl(String url);
  // ... existing endpoints
}
```

From `flutter_app/lib/services/tag_service.dart` (pattern reference):
```dart
class TagService {
  Future<List<TagStatistics>> getTagStatistics() async
  Future<Tag> updateTag(int tagId, {...}) async
}
```

From `flutter_app/lib/providers/tag_provider.dart` (pattern reference):
```dart
class TagProvider extends ChangeNotifier {
  Future<void> loadStatistics() async
  // ... state fields + notifyListeners()
}
```

From backend `internal/service/admin_service.go`:
```go
type TaskPlatformOverview struct {
    Health  HealthStatus               `json:"health"`
    Queue   QueueOverview              `json:"queue"`
    Sidecar SidecarDiagnosticsOverview `json:"sidecar"`
    Batches map[string]int64           `json:"batches"`
    Tasks   map[string]int64           `json:"tasks"`
}
type QueueOverview struct { IsRunning, IsPaused bool; QueueSize, WorkerCount int }
type SidecarDiagnosticsOverview struct { State, LastProbeAt, LastProbeResult, LastErrorSummary string }
```

From backend `internal/service/task_read_service.go`:
```go
type TaskBatchReadModel struct {
    ID int64; SourceType, SummaryLabel, Status string; TotalImages, NewImages int64
    StatusCounts, TaskTypeCounts map[string]int64; FailureGroups []TaskBatchFailureGroup
}
type TaskReadModel struct {
    ID, BatchID, ImageID int64; ImagePath, ImageFilename, TaskType, Status, ErrorSummary string
}
type TaskBatchFailureGroup struct { ReasonKey, ReasonLabel string; Count int64; RetryRecommended bool; RetryHint string }
```

From Plan 01 `internal/service/monitoring_event_bus.go`:
```go
type MonitoringEvent struct {
    Type      string          `json:"type"` // "overview", "batches", "sidecar"
    Payload   json.RawMessage `json:"payload"`
    Timestamp time.Time       `json:"timestamp"`
}
```

Locked decisions from `20-CONTEXT.md`:
```text
D-04 batch-first + click-to-drill-down
D-08 extended to Go runtime metrics
D-11 WebSocket real-time push
D-12 connect on enter, disconnect on leave, reconnect on disconnect
D-16 monitoring page works even when sidecar is stopped
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green typed monitoring models and service contracts</name>
  <files>
    flutter_app/lib/models/monitoring_models.dart,
    flutter_app/lib/services/monitoring_service.dart,
    flutter_app/test/services/monitoring_service_test.dart
  </files>
  <read_first>
    flutter_app/lib/models/tag.dart,
    flutter_app/lib/services/tag_service.dart,
    flutter_app/test/services/tag_service_test.dart,
    flutter_app/lib/config/api_config.dart,
    .planning/phases/20-operations-monitoring/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: MonitoringOverview.fromJson parses backend TaskPlatformOverview JSON with nested queue, sidecar, batches map, and tasks map
    - Test 2: BatchRow.fromJson parses TaskBatchReadModel JSON with status_counts, task_type_counts, and failure_groups
    - Test 3: TaskDetail.fromJson parses TaskReadModel JSON with image_id, image_filename, status, and error_summary
    - Test 4: MonitoringService.fetchOverview calls GET /admin/api/task-platform/overview and returns typed MonitoringOverview
    - Test 5: MonitoringService.fetchBatches calls GET /admin/api/task-batches and returns typed List&lt;BatchRow&gt;
    - Test 6: MonitoringService.fetchTasks calls GET /admin/api/tasks?batch_id=X and returns typed List&lt;TaskDetail&gt;
    - Test 7: MonitoringService.restartSidecar calls POST /admin/api/actions/sidecar/restart and returns RestartImpact with interrupted_task_count
  </behavior>
  <action>
    1. Add `web_socket_channel: ^3.0.0` (or latest compatible) to `flutter_app/pubspec.yaml`.
    2. Create `flutter_app/lib/models/monitoring_models.dart` with these types:
       - `MonitoringOverview` — maps to TaskPlatformOverview: health, queue (isRunning, isPaused, queueSize, workerCount), sidecar (state, lastProbeAt, lastProbeResult, lastErrorSummary), batches (Map&lt;String, int&gt;), tasks (Map&lt;String, int&gt;)
       - `BatchRow` — maps to TaskBatchReadModel: id, sourceType, summaryLabel, status, totalImages, newImages, statusCounts, taskTypeCounts, failureGroups
       - `TaskDetail` — maps to TaskReadModel: id, batchId, imageId, imagePath, imageFilename, taskType, status, errorSummary
       - `FailureGroup` — maps to TaskBatchFailureGroup: reasonKey, reasonLabel, count, retryRecommended, retryHint
       - `SidecarStatusCard` — presentation model: state (display string), semanticColor, uptime, lastProbeTime, lastErrorSummary, canRestart
       - `GoRuntimeMetrics` — presentation model: queueDepth, activeWorkers, pendingTasks, isRunning, isPaused
       - `MonitoringWsEvent` — WebSocket event: type (String), payload (raw JSON string), timestamp (DateTime)
       - `RestartImpact` — restart result: interruptedTaskCount
       All models must have `fromJson` factory constructors and `toJson` methods.
    3. Create `flutter_app/lib/services/monitoring_service.dart` with a `MonitoringService` class:
       - `fetchOverview()` → GET `${ApiConfig.hostUrl}/admin/api/task-platform/overview`
       - `fetchBatches({int? limit})` → GET `${ApiConfig.hostUrl}/admin/api/task-batches`
       - `fetchTasks({int? batchId, int? limit})` → GET `${ApiConfig.hostUrl}/admin/api/tasks`
       - `restartSidecar()` → POST `${ApiConfig.hostUrl}/admin/api/actions/sidecar/restart`
       Use `ApiConfig.hostUrl` (NOT `baseUrl`) since admin endpoints are under `/admin/api`, not `/api/v1`.
       Include Basic Auth headers from app configuration.
    4. Add failing tests in `flutter_app/test/services/monitoring_service_test.dart` using mock HTTP client.
    5. Stage commits as:
       - `test(20-02): add failing monitoring service coverage`
       - `feat(20-02): add typed monitoring models and service contracts`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/services/monitoring_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/models/monitoring_models.dart` exists
    - `flutter_app/lib/models/monitoring_models.dart` contains `class MonitoringOverview`
    - `flutter_app/lib/models/monitoring_models.dart` contains `class BatchRow`
    - `flutter_app/lib/models/monitoring_models.dart` contains `class TaskDetail`
    - `flutter_app/lib/models/monitoring_models.dart` contains `class RestartImpact`
    - `flutter_app/lib/services/monitoring_service.dart` contains `fetchOverview`
    - `flutter_app/lib/services/monitoring_service.dart` contains `fetchBatches`
    - `flutter_app/lib/services/monitoring_service.dart` contains `restartSidecar`
    - `flutter_app/lib/services/monitoring_service.dart` contains `/admin/api/task-platform/overview`
    - `cd flutter_app; flutter test test/services/monitoring_service_test.dart` exits `0`
  </acceptance_criteria>
  <done>The Flutter client layer can consume every backend monitoring contract without raw-map parsing in widgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green provider-owned monitoring state with WebSocket lifecycle</name>
  <files>
    flutter_app/lib/providers/monitoring_provider.dart,
    flutter_app/test/providers/monitoring_provider_test.dart
  </files>
  <read_first>
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/models/monitoring_models.dart,
    flutter_app/lib/services/monitoring_service.dart,
    .planning/phases/20-operations-monitoring/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: provider loads initial overview and batch list on connect(), setting isLoading=false when complete
    - Test 2: provider.connect() establishes WebSocket to ws://host/admin/api/monitoring/ws and updates state on incoming events
    - Test 3: provider.disconnect() closes WebSocket and cleans up resources (D-12)
    - Test 4: provider handles WebSocket errors by setting wsConnected=false and attempting reconnection with backoff
    - Test 5: provider.restartSidecar() calls service.restartSidecar(), updates state with impact count, and does NOT auto-disconnect WebSocket
    - Test 6: provider exposes selectedBatchId for task drilldown; selectBatch(id) triggers fetchTasks(batchId: id)
    - Test 7: provider exposes edge state: serviceUnavailable when REST calls fail, with retry action
    - Test 8: provider works correctly even when sidecar state is "stopped" — batch list still loads (D-16)
  </behavior>
  <action>
    1. Add failing provider tests in `flutter_app/test/providers/monitoring_provider_test.dart` for:
       - `connect()` — establishes WebSocket, loads initial data
       - `disconnect()` — closes WebSocket, cleans up
       - `restartSidecar()` — calls service, reports impact
       - `selectBatch(int? id)` — sets selected batch, loads tasks
       - `retryLoad()` — retries initial data load on failure
       State fields: `overview`, `batches`, `tasks`, `wsConnected`, `isLoading`, `isRestarting`, `serviceUnavailable`, `restartImpact`, `selectedBatchId`
    2. Create `flutter_app/lib/providers/monitoring_provider.dart` with a `MonitoringProvider extends ChangeNotifier`:
       - Constructor takes `MonitoringService` and a WebSocket URI factory
       - `connect()`: loads overview + batches via REST, opens WebSocket to `${wsHost}/admin/api/monitoring/ws`
       - `disconnect()`: closes WebSocket channel, cancels subscriptions
       - On WebSocket message: parse `MonitoringWsEvent`, update relevant state slice based on event type
       - `restartSidecar()`: set isRestarting=true, call service.restartSidecar(), store RestartImpact, set isRestarting=false, trigger data refresh
       - `selectBatch(int? id)`: set selectedBatchId, if non-null load tasks for that batch
       - `retryLoad()`: retry the initial REST data load
       - WebSocket reconnection: on error/close, wait with exponential backoff (1s, 2s, 4s, max 30s), retry connect. Only reconnect while provider is not explicitly disconnected.
    3. Do NOT embed navigation logic — the provider exposes state only, widgets decide what to show.
    4. Stage commits as:
       - `test(20-02): add failing monitoring provider coverage`
       - `feat(20-02): add monitoring provider with WebSocket lifecycle and reconnection`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/providers/monitoring_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `class MonitoringProvider`
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `wsConnected`
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `restartSidecar`
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `selectBatch`
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `serviceUnavailable`
    - `flutter_app/lib/providers/monitoring_provider.dart` contains `disconnect`
    - `cd flutter_app; flutter test test/providers/monitoring_provider_test.dart` exits `0`
  </acceptance_criteria>
  <done>Desktop monitoring flows have a test-backed provider with WebSocket lifecycle, reconnection, restart action, and edge state handling before widget work starts.</done>
</task>

</tasks>

<verification>
- Keep service tests and provider tests green independently; do not jump to widget work with unverified raw-map logic.
- Confirm WebSocket reconnection works with exponential backoff.
- Confirm batch list loads even when sidecar status is "stopped" (D-16).
</verification>

<success_criteria>
- Phase 20 Flutter data layer is typed, testable, and ready to drive a Fluent-native monitoring workspace.
- WebSocket connection lifecycle matches D-12 (connect on enter, disconnect on leave).
- Edge states are handled per D-15 and D-16.
</success_criteria>

<commit_strategy>
- Commit 1: `test(20-02): add failing monitoring service coverage`
- Commit 2: `feat(20-02): add typed monitoring models and service contracts`
- Commit 3: `deps(20-02): add web_socket_channel to flutter_app`
- Commit 4: `test(20-02): add failing monitoring provider coverage`
- Commit 5: `feat(20-02): add monitoring provider with WebSocket lifecycle and reconnection`
</commit_strategy>

<output>
After completion, create `.planning/phases/20-operations-monitoring/20-02-SUMMARY.md`
</output>
