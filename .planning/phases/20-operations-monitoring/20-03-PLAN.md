---
phase: 20-operations-monitoring
plan: 03
type: execute
wave: 2
depends_on: [20-02]
files_modified:
  - flutter_app/lib/providers/navigation_provider.dart
  - flutter_app/lib/app/fluent_app_shell.dart
  - flutter_app/lib/app/fluent_screens.dart
  - flutter_app/lib/config/api_config.dart
  - flutter_app/lib/widgets/monitoring/monitoring_workspace.dart
  - flutter_app/lib/widgets/monitoring/batch_list_section.dart
  - flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart
  - flutter_app/test/app/fluent_app_shell_test.dart
  - flutter_app/test/providers/navigation_provider_test.dart
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Admin can navigate to 运营监控 via the 6th sidebar item and see batch list + sidecar diagnostics in dual-area layout"
    - "Batch rows display status tags with semantic color, progress bars, timestamps, and click-to-drill task details"
    - "Sidecar diagnostic card shows status with color band, runtime metrics, error summary, and restart button with confirmation dialog"
    - "WebSocket connection status indicator shows connected/disconnected state; disconnect banner offers manual reconnect"
  artifacts:
    - path: "flutter_app/lib/providers/navigation_provider.dart"
      provides: "updated navigation with 6 items including operationsMonitoringIndex"
      exports: ["NavigationProvider"]
    - path: "flutter_app/lib/app/fluent_app_shell.dart"
      provides: "6th PaneItem for 运营监控"
      exports: ["FluentAppShell"]
    - path: "flutter_app/lib/widgets/monitoring/monitoring_workspace.dart"
      provides: "ScaffoldPage workspace with dual-area layout and WebSocket lifecycle"
      exports: ["OperationsMonitoringWorkspace"]
    - path: "flutter_app/lib/widgets/monitoring/batch_list_section.dart"
      provides: "batch list rows with status tags, progress, timestamps, and click-to-drill"
      exports: ["BatchListSection"]
    - path: "flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart"
      provides: "sidecar status card, runtime metrics, error summary, restart button with confirmation"
      exports: ["SidecarDiagnosticSection"]
  key_links:
    - from: "flutter_app/lib/app/fluent_app_shell.dart"
      to: "flutter_app/lib/widgets/monitoring/monitoring_workspace.dart"
      via: "6th PaneItem body"
      pattern: "FluentOperationsMonitoringPage"
    - from: "flutter_app/lib/widgets/monitoring/monitoring_workspace.dart"
      to: "flutter_app/lib/providers/monitoring_provider.dart"
      via: "Consumer-driven real-time state"
      pattern: "Consumer<MonitoringProvider>"
    - from: "flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart"
      to: "flutter_app/lib/providers/monitoring_provider.dart"
      via: "restart action and impact dialog"
      pattern: "restartSidecar"
---

<objective>
Build the Flutter desktop monitoring workspace and integrate it into the navigation shell.

Purpose: This plan delivers D-01/D-02/D-03 (single-page dual-area monitoring workspace as 6th nav item), D-04/D-05/D-06 (batch-first monitoring with task drilldown), D-07/D-08 (sidecar diagnostics with Go runtime metrics), D-09/D-10 (restart button with confirmation), and D-15/D-16 (edge state handling).

Output: A fully integrated operations monitoring page in the desktop shell with real-time updates, batch monitoring, sidecar diagnostics, and restart capability.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-operations-monitoring/20-CONTEXT.md
@.planning/phases/20-operations-monitoring/20-UI-SPEC.md
@.planning/phases/20-operations-monitoring/20-01-PLAN.md
@.planning/phases/20-operations-monitoring/20-02-PLAN.md
@.planning/phases/19-tag-management/19-03-SUMMARY.md

<interfaces>
From `flutter_app/lib/providers/navigation_provider.dart`:
```dart
class NavigationProvider extends ChangeNotifier {
  static const int galleryIndex = 0;
  static const int duplicateIndex = 1;
  static const int searchIndex = 2;
  static const int tagManagementIndex = 3;
  static const int settingsIndex = 4;
  static const int itemCount = 5;
  // ...
}
```

From `flutter_app/lib/providers/monitoring_provider.dart` (Plan 02 output):
```dart
class MonitoringProvider extends ChangeNotifier {
  MonitoringOverview? get overview;
  List<BatchRow> get batches;
  List<TaskDetail>? get tasks;
  SidecarStatusCard? get sidecarStatus;
  GoRuntimeMetrics? get runtimeMetrics;
  bool get wsConnected;
  bool get isLoading;
  bool get isRestarting;
  bool get serviceUnavailable;
  RestartImpact? get restartImpact;
  int? get selectedBatchId;

  void connect();
  void disconnect();
  Future<void> restartSidecar();
  void selectBatch(int? id);
  Future<void> retryLoad();
}
```

From `flutter_app/lib/config/api_config.dart`:
```dart
class ApiConfig {
  static String get hostUrl => _hostUrl;
  static String get baseUrl => '$hostUrl/api/v1';
  // ... existing endpoints
}
```

From `20-UI-SPEC.md` locked decisions:
```text
D-01: 运营监控 as single 6th nav item
D-02: Single-page dual-area layout (batch monitoring + sidecar diagnostics)
D-03: Reuse Phase 17/19 independent nav page pattern
D-04: Batch-first + click-to-drill-down
D-07: Status card + error summary list
D-08: Extended to Go runtime metrics
D-09: Restart button in diagnostic card
D-10: Double-confirm with impact explanation
D-15: Service unavailable + retry
D-16: Batch list works even when sidecar stopped
```

UI color tokens:
```text
accent: #2563EB
success: #16A34A
warning: #D97706
danger: #C42B1C
primary-bg: #F5F7FB
secondary-bg: #E7ECF5
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green navigation integration and API config</name>
  <files>
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/app/fluent_app_shell.dart,
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/config/api_config.dart,
    flutter_app/test/providers/navigation_provider_test.dart,
    flutter_app/test/app/fluent_app_shell_test.dart
  </files>
  <read_first>
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/app/fluent_app_shell.dart,
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/config/api_config.dart,
    flutter_app/test/providers/navigation_provider_test.dart,
    flutter_app/test/app/fluent_app_shell_test.dart,
    .planning/phases/20-operations-monitoring/20-CONTEXT.md,
    .planning/phases/20-operations-monitoring/20-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: NavigationProvider.itemCount is 6
    - Test 2: NavigationProvider.operationsMonitoringIndex is 5
    - Test 3: NavigationProvider._pageTitles contains '运营监控' at index 5
    - Test 4: fluent_app_shell renders a PaneItem with icon FluentIcons.diagnostic and title '运营监控'
    - Test 5: ApiConfig.adminOverview returns '$hostUrl/admin/api/task-platform/overview'
    - Test 6: ApiConfig.monitoringWs returns 'ws://...' WebSocket URL
  </behavior>
  <action>
    1. Add failing tests in `flutter_app/test/providers/navigation_provider_test.dart`:
       - Assert `NavigationProvider.itemCount == 6`
       - Assert `NavigationProvider.operationsMonitoringIndex == 5`
       - Assert `_pageTitles[5] == '运营监控'`
    2. Update `flutter_app/lib/providers/navigation_provider.dart`:
       - Add `static const int operationsMonitoringIndex = 5;`
       - Change `itemCount` from 5 to 6
       - Add `'运营监控'` to `_pageTitles`
    3. Add failing test in `flutter_app/test/app/fluent_app_shell_test.dart`:
       - Assert the shell renders a 6th PaneItem with `FluentIcons.diagnostic` icon
    4. Update `flutter_app/lib/app/fluent_app_shell.dart`:
       - Add 6th PaneItem after settings:
         ```dart
         PaneItem(
           icon: const Icon(FluentIcons.diagnostic),
           title: const Text('运营监控'),
           body: _ShellPage(
             onImportLibrary: onImportLibrary,
             importService: importService,
             child: const FluentOperationsMonitoringPage(),
           ),
         ),
         ```
    5. Add `FluentOperationsMonitoringPage` to `flutter_app/lib/app/fluent_screens.dart`:
       ```dart
       class FluentOperationsMonitoringPage extends StatelessWidget {
         const FluentOperationsMonitoringPage({super.key});
         @override
         Widget build(BuildContext context) {
           return const OperationsMonitoringWorkspace();
         }
       }
       ```
    6. Update `flutter_app/lib/config/api_config.dart` with admin monitoring endpoints:
       ```dart
       // Admin monitoring endpoints (under /admin/api, not /api/v1)
       static String get adminOverview => '$hostUrl/admin/api/task-platform/overview';
       static String get adminBatches => '$hostUrl/admin/api/task-batches';
       static String adminTasks({int? batchId}) {
         var url = '$hostUrl/admin/api/tasks';
         if (batchId != null) url += '?batch_id=$batchId';
         return url;
       }
       static String get sidecarRestart => '$hostUrl/admin/api/actions/sidecar/restart';
       static String get monitoringWs {
         final wsHost = hostUrl.replaceFirst('http', 'ws');
         return '$wsHost/admin/api/monitoring/ws';
       }
       ```
    7. Stage commits as:
       - `test(20-03): add failing navigation and API config coverage`
       - `feat(20-03): add 运营监控 navigation entry, screens, and API config`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/providers/navigation_provider_test.dart test/app/fluent_app_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/providers/navigation_provider.dart` contains `operationsMonitoringIndex = 5`
    - `flutter_app/lib/providers/navigation_provider.dart` contains `itemCount = 6`
    - `flutter_app/lib/providers/navigation_provider.dart` contains `运营监控`
    - `flutter_app/lib/app/fluent_app_shell.dart` contains `FluentIcons.diagnostic`
    - `flutter_app/lib/app/fluent_screens.dart` contains `FluentOperationsMonitoringPage`
    - `flutter_app/lib/config/api_config.dart` contains `monitoringWs`
    - `flutter_app/lib/config/api_config.dart` contains `sidecarRestart`
    - `cd flutter_app; flutter test test/providers/navigation_provider_test.dart test/app/fluent_app_shell_test.dart` exits `0`
  </acceptance_criteria>
  <done>The 6th navigation item is wired, API config supports monitoring endpoints, and the monitoring page placeholder exists in the shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green monitoring workspace with dual-area layout and WebSocket lifecycle</name>
  <files>
    flutter_app/lib/widgets/monitoring/monitoring_workspace.dart,
    flutter_app/lib/widgets/monitoring/batch_list_section.dart
  </files>
  <read_first>
    flutter_app/lib/widgets/tag_management/tag_management_workspace.dart,
    flutter_app/lib/providers/monitoring_provider.dart,
    flutter_app/lib/models/monitoring_models.dart,
    .planning/phases/20-operations-monitoring/20-UI-SPEC.md,
    .planning/phases/20-operations-monitoring/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: OperationsMonitoringWorkspace calls MonitoringProvider.connect() in initState
    - Test 2: OperationsMonitoringWorkspace calls MonitoringProvider.disconnect() in dispose
    - Test 3: When wsConnected is false, workspace shows disconnect banner with 重新连接 button
    - Test 4: When serviceUnavailable is true, workspace shows 服务暂时不可用 message with 重试 button
    - Test 5: BatchListSection renders batch rows with status labels, progress bars, and timestamps
    - Test 6: Clicking a batch row calls provider.selectBatch(batchId)
  </behavior>
  <action>
    1. Create `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` as a `StatefulWidget`:
       - `initState`: calls `Provider.of<MonitoringProvider>(context, listen: false).connect()`
       - `dispose`: calls `provider.disconnect()`
       - `build`: returns `ScaffoldPage` with:
         a. `PageHeader` with title `批次任务监控` and a WebSocket status indicator in `commandBar` (small label: `已连接` in success color or `已断开` in danger color)
         b. If `serviceUnavailable`: show full-page error state per UI-SPEC ("服务暂时不可用。检查后端连接后重试。" + 重试 button)
         c. If `!wsConnected` (but not serviceUnavailable): show disconnect banner at top ("实时连接已断开，数据可能不是最新。" + 重新连接 button)
         d. `SingleChildScrollView` with dual-area content:
            - Top: `BatchListSection`
            - Divider with 24px spacing (lg)
            - Bottom: `SidecarDiagnosticSection`
       - Wrap content in `Consumer<MonitoringProvider>` for reactive updates
       - Use `xl` (32px) horizontal padding for both areas
    2. Create `flutter_app/lib/widgets/monitoring/batch_list_section.dart`:
       - Section heading `批次任务监控` in Heading typography (20px, weight 600)
       - If batches is empty: show empty state ("暂无导入任务" + subtitle per UI-SPEC)
       - Vertical column of batch rows, each row showing:
         a. Status label as a colored badge (等待中=warning, 进行中=accent, 已完成=success, 失败=danger) — use semantic color with white text on colored background
         b. Batch name (summaryLabel or "Batch #${id}")
         c. Progress: linear ProgressBar (4px height, accent color) with percentage text
         d. Time: creation time in `YYYY-MM-DD HH:mm` format
         e. Error count badge if failed tasks > 0
       - Row minimum height 48px (accessibility)
       - On tap: call `provider.selectBatch(id)` — if already selected, deselect (set null)
       - If a batch is selected (selectedBatchId != null and matches), show task detail rows below:
         - Each task row: image filename, status label, error_summary text, retry hint if recommended
         - Use TaskBatchFailureGroup data from the batch's failure_groups for retry hints
    3. Ensure all text uses UI-SPEC typography tokens (Body 16px/400, Label 14px/600, Heading 20px/600)
    4. Stage commits as:
       - `feat(20-03): add monitoring workspace with dual-area layout and batch list`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/widgets/monitoring/monitoring_workspace_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` exists
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `class OperationsMonitoringWorkspace`
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `connect()`
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `disconnect()`
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `已连接`
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `已断开`
    - `flutter_app/lib/widgets/monitoring/monitoring_workspace.dart` contains `服务暂时不可用`
    - `flutter_app/lib/widgets/monitoring/batch_list_section.dart` exists
    - `flutter_app/lib/widgets/monitoring/batch_list_section.dart` contains `class BatchListSection`
    - `flutter_app/lib/widgets/monitoring/batch_list_section.dart` contains `暂无导入任务`
  </acceptance_criteria>
  <done>The monitoring workspace renders the dual-area layout with batch rows, WebSocket lifecycle, and edge state handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Red-green sidecar diagnostic section with restart confirmation</name>
  <files>
    flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart
  </files>
  <read_first>
    flutter_app/lib/providers/monitoring_provider.dart,
    flutter_app/lib/models/monitoring_models.dart,
    .planning/phases/20-operations-monitoring/20-UI-SPEC.md,
    .planning/phases/20-operations-monitoring/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: SidecarDiagnosticSection renders status card with correct color band for ready (success), degraded (warning), stopped (danger)
    - Test 2: Restart button is enabled only when sidecar state is "degraded" or "stopped"
    - Test 3: Clicking restart shows ContentDialog with title "确认重启 Sidecar" and impact count message
    - Test 4: Confirming restart calls provider.restartSidecar() and shows loading state on button
    - Test 5: Go runtime metrics show queue depth, active workers, and pending tasks in compact metric cards
    - Test 6: Error summary shows recent errors with type, timestamp, and message; empty state shows "近期无错误记录"
  </behavior>
  <action>
    1. Create `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` with:
       a. **Section heading**: `Sidecar 诊断` in Heading typography
       b. **Status card** (min height 320px for diagnostic area):
          - Top 3px color band using semantic color based on state:
            - ready → `#16A34A` (success), display text "就绪"
            - degraded → `#D97706` (warning), display text "降级"
            - stopped → `#C42B1C` (danger), display text "已停止"
            - starting → `#2563EB` (accent), display text "启动中"
            - not_started/not_configured → `#D97706`, display text "未配置"
          - Large state value text (Display typography: 28px, weight 600) in semantic color
          - Right side: uptime (calculated from overview or omitted), last probe time in `YYYY-MM-DD HH:mm`
          - Restart button: outlined `Button` with text `重启 Sidecar`, enabled only when state is "degraded" or "stopped" per UI-SPEC
          - When isRestarting: show `ProgressRing` inline and disable button
       c. **Go runtime metrics row**: 3 compact cards horizontally arranged showing:
          - Queue depth (from queue.queue_size)
          - Active workers (from queue.worker_count)
          - Pending tasks (from overview tasks map "pending" or "ready" count)
          - Each card: Label-sized metric name + Body-sized metric value
       d. **Error summary list**:
          - If sidecar status has lastErrorSummary and it's non-empty, show it as the most recent error
          - Display up to 10 error entries (from backend error data; initially only the lastErrorSummary is available)
          - Empty state: `近期无错误记录` per UI-SPEC
       e. **Restart confirmation dialog**:
          - Triggered by restart button tap
          - Use `ContentDialog` with:
            - Title: `确认重启 Sidecar`
            - Body: `重启将中断正在进行的 {N} 个计算任务。确定要继续吗？` where N comes from RestartImpact.interruptedTaskCount or a fallback from the current running task count
            - Actions: `取消` (secondary button, closes dialog) + `确认重启` (danger style using `#C42B1C`, calls `provider.restartSidecar()`)
          - After confirmation: set isRestarting=true, await restartSidecar(), then isRestarting=false, close dialog
    2. Wire the diagnostic section into `monitoring_workspace.dart` below the divider
    3. Stage commits as:
       - `feat(20-03): add sidecar diagnostic section with restart confirmation`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/widgets/monitoring/sidecar_diagnostic_section_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` exists
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `class SidecarDiagnosticSection`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `确认重启 Sidecar`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `interruptedTaskCount`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `重启 Sidecar`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `近期无错误记录`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `就绪`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `降级`
    - `flutter_app/lib/widgets/monitoring/sidecar_diagnostic_section.dart` contains `已停止`
  </acceptance_criteria>
  <done>The sidecar diagnostic section displays status with color coding, runtime metrics, error summary, and restart confirmation matching UI-SPEC exactly.</done>
</task>

</tasks>

<verification>
- Run `cd flutter_app; flutter test` to ensure all tests pass.
- Verify the monitoring page renders correctly by navigating to 运营监控 in the desktop shell.
- Verify WebSocket connection status indicator updates in real-time.
- Verify batch list shows and batch drilldown works.
- Verify sidecar status card shows correct color band.
- Verify restart confirmation dialog shows impact count.
</verification>

<success_criteria>
- All 3 Phase 20 success criteria from ROADMAP.md are satisfied:
  1. Admin can view batch and task status from import task monitoring entry (OPS-01)
  2. Admin can see Python sidecar running status, error summary, and manual restart (OPS-02)
  3. Admin can diagnose sidecar issues without checking external logs
- UI matches UI-SPEC visual and interaction contracts.
- WebSocket lifecycle matches D-12.
- Edge states handled per D-15 and D-16.
</success_criteria>

<commit_strategy>
- Commit 1: `test(20-03): add failing navigation and API config coverage`
- Commit 2: `feat(20-03): add 运营监控 navigation entry, screens, and API config`
- Commit 3: `feat(20-03): add monitoring workspace with dual-area layout and batch list`
- Commit 4: `feat(20-03): add sidecar diagnostic section with restart confirmation`
</commit_strategy>

<output>
After completion, create `.planning/phases/20-operations-monitoring/20-03-SUMMARY.md`
</output>
