---
phase: 17-desktop-shell-foundation
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - flutter_app/lib/app/fluent_screens.dart
  - flutter_app/lib/widgets/fluent_gallery_content.dart
  - flutter_app/lib/widgets/gallery_filter_panel.dart
  - flutter_app/lib/providers/image_provider.dart
  - flutter_app/lib/providers/tag_provider.dart
  - flutter_app/test/widgets/fluent_gallery_content_test.dart
  - flutter_app/test/widgets/gallery_filter_panel_test.dart
  - flutter_app/test/providers/image_provider_has_tags_test.dart
autonomous: true
requirements: [DSK-02, DSK-03]

must_haves:
  truths:
    - "User browses the gallery in square grid mode by default"
    - "Gallery tiles remain visually square while column count adjusts with width"
    - "User can open and use a persistent right-side tag filter panel"
    - "Selecting tags or untagged-only filtering applies immediately to gallery results"
  artifacts:
    - path: "flutter_app/lib/widgets/fluent_gallery_content.dart"
      provides: "Grid-first gallery rendering with stable square-tile behavior"
      exports: ["FluentGalleryContent"]
    - path: "flutter_app/lib/widgets/gallery_filter_panel.dart"
      provides: "Persistent right-side filter panel"
      exports: ["GalleryFilterPanel"]
    - path: "flutter_app/test/widgets/gallery_filter_panel_test.dart"
      provides: "Widget coverage for panel layout and immediate-apply filter behavior"
  key_links:
    - from: "flutter_app/lib/app/fluent_screens.dart"
      to: "flutter_app/lib/widgets/gallery_filter_panel.dart"
      via: "gallery workspace row composition"
      pattern: "GalleryFilterPanel"
    - from: "flutter_app/lib/widgets/gallery_filter_panel.dart"
      to: "flutter_app/lib/providers/tag_provider.dart"
      via: "tag selection state"
      pattern: "toggleTag"
    - from: "flutter_app/lib/widgets/gallery_filter_panel.dart"
      to: "flutter_app/lib/providers/image_provider.dart"
      via: "immediate gallery reload"
      pattern: "set(TagFilter|HasTagsFilter)"
---

<objective>
Turn the gallery into a desktop workspace: square-grid first, right-side filter panel, and provider-driven immediate filtering.

Purpose: This plan implements the user-visible core of Phase 17 by satisfying D-06 through D-11 and the roadmap’s gallery grid/filter success criteria without leaking into viewer, tag-governance, or performance-phase scope.

Output: A tested gallery workspace with stable square tiles and an accessible right-side tag filter panel.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desktop-shell-foundation/17-CONTEXT.md
@.planning/phases/17-desktop-shell-foundation/17-RESEARCH.md
@.planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md

<interfaces>
From `flutter_app/lib/providers/image_provider.dart`:
```dart
enum ViewMode { grid, masonry }
Future<void> setTagFilter(List<int> tagIds)
Future<void> setHasTagsFilter(bool? hasTags)
bool? get hasTagsFilter
List<int> get selectedTagIds
ViewMode get viewMode
```

From `flutter_app/lib/providers/tag_provider.dart`:
```dart
Set<int> get selectedTagIds
List<Tag> get selectedTags
Future<void> loadTags()
void toggleTag(int tagId)
void clearSelection()
```

From `17-UI-SPEC.md`:
```text
Filter panel width target: 320px default
Tag selection and untagged toggle apply immediately
Selected-tag chips remain status feedback / quick removal
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the gallery workspace and right filter panel</name>
  <files>
    flutter_app/test/widgets/gallery_filter_panel_test.dart,
    flutter_app/test/widgets/fluent_gallery_content_test.dart,
    flutter_app/lib/widgets/gallery_filter_panel.dart,
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/widgets/fluent_gallery_content.dart
  </files>
  <read_first>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/widgets/fluent_gallery_content.dart,
    flutter_app/test/widgets/fluent_gallery_content_test.dart,
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/lib/providers/tag_provider.dart,
    .planning/phases/17-desktop-shell-foundation/17-CONTEXT.md,
    .planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: gallery page composes `FluentGalleryContent` beside a `GalleryFilterPanel` instead of opening a dialog as the primary filter surface
    - Test 2: `GalleryFilterPanel` renders heading `Filter by Tags` and `Show untagged images only`
    - Test 3: grid mode remains the default and square-grid rendering path is selected first
    - Test 4: selected-tag chips remain visible as status feedback in the gallery header
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/widgets/gallery_filter_panel_test.dart` to assert that the gallery workspace contains a persistent right-side `GalleryFilterPanel` with heading `Filter by Tags`, an untagged toggle labeled `Show untagged images only`, and keyboard-reachable filter controls.
    2. Extend `flutter_app/test/widgets/fluent_gallery_content_test.dart` so it continues to protect pagination behavior and also proves grid mode remains the default rendering path.
    3. Create `flutter_app/lib/widgets/gallery_filter_panel.dart` as a focused widget that:
       - Renders a `320px` wide panel target.
       - Loads tags from `TagProvider`.
       - Presents tag selection plus the untagged-only control.
       - Leaves tag governance features out of scope.
    4. Update `flutter_app/lib/app/fluent_screens.dart` so the gallery page uses a horizontal workspace with `Expanded(FluentGalleryContent)` plus the new `GalleryFilterPanel`, replacing dialog-primary filtering per D-09.
    5. Update `flutter_app/lib/widgets/fluent_gallery_content.dart` only as needed to preserve square-grid-first behavior while keeping masonry secondary and non-primary.
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/widgets/fluent_gallery_content_test.dart test/widgets/gallery_filter_panel_test.dart test/app/fluent_screens_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/widgets/gallery_filter_panel.dart` contains `Filter by Tags`
    - `flutter_app/lib/widgets/gallery_filter_panel.dart` contains `Show untagged images only`
    - `flutter_app/lib/app/fluent_screens.dart` contains `GalleryFilterPanel`
    - `flutter_app/lib/widgets/fluent_gallery_content.dart` contains `ViewMode.grid`
    - `flutter test test/widgets/fluent_gallery_content_test.dart test/widgets/gallery_filter_panel_test.dart test/app/fluent_screens_test.dart` exits `0`
  </acceptance_criteria>
  <done>The gallery page is structurally converted to a persistent workspace layout, and widget tests prove the panel and grid-first behavior exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove immediate-apply filtering and square-tile constraints</name>
  <files>
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/widgets/fluent_gallery_content.dart,
    flutter_app/test/providers/image_provider_has_tags_test.dart,
    flutter_app/test/widgets/gallery_filter_panel_test.dart,
    flutter_app/test/widgets/fluent_gallery_content_test.dart
  </files>
  <read_first>
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/test/providers/image_provider_has_tags_test.dart,
    flutter_app/test/widgets/fluent_gallery_content_test.dart,
    flutter_app/lib/widgets/gallery_filter_panel.dart,
    .planning/phases/17-desktop-shell-foundation/17-CONTEXT.md,
    .planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: selecting a tag from the panel immediately calls the provider reload path without an Apply button
    - Test 2: toggling untagged-only immediately calls `setHasTagsFilter(false)`
    - Test 3: clearing filter chips returns the gallery to the unfiltered state
    - Test 4: grid tiles remain square-oriented by using a square-constraining grid delegate rather than free-form tile heights
  </behavior>
  <action>
    1. Extend `flutter_app/test/providers/image_provider_has_tags_test.dart` with any missing red tests needed to prove immediate application and mutual exclusivity remain intact when driven from the filter panel.
    2. Extend `flutter_app/test/widgets/gallery_filter_panel_test.dart` so user interaction with a tag control or untagged toggle triggers the corresponding `ImageListProvider` methods immediately.
    3. Update `flutter_app/lib/providers/image_provider.dart` and `flutter_app/lib/providers/tag_provider.dart` only as needed to keep tag selection, untagged filtering, and quick-clear behavior deterministic and testable.
    4. Update `flutter_app/lib/widgets/fluent_gallery_content.dart` so the primary grid path preserves square-tile intent from D-08. Use a grid delegate or constrained card layout that keeps tile aspect visually square while still allowing column-count changes at different widths.
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/providers/image_provider_has_tags_test.dart test/widgets/gallery_filter_panel_test.dart test/widgets/fluent_gallery_content_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/providers/image_provider.dart` contains `setTagFilter`
    - `flutter_app/lib/providers/image_provider.dart` contains `setHasTagsFilter`
    - `flutter_app/lib/widgets/fluent_gallery_content.dart` contains square-grid-oriented delegate logic
    - `flutter_app/test/providers/image_provider_has_tags_test.dart` exits `0`
    - `flutter_app/test/widgets/gallery_filter_panel_test.dart` exits `0`
    - `flutter_app/test/widgets/fluent_gallery_content_test.dart` exits `0`
  </acceptance_criteria>
  <done>Filter changes apply immediately, chip clearing works, and square-tile behavior is backed by tests instead of visual guesswork.</done>
</task>

</tasks>

<verification>
- Confirm gallery workspace tests stay green after provider changes.
- Confirm filtering remains immediate and does not regress pagination tests.
</verification>

<success_criteria>
- DSK-02 is satisfied by grid-first square-tile browsing.
- DSK-03 is satisfied by a persistent accessible right-side filter panel with immediate apply.
- No viewer, tag governance, operations, or performance-phase work leaks into this plan.
</success_criteria>

<commit_strategy>
- Commit 1: `test(17-02): add failing gallery workspace and filter panel coverage`
- Commit 2: `feat(17-02): add persistent filter panel and grid-first gallery workspace`
- Commit 3: `refactor(17-02): tighten provider-driven filter application and square-tile layout`
</commit_strategy>

<output>
After completion, create `.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md`
</output>
