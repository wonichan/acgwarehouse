---
phase: 17-desktop-shell-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flutter_app/lib/app/fluent_app_shell.dart
  - flutter_app/lib/app/fluent_screens.dart
  - flutter_app/lib/providers/navigation_provider.dart
  - flutter_app/test/app/fluent_app_shell_test.dart
  - flutter_app/test/app/desktop_shell_top_bar_test.dart
  - flutter_app/test/providers/navigation_provider_test.dart
autonomous: true
requirements: [DSK-01]

must_haves:
  truths:
    - "User sees a persistent desktop top bar with search, import, and settings actions"
    - "Submitting a query from the shell search box switches to the search view and starts search execution"
    - "Settings stays reachable from the shell without leaving the desktop application chrome"
  artifacts:
    - path: "flutter_app/lib/app/fluent_app_shell.dart"
      provides: "Custom shell title bar and top-level action wiring"
      exports: ["FluentAppShell"]
    - path: "flutter_app/lib/providers/navigation_provider.dart"
      provides: "Shell navigation and top-bar visibility/state contract"
      exports: ["NavigationProvider"]
    - path: "flutter_app/test/app/desktop_shell_top_bar_test.dart"
      provides: "Widget coverage for top-bar search/import/settings behavior"
  key_links:
    - from: "flutter_app/lib/app/fluent_app_shell.dart"
      to: "flutter_app/lib/providers/navigation_provider.dart"
      via: "Consumer-driven shell state"
      pattern: "Consumer<NavigationProvider>"
    - from: "flutter_app/lib/app/fluent_app_shell.dart"
      to: "flutter_app/lib/app/fluent_screens.dart"
      via: "NavigationPane body composition"
      pattern: "Fluent(Search|Gallery|Duplicate|TagManagement|Settings)Page"
    - from: "flutter_app/lib/app/fluent_app_shell.dart"
      to: "flutter_app/lib/providers/search_provider.dart"
      via: "top-bar query submit handoff"
      pattern: "search\(query:"
---

<objective>
Create the desktop shell contract for Phase 17: a custom top bar that owns search, import, and settings access while preserving the existing left navigation for view switching.

Purpose: This plan establishes the Windows Photos-inspired shell required by D-01 through D-05 and creates the safe contract that later plans will use for gallery workspace and import wiring.

Output: A tested custom top bar, shell-aware navigation state, and widget coverage that proves the top bar drives the desktop experience.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desktop-shell-foundation/17-CONTEXT.md
@.planning/phases/17-desktop-shell-foundation/17-RESEARCH.md
@.planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md

<interfaces>
From `flutter_app/lib/providers/navigation_provider.dart`:
```dart
class NavigationProvider extends ChangeNotifier {
  static const int galleryIndex = 0;
  static const int duplicateIndex = 1;
  static const int searchIndex = 2;
  static const int tagManagementIndex = 3;
  static const int settingsIndex = 4;
  int get selectedIndex;
  String get currentPageTitle;
  void setSelectedIndex(int index);
}
```

From `flutter_app/lib/providers/search_provider.dart`:
```dart
Future<void> search({
  String? query,
  List<int>? tagIds,
  String? sortBy,
  String? sortOrder,
  bool refresh = true,
})
```

From `17-UI-SPEC.md`:
```text
Top-bar order: drag/title region -> persistent search box -> import action -> settings action
Primary focal point: persistent top-bar search box
Primary CTA copy: Import Library
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the desktop top bar contract</name>
  <files>
    flutter_app/test/app/desktop_shell_top_bar_test.dart,
    flutter_app/test/app/fluent_app_shell_test.dart,
    flutter_app/test/providers/navigation_provider_test.dart,
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/app/fluent_app_shell.dart
  </files>
  <read_first>
    flutter_app/lib/app/fluent_app_shell.dart,
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/test/app/fluent_app_shell_test.dart,
    flutter_app/test/providers/navigation_provider_test.dart,
    .planning/phases/17-desktop-shell-foundation/17-CONTEXT.md,
    .planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: shell renders a visible search input with placeholder text `Search images and tags`
    - Test 2: shell renders `Import Library` and `Open Settings` actions in the top bar
    - Test 3: submitting a shell search query switches navigation to `NavigationProvider.searchIndex`
    - Test 4: activating settings action switches navigation to `NavigationProvider.settingsIndex`
    - Test 5: navigation provider exposes any new shell-state API without breaking the existing five-index contract
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/app/desktop_shell_top_bar_test.dart` that pump `FluentAppShell` inside `FluentApp` with `NavigationProvider`, `SearchProvider`, and any required test doubles. Assert the shell contains a persistent `TextBox` placeholder `Search images and tags`, an action labeled `Import Library`, and an action labeled `Open Settings`.
    2. Extend `flutter_app/test/app/fluent_app_shell_test.dart` so it still verifies the five navigation destinations and also proves the shell top bar remains present while navigation changes.
    3. Update `flutter_app/test/providers/navigation_provider_test.dart` only if new shell-specific state/methods are added. Preserve the existing five-page index contract exactly.
    4. Implement the minimal production changes in `flutter_app/lib/providers/navigation_provider.dart` and `flutter_app/lib/app/fluent_app_shell.dart` to satisfy the tests:
       - Keep left navigation indices exactly as today.
       - Replace the plain title text area with a custom top-bar composition inside `TitleBar`.
       - Add a persistent search `TextBox` in the shell.
       - On submit, call `SearchProvider.search(query: value.trim())` and switch navigation to `searchIndex` per D-04/D-05.
       - Add a secondary `Open Settings` action that switches navigation to `settingsIndex`.
       - Add an `Import Library` action placeholder wired through a callback boundary (the real backend/client integration lands in Plan 03).
       - Preserve a `DragToMoveArea` region so the desktop title bar still supports window dragging per D-03.
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/app/fluent_app_shell_test.dart test/app/desktop_shell_top_bar_test.dart test/providers/navigation_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/app/fluent_app_shell.dart` contains `Search images and tags`
    - `flutter_app/lib/app/fluent_app_shell.dart` contains `Import Library`
    - `flutter_app/lib/app/fluent_app_shell.dart` contains `Open Settings`
    - `flutter_app/lib/app/fluent_app_shell.dart` contains `DragToMoveArea`
    - `flutter_app/test/app/desktop_shell_top_bar_test.dart` contains `Search images and tags`
    - `flutter_app/test/app/desktop_shell_top_bar_test.dart` contains `Import Library`
    - `flutter_app/test/app/desktop_shell_top_bar_test.dart` contains `Open Settings`
    - `flutter test test/app/fluent_app_shell_test.dart test/app/desktop_shell_top_bar_test.dart test/providers/navigation_provider_test.dart` exits `0`
  </acceptance_criteria>
  <done>The shell owns persistent search, import, and settings affordances, and top-bar behavior is proven by widget tests before later plans build on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove page-level ownership of shell actions</name>
  <files>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/test/app/fluent_screens_test.dart,
    flutter_app/test/app/fluent_app_shell_test.dart
  </files>
  <read_first>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/test/app/fluent_screens_test.dart,
    flutter_app/lib/app/fluent_app_shell.dart,
    .planning/phases/17-desktop-shell-foundation/17-CONTEXT.md,
    .planning/phases/17-desktop-shell-foundation/17-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: gallery and search pages still render their `ScaffoldPage` bodies inside the shell
    - Test 2: shell-level actions are no longer duplicated as the primary `CommandBar` affordances inside the page header
    - Test 3: the gallery page keeps its status chips but no longer treats filter/search/settings as the main shell action zone
  </behavior>
  <action>
    1. Add or extend tests in `flutter_app/test/app/fluent_screens_test.dart` and `flutter_app/test/app/fluent_app_shell_test.dart` so they fail until `fluent_screens.dart` stops owning shell-level search/settings responsibilities.
    2. Refactor `flutter_app/lib/app/fluent_screens.dart` so page-level headers keep page-specific content only:
       - Preserve gallery title and selected-tag chip feedback per D-11.
       - Remove search/settings ownership from page command bars because those now belong to the shell.
       - Keep duplicate/page-specific affordances that remain local to each page.
       - Do not introduce viewer, filmstrip, tag-management redesign, or operations-monitoring scope.
    3. Keep page components compatible with the five existing navigation destinations and current body widgets.
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/app/fluent_screens_test.dart test/app/fluent_app_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/app/fluent_screens.dart` still contains `PageHeader`
    - `flutter_app/lib/app/fluent_screens.dart` still contains `图库`
    - `flutter_app/lib/app/fluent_screens.dart` no longer uses page-level search entry as the primary shell affordance
    - `flutter_app/test/app/fluent_screens_test.dart` exits `0`
    - `flutter_app/test/app/fluent_app_shell_test.dart` exits `0`
  </acceptance_criteria>
  <done>Shell responsibilities are centralized in the top bar, while page screens remain focused on page content and status feedback.</done>
</task>

</tasks>

<verification>
- Confirm shell widget tests pass before any later plan consumes the top-bar contract.
- Confirm navigation indices and existing destinations remain intact.
</verification>

<success_criteria>
- DSK-01 shell access contract exists in code and tests.
- Top-bar search and settings navigation are proven automatically.
- Later plans can build gallery and import behavior without redefining shell chrome.
</success_criteria>

<commit_strategy>
- Commit 1: `test(17-01): add failing desktop shell top bar coverage`
- Commit 2: `feat(17-01): add custom top bar and shell action routing`
- Commit 3: `refactor(17-01): remove duplicated shell ownership from page headers`
</commit_strategy>

<output>
After completion, create `.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md`
</output>
