---
phase: 02-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [IMPT-05]
user_setup:
  - service: 腾讯云 COS
    why: 缩略图云存储
    env_vars:
      - name: COS_SECRET_ID
        source: "腾讯云控制台 -> 访问管理 -> API密钥管理"
      - name: COS_SECRET_KEY
        source: "腾讯云控制台 -> 访问管理 -> API密钥管理"
    dashboard_config:
      - task: "确认存储桶已创建"
        location: "腾讯云控制台 -> 对象存储 -> 存储桶列表 -> acgwarehouse-1301393037"

must_haves:
  truths:
    - "系统可以生成指定尺寸的缩略图"
    - "缩略图可以上传到腾讯云 COS"
    - "API 返回完整的 COS 缩略图 URL"
    - "缩略图生成不阻塞图片导入流程"
  artifacts:
    - path: "internal/service/thumbnail_service.go"
      provides: "缩略图生成逻辑"
      exports: ["ThumbnailService", "GenerateThumbnail"]
    - path: "internal/service/cos_service.go"
      provides: "COS 上传逻辑"
      exports: ["COSService", "Upload"]
    - path: "internal/domain/image.go"
      provides: "Image 模型扩展"
      contains: "ThumbnailSmallUrl, ThumbnailLargeUrl"
    - path: "migrations/002_add_thumbnail_fields.up.sql"
      provides: "数据库 Schema 变更"
  key_links:
    - from: "internal/service/thumbnail_service.go"
      to: "internal/service/cos_service.go"
      via: "Upload 调用"
      pattern: "cos.*Upload"
    - from: "internal/service/thumbnail_service.go"
      to: "disintegration/imaging"
      via: "图片缩放"
      pattern: "imaging\\.Resize"
---

<objective>
实现缩略图生成服务与腾讯云 COS 云存储集成，支持按需生成多种尺寸缩略图并上传到云端。

Purpose: 为前端图片浏览提供轻量级缩略图，避免内存爆炸问题，同时利用 COS CDN 加速多端访问。
Output: 可运行的缩略图生成服务，支持上传到腾讯云 COS 并返回可访问的 URL。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai/02-CONTEXT.md
@.planning/phases/02-ai/02-RESEARCH.md
@.planning/phases/02-ai/02-VALIDATION.md

# 现有代码接口
@internal/domain/image.go
@internal/service/metadata_service.go
@internal/worker/job_manager.go
@internal/repository/schema.go
</context>

<interfaces>
<!-- 从现有代码提取的关键接口 -->

From internal/domain/image.go:
```go
type Image struct {
    ID         int64     `json:"id"`
    Path       string    `json:"path"`
    Filename   string    `json:"filename"`
    SourceRoot string    `json:"source_root"`
    FileSize   int64     `json:"file_size"`
    Width      int       `json:"width"`
    Height     int       `json:"height"`
    Format     string    `json:"format"`
    PHash      int64     `json:"phash"`
    CreatedAt  time.Time `json:"created_at"`
    UpdatedAt  time.Time `json:"updated_at"`
}
```

From internal/service/metadata_service.go:
```go
type MetadataService struct {}
func (s *MetadataService) ExtractMetadata(path string) (*domain.Image, error)
```

From internal/worker/job_manager.go:
```go
type JobFunc func(ctx context.Context, id int64, payload string) error
type Manager struct {}
func (m *Manager) RegisterHandler(jobType string, handler JobFunc)
func (m *Manager) Start(ctx context.Context)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 扩展 Image 模型与数据库 Schema</name>
  <files>internal/domain/image.go, migrations/002_add_thumbnail_fields.up.sql, migrations/002_add_thumbnail_fields.down.sql, internal/repository/schema.go</files>
  <behavior>
    - Test 1: Image 结构体包含 ThumbnailSmallUrl 和 ThumbnailLargeUrl 字段
    - Test 2: 数据库迁移可成功添加 thumbnail_small_url 和 thumbnail_large_url 列
    - Test 3: 迁移可回滚，列被正确删除
  </behavior>
  <action>
    1. 在 `internal/domain/image.go` 的 Image 结构体中添加两个新字段：
       - `ThumbnailSmallUrl string` (小缩略图 ~200px)
       - `ThumbnailLargeUrl string` (大缩略图 ~600px)
    
    2. 创建 `migrations/002_add_thumbnail_fields.up.sql`：
       ```sql
       ALTER TABLE images ADD COLUMN thumbnail_small_url TEXT;
       ALTER TABLE images ADD COLUMN thumbnail_large_url TEXT;
       ```
    
    3. 创建 `migrations/002_add_thumbnail_fields.down.sql`：
       ```sql
       ALTER TABLE images DROP COLUMN thumbnail_large_url;
       ALTER TABLE images DROP COLUMN thumbnail_small_url;
       ```
    
    4. 更新 `internal/repository/schema.go` 中的 scanSchemaSQL 不需要修改（迁移脚本会处理），但确保新字段在查询中被正确处理。
    
    使用 `disintegration/imaging` 库进行图片处理。
    不要在数据库中存储图片二进制数据，只存 URL。
  </action>
  <verify>
    <automated>go test ./internal/domain/... -run TestImageFields</automated>
  </verify>
  <done>Image 模型包含缩略图 URL 字段，数据库迁移脚本可执行且可回滚</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 实现缩略图生成服务</name>
  <files>internal/service/thumbnail_service.go, internal/service/thumbnail_service_test.go</files>
  <behavior>
    - Test 1: 给定有效图片路径，GenerateThumbnail 返回指定尺寸的缩略图字节
    - Test 2: 生成的缩略图尺寸正确（宽度为目标宽度，高度按比例）
    - Test 3: JPEG 质量参数正确应用（小图 85，大图 90）
    - Test 4: 无效图片路径返回错误
    - Test 5: 不支持的图片格式返回错误
  </behavior>
  <action>
    创建 `internal/service/thumbnail_service.go`，实现 ThumbnailService：
    
    ```go
    package service
    
    import (
        "bytes"
        "image"
        _ "image/gif"
        _ "image/jpeg"
        _ "image/png"
        "os"
        
        "github.com/disintegration/imaging"
        _ "golang.org/x/image/webp"
    )
    
    type ThumbnailService struct {
        SmallWidth  int  // 默认 200
        LargeWidth  int  // 默认 600
        SmallQuality int // 默认 85
        LargeQuality int // 默认 90
    }
    
    type Thumbnail struct {
        Data   []byte
        Width  int
        Height int
        Size   string // "small" or "large"
    }
    
    func NewThumbnailService() *ThumbnailService {
        return &ThumbnailService{
            SmallWidth:   200,
            LargeWidth:   600,
            SmallQuality: 85,
            LargeQuality: 90,
        }
    }
    
    // GenerateThumbnail 生成指定尺寸的缩略图
    func (s *ThumbnailService) GenerateThumbnail(imgPath string, size string) (*Thumbnail, error) {
        // 1. 打开图片文件
        // 2. 解码图片
        // 3. 根据 size 选择目标宽度和质量
        // 4. 使用 imaging.Resize 缩放（Lanczos 滤镜）
        // 5. 编码为 JPEG
        // 6. 返回 Thumbnail 结构
    }
    
    // GenerateBoth 生成小图和大图两种缩略图
    func (s *ThumbnailService) GenerateBoth(imgPath string) (small, large *Thumbnail, err error) {
        // 调用 GenerateThumbnail 两次
    }
    ```
    
    使用 `disintegration/imaging` 库：
    - `imaging.Open()` 打开图片
    - `imaging.Resize(src, width, 0, imaging.Lanczos)` 缩放
    - `imaging.Encode(&buf, resized, imaging.JPEG, imaging.JPEGQuality(quality))` 编码
    
    不要使用 CGO 依赖的库。确保支持 JPG、PNG、WebP、GIF 格式。
  </action>
  <verify>
    <automated>go test ./internal/service/... -run TestThumbnailService -v</automated>
  </verify>
  <done>ThumbnailService 可生成指定尺寸的 JPEG 缩略图，支持 JPG/PNG/WebP/GIF 格式</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: 实现 COS 上传服务</name>
  <files>internal/service/cos_service.go, internal/service/cos_service_test.go, internal/config/config.go</files>
  <behavior>
    - Test 1: Upload 方法返回正确的 COS URL
    - Test 2: 上传失败时返回错误（使用 mock 服务器测试）
    - Test 3: 对象 Key 格式正确 (thumbnails/{image_id}_{size}.jpg)
  </behavior>
  <action>
    1. 扩展 `internal/config/config.go` 添加 COS 配置：
       ```go
       type COSConfig struct {
           BucketURL string `yaml:"bucket_url"`
           SecretID  string `yaml:"secret_id"`  // 可从环境变量 COS_SECRET_ID 读取
           SecretKey string `yaml:"secret_key"` // 可从环境变量 COS_SECRET_KEY 读取
       }
       
       type Config struct {
           // ... 现有字段
           COS COSConfig `yaml:"cos"`
       }
       ```
    
    2. 创建 `internal/service/cos_service.go`：
       ```go
       package service
       
       import (
           "bytes"
           "context"
           "fmt"
           "net/http"
           "net/url"
           "os"
           
           "github.com/tencentyun/cos-go-sdk-v5"
       )
       
       type COSService struct {
           client    *cos.Client
           bucketURL string
       }
       
       func NewCOSService(cfg *COSConfig) (*COSService, error) {
           // 从环境变量获取凭证（优先级高于配置文件）
           secretID := os.Getenv("COS_SECRET_ID")
           if secretID == "" {
               secretID = cfg.SecretID
           }
           secretKey := os.Getenv("COS_SECRET_KEY")
           if secretKey == "" {
               secretKey = cfg.SecretKey
           }
           
           // 初始化 COS 客户端
           u, _ := url.Parse(cfg.BucketURL)
           b := &cos.BaseURL{BucketURL: u}
           client := cos.NewClient(b, &http.Client{
               Transport: &cos.AuthorizationTransport{
                   SecretID:  secretID,
                   SecretKey: secretKey,
               },
           })
           
           return &COSService{
               client:    client,
               bucketURL: cfg.BucketURL,
           }, nil
       }
       
       // Upload 上传缩略图到 COS
       func (s *COSService) Upload(ctx context.Context, imageID int64, size string, data []byte) (string, error) {
           // key: thumbnails/{image_id}_{size}.jpg
           key := fmt.Sprintf("thumbnails/%d_%s.jpg", imageID, size)
           
           _, err := s.client.Object.Put(ctx, key, bytes.NewReader(data), &cos.ObjectPutOptions{
               ObjectPutHeaderOptions: &cos.ObjectPutHeaderOptions{
                   ContentType: "image/jpeg",
               },
           })
           if err != nil {
               return "", err
           }
           
           // 返回完整 URL
           return fmt.Sprintf("%s/%s", s.bucketURL, key), nil
       }
       ```
    
    3. 更新 `config.yaml` 添加 COS 配置示例：
       ```yaml
       cos:
         bucket_url: "https://acgwarehouse-1301393037.cos.ap-shanghai.myqcloud.com"
         secret_id: ""  # 从环境变量 COS_SECRET_ID 读取
         secret_key: "" # 从环境变量 COS_SECRET_KEY 读取
       ```
    
    测试时使用 httptest 创建 mock 服务器，不要在测试中连接真实的 COS。
  </action>
  <verify>
    <automated>go test ./internal/service/... -run TestCOSService -v</automated>
  </verify>
  <done>COSService 可上传缩略图到腾讯云 COS 并返回可访问的 URL</done>
</task>

<task type="auto">
  <name>task 4: 注册缩略图生成异步任务</name>
  <files>internal/worker/thumbnail_handler.go, internal/worker/thumbnail_handler_test.go, cmd/server/main.go</files>
  <action>
    1. 创建 `internal/worker/thumbnail_handler.go`：
       ```go
       package worker
       
       import (
           "context"
           "encoding/json"
           
           "github.com/wonichan/acgwarehouse-backend/internal/domain"
           "github.com/wonichan/acgwarehouse-backend/internal/repository"
           "github.com/wonichan/acgwarehouse-backend/internal/service"
       )
       
       type ThumbnailHandler struct {
           thumbnailSvc *service.ThumbnailService
           cosSvc       *service.COSService
           imageRepo    *repository.ImageRepository
       }
       
       func NewThumbnailHandler(...) *ThumbnailHandler { ... }
       
       // Handle 处理缩略图生成任务
       // payload: {"image_id": 123, "path": "/path/to/image.jpg"}
       func (h *ThumbnailHandler) Handle(ctx context.Context, jobID int64, payload string) error {
           // 1. 解析 payload
           // 2. 生成大小缩略图
           // 3. 上传到 COS
           // 4. 更新 images 表的 thumbnail_small_url 和 thumbnail_large_url
       }
       ```
    
    2. 在 `cmd/server/main.go` 中注册任务处理器：
       ```go
       // 初始化服务
       thumbnailSvc := service.NewThumbnailService()
       cosSvc, _ := service.NewCOSService(&cfg.COS)
       
       // 注册异步任务处理器
       thumbnailHandler := worker.NewThumbnailHandler(thumbnailSvc, cosSvc, imageRepo)
       jobManager.RegisterHandler("thumbnail_generate", thumbnailHandler.Handle)
       ```
    
    缩略图生成是按需触发，不在图片导入时同步执行。
  </action>
  <verify>
    <automated>go test ./internal/worker/... -run TestThumbnailHandler -v</automated>
  </verify>
  <done>缩略图生成任务可在 JobManager 中注册并执行</done>
</task>

</tasks>

<verification>
- [ ] `go test ./internal/service/...` 全部通过
- [ ] `go test ./internal/worker/...` 全部通过
- [ ] `go test ./internal/domain/...` 全部通过
- [ ] 数据库迁移可执行：`make migrate-up`
- [ ] 缩略图生成命令可触发：手动添加测试任务
</verification>

<success_criteria>
1. 系统可以生成 ~200px 和 ~600px 两种尺寸的 JPEG 缩略图
2. 缩略图可上传到腾讯云 COS 存储桶
3. Image 模型包含 ThumbnailSmallUrl 和 ThumbnailLargeUrl 字段
4. 异步任务处理器可正确注册和执行
5. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai/02-01-SUMMARY.md`
</output>