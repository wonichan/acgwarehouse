---
phase: 14-backfill-recovery-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/service/ai_backfill_service.go
  - internal/service/ai_backfill_service_test.go
  - internal/repository/image_repository.go
  - internal/repository/image_repository_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
autonomous: true
requirements:
  - AIQ-03
must_haves:
  truths:
    - Admin can preview a backfill operation for the current filtered result instead of blindly queueing the whole library.
    - Images with existing AI tags or active AI tasks are skipped and reported by reason.
    - A backfill request with zero eligible images returns an explicit no-op explanation instead of a fake success.
  artifacts:
    - path: internal/service/ai_backfill_service.go
      provides: filter-aware backfill preview and execute orchestration
    - path: internal/repository/image_repository.go
      provides: filtered missing-AI-tag candidate queries and skip-reason counts
    - path: internal/handler/admin_handler.go
      provides: preview and execute admin endpoints for backfill
  key_links:
    - from: internal/service/ai_backfill_service.go
      to: internal/repository/image_repository.go
      via: candidate preview and execution queries
      pattern: Preview.*Backfill|Execute.*Backfill
    - from: internal/handler/admin_handler.go
      to: internal/service/ai_backfill_service.go
      via: preview and execute HTTP endpoints
      pattern: backfill
    - from: internal/handler/routes.go
      to: /admin/api/actions/backfill
      via: admin route registration
      pattern: backfill
---

<objective>
Define the Phase 14 backfill contract for “images currently missing AI tags” so the executor can preview, validate, and then enqueue a new manual batch for the full filtered result.

Purpose: Satisfy AIQ-03 while honoring D-01 through D-13 exactly, especially filtered-scope execution, skip-reason visibility, and explicit no-op feedback.
Output: A tested service contract, repository queries, and admin endpoints for backfill preview + execute.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
@.planning/phases/14-backfill-recovery-operations/14-RESEARCH.md
@internal/handler/ai_tag_handler.go
@internal/repository/image_repository.go
@internal/handler/admin_handler.go
@internal/handler/routes.go

<interfaces>
From `internal/handler/ai_tag_handler.go`:
```go
type aiTagBatchResponse struct {
    BatchID      int64  `json:"batch_id"`
    SourceType   string `json:"source_type"`
    Status       string `json:"status"`
    CreatedTasks int    `json:"created_tasks"`
    SkippedTasks int    `json:"skipped_tasks"`
}
```

From `internal/repository/image_repository.go`:
```go
FindImagesWithoutAITags(ctx context.Context, limit int) ([]domain.Image, error)
FindUntagged(ctx context.Context, limit, offset int, sortBy, sortDir string) ([]domain.Image, error)
CountUntagged(ctx context.Context) (int64, error)
```

Locked rules to implement:
```text
- D-01/D-03: current filtered result means the full filtered result, not the current page.
- D-02: backfill is blocked when no narrowing filter exists.
- D-05/D-09/D-12: skip reasons must at least separate existing AI tags vs active AI tasks.
- D-10/D-13: preview must expose hit count, created-task count, skip count, and explicit zero-create messaging.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the filtered backfill preview service</name>
  <files>internal/service/ai_backfill_service.go, internal/service/ai_backfill_service_test.go, internal/repository/image_repository.go, internal/repository/image_repository_test.go</files>
  <read_first>
    - internal/repository/image_repository.go
    - internal/repository/image_repository_test.go
    - internal/handler/image_handler.go
    - internal/handler/ai_tag_handler.go
    - .planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: preview rejects an unfiltered request per D-02 and returns a business error explaining that the scope must be narrowed first.
    - Test 2: preview classifies the full filtered result into hit count, enqueueable count, skipped-with-ai-tag count, and skipped-with-active-task count per D-05, D-09, and D-12.
    - Test 3: execute creates a `manual_batch` batch only for enqueueable candidates and returns an explicit no-op result when the preview has zero eligible images per D-13.
  </behavior>
  <action>Create a dedicated backfill orchestration service instead of embedding this logic in handlers. Add a filter-aware repository contract that reuses the existing image-list semantics (`tag_ids`, `has_tags`, `sort_by`, `sort_dir`) and can evaluate the full filtered result rather than a single page. The service must expose preview and execute methods, must classify skip reasons into at least `has_ai_tag` and `has_active_ai_task`, and must call `TaskPlatformService.PlanBatch()` plus `QueueTask()` with `domain.TaskBatchSourceManualBatch` for eligible candidates only. Do not allow an empty filter scope; do not silently treat zero created tasks as success.</action>
  <acceptance_criteria>
    - `internal/service/ai_backfill_service.go` contains exported preview/execute methods for backfill orchestration.
    - `internal/repository/image_repository.go` contains a filter-aware backfill candidate query contract, not only the old `FindImagesWithoutAITags(limit)` scan helper.
    - `internal/service/ai_backfill_service_test.go` contains failing-then-passing tests for unfiltered rejection, skip-reason counts, and zero-create behavior.
    - `go test ./internal/service/... ./internal/repository/... -run "Backfill|FindImagesWithoutAITags|AITag" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/service/... ./internal/repository/... -run "Backfill|FindImagesWithoutAITags|AITag" -count=1</automated>
  </verify>
  <done>The codebase has a tested backend contract that can preview and execute a filtered missing-AI backfill without relying on page-local image IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the backfill preview and execute admin endpoints</name>
  <files>internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go</files>
  <read_first>
    - internal/handler/admin_handler.go
    - internal/handler/admin_handler_test.go
    - internal/handler/routes.go
    - internal/service/ai_backfill_service.go
    - .planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: preview endpoint returns 400 for missing narrowing filters and returns structured preview counts for a valid filtered request.
    - Test 2: execute endpoint returns `success`, `batch_id`, `created_tasks`, and structured skip counts when work is created.
    - Test 3: execute endpoint returns a non-successful no-op payload when the current filtered result contains no eligible images.
  </behavior>
  <action>Add dedicated admin endpoints for backfill preview and execution, for example under `/admin/api/actions/backfill/preview` and `/admin/api/actions/backfill/execute`. Parse the exact filter fields the service supports (`tag_ids`, `has_tags`, `sort_by`, `sort_dir`), reject unfiltered requests, and return structured JSON that includes hit count, create count, skipped total, skipped-by-reason breakdown, and new batch metadata when created. Keep the route shape under `/admin/api/*`; do not overload the existing `/api/v1/images/batch-ai-tags` endpoint with preview semantics.</action>
  <acceptance_criteria>
    - `internal/handler/admin_handler.go` contains dedicated backfill preview and execute handlers.
    - `internal/handler/routes.go` registers the new admin backfill routes.
    - `internal/handler/admin_handler_test.go` asserts both the validation path and the successful structured response path.
    - `go test ./internal/handler/... -run "Backfill|AITag" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handler/... -run "Backfill|AITag" -count=1</automated>
  </verify>
  <done>Phase 14 has stable admin HTTP contracts for previewing and executing a filtered missing-AI backfill.</done>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(14-01): add failing backfill preview service coverage`
- Commit 2 (GREEN): `feat(14-01): add filter-aware backfill preview and enqueue service`
- Commit 3 (RED): `test(14-01): add failing admin backfill endpoint coverage`
- Commit 4 (GREEN): `feat(14-01): expose admin backfill preview and execute endpoints`
</commit_strategy>

<verification>
Run `go test ./internal/service/... ./internal/repository/... -run "Backfill|FindImagesWithoutAITags|AITag" -count=1` after Task 1, then `go test ./internal/handler/... -run "Backfill|AITag" -count=1` after Task 2.
</verification>

<success_criteria>
The phase has a test-first backend contract for filtered backfill preview and execution that enforces scope narrowing, reports skip reasons, and creates a new manual batch only for eligible images.
</success_criteria>

<output>
After completion, create `.planning/phases/14-backfill-recovery-operations/14-01-SUMMARY.md`
</output>
