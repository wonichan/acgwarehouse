---
phase: 14-backfill-recovery-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/worker/job_manager.go
  - internal/service/task_platform_service_test.go
  - internal/repository/task_batch_read_repository.go
  - internal/repository/task_batch_read_repository_test.go
  - internal/service/task_read_service.go
  - internal/service/task_read_service_test.go
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - A failed image task does not prevent sibling tasks in the same batch from continuing to completion.
    - Failed and partially failed batches expose grouped failure reasons with counts.
    - Each grouped failure reason carries a retry recommendation so operators can judge whether retry is appropriate.
  artifacts:
    - path: internal/service/task_read_service.go
      provides: structured batch failure summary contract for the admin UI
    - path: internal/repository/task_batch_read_repository.go
      provides: aggregated failure grouping and retryability data
    - path: internal/service/task_platform_service_test.go
      provides: regression coverage for per-image isolation semantics
  key_links:
    - from: internal/worker/job_manager.go
      to: internal/service/task_platform_service.go
      via: failed jobs updating only their own platform task status
      pattern: failed
    - from: internal/repository/task_batch_read_repository.go
      to: internal/service/task_read_service.go
      via: grouped failure summary records
      pattern: failure
    - from: internal/service/task_read_service.go
      to: web/admin/app.js
      via: JSON contract for batch-level grouped failure visibility
      pattern: retry
---

<objective>
Strengthen and prove single-image failure isolation, then extend the batch read model so failed batches expose grouped failure reasons and retryability guidance directly at batch-list level.

Purpose: Satisfy SAFE-01 and SAFE-02 without changing the Phase 13 batch-first recovery model.
Output: regression tests for isolation semantics plus a structured failure-summary contract for downstream admin UI work.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
@.planning/phases/14-backfill-recovery-operations/14-RESEARCH.md
@internal/worker/job_manager.go
@internal/service/task_platform_service.go
@internal/repository/task_batch_read_repository.go
@internal/service/task_read_service.go

<interfaces>
From `internal/domain/task_batch.go`:
```go
const (
    TaskBatchStatusPartialFailed = "partial_failed"
    TaskBatchStatusFailed        = "failed"
)
```

From `internal/service/task_read_service.go`:
```go
type TaskBatchReadModel struct {
    FailureSummary string `json:"failure_summary,omitempty"`
}
```

Locked rules to implement:
```text
- D-14/D-15: batch failure visibility must be grouped by reason + count, not only the latest error string.
- D-16: each grouped reason must indicate whether retry is recommended.
- D-17: the grouped summary must be available at batch-list level.
- D-18/D-19: grouped failure visibility supports retry decisions; it must not replace the existing retry-failed action semantics.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock single-image failure isolation with regression tests</name>
  <files>internal/worker/job_manager.go, internal/service/task_platform_service_test.go</files>
  <read_first>
    - internal/worker/job_manager.go
    - internal/service/task_platform_service.go
    - internal/service/task_platform_service_test.go
    - internal/app/bootstrap.go
    - .planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: when one AI-tag job fails, sibling jobs in the same batch still reach terminal states and the batch becomes `partial_failed`, not blocked.
    - Test 2: marking one job failed updates only that task’s error summary and does not cancel or regress unrelated tasks.
    - Test 3: queue processing continues after a failed job instead of pausing or dropping subsequent persisted jobs.
  </behavior>
  <action>Start by writing failing service/worker regression tests around the current task-platform and worker flow. If the tests expose a real gap, patch the smallest seam necessary in `internal/worker/job_manager.go` or adjacent task-platform status handling so a failed job only affects its own task and the batch aggregate status. Do not change the locked semantics into “stop the batch on first failure,” and do not convert failed work into silent retries.</action>
  <acceptance_criteria>
    - `internal/service/task_platform_service_test.go` contains an explicit isolation regression covering one failed task plus surviving sibling tasks.
    - `internal/worker/job_manager.go` does not contain any batch-wide stop behavior triggered by a single task failure.
    - `go test ./internal/service/... -run "MarkJobFailed|PartialFailed|Isolation" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/service/... -run "MarkJobFailed|PartialFailed|Isolation" -count=1</automated>
  </verify>
  <done>The codebase has executable proof that one failed image task does not block the rest of the batch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add grouped failure summaries and retry hints to the batch read model</name>
  <files>internal/repository/task_batch_read_repository.go, internal/repository/task_batch_read_repository_test.go, internal/service/task_read_service.go, internal/service/task_read_service_test.go</files>
  <read_first>
    - internal/repository/task_batch_read_repository.go
    - internal/repository/task_batch_read_repository_test.go
    - internal/service/task_read_service.go
    - internal/service/task_read_service_test.go
    - internal/service/admin_service.go
    - .planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a failed batch with multiple identical error summaries returns grouped reasons with counts instead of only one latest error string.
    - Test 2: grouped failure records classify at least retryable transient failures vs non-retryable configuration/data failures.
    - Test 3: the task read service surfaces the grouped failure array in the JSON model consumed by the admin page while preserving the existing short `failure_summary` field for compatibility.
  </behavior>
  <action>Add a structured failure summary contract to the batch read path, such as `FailureGroups []TaskBatchFailureGroup`, where each group contains `reason_key`, `reason_label`, `count`, `retry_recommended`, and `retry_hint`. Implement the aggregation in the read repository from `platform_tasks.error_summary`, keeping `failure_summary` as a short fallback string. Add a deterministic backend classifier for retryability; for example, timeout/rate-limit/network errors should be retry-recommended, while auth/configuration/malformed-input/missing-file errors should not. Do not make the browser infer retryability.</action>
  <acceptance_criteria>
    - `internal/service/task_read_service.go` defines a structured batch failure-group JSON contract.
    - `internal/repository/task_batch_read_repository.go` aggregates failure groups with counts from task rows.
    - `internal/repository/task_batch_read_repository_test.go` and `internal/service/task_read_service_test.go` assert grouped counts and retry hints.
    - `go test ./internal/repository/... ./internal/service/... -run "FailureSummary|RetryHint|TaskRead" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/repository/... ./internal/service/... -run "FailureSummary|RetryHint|TaskRead" -count=1</automated>
  </verify>
  <done>The batch read contract contains grouped failure reasons and retry guidance that downstream admin UI code can render directly.</done>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(14-02): add failing isolation regression coverage`
- Commit 2 (GREEN): `fix(14-02): preserve sibling task progress after single-task failure`
- Commit 3 (RED): `test(14-02): add failing grouped failure summary coverage`
- Commit 4 (GREEN): `feat(14-02): expose grouped failure reasons and retry hints`
</commit_strategy>

<verification>
Run `go test ./internal/service/... -run "MarkJobFailed|PartialFailed|Isolation" -count=1` after Task 1, then `go test ./internal/repository/... ./internal/service/... -run "FailureSummary|RetryHint|TaskRead" -count=1` after Task 2.
</verification>

<success_criteria>
Phase 14 has explicit regression protection for per-image failure isolation and a backend-owned grouped failure summary contract that can drive recovery UX without additional inference in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/14-backfill-recovery-operations/14-02-SUMMARY.md`
</output>
