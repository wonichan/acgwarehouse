---
phase: 14-backfill-recovery-operations
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - web/admin/index.html
  - web/admin/app.js
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
autonomous: false
requirements:
  - AIQ-03
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - Admin can trigger backfill only from a narrowed current filter scope and sees hit/create/skip counts before confirming.
    - Successful backfill reuses the retry-style feedback pattern: toast plus navigation to the new batch.
    - Failed and partially failed batches show grouped reasons and retry guidance directly in the batch list.
  artifacts:
    - path: web/admin/index.html
      provides: Phase 14 backfill controls and failure-summary presentation slots
    - path: web/admin/app.js
      provides: preview/execute UX, zero-create feedback, grouped failure rendering, and batch navigation
    - path: internal/handler/admin_handler.go
      provides: final JSON shaping for the admin UX contract
  key_links:
    - from: web/admin/app.js
      to: /admin/api/actions/backfill/preview
      via: preflight confirmation flow
      pattern: preview
    - from: web/admin/app.js
      to: /admin/api/actions/backfill/execute
      via: create batch and navigate to new batch
      pattern: execute
    - from: web/admin/app.js
      to: task_batches.failure_groups
      via: batch list rendering
      pattern: retry
---

<objective>
Wire the Phase 14 admin recovery UX: a filtered-scope backfill action with preflight confirmation, zero-create handling, batch jump feedback, and grouped failure visibility at batch-list level.

Purpose: Complete the operator-facing recovery loop without collapsing the separate mental models of “retry failed tasks” and “backfill missing AI tags.”
Output: Admin UI wiring, final handler response shaping, and a final human-verification checkpoint.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
@.planning/phases/14-backfill-recovery-operations/14-RESEARCH.md
@.planning/phases/13-backend-monitoring-queue-control/13-04-SUMMARY.md
@internal/handler/admin_handler.go
@web/admin/index.html
@web/admin/app.js

<interfaces>
From Phase 13 retry UX baseline:
```text
- success toast includes created count
- UI navigates to the newly created batch after a short refresh
```

From Plan 14-01 output contract:
```text
- preview response exposes hit_count, created_task_count, skipped_count, skipped_by_reason
- execute response exposes batch_id, created_tasks, skipped_by_reason, and explicit no-op messaging
```

From Plan 14-02 output contract:
```text
- task batch payload exposes failure_groups[] with reason label, count, retry_recommended, and retry_hint
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the admin backfill action and grouped failure summaries</name>
  <files>web/admin/index.html, web/admin/app.js, internal/handler/admin_handler.go, internal/handler/admin_handler_test.go</files>
  <read_first>
    - web/admin/index.html
    - web/admin/app.js
    - internal/handler/admin_handler.go
    - internal/handler/admin_handler_test.go
    - .planning/phases/14-backfill-recovery-operations/14-CONTEXT.md
    - .planning/phases/13-backend-monitoring-queue-control/13-04-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: the admin payload includes the grouped failure summary fields expected by the browser.
    - Test 2: the browser builds a preview-first flow that blocks execute when no narrowing filters are present and surfaces a clear zero-create message.
    - Test 3: a successful execute flow shows a toast with created-task count and navigates to the new batch, matching the Phase 13 retry experience.
  </behavior>
  <action>Add a dedicated Phase 14 backfill action area to the admin monitor instead of overloading failed-batch retry controls. The UI must support a narrowed filter scope only, must call the preview endpoint before execute, must render the preflight counts required by D-10, and must show the D-13 zero-create explanation when nothing is eligible. Extend batch-row rendering so `failed` and `partial_failed` rows show grouped reason + count summaries and the backend-provided retry recommendation directly in the batch list. Preserve “Retry failed tasks” as the primary failed-batch action; do not replace it with backfill.</action>
  <acceptance_criteria>
    - `web/admin/index.html` contains a dedicated Phase 14 backfill control area separate from retry-failed controls.
    - `web/admin/app.js` contains preview-first backfill logic and renders grouped failure summaries from structured payload data.
    - `internal/handler/admin_handler_test.go` asserts the final payload shape required by the browser.
    - `go test ./internal/handler/... -run "Backfill|TaskPlatform" -count=1` exits 0.
    - `node --check web/admin/app.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handler/... -run "Backfill|TaskPlatform" -count=1 && node --check web/admin/app.js</automated>
  </verify>
  <done>The admin UI can preview and execute a filtered backfill, renders grouped failure reasons at batch-list level, and preserves the existing retry-failed recovery path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the full operator recovery loop</name>
  <files>web/admin/index.html, web/admin/app.js, internal/handler/admin_handler.go, internal/handler/admin_handler_test.go</files>
  <action>Run the focused automated verification first, then pause for human verification of the end-to-end admin recovery flow. This checkpoint exists because the final acceptance depends on UI wording, information density, and operator comprehension, not only API correctness.</action>
  <what-built>Complete Phase 14 operator flow: filtered backfill preview/execute, zero-create handling, grouped batch failure visibility, and preserved retry-failed semantics</what-built>
  <how-to-verify>
    1. Start the server and open `/admin-ui/`.
    2. Narrow the backfill scope using the Phase 14 filter controls; confirm there is no way to run backfill with an unfiltered whole-library scope.
    3. Trigger preview and confirm the dialog or inline confirmation shows at least hit count, created-task count, skipped total, and skip buckets for “already has AI tags” and “already has active AI task”.
    4. Execute a preview with eligible images and confirm the toast reports created-task count and navigates to the newly created batch.
    5. Execute a preview with zero eligible images and confirm the UI explicitly explains that there is nothing to backfill instead of showing generic success.
    6. Load a `failed` or `partial_failed` batch and confirm the batch row itself shows grouped failure reasons with counts and a retry recommendation.
    7. Confirm `Retry failed tasks` remains the main action for failed batches and still creates a new retry batch rather than reusing the backfill action.
  </how-to-verify>
  <verify>
    <automated>go test ./internal/... -run "Backfill|Retry|Failure|TaskPlatform" -count=1 && node --check web/admin/app.js</automated>
  </verify>
  <done>Human verification confirms that the Phase 14 operator loop is understandable, correctly scoped, and consistent with the Phase 13 retry baseline.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(14-03): add failing admin phase-14 payload and UI contract coverage`
- Commit 2 (GREEN): `feat(14-03): wire admin backfill flow and grouped failure summaries`
- Commit 3 (CHECKPOINT): no additional commit until the human-verify checkpoint is approved
</commit_strategy>

<verification>
Run `go test ./internal/handler/... -run "Backfill|TaskPlatform" -count=1 && node --check web/admin/app.js` before the human-verification checkpoint, then run `go test ./internal/... -run "Backfill|Retry|Failure|TaskPlatform" -count=1 && node --check web/admin/app.js` once checkpoint feedback is resolved.
</verification>

<success_criteria>
The operator-facing Phase 14 loop is complete: backfill is filtered and explainable, failure visibility is grouped and actionable, and the retry-failed recovery path remains distinct and intact.
</success_criteria>

<output>
After completion, create `.planning/phases/14-backfill-recovery-operations/14-03-SUMMARY.md`
</output>
