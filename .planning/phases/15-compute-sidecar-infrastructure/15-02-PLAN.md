---
phase: 15-compute-sidecar-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/runtime_manifest.go
  - internal/app/runtime_manifest_test.go
  - internal/app/app.go
  - cmd/server/main.go
  - flutter_app/lib/config/api_config.dart
  - flutter_app/lib/providers/config_provider.dart
  - flutter_app/lib/bootstrap/runtime_manifest_loader.dart
  - flutter_app/lib/main.dart
  - flutter_app/test/bootstrap/runtime_manifest_loader_test.dart
  - flutter_app/test/config/api_config_test.dart
autonomous: true
requirements:
  - COMP-06
must_haves:
  truths:
    - Runtime manifest is the required production discovery path for Flutter to discover current Go base URL.
    - Manifest schema exposes Go address only; Python endpoint stays internal to Go orchestration.
    - Hardcoded `localhost:8080` remains dev fallback only, not the product contract.
  artifacts:
    - path: internal/app/runtime_manifest.go
      provides: atomic runtime manifest writer and schema versioned payload
    - path: flutter_app/lib/bootstrap/runtime_manifest_loader.dart
      provides: early boot manifest read and `ApiConfig` base URL update flow
    - path: flutter_app/lib/main.dart
      provides: app startup sequence that applies runtime-discovered Go base URL before API usage
  key_links:
    - from: internal/app/runtime_manifest.go
      to: flutter_app/lib/bootstrap/runtime_manifest_loader.dart
      via: versioned JSON schema (`go.base_url`) consumption
      pattern: base_url|version
    - from: flutter_app/lib/bootstrap/runtime_manifest_loader.dart
      to: flutter_app/lib/config/api_config.dart
      via: runtime URL override before first API call
      pattern: updateBaseUrl
---

<objective>
Deliver manifest-based Go URL discovery for desktop startup so Flutter connects to the runtime Go endpoint without depending on fixed ports.

Purpose: satisfy COMP-06 and D-04..D-07 exactly by replacing hardcoded host assumptions with a Go-produced runtime manifest and Flutter bootstrap loader.
Output: tested Go manifest writer plus Flutter startup loader with explicit dev-only fallback behavior.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
@.planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
@internal/app/app.go
@cmd/server/main.go
@flutter_app/lib/config/api_config.dart
@flutter_app/lib/providers/config_provider.dart
@flutter_app/lib/main.dart

<interfaces>
Manifest contract constraints:
```text
- D-04: runtime manifest is mandatory for desktop address discovery.
- D-05: localhost:8080 can exist only as development fallback.
- D-06: Python endpoint MUST NOT be exposed to Flutter contract.
- D-07: manifest is a desktop-time mechanism, not a permanent deployment lock.
```

Recommended minimum JSON shape:
```json
{
  "version": 1,
  "generated_at": "<RFC3339>",
  "go": {
    "base_url": "http://127.0.0.1:<port>",
    "ready": true
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build atomic Go runtime manifest writer with schema guarantees</name>
  <files>internal/app/runtime_manifest.go, internal/app/runtime_manifest_test.go, internal/app/app.go, cmd/server/main.go</files>
  <read_first>
    - internal/app/app.go
    - internal/app/app_test.go
    - cmd/server/main.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
    - .planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
  </read_first>
  <behavior>
    - RED: add tests asserting manifest writes include `version`, `generated_at`, and `go.base_url` fields with valid values.
    - RED: add tests asserting writes are atomic (temp-file then rename) and never leave partial JSON at target path.
    - RED: add tests asserting schema does not include Python endpoint fields.
    - GREEN: implement minimal manifest writer and startup wiring to pass tests.
    - REFACTOR: centralize manifest path resolution and serialization helpers while keeping tests green.
  </behavior>
  <action>Add a Go-owned runtime manifest component that writes a versioned JSON file atomically at startup using the actual Go listen address (host+dynamic port), and updates/removes it on shutdown as defined by the chosen cleanup policy. Wire manifest generation from app/server startup code after Go address is known. Keep manifest payload strictly limited to Go discovery metadata needed by Flutter in Phase 15.</action>
  <acceptance_criteria>
    - `internal/app/runtime_manifest.go` implements versioned JSON payload generation and atomic file write semantics.
    - `internal/app/runtime_manifest_test.go` verifies field presence/validity and rejects Python endpoint leakage.
    - `go test ./internal/app/... -run "Manifest|Runtime" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app/... -run "Manifest|Runtime" -count=1</automated>
  </verify>
  <done>Go startup produces a valid atomic runtime manifest containing only Flutter-safe Go discovery fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Flutter bootstrap manifest loader and dev-only fallback path</name>
  <files>flutter_app/lib/bootstrap/runtime_manifest_loader.dart, flutter_app/lib/config/api_config.dart, flutter_app/lib/providers/config_provider.dart, flutter_app/lib/main.dart, flutter_app/test/bootstrap/runtime_manifest_loader_test.dart, flutter_app/test/config/api_config_test.dart</files>
  <read_first>
    - flutter_app/lib/main.dart
    - flutter_app/lib/config/api_config.dart
    - flutter_app/lib/providers/config_provider.dart
    - flutter_app/test/providers/navigation_provider_test.dart
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
  </read_first>
  <behavior>
    - RED: add tests proving valid manifest `go.base_url` updates `ApiConfig` before first API consumer reads base URL.
    - RED: add tests proving manifest-missing or malformed behavior uses explicit development fallback (`http://localhost:8080`) only in dev mode.
    - RED: add tests proving when manifest exists, startup does not ignore it in favor of hardcoded default host.
    - GREEN: minimally implement loader integration in app startup and config update path.
    - REFACTOR: keep bootstrap logic isolated in dedicated loader helper and preserve provider/config API clarity.
  </behavior>
  <action>Create `runtime_manifest_loader.dart` to read and validate the runtime manifest at app boot, then call `ApiConfig.updateBaseUrl(...)` (and provider sync if needed) before API traffic starts. Keep `localhost:8080` as explicit development fallback only. Do not parse or depend on Python endpoint data in Flutter code.</action>
  <acceptance_criteria>
    - `flutter_app/lib/bootstrap/runtime_manifest_loader.dart` exists and applies `go.base_url` to startup configuration.
    - `flutter_app/lib/main.dart` invokes manifest loader before creating app API-dependent layers.
    - `flutter_app/test/bootstrap/runtime_manifest_loader_test.dart` and `flutter_app/test/config/api_config_test.dart` cover manifest success and fallback behavior.
    - `flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart</automated>
  </verify>
  <done>Flutter startup discovers and applies runtime Go URL from manifest, with dev-only fallback retained and hardcoded port no longer acting as product contract.</done>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(15-02): add failing runtime manifest schema and atomic-write coverage`
- Commit 2 (GREEN): `feat(15-02): add Go runtime manifest writer for dynamic Go endpoint discovery`
- Commit 3 (RED): `test(15-02): add failing Flutter manifest bootstrap coverage`
- Commit 4 (GREEN): `feat(15-02): load runtime manifest at Flutter startup with dev fallback`
</commit_strategy>

<verification>
Run `go test ./internal/app/... -run "Manifest|Runtime" -count=1` after Task 1, then `flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart` after Task 2.
</verification>

<success_criteria>
Phase 15 startup no longer depends on fixed Go ports in product behavior: Go publishes runtime endpoint metadata atomically and Flutter consumes it before initiating API traffic, while Python addressing remains internal to Go.
</success_criteria>

<output>
After completion, create `.planning/phases/15-compute-sidecar-infrastructure/15-02-SUMMARY.md`
</output>
