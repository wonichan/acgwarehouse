---
phase: 15-compute-sidecar-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/app/app.go
  - internal/app/bootstrap.go
  - internal/app/app_test.go
  - internal/handler/health_handler.go
  - internal/handler/health_handler_test.go
  - internal/handler/routes_test.go
  - internal/sidecar/runtime.go
  - internal/sidecar/runtime_test.go
autonomous: true
requirements:
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - Go is the only orchestrator for Python sidecar lifecycle; Flutter never governs Python process lifecycle directly.
    - Python unavailability transitions the system to degraded availability without turning `/health` or `/ready` into Python-gated checks.
    - Sidecar lifecycle has explicit startup timeout, probe state, graceful shutdown, and forced-kill fallback with process reap guarantees.
  artifacts:
    - path: internal/sidecar/runtime.go
      provides: sidecar state machine, process spawn/probe/stop contract, and degraded transition semantics
    - path: internal/app/app.go
      provides: app lifecycle wiring for sidecar runtime start and stop
    - path: internal/handler/health_handler.go
      provides: preserved layered health semantics (`/health` liveness, `/ready` Go readiness)
  key_links:
    - from: internal/app/app.go
      to: internal/sidecar/runtime.go
      via: startup, probe, shutdown lifecycle orchestration
      pattern: Start|Stop|Degraded|Ready
    - from: internal/handler/health_handler.go
      to: internal/sidecar/runtime.go
      via: semantic boundary asserting no direct Python gating in base health endpoints
      pattern: Health|Ready
---

<objective>
Build the Go-owned sidecar lifecycle infrastructure with explicit state transitions, bounded startup, safe shutdown, and degraded-availability semantics while preserving layered `/health` and `/ready` contracts.

Purpose: satisfy COMP-01 and COMP-02 exactly under D-01..D-03 and D-08..D-10 without pulling Phase 16 compute-migration work into Phase 15.
Output: tested sidecar runtime and app wiring that prove Go can orchestrate Python lifecycle safely and remain usable when Python is unavailable.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
@.planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
@internal/app/app.go
@internal/app/bootstrap.go
@internal/handler/health_handler.go
@internal/handler/routes.go

<interfaces>
Locked boundaries to preserve:
```text
- D-01/D-02: Go is sole lifecycle owner (spawn, wait, probe, stop, error propagation).
- D-03/D-14: local desktop startup today, but no permanent same-machine/same-process contract leak.
- D-08: /health = Go liveness, /ready = Go readiness only.
- D-09: sidecar details go to admin diagnostics, not base health endpoints.
- D-10: sidecar unavailable => degraded availability, not full outage.
- D-11/D-12: Python remains pure compute boundary; no DB persistence, repository ownership, batch orchestration, or business-ID semantics move to Python in Phase 15.
- D-13: only infrastructure shells for future compute APIs are allowed; duplicate-detection migration itself stays out of scope.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sidecar runtime state machine with bounded lifecycle controls</name>
  <files>internal/sidecar/runtime.go, internal/sidecar/runtime_test.go</files>
  <read_first>
    - internal/app/app.go
    - internal/app/app_test.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
    - .planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
  </read_first>
  <behavior>
    - RED: add tests proving transitions `NotStarted -> Starting -> Ready` for successful startup handshake and probe.
    - RED: add tests proving startup timeout transitions to `Degraded` with a diagnosable last error summary.
    - RED: add tests proving shutdown attempts graceful HTTP stop first, then force kill on timeout, and always waits/reaps child process.
    - GREEN: implement minimal runtime state machine and process control to satisfy each failing test.
    - REFACTOR: extract probe and shutdown helpers while keeping all lifecycle tests green.
  </behavior>
  <action>Create `internal/sidecar/runtime.go` as a dedicated runtime component with explicit states (`not_started`, `starting`, `ready`, `degraded`, `stopping`, `stopped`). Implement process spawn, startup timeout handling, readiness probe loop, last probe metadata capture, and graceful shutdown with forced-kill fallback plus reap/wait guarantee. Keep the runtime contract Go-internal; do not introduce any Flutter-facing Python endpoint or product API contract.</action>
  <acceptance_criteria>
    - `internal/sidecar/runtime.go` defines a runtime type with explicit sidecar states including `degraded`.
    - `internal/sidecar/runtime_test.go` contains RED->GREEN coverage for startup success, startup timeout degraded transition, and shutdown reap behavior.
    - `go test ./internal/sidecar/... -run "Runtime|Lifecycle|Degraded|Shutdown" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/sidecar/... -run "Runtime|Lifecycle|Degraded|Shutdown" -count=1</automated>
  </verify>
  <done>There is a test-backed sidecar runtime component that enforces bounded startup/shutdown behavior and explicit degraded transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sidecar runtime into App startup and shutdown orchestration</name>
  <files>internal/app/app.go, internal/app/bootstrap.go, internal/app/app_test.go</files>
  <read_first>
    - internal/app/app.go
    - internal/app/bootstrap.go
    - internal/app/app_test.go
    - internal/sidecar/runtime.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
  </read_first>
  <behavior>
    - RED: add app lifecycle tests proving startup initializes sidecar runtime from Go and records degraded state (not fatal app abort) when Python fails to become ready.
    - RED: add app lifecycle tests proving the normal desktop-ready path is reported only after both Go and Python are ready, while degraded startup is reported as a separate non-fully-ready operational mode.
    - RED: add app shutdown tests proving sidecar stop is invoked exactly once under normal and repeated shutdown calls.
    - GREEN: minimally wire sidecar runtime initialization/start in app bootstrap/run and stop in app shutdown.
    - REFACTOR: isolate sidecar startup dependencies behind small app-internal interfaces, preserving existing scheduler/job-manager behavior.
  </behavior>
  <action>Extend `App` to own a sidecar runtime dependency and wire it in startup/shutdown paths: initialize in bootstrap, start before serving traffic, and stop during shutdown. Reconcile COMP-01 with D-10 explicitly by implementing two distinct outcomes: (1) normal desktop-ready startup is reported only after both Go and Python are ready; (2) if Python startup fails or times out, the application must not report full sidecar-ready startup success, but it must enter a degraded operational mode where Go remains usable for non-compute flows. Keep `Flutter -> Go -> Python` orchestration direction intact by ensuring sidecar is never managed from Flutter code. Do not move duplicate-detection logic, repository access, database persistence, batch orchestration, or business-ID semantics into Python-facing contracts in this plan.</action>
  <acceptance_criteria>
    - `internal/app/app.go` includes sidecar runtime lifecycle calls in startup and shutdown.
    - `internal/app/app_test.go` asserts both-ready startup success as a distinct path and degraded startup as a separate non-fully-ready path.
    - `internal/app/app_test.go` asserts idempotent shutdown interactions with sidecar runtime.
    - `internal/app/app.go` and `internal/sidecar/runtime.go` do not add repository/database imports or Python-facing business-ID fields such as `batch_id`, `image_id`, `record_id`, or persistence calls.
    - `go test ./internal/app/... -run "Sidecar|Shutdown|Degraded" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app/... -run "Sidecar|Shutdown|Degraded" -count=1</automated>
  </verify>
  <done>Go startup/shutdown now orchestrates Python sidecar lifecycle through a single app-owned runtime, with degraded availability preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock `/health` and `/ready` layered semantics with regression tests</name>
  <files>internal/handler/health_handler.go, internal/handler/health_handler_test.go, internal/handler/routes_test.go</files>
  <read_first>
    - internal/handler/health_handler.go
    - internal/handler/routes.go
    - internal/handler/routes_test.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
  </read_first>
  <behavior>
    - RED: add failing tests asserting `/health` returns Go liveness even when sidecar runtime state is degraded.
    - RED: add failing tests asserting `/ready` represents Go readiness only and is not converted into Python aggregate readiness.
    - GREEN: implement minimal handler wiring necessary to satisfy the tests without expanding endpoint scope.
    - REFACTOR: keep health payload concise and stable while retaining semantic boundaries.
  </behavior>
  <action>Preserve and test layered health semantics so base health endpoints remain Go-scoped. If code changes are needed, keep them minimal and explicit in handlers/tests. Do not move sidecar diagnostics into `/health` or `/ready`; those details belong in admin overview work (Plan 15-03).</action>
  <acceptance_criteria>
    - `internal/handler/health_handler_test.go` explicitly asserts `/health` and `/ready` remain Go-scoped under degraded sidecar conditions.
    - `internal/handler/routes_test.go` continues to verify route registration without introducing Python endpoint coupling.
    - `go test ./internal/handler/... -run "Health|Ready|Routes" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handler/... -run "Health|Ready|Routes" -count=1</automated>
  </verify>
  <done>Layered health semantics are regression-protected and remain compliant with D-08 and D-09.</done>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(15-01): add failing sidecar runtime lifecycle coverage`
- Commit 2 (GREEN): `feat(15-01): add Go-side sidecar runtime state machine`
- Commit 3 (RED): `test(15-01): add failing app lifecycle orchestration coverage`
- Commit 4 (GREEN): `feat(15-01): wire sidecar runtime into app startup and shutdown`
- Commit 5 (RED): `test(15-01): add failing layered health semantic regressions`
- Commit 6 (GREEN): `fix(15-01): preserve health and ready boundaries under degraded sidecar`
</commit_strategy>

<verification>
Run `go test ./internal/sidecar/... -run "Runtime|Lifecycle|Degraded|Shutdown" -count=1` after Task 1, `go test ./internal/app/... -run "Sidecar|Shutdown|Degraded" -count=1` after Task 2, then `go test ./internal/handler/... -run "Health|Ready|Routes" -count=1` after Task 3.
</verification>

<success_criteria>
Phase 15 has a test-first Go sidecar lifecycle foundation where normal desktop-ready startup occurs only after both Go and Python are ready, degraded mode remains operationally usable without claiming full sidecar-ready success, base health semantics stay layered and Go-scoped, and Python remains a pure-compute boundary with no database or business-ID leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-compute-sidecar-infrastructure/15-01-SUMMARY.md`
</output>
