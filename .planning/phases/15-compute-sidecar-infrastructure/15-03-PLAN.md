---
phase: 15-compute-sidecar-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified:
  - internal/service/admin_service.go
  - internal/service/admin_service_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/health_handler_test.go
  - internal/handler/routes.go
  - internal/handler/routes_test.go
  - internal/app/app.go
  - internal/app/app_test.go
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-06
must_haves:
  truths:
    - Sidecar diagnostics are exposed via admin overview contract, not `/health` or `/ready` payload inflation.
    - Python-side failures remain diagnosable and visible while Go gallery-serving path remains degraded-available.
    - Flutter-facing contract remains Go-only even after diagnostics wiring; no Python endpoint leakage.
  artifacts:
    - path: internal/service/admin_service.go
      provides: sidecar diagnostic summary fields in task platform overview
    - path: internal/handler/admin_handler.go
      provides: admin API serialization of sidecar state/probe/error diagnostics
    - path: internal/app/app_test.go
      provides: degraded-availability regression proving Go service usability when sidecar unavailable
  key_links:
    - from: internal/service/admin_service.go
      to: internal/app/app.go
      via: injected sidecar runtime status provider
      pattern: sidecar|diagnostic|probe
    - from: internal/handler/admin_handler.go
      to: /admin/api/task-platform/overview
      via: JSON contract extension for sidecar diagnostics
      pattern: overview|sidecar
---

<objective>
Complete Phase 15 observability and degraded-availability integration by surfacing sidecar diagnostics in admin overview and locking end-to-end behavioral invariants.

Purpose: close COMP-01 and COMP-02 execution with explicit diagnostics visibility while retaining COMP-06 contract boundaries and D-08..D-10 semantics.
Output: tested admin diagnostics payload and regressions proving Python outages do not create total application outage.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
@.planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
@internal/service/admin_service.go
@internal/service/admin_service_test.go
@internal/handler/admin_handler.go
@internal/handler/admin_handler_test.go
@internal/handler/routes.go
@internal/app/app.go
@internal/app/app_test.go

<interfaces>
Locked boundaries to enforce:
```text
- D-09: sidecar state, last probe result, and error summary belong in admin overview diagnostics.
- D-10: sidecar unavailable => degraded mode, main gallery path still usable.
- D-06: Python endpoint is internal; do not expose in Flutter-facing contracts.
- D-11/D-12: Python remains pure compute boundary; no DB persistence, repository ownership, or business-ID semantics move to Python.
- D-13: infrastructure-only shells are allowed; duplicate-detection migration remains Phase 16 work.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend admin overview contract with sidecar diagnostics</name>
  <files>internal/service/admin_service.go, internal/service/admin_service_test.go, internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go, internal/handler/routes_test.go</files>
  <read_first>
    - internal/service/admin_service.go
    - internal/service/admin_service_test.go
    - internal/handler/admin_handler.go
    - internal/handler/admin_handler_test.go
    - internal/handler/routes.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-CONTEXT.md
  </read_first>
  <behavior>
    - RED: add tests asserting admin overview response includes sidecar `state`, `last_probe_at`, `last_probe_result`, and `last_error_summary` fields.
    - RED: add tests asserting diagnostics are available from `/admin/api/task-platform/overview` while `/health` and `/ready` remain unchanged.
    - GREEN: implement minimal admin service + handler wiring using a Go-internal sidecar status provider interface.
    - REFACTOR: keep diagnostics DTO bounded and stable; avoid spreading runtime internals into unrelated handler payloads.
  </behavior>
  <action>Introduce a small sidecar diagnostics read interface (Go-internal) that AdminService consumes to append sidecar diagnostics into task-platform overview. Update admin handler serialization and tests accordingly. Keep diagnostics concise, operator-focused, and independent from base health endpoints. Do not expose Python host/port or process command line to Flutter contracts.</action>
  <acceptance_criteria>
    - `internal/service/admin_service.go` adds sidecar diagnostics fields to task-platform overview model.
    - `internal/service/admin_service_test.go` and `internal/handler/admin_handler_test.go` assert required diagnostics fields and values.
    - `internal/handler/routes_test.go` continues to pass and confirms admin overview route availability.
    - No new admin or Flutter-facing payload field exposes Python URL/port, database handles, batch identifiers, image identifiers, or duplicate-detection business objects.
    - `go test ./internal/service/... ./internal/handler/... -run "Admin|Overview|Sidecar|Health|Ready" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/service/... ./internal/handler/... -run "Admin|Overview|Sidecar|Health|Ready" -count=1</automated>
  </verify>
  <done>Operators can observe sidecar state and recent probe/error summaries via admin overview without changing base health semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add degraded-availability end-to-end regressions for sidecar failure scenarios</name>
  <files>internal/app/app.go, internal/app/app_test.go, internal/handler/health_handler_test.go</files>
  <read_first>
    - internal/app/app.go
    - internal/app/app_test.go
    - internal/handler/health_handler.go
    - internal/handler/health_handler_test.go
    - .planning/phases/15-compute-sidecar-infrastructure/15-RESEARCH.md
  </read_first>
  <behavior>
    - RED: add failing integration-style tests where sidecar startup fails but Go app still serves `/health`, `/ready`, and representative gallery API endpoints.
    - RED: add failing tests where sidecar crash-after-start updates diagnostics to degraded while Go process remains live.
    - GREEN: implement minimal lifecycle/diagnostic propagation fixes to satisfy degraded-availability behavior.
    - REFACTOR: simplify failure-path state propagation so degraded signaling is deterministic and testable.
  </behavior>
  <action>Add regression coverage for degraded mode invariants: Python unavailable or crashing must not block core Go serving path, and sidecar diagnostics must reflect current degraded state plus latest error summary. Keep scope within infrastructure behavior only; do not add duplicate-detection migration or auto-recovery policy beyond current phase boundaries.</action>
  <acceptance_criteria>
    - `internal/app/app_test.go` includes failing-then-passing degraded availability tests for startup failure and post-start crash paths.
    - `internal/handler/health_handler_test.go` confirms base health endpoints remain Go-scoped during degraded sidecar conditions.
    - Tests preserve distinction between full sidecar-ready startup and degraded operational mode; degraded mode must not be treated as full sidecar-ready success.
    - `go test ./internal/app/... ./internal/handler/... -run "Degraded|Sidecar|Health|Ready" -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app/... ./internal/handler/... -run "Degraded|Sidecar|Health|Ready" -count=1</automated>
  </verify>
  <done>The codebase has regression-proof degraded behavior: sidecar failure remains diagnosable without causing complete service outage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute cross-stack contract verification for Phase 15 closure</name>
  <files>internal/app/app_test.go, internal/service/admin_service_test.go, flutter_app/test/bootstrap/runtime_manifest_loader_test.dart</files>
  <read_first>
    - .planning/phases/15-compute-sidecar-infrastructure/15-01-PLAN.md
    - .planning/phases/15-compute-sidecar-infrastructure/15-02-PLAN.md
    - internal/app/app_test.go
    - internal/service/admin_service_test.go
    - flutter_app/test/bootstrap/runtime_manifest_loader_test.dart
  </read_first>
  <behavior>
    - RED: add explicit closure tests (or extend existing tests) that fail if manifest-driven Go discovery is bypassed or if sidecar diagnostics disappear from admin overview.
    - GREEN: perform minimal adjustments to satisfy closure checks.
    - REFACTOR: remove duplication in test fixtures while preserving assertion clarity.
  </behavior>
  <action>Finalize a Phase-15 closure verification layer that ties together runtime manifest discovery, Go-only health semantics, and admin diagnostics. Ensure commands run deterministically and no test depends on hardcoded port assumptions. Keep assertions measurable and specific to COMP-01/02/06.</action>
  <acceptance_criteria>
    - Cross-stack focused test commands pass for Go lifecycle/diagnostics and Flutter manifest bootstrap behavior.
    - No new Flutter-facing contract exposes Python endpoint or sidecar internal URL fields.
    - Closure checks confirm Phase 15 still contains no Python-side database persistence, repository ownership, batch orchestration, or duplicate-detection business logic migration.
    - `go test ./internal/app/... ./internal/service/... ./internal/handler/... -run "Sidecar|Manifest|Admin|Degraded|Health|Ready" -count=1` exits 0.
    - `flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/app/... ./internal/service/... ./internal/handler/... -run "Sidecar|Manifest|Admin|Degraded|Health|Ready" -count=1; if ($?) { flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart }</automated>
  </verify>
  <done>Phase 15 invariants are demonstrably locked across Go and Flutter with automated contract-focused verification.</done>
</task>

</tasks>

<commit_strategy>
- Commit 1 (RED): `test(15-03): add failing admin sidecar diagnostics contract coverage`
- Commit 2 (GREEN): `feat(15-03): expose sidecar diagnostics in admin overview`
- Commit 3 (RED): `test(15-03): add failing degraded-availability regressions`
- Commit 4 (GREEN): `fix(15-03): preserve degraded availability and diagnostics propagation`
- Commit 5 (RED): `test(15-03): add failing phase-closure cross-stack contract checks`
- Commit 6 (GREEN): `test(15-03): lock phase-15 cross-stack verification matrix`
</commit_strategy>

<verification>
Run `go test ./internal/service/... ./internal/handler/... -run "Admin|Overview|Sidecar|Health|Ready" -count=1` after Task 1, `go test ./internal/app/... ./internal/handler/... -run "Degraded|Sidecar|Health|Ready" -count=1` after Task 2, then `go test ./internal/app/... ./internal/service/... ./internal/handler/... -run "Sidecar|Manifest|Admin|Degraded|Health|Ready" -count=1; if ($?) { flutter test test/bootstrap/runtime_manifest_loader_test.dart test/config/api_config_test.dart }` after Task 3.
</verification>

<success_criteria>
Phase 15 is operationally complete: sidecar lifecycle remains Go-controlled, runtime Go URL discovery is manifest-driven, diagnostics are visible in admin overview, and Python failures result in degraded-but-usable behavior rather than full outage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-compute-sidecar-infrastructure/15-03-SUMMARY.md`
</output>
