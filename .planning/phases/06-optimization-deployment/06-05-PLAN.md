---
phase: 06-optimization-deployment
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03, 06-04]
files_modified:
  - test/perf/gallery_benchmark_test.go
  - test/perf/testdata_generator.go
  - docs/performance-report.md
  - docs/deployment.md
  - README.md
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
must_haves:
  truths:
    - "性能优化结果有可复现的测试数据、命令和报告"
    - "部署文档写清楚目录约定、启动、升级、备份和恢复"
    - "管理后台与 Docker 部署路径都被纳入最终交付说明"
  artifacts:
    - path: "test/perf/gallery_benchmark_test.go"
      provides: "大图库浏览链路的自动化基准/烟测入口"
      contains: "Benchmark"
    - path: "docs/performance-report.md"
      provides: "性能测试结果与方法说明"
      contains: "10k"
    - path: "docs/deployment.md"
      provides: "单机部署与持久化操作文档"
      contains: "docker compose up -d"
  key_links:
    - from: "docs/deployment.md"
      to: "docker-compose.yml"
      via: "documented startup workflow"
      pattern: "docker compose up -d"
    - from: "docs/performance-report.md"
      to: "test/perf/gallery_benchmark_test.go"
      via: "reported benchmark method"
      pattern: "benchmark"
---

<objective>
Close Phase 6 with reproducible benchmark artifacts, a performance report, and end-user deployment documentation that reflects the actual shipped deployment and admin/dashboard paths.

Purpose: The phase is not done when features exist; it is done when deployment and performance claims can be repeated by another operator.
Output: Benchmark/data-generation files, `docs/performance-report.md`, `docs/deployment.md`, and top-level documentation pointers.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-optimization-deployment/06-CONTEXT.md
@.planning/phases/06-optimization-deployment/06-RESEARCH.md
@.planning/phases/06-optimization-deployment/06-01-PLAN.md
@.planning/phases/06-optimization-deployment/06-03-PLAN.md
@.planning/phases/06-optimization-deployment/06-04-PLAN.md
@docker-compose.yml

<interfaces>
This plan consumes the concrete outputs from Plans `06-01`, `06-03`, and `06-04`; do not write docs against hypothetical paths or endpoints.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: build the reproducible gallery benchmark harness</name>
  <files>test/perf/gallery_benchmark_test.go, test/perf/testdata_generator.go</files>
  <action>
    Create the artifacts that let Phase 6 performance claims be repeated.

    Requirements:
    1. Add a generator/helper that can create or seed a 10k-scale image metadata dataset suitable for local SQLite benchmarking without relying on real personal images.
    2. Add a benchmark or deterministic smoke test covering the gallery list path and document the exact invocation in code comments if needed.
    3. Keep runtime under control so the verification command remains practical for execution sessions.

    Avoid synthetic benchmarks that skip the actual repository/handler chain entirely.
  </action>
  <verify>
    <automated>go test ./test/perf/... -run ^$ -bench . -benchmem -count=1</automated>
  </verify>
  <done>The repository contains runnable benchmark/support code that reproduces the large-gallery validation path without personal data.</done>
</task>

<task type="auto">
  <name>task 2: publish the final deployment guide and performance report</name>
  <files>docs/performance-report.md, docs/deployment.md, README.md</files>
  <action>
    Write the phase-close documentation from the real delivered artifacts.

    Requirements:
    1. `docs/performance-report.md` must state dataset size, the exact benchmark command used, measured results, bottlenecks found, and the optimization changes that mattered.
    2. `docs/deployment.md` must cover directory layout, copying/editing the YAML config, `docker compose up -d`, health checks, admin dashboard access, upgrade steps, backup, and restore guidance for SQLite/image directories.
    3. Update `README.md` so the deployment and admin entry points are discoverable.
    4. Explicitly call out that PostgreSQL is out of scope for the shipped Phase 6 deployment path.
  </action>
  <verify>
    <automated>docker compose config && go test ./... && cd flutter_app && flutter test</automated>
  </verify>
  <done>The docs and report match the actual shipped artifacts, include reproducible commands, and make the Phase 6 deployment path understandable to another operator.</done>
</task>

</tasks>

<verification>
- `go test ./test/perf/... -run ^$ -bench . -benchmem -count=1`
- `docker compose config`
- `go test ./...`
- `cd flutter_app && flutter test`
- Manual review: follow `docs/deployment.md` on a fresh local path and confirm the documented admin URL and health checks are correct.
</verification>

<success_criteria>
- Performance claims are evidenced, not implied.
- Deployment docs explain the actual Docker/YAML/admin flow shipped in this phase.
- Another OpenCode executor can verify the phase without inferring hidden setup steps.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimization-deployment/06-05-SUMMARY.md`
</output>
