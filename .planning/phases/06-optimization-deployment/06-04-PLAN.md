---
phase: 06-optimization-deployment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/repository/image_repository.go
  - internal/repository/image_repository_test.go
  - internal/handler/image_handler.go
  - internal/handler/image_handler_test.go
  - flutter_app/lib/services/api_service.dart
  - flutter_app/lib/providers/image_provider.dart
  - flutter_app/lib/screens/gallery_screen.dart
  - flutter_app/test/services/api_service_test.dart
  - flutter_app/test/providers/image_provider_test.dart
  - flutter_app/test/screens/gallery_screen_test.dart
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "10k+ 图片场景下图库列表仍通过分页稳定加载，而不是一次性拉全量"
    - "滚动浏览会继续加载下一页，并在末页停止请求"
    - "前后端图片列表契约一致，优化没有建立在兼容猜测上"
  artifacts:
    - path: "internal/repository/image_repository.go"
      provides: "改良后的图片列表查询与分页能力"
      contains: "FindAll"
    - path: "internal/handler/image_handler.go"
      provides: "统一后的图片列表响应契约"
      contains: "next_cursor"
    - path: "flutter_app/lib/providers/image_provider.dart"
      provides: "滚动续载状态机"
      contains: "hasMore"
  key_links:
    - from: "internal/handler/image_handler.go"
      to: "flutter_app/lib/services/api_service.dart"
      via: "image list JSON contract"
      pattern: "next_cursor|has_more"
    - from: "flutter_app/lib/screens/gallery_screen.dart"
      to: "flutter_app/lib/providers/image_provider.dart"
      via: "scroll-triggered pagination"
      pattern: "loadImages"
---

<objective>
Optimize the gallery browsing path for large SQLite-backed libraries by tightening the image list API contract and finishing the Flutter-side incremental loading flow.

Purpose: Phase 6 performance acceptance is about daily browsing feel in a 10k+ library, so the list query and scroll loop must be robust before benchmark/report work begins.
Output: Improved image repository/handler behavior, aligned Flutter API parsing, and tested incremental loading in the gallery UI.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-optimization-deployment/06-CONTEXT.md
@.planning/phases/06-optimization-deployment/06-RESEARCH.md
@internal/repository/image_repository.go
@internal/handler/image_handler.go
@flutter_app/lib/services/api_service.dart
@flutter_app/lib/providers/image_provider.dart
@flutter_app/lib/screens/gallery_screen.dart

<interfaces>
From `internal/repository/image_repository.go`:
```go
type ImageRepository interface {
    FindAll(limit, offset int) ([]domain.Image, error)
    FindByTagIDs(ctx context.Context, tagIDs []int64, limit, offset int) ([]domain.Image, error)
    Count() (int64, error)
    CountByTagIDs(ctx context.Context, tagIDs []int64) (int64, error)
}
```

From `flutter_app/lib/providers/image_provider.dart`:
```dart
class ImageListProvider extends ChangeNotifier {
  Future<void> loadImages({bool refresh = false}) async
  bool get hasMore
  bool get isLoading
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: tighten the large-library image list backend contract</name>
  <files>internal/repository/image_repository.go, internal/repository/image_repository_test.go, internal/handler/image_handler.go, internal/handler/image_handler_test.go</files>
  <behavior>
    - Test 1: image list responses return one consistent field shape that matches the Flutter client.
    - Test 2: pagination for large result sets returns stable `next_cursor` / `has_more` behavior across multiple pages.
    - Test 3: tag-filtered image queries preserve the same contract and do not regress total-count behavior.
  </behavior>
  <action>
    Improve the backend side of the gallery browsing path.

    Requirements:
    1. Unify the image list JSON response so the Flutter client and server agree on field names and pagination semantics.
    2. Replace or harden the current large-offset approach with a more stable pagination strategy suitable for 10k+ SQLite rows; keep the contract explicit in tests.
    3. Preserve sort and tag-filter behavior while improving pagination correctness.
    4. Keep the work limited to the gallery/image-list chain; do not broaden into unrelated search or duplicate flows.
  </action>
  <verify>
    <automated>go test ./internal/repository/... ./internal/handler/... -run Image -count=1</automated>
  </verify>
  <done>The backend image list API is contract-consistent, paginates predictably for large libraries, and remains covered by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: complete the Flutter infinite-loading gallery flow</name>
  <files>flutter_app/lib/services/api_service.dart, flutter_app/lib/providers/image_provider.dart, flutter_app/lib/screens/gallery_screen.dart, flutter_app/test/services/api_service_test.dart, flutter_app/test/providers/image_provider_test.dart, flutter_app/test/screens/gallery_screen_test.dart</files>
  <behavior>
    - Test 1: `ApiService` parses the unified backend list contract correctly.
    - Test 2: `ImageListProvider` prevents duplicate in-flight loads, resets pagination on refresh/sort/filter changes, and stops at the last page.
    - Test 3: `GalleryScreen` triggers additional loads near scroll-end and keeps refresh behavior intact.
  </behavior>
  <action>
    Finish the large-gallery UX on the Flutter side.

    Requirements:
    1. Update `ApiService` to consume the unified list contract from task 1.
    2. Upgrade `ImageListProvider` into a real incremental-loading state machine for refresh, sort changes, tag-filter changes, and end-of-list detection.
    3. Add a `ScrollController` or equivalent threshold-based mechanism in `GalleryScreen` to load the next page automatically during browsing.
    4. Keep the existing grid/masonry choice and current app structure; do not redesign the product.
  </action>
  <verify>
    <automated>cd flutter_app && flutter test test/services/api_service_test.dart test/providers/image_provider_test.dart test/screens/gallery_screen_test.dart</automated>
  </verify>
  <done>The gallery can browse large libraries incrementally with automated coverage for pagination behavior and scroll-triggered loading.</done>
</task>

</tasks>

<verification>
- `go test ./internal/repository/... ./internal/handler/... -run Image -count=1`
- `cd flutter_app && flutter test test/services/api_service_test.dart test/providers/image_provider_test.dart test/screens/gallery_screen_test.dart`
- Manual browse check with seeded data: scroll continuously, trigger refresh, change sort, and confirm no duplicate-load thrash.
</verification>

<success_criteria>
- The gallery no longer behaves like a first-page-only view.
- Frontend and backend share one explicit list contract.
- The browsing chain is ready for reproducible benchmark/report work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimization-deployment/06-04-SUMMARY.md`
</output>
