---
phase: 06-optimization-deployment
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - web/admin/index.html
  - web/admin/app.js
  - web/admin/styles.css
  - internal/handler/routes.go
  - internal/handler/admin_handler_test.go
autonomous: true
requirements:
  - DEPL-02
must_haves:
  truths:
    - "用户可以在浏览器访问基础 Web 管理后台"
    - "首页显示服务状态、任务队列状态、图库规模、存储路径和最近错误"
    - "安全操作按钮直接调用真实的 `/admin/api/*` 接口"
  artifacts:
    - path: "web/admin/index.html"
      provides: "单页运维仪表盘骨架"
      contains: "admin"
    - path: "web/admin/app.js"
      provides: "面板数据拉取与操作交互"
      contains: "fetch('/admin/api/summary'"
    - path: "web/admin/styles.css"
      provides: "状态卡片与错误列表样式"
      contains: ".status-card"
  key_links:
    - from: "web/admin/app.js"
      to: "/admin/api/summary"
      via: "dashboard bootstrap fetch"
      pattern: "/admin/api/summary"
    - from: "internal/handler/routes.go"
      to: "web/admin/index.html"
      via: "static admin page serving"
      pattern: "admin"
---

<objective>
Deliver the actual single-page admin dashboard UI that consumes the backend contract from Plan 06-02 without turning into a second product client.

Purpose: Phase 6 requires an accessible Web admin dashboard, not just hidden JSON endpoints.
Output: Static admin page assets, route wiring for `/admin`, and tests/smoke coverage proving the page and API hooks are live.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-optimization-deployment/06-CONTEXT.md
@.planning/phases/06-optimization-deployment/06-RESEARCH.md
@.planning/phases/06-optimization-deployment/06-02-PLAN.md
@internal/handler/routes.go

<interfaces>
Expected admin API from Plan `06-02`:
```text
GET  /admin/api/summary
GET  /admin/api/jobs
POST /admin/api/actions/scan
POST /admin/api/actions/jobs/pause
POST /admin/api/actions/jobs/resume
POST /admin/api/actions/jobs/retry-failed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: build the single-page dashboard shell and data rendering</name>
  <files>web/admin/index.html, web/admin/app.js, web/admin/styles.css</files>
  <action>
    Create a deliberately utilitarian local-ops dashboard.

    Requirements:
    1. Build a single page with sections for health/status, queue status, library scale, storage/path summary, recent errors, and safe actions.
    2. Use `web/admin/app.js` to fetch `/admin/api/summary` and `/admin/api/jobs`, refresh the view, and surface loading/error states clearly.
    3. Style the page for desktop and tablet-width browser usage with obvious status cards and warning/error emphasis.
    4. Keep the page lightweight and static; do not introduce a new frontend framework or expand into image/tag/collection CRUD.
  </action>
  <verify>
    <automated>go test ./internal/handler/... -run Admin -count=1</automated>
  </verify>
  <done>The repository contains a complete admin dashboard page that can render real server state and present the allowed actions clearly.</done>
</task>

<task type="auto">
  <name>task 2: wire the admin page into Gin and prove actions reach live endpoints</name>
  <files>internal/handler/routes.go, internal/handler/admin_handler_test.go, web/admin/app.js</files>
  <action>
    Connect the page to the server and verify the end-to-end wiring.

    Requirements:
    1. Update `internal/handler/routes.go` so `/admin` serves the static dashboard and the existing admin protection applies to both the page and the API.
    2. Extend handler tests to cover the page route and at least one dashboard action flow from HTTP request to service call.
    3. Ensure the JavaScript action buttons call the exact backend endpoints from Plan 06-02 and refresh the UI after success.

    Avoid adding a separate asset server, reverse proxy dependency, or a different route namespace.
  </action>
  <verify>
    <automated>go test ./internal/handler/... ./cmd/server/... -run Admin -count=1</automated>
  </verify>
  <done>The admin page is reachable at `/admin`, protected, and its buttons invoke the real admin API contract end-to-end.</done>
</task>

</tasks>

<verification>
- `go test ./internal/handler/... ./cmd/server/... -run Admin -count=1`
- `go build ./cmd/server`
- Manual browser check: open `/admin`, confirm cards render, error list is visible when populated, and action buttons refresh state after use.
</verification>

<success_criteria>
- The dashboard remains an ops page, not a new application frontend.
- Health, queue, scale, path, and recent-error information is visible on first load.
- Safe operation buttons are wired to live backend routes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimization-deployment/06-03-SUMMARY.md`
</output>
