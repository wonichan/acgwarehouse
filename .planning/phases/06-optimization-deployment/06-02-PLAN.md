---
phase: 06-optimization-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/repository/job_repository.go
  - internal/repository/job_repository_test.go
  - internal/service/admin_service.go
  - internal/service/admin_service_test.go
  - internal/worker/job_manager.go
  - internal/worker/job_manager_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
  - cmd/server/main.go
autonomous: true
requirements:
  - DEPL-02
must_haves:
  truths:
    - "管理后台有真实数据接口，而不是静态占位页"
    - "用户可以查看健康状态、任务队列概况和图库规模信息"
    - "用户可以执行少量安全运维动作：手动扫描、暂停/恢复后台任务、重试失败任务"
  artifacts:
    - path: "internal/service/admin_service.go"
      provides: "管理后台聚合服务与运维动作编排"
      contains: "type AdminService"
    - path: "internal/handler/admin_handler.go"
      provides: "`/admin/api/*` HTTP 接口"
      exports: ["NewAdminHandler"]
    - path: "internal/worker/job_manager.go"
      provides: "后台任务暂停/恢复控制能力"
      contains: "Pause"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/handler/routes.go"
      via: "admin dependencies wiring"
      pattern: "Admin"
    - from: "internal/handler/admin_handler.go"
      to: "internal/service/admin_service.go"
      via: "summary and action endpoints"
      pattern: "AdminService"
---

<objective>
Define and implement the backend contract for the local/internal admin dashboard, including status aggregation, light protection, and a minimal set of safe operations.

Purpose: The dashboard cannot be useful unless the server exposes real admin APIs and runtime controls that match the phase boundary.
Output: Admin config fields, runtime control methods, repository/service/handler layers, route wiring, and tests for `/admin/api/*`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-optimization-deployment/06-CONTEXT.md
@.planning/phases/06-optimization-deployment/06-RESEARCH.md
@internal/config/config.go
@internal/repository/job_repository.go
@internal/worker/job_manager.go
@internal/handler/routes.go
@cmd/server/main.go

<interfaces>
From `internal/config/config.go`:
```go
type Config struct {
    Server   ServerConfig
    Database DatabaseConfig
    Storage  StorageConfig
    AI       AIConfig
    COS      COSConfig
}
```

From `internal/repository/job_repository.go`:
```go
type JobRepository interface {
    Save(job *domain.AsyncJob) error
    FindByID(id int64) (*domain.AsyncJob, error)
    FindByStatus(status string) ([]domain.AsyncJob, error)
    FindByType(jobType string) ([]domain.AsyncJob, error)
    Update(job *domain.AsyncJob) error
}
```

From `internal/worker/job_manager.go`:
```go
type Manager struct { ... }
func NewManager(jobRepo repository.JobRepository) *Manager
func (m *Manager) Start(ctx context.Context)
func (m *Manager) Stop()
func (m *Manager) AddJob(ctx context.Context, jobType, payload string) (int64, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add admin runtime contracts and safe operations</name>
  <files>internal/config/config.go, internal/repository/job_repository.go, internal/repository/job_repository_test.go, internal/service/admin_service.go, internal/service/admin_service_test.go, internal/worker/job_manager.go, internal/worker/job_manager_test.go</files>
  <behavior>
    - Test 1: admin summary aggregation returns health/config/task-count data without exposing raw secrets.
    - Test 2: the job manager can pause and resume background consumption without losing queue state.
    - Test 3: retry-failed action only re-queues failed jobs and leaves running/finished jobs untouched.
  </behavior>
  <action>
    Create the backend contracts that the dashboard will consume.

    Requirements:
    1. Extend `internal/config/config.go` with an `Admin` section suitable for simple local/internal protection, keeping YAML-first configuration and optional env overrides.
    2. Extend `JobRepository` with the minimal read/write operations needed for recent jobs, failed jobs, and retry semantics; add repository tests.
    3. Extend `worker.Manager` with explicit pause/resume control and observable state helpers instead of overloading `Stop()`.
    4. Add `internal/service/admin_service.go` to aggregate health/status/stats and to orchestrate scan / pause / resume / retry-failed actions using existing scanner and worker primitives.
    5. Keep the feature SQLite-only and do not introduce full authentication, role models, or public-internet hardening.
  </action>
  <verify>
    <automated>go test ./internal/repository/... ./internal/service/... ./internal/worker/... -run "Admin|JobManager|JobRepository" -count=1</automated>
  </verify>
  <done>The backend exposes tested runtime contracts for admin status and safe operations, with no hidden dependency on manual SQL from handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: expose protected admin HTTP endpoints and wire them into server bootstrap</name>
  <files>internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go, cmd/server/main.go</files>
  <behavior>
    - Test 1: protected `/admin/api/summary` returns 200 for valid admin credentials and 401/403 for missing or invalid credentials.
    - Test 2: action endpoints call the admin service and return structured success/error payloads.
    - Test 3: `cmd/server/main.go` wires the admin dependencies so the endpoints are live in the real server bootstrap path.
  </behavior>
  <action>
    Add the HTTP layer for the dashboard contract.

    Requirements:
    1. Create `internal/handler/admin_handler.go` with endpoints for summary, jobs, scan trigger, pause, resume, and retry-failed.
    2. Protect `/admin` and `/admin/api/*` with a simple config-driven guard appropriate for local/internal use; Basic Auth is the expected default.
    3. Update `internal/handler/routes.go` to register the admin API group without polluting `/api/v1`.
    4. Update `cmd/server/main.go` to construct the admin service and pass the required dependencies.

    Avoid login pages, sessions, JWTs, and any multi-user permission model.
  </action>
  <verify>
    <automated>go test ./internal/handler/... ./cmd/server/... -run Admin -count=1</automated>
  </verify>
  <done>The server exposes protected, tested `/admin/api/*` routes that match the phase boundary and are wired in the real bootstrap path.</done>
</task>

</tasks>

<verification>
- `go test ./internal/repository/... ./internal/service/... ./internal/worker/... -run "Admin|JobManager|JobRepository" -count=1`
- `go test ./internal/handler/... ./cmd/server/... -run Admin -count=1`
- `go build ./cmd/server`
</verification>

<success_criteria>
- Admin endpoints expose real health/task/library data.
- Only the allowed safe actions exist; no business-management sprawl appears.
- Protection remains simple and config-driven, appropriate for local/internal deployment only.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimization-deployment/06-02-SUMMARY.md`
</output>
