---
phase: 06-optimization-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - deploy/config/config.example.yaml
  - Makefile
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "用户可以通过一次 `docker compose up -d` 启动服务"
    - "SQLite 数据文件和图片目录都映射到宿主机可见路径"
    - "容器启动后健康检查可直接验证服务存活"
  artifacts:
    - path: "Dockerfile"
      provides: "Go 服务与管理后台静态资源的镜像构建定义"
      contains: "FROM"
    - path: "docker-compose.yml"
      provides: "单机部署入口与持久化卷定义"
      contains: "volumes:"
    - path: "deploy/config/config.example.yaml"
      provides: "脱敏的部署配置模板"
      contains: "database:"
  key_links:
    - from: "docker-compose.yml"
      to: "deploy/config/config.example.yaml"
      via: "挂载后的 YAML 配置路径"
      pattern: "config"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "compose build"
      pattern: "build:"
---

<objective>
Create the single-machine deployment package for Phase 6 with Docker Compose, host-visible persistence, and a sanitized YAML-first configuration path.

Purpose: The user locked deployment around `docker compose up -d`, SQLite-only runtime, and host-mounted library/database directories, so the package itself must encode those decisions instead of leaving them to guesswork.
Output: `Dockerfile`, `.dockerignore`, `docker-compose.yml`, a sanitized deployment config template, and build helpers in `Makefile`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-optimization-deployment/06-CONTEXT.md
@.planning/phases/06-optimization-deployment/06-RESEARCH.md
@internal/config/config.go
@cmd/server/main.go
@Makefile
</context>

<tasks>

<task type="auto">
  <name>task 1: build the SQLite-only Docker packaging entrypoint</name>
  <files>Dockerfile, .dockerignore, docker-compose.yml, Makefile</files>
  <action>
    Add the deployment artifacts that make the current Go server runnable as a single-machine Docker Compose service.

    Requirements:
    1. Create a multi-stage `Dockerfile` that builds `./cmd/server` and runs the resulting binary in a slim runtime image.
    2. Add `.dockerignore` entries for local state and build noise, including `data/`, `library/`, Flutter build output, git metadata, and test caches.
    3. Create `docker-compose.yml` with one application service, `restart: unless-stopped`, a healthcheck against `/health`, and explicit host mounts for the YAML config, SQLite data directory, and image library directory.
    4. Keep the runtime SQLite-only; do not introduce PostgreSQL services, migration sidecars, or `.env`-driven primary config.
    5. Extend `Makefile` with non-watch helper commands for image build and compose smoke verification if they improve repeatability.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>The repository contains a valid single-service Docker Compose deployment path aligned with SQLite-only runtime and host-mounted persistence.</done>
</task>

<task type="auto">
  <name>task 2: publish the sanitized deployment configuration contract</name>
  <files>deploy/config/config.example.yaml, docker-compose.yml, Makefile</files>
  <action>
    Create the deployment configuration template that operators will copy and edit before startup.

    Requirements:
    1. Add `deploy/config/config.example.yaml` as a fully sanitized YAML example with container-internal paths such as `/data/acgwarehouse.db` and `/library/...`.
    2. Ensure the example covers the currently implemented runtime sections needed for startup (`server`, `database`, `storage`, `ai`, and any already-existing optional sections), but does not guess future admin config fields before the admin contract is implemented.
    3. Make the compose file reference this config location through a clear mount convention rather than assuming the repository root `config.yaml` is safe to ship.
    4. Document the expected copy/edit/start workflow in comments or helper targets where useful.

    Avoid reusing the checked-in local `config.yaml` as the deployment template.
  </action>
  <verify>
    <automated>docker compose config && go test ./cmd/server/... -count=1</automated>
  </verify>
  <done>A new operator-facing config template exists, is secret-free, and the compose path points at that template instead of the local development config while leaving future admin keys to the admin-contract plan.</done>
</task>

</tasks>

<verification>
- `docker compose config`
- `go test ./cmd/server/... -count=1`
- `docker compose up -d` starts the service and `curl http://localhost:8080/health` returns 200 in the executor environment if Docker is available.
</verification>

<success_criteria>
- A new machine can follow the repository deployment path without inventing directory conventions.
- Host-visible paths exist for config, SQLite data, and the image library.
- The deployment artifacts do not mention PostgreSQL, Kubernetes, or `.env` as the primary path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optimization-deployment/06-01-SUMMARY.md`
</output>
