---
phase: 13-backend-monitoring-queue-control
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - internal/service/admin_service.go
  - internal/service/admin_service_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
  - web/admin/app.js
autonomous: true
requirements:
  - PIPE-02
  - OPS-01
must_haves:
  truths:
    - 管理员可以在顶部同时看到队列运行态和平台统计。
    - 管理员看到的计数覆盖 pending、queued、running、completed、failed、cancelled 等平台状态。
    - 批次页的数据来源是平台批次/任务视图，而不是旧 jobs 汇总猜测。
  artifacts:
    - path: internal/service/admin_service.go
      provides: 平台概览 DTO 与聚合逻辑
    - path: internal/handler/admin_handler.go
      provides: 平台概览 API
    - path: web/admin/app.js
      provides: 顶部概览渲染与新接口接线
  key_links:
    - from: internal/handler/routes.go
      to: internal/handler/admin_handler.go
      via: 新增 /admin/api/task-platform/overview 路由
      pattern: task-platform/overview
    - from: web/admin/app.js
      to: /admin/api/task-platform/overview
      via: 顶部监控卡片加载
      pattern: task-platform/overview
---

<objective>
补齐后台监控页缺失的平台概览契约，把旧 `summary` 的 job 视角提升为真正的任务平台监控视角，并接到 Phase 13 页面顶部。

Purpose: 满足 OPS-01 和 PIPE-02 的核心可观测性，避免前端用多个旧接口拼出不完整状态。
Output: 新的平台概览 API、相应 service/handler 测试，以及前端顶部概览接线。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-monitoring-queue-control/13-CONTEXT.md
@.planning/phases/13-backend-monitoring-queue-control/13-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-04-SUMMARY.md
@internal/service/admin_service.go
@internal/service/task_read_service.go
@internal/handler/admin_handler.go
@internal/handler/routes.go
@internal/worker/job_manager.go

<interfaces>
From `internal/service/admin_service.go`:
```go
type Summary struct {
    Health  HealthStatus  `json:"health"`
    Config  ConfigSummary `json:"config"`
    Tasks   TaskSummary   `json:"tasks"`
    Library LibraryStats  `json:"library"`
}

type JobManagerControl interface {
    Pause()
    Resume()
    IsPaused() bool
    AddJob(ctx context.Context, jobType, payload string) (int64, error)
}
```

From `internal/worker/job_manager.go`:
```go
type Stats struct {
    QueueSize   int
    IsRunning   bool
    IsPaused    bool
    WorkerCount int
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为平台概览接口补失败测试并实现聚合 DTO</name>
  <files>internal/service/admin_service.go, internal/service/admin_service_test.go</files>
  <behavior>
    - Test 1: 概览返回队列暂停态、队列规模、worker 数、批次状态计数与平台任务状态计数。
    - Test 2: 概览在没有 taskReadSvc 或没有批次数据时返回空计数而不是报错。
    - Test 3: 概览保留旧健康/配置/图库统计字段，避免后台现有卡片回退。
  </behavior>
  <action>先在 `admin_service_test.go` 写失败测试，再在 `admin_service.go` 新增平台概览 DTO 与读取逻辑。优先复用 `jobManager` 运行态和 `TaskReadService` / repository 读模型；如需新增轻量 read repository 接口，保持在 admin/task-read 既有分层内。不要把平台统计塞回裸 `TaskSummary` 四个旧字段里，也不要只暴露 async_jobs ready/running/finished/failed。</action>
  <verify>
    <automated>go test ./internal/service/... -run "Admin|TaskRead" -count=1</automated>
  </verify>
  <done>Service 层能返回面向 Phase 13 的平台概览，覆盖队列运行态、批次状态、平台任务状态和旧基础信息。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 暴露平台概览路由并把顶部卡片切到新接口</name>
  <files>internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go, web/admin/app.js</files>
  <behavior>
    - Test 1: `GET /admin/api/task-platform/overview` 返回 200 和平台概览 JSON。
    - Test 2: handler 在 service 错误时返回 500 与错误信息。
    - Test 3: 前端从新接口渲染顶部概览，不再依赖旧 `/summary` 推导平台状态。
  </behavior>
  <action>新增独立平台概览接口，沿用 `/admin/api/*` 和 `success/message/data` / JSON 风格；在 `app.js` 中新增加载与渲染逻辑，把顶部概览改为“平台统计 + 队列运行态”。保留旧 `/summary` 兼容，但 Phase 13 页面应优先消费新接口。不要新增平行页面或更换后台路由前缀。</action>
  <verify>
    <automated>go test ./internal/handler/... -run "AdminHandler|Routes" -count=1 && go test ./internal/service/... -run "Admin|TaskRead" -count=1</automated>
  </verify>
  <done>后台存在可测试的平台概览接口，前端顶部卡片使用真实平台计数和队列状态渲染。</done>
</task>

</tasks>

<verification>
运行 `go test ./internal/service/... ./internal/handler/... -run "Admin|TaskRead|Routes" -count=1`，确认新概览接口与旧后台接口可并存。
</verification>

<success_criteria>
顶部概览能够展示批次/任务状态分布、队列暂停态和待处理规模；平台监控不再依赖旧 jobs 汇总推断。
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-monitoring-queue-control/13-02-SUMMARY.md`
</output>
