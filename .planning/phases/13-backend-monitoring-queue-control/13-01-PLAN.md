---
phase: 13-backend-monitoring-queue-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/admin/index.html
  - web/admin/app.js
  - web/admin/styles.css
autonomous: true
requirements:
  - PIPE-02
  - OPS-01
must_haves:
  truths:
    - 管理员进入后台页即可先看到批次列表，而不是裸 job 列表。
    - 管理员选中批次后可以在同页看到该批次任务明细。
    - 页面默认把异常与进行中的内容优先暴露出来。
  artifacts:
    - path: web/admin/index.html
      provides: 批次在上、任务明细在下的监控布局骨架
    - path: web/admin/app.js
      provides: 批次选择、筛选、刷新与明细联动逻辑
    - path: web/admin/styles.css
      provides: 表格化批次视图、状态徽标和异常区域样式
  key_links:
    - from: web/admin/app.js
      to: /admin/api/task-batches
      via: 批次列表加载与状态/来源筛选
      pattern: task-batches
    - from: web/admin/app.js
      to: /admin/api/tasks
      via: 选中批次后的明细查询
      pattern: batch_id
---

<objective>
把现有静态后台页改造成“批次列表在上、任务明细在下”的批次优先监控台，先用已存在的批次/任务读接口完成主工作台交互。

Purpose: 让 Phase 13 的主入口先从“最近 jobs”切到“按批次监控”，与 Phase 11 平台语义对齐。
Output: 可刷新的批次主视图、批次筛选、异常优先的任务明细区、最近错误与手动/自动刷新联动。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-monitoring-queue-control/13-CONTEXT.md
@.planning/phases/13-backend-monitoring-queue-control/13-RESEARCH.md
@.planning/phases/13-backend-monitoring-queue-control/13-VALIDATION.md
@web/admin/index.html
@web/admin/app.js
@web/admin/styles.css
@internal/service/task_read_service.go
@internal/handler/admin_handler.go

<interfaces>
From `internal/service/task_read_service.go`:
```go
type TaskBatchReadModel struct {
    ID             int64            `json:"id"`
    SourceType     string           `json:"source_type"`
    SummaryLabel   string           `json:"summary_label"`
    Status         string           `json:"status"`
    SourceSummary  string           `json:"source_summary"`
    FailureSummary string           `json:"failure_summary,omitempty"`
    StatusCounts   map[string]int64 `json:"status_counts"`
    TaskTypeCounts map[string]int64 `json:"task_type_counts"`
}

type TaskReadModel struct {
    ID            int64  `json:"id"`
    BatchID       int64  `json:"batch_id"`
    ImageFilename string `json:"image_filename"`
    TaskType      string `json:"task_type"`
    Status        string `json:"status"`
    ErrorSummary  string `json:"error_summary,omitempty"`
}
```

From `internal/handler/admin_handler.go`:
```go
GET /admin/api/task-batches -> {"task_batches": [...]}
GET /admin/api/tasks?batch_id={id} -> {"tasks": [...]}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: 重构后台页面为批次优先监控布局</name>
  <files>web/admin/index.html, web/admin/styles.css</files>
  <action>将现有“服务状态 / 任务队列 / 最近任务”页面重构为批次监控工作台：顶部保留概览与刷新区，中间使用表格行 + 状态徽标展示批次列表，下方提供任务明细表和独立异常区。严格遵守 Context 锁定决策：批次列表在上、任务明细在下；不改成卡片堆叠；默认要有状态与来源筛选入口；不要引入 Phase 14 的补跑恢复入口。</action>
  <verify>
    <automated>rg -n "batch|task-detail|error|refresh|filter" web/admin/index.html web/admin/styles.css</automated>
  </verify>
  <done>页面骨架存在批次表、任务明细区、异常区、刷新区和筛选区，且布局与锁定决策一致。</done>
</task>

<task type="auto">
  <name>task 2: 接入批次列表、选中批次与异常优先明细加载</name>
  <files>web/admin/app.js</files>
  <action>在前端状态里新增批次列表、当前选中批次、批次筛选与任务明细状态；调用 `/admin/api/task-batches` 和 `/admin/api/tasks?batch_id=` 实现主视图。批次列表默认按“未完成优先，其次最新时间倒序”在前端做一次稳定排序；任务明细默认把 `failed` 与 `running` 顶到前面，再展示其余任务；保留手动刷新与 30 秒自动刷新。不要把“最近 jobs”继续作为首页主列表，也不要退回裸 async_jobs 视角。</action>
  <verify>
    <automated>test -f web/admin/app.js && go test ./internal/handler/... -run "AdminHandler_(GetTaskBatches|GetTasks)" -count=1</automated>
  </verify>
  <done>页面加载后展示真实批次数据，选中批次会联动明细区，异常与进行中的任务优先可见，刷新逻辑不会打断选中态。</done>
</task>

</tasks>

<verification>
执行 `go test ./internal/handler/... -run "AdminHandler_(GetTaskBatches|GetTasks)" -count=1`，再启动服务人工确认 `/admin-ui/` 已切换为批次优先布局。
</verification>

<success_criteria>
管理员打开后台页时先看到批次列表；批次筛选只暴露状态和来源类型；选中批次后同页出现异常优先的任务明细。
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-monitoring-queue-control/13-01-SUMMARY.md`
</output>
