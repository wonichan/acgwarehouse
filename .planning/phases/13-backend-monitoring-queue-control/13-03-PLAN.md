---
phase: 13-backend-monitoring-queue-control
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - internal/repository/platform_task_repository.go
  - internal/repository/task_batch_repository.go
  - internal/service/admin_service.go
  - internal/service/admin_service_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
  - web/admin/index.html
  - web/admin/app.js
autonomous: true
requirements:
  - OPS-02
  - OPS-03
  - OPS-05
  - OPS-06
must_haves:
  truths:
    - 管理员可以暂停和继续全局队列。
    - 管理员可以按批次取消任务，也可以在明细里补充取消单任务。
    - 管理员可以清空尚未执行的 pending/queued 任务而不影响 running。
  artifacts:
    - path: internal/service/admin_service.go
      provides: pause/resume/cancel/clear 控制语义
    - path: internal/handler/admin_handler.go
      provides: 控制动作 HTTP 接口与参数校验
    - path: web/admin/app.js
      provides: 强确认、操作提交与刷新反馈
  key_links:
    - from: web/admin/app.js
      to: /admin/api/actions/jobs/pause
      via: 全局队列暂停/继续按钮
      pattern: actions/jobs/(pause|resume)
    - from: web/admin/app.js
      to: /admin/api/actions/jobs/clear-queue
      via: 全局清空待执行队列确认提交
      pattern: clear-queue
    - from: web/admin/app.js
      to: /admin/api/actions/task-batches/
      via: 批次取消确认提交
      pattern: task-batches
    - from: web/admin/app.js
      to: /admin/api/actions/tasks/
      via: 单任务取消与明细行操作
      pattern: tasks
---

<objective>
为 Phase 13 补齐全局暂停/继续、批次取消、单任务取消与清空待执行队列这些核心控制动作，并把强确认与反馈接到后台页。

Purpose: 满足运营控制要求，让管理员无需逐图干预即可控制队列风险与积压。
Output: 平台控制服务方法、动作路由、handler 测试，以及带强确认的前端按钮与反馈。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-monitoring-queue-control/13-CONTEXT.md
@.planning/phases/13-backend-monitoring-queue-control/13-RESEARCH.md
@internal/service/admin_service.go
@internal/handler/admin_handler.go
@internal/handler/routes.go
@internal/worker/job_manager.go
@internal/domain/task_batch.go
@internal/domain/platform_task.go

<interfaces>
From `internal/domain/task_batch.go`:
```go
const (
    TaskBatchStatusPending       = "pending"
    TaskBatchStatusRunning       = "running"
    TaskBatchStatusCompleted     = "completed"
    TaskBatchStatusPartialFailed = "partial_failed"
    TaskBatchStatusFailed        = "failed"
    TaskBatchStatusCancelled     = "cancelled"
)
```

From `internal/domain/platform_task.go`:
```go
const (
    PlatformTaskStatusPending   = "pending"
    PlatformTaskStatusQueued    = "queued"
    PlatformTaskStatusRunning   = "running"
    PlatformTaskStatusFailed    = "failed"
    PlatformTaskStatusCancelled = "cancelled"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为取消与清空语义补失败测试并实现服务层控制</name>
  <files>internal/repository/platform_task_repository.go, internal/repository/task_batch_repository.go, internal/service/admin_service.go, internal/service/admin_service_test.go</files>
  <behavior>
    - Test 1: `clear queue` 只清空 `pending/queued`，`running` 不受影响。
    - Test 2: 批次取消只影响该批次可取消任务，并刷新批次聚合状态。
    - Test 3: 单任务取消不会误伤其他任务。
  </behavior>
  <action>先补 service/repository 失败测试，再实现控制逻辑。优先扩展既有 repository 而不是新建平行数据访问层；所有取消/清空动作都必须返回影响数量，供确认文案和 toast 使用。`pause` / `resume` 保持全局 job manager 语义；`cancel` 和 `clear queue` 必须操作平台任务状态并刷新批次状态。不要让 `clear queue` 改动 `running`，不要把 `pause` / `resume` 下沉到批次级。</action>
  <verify>
    <automated>go test ./internal/service/... -run "PauseResume|Cancel|Clear" -count=1</automated>
  </verify>
  <done>服务层能够可靠执行全局暂停/继续、批次取消、单任务取消和待执行队列清空，并返回受影响数量。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 暴露控制动作接口并接入强确认交互</name>
  <files>internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go, web/admin/index.html, web/admin/app.js</files>
  <behavior>
    - Test 1: 新动作接口校验非法 ID / 非法请求并返回 400。
    - Test 2: 成功响应返回 `success`、`message` 与影响数量。
    - Test 3: 前端 destructive action 先确认再提交，成功后刷新批次与明细。
  </behavior>
  <action>新增动作路由（建议维持 `/admin/api/actions/...` 命名），为批次取消、任务取消、清空队列提供 handler；在前端添加按钮、确认弹窗/确认流程和 toast 反馈。确认文案必须明确作用域与任务数量；成功后刷新顶部概览、批次列表与当前明细。不要引入无确认的破坏性快捷按钮，也不要要求管理员逐图取消。</action>
  <verify>
    <automated>go test ./internal/handler/... -run "Pause|Resume|Cancel|Clear" -count=1</automated>
  </verify>
  <done>后台页具备全局 pause/resume、批次/任务取消、清空队列入口，所有破坏性动作都先确认并反馈影响数量。</done>
</task>

</tasks>

<verification>
运行 `go test ./internal/service/... ./internal/handler/... -run "PauseResume|Cancel|Clear" -count=1`，并确认路由覆盖全局与批次/任务两级控制。
</verification>

<success_criteria>
管理员无需逐图即可暂停/恢复队列、取消批次或单任务、清空未执行队列；所有破坏性操作都清晰展示影响范围与数量。
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-monitoring-queue-control/13-03-SUMMARY.md`
</output>
