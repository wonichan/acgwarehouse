---
phase: 13-backend-monitoring-queue-control
plan: 04
type: execute
wave: 4
depends_on:
  - 13-03
files_modified:
  - internal/service/admin_service.go
  - internal/service/admin_service_test.go
  - internal/handler/admin_handler.go
  - internal/handler/admin_handler_test.go
  - internal/handler/routes.go
  - web/admin/app.js
autonomous: false
requirements:
  - OPS-04
must_haves:
  truths:
    - 管理员可以从批次级重试失败任务。
    - 管理员可以从任务明细补充重试单个失败任务。
    - 重试结果会形成新批次，并给出数量与跳转入口反馈。
  artifacts:
    - path: internal/service/admin_service.go
      provides: 失败任务重试的新批次语义
    - path: internal/handler/admin_handler.go
      provides: 批次级/任务级重试接口
    - path: web/admin/app.js
      provides: 重试操作反馈与新批次跳转
  key_links:
    - from: internal/service/admin_service.go
      to: TaskPlatformService
      via: 按失败任务重新创建批次
      pattern: new batch
    - from: web/admin/app.js
      to: /admin/api/actions/task-batches/
      via: 批次级重试按钮
      pattern: retry
    - from: web/admin/app.js
      to: /admin/api/actions/tasks/
      via: 任务明细行失败重试与新批次定位
      pattern: tasks/.*/retry
---

<objective>
把失败任务重试从旧 `async_jobs` 复活语义升级为“失败平台任务重新形成新批次”，并把批次级主入口、任务级补充入口和操作反馈做完整。

Purpose: 满足 OPS-04，同时保持 Phase 11 已锁定的“每次触发动作都是新的可追踪批次”语义。
Output: 批次级/任务级重试服务、HTTP 接口、前端 toast 与新批次跳转反馈，以及最终人工验证指引。
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-monitoring-queue-control/13-CONTEXT.md
@.planning/phases/13-backend-monitoring-queue-control/13-RESEARCH.md
@.planning/phases/11-task-platform-batch-model/11-04-SUMMARY.md
@internal/service/admin_service.go
@internal/handler/admin_handler.go
@internal/handler/routes.go
@internal/domain/platform_task.go

<interfaces>
From `internal/domain/platform_task.go`:
```go
const PlatformTaskStatusFailed = "failed"
```

Locked retry rules from Context:
```text
- 批次级失败重试是主入口，任务级重试是补充入口。
- 仅 failed 状态任务允许重试。
- 重试结果必须形成新的可追踪批次，而不是覆盖原批次状态。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: 为失败重试写失败测试并实现新批次重试语义</name>
  <files>internal/service/admin_service.go, internal/service/admin_service_test.go</files>
  <behavior>
    - Test 1: 批次级重试只收集该批次 `failed` 任务并创建新批次。
    - Test 2: 单任务重试只允许 `failed`，其他状态返回业务错误。
    - Test 3: 响应返回新批次数量和新批次标识，而不是旧 job reset count。
  </behavior>
  <action>先补失败测试，再把 `RetryFailedJobs` 从旧 async job 语义迁移到平台任务语义；必要时新增更明确的方法（如 `RetryFailedBatchTasks` / `RetryFailedTask`），并通过既有 `TaskPlatformService` 或等价平台入口创建新批次。不要继续使用“把 failed job 改回 ready”的旧实现；要保留原失败任务的历史可追踪性。</action>
  <verify>
    <automated>go test ./internal/service/... -run "RetryFailed|RetryBatch|RetryTask" -count=1</automated>
  </verify>
  <done>服务层支持批次级和单任务失败重试，且每次重试都创建新的可追踪批次并返回结果摘要。</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: 暴露重试接口并把重试反馈接入后台页</name>
  <files>internal/handler/admin_handler.go, internal/handler/admin_handler_test.go, internal/handler/routes.go, web/admin/app.js</files>
  <behavior>
    - Test 1: handler 对非 failed 重试请求返回 400/业务错误。
    - Test 2: 成功重试返回 `success`、重试任务数量和新批次标识。
    - Test 3: 前端 toast 文案展示重试数量，并能定位到新批次。
  </behavior>
  <action>新增批次级主入口和任务级补充入口的重试路由与 handler；在前端为批次行和任务明细行增加重试操作，成功后 toast 中展示创建数量，并提供跳转/选中新批次的动作。不要把 `cancelled`、`completed`、`skipped` 混入重试入口，也不要默默刷新而不给反馈。</action>
  <verify>
    <automated>go test ./internal/handler/... -run "Retry" -count=1 && go test ./internal/service/... -run "RetryFailed|RetryBatch|RetryTask" -count=1</automated>
  </verify>
  <done>后台页可以从批次级和任务级触发失败重试，并明确反馈新批次与重试数量。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 13 完整后台监控与控制闭环：批次监控、平台概览、pause/resume、cancel/clear、failed retry</what-built>
  <how-to-verify>
    1. 启动服务并访问 `/admin-ui/`。
    2. 确认页面主结构为“批次列表在上、任务明细在下”，顶部概览显示队列运行态与平台统计。
    3. 选中一个含失败任务的批次，确认任务明细默认把异常/进行中任务顶到前面。
    4. 触发一次批次取消或清空队列，确认先出现强确认，且文案展示影响范围与数量。
    5. 对失败批次执行重试，确认 toast 显示新建任务数量，并能跳到或定位到新批次。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
先执行 `go test ./internal/service/... ./internal/handler/... -run "Retry|Cancel|Clear|PauseResume" -count=1`，再进行最终人工验证。
</verification>

<success_criteria>
失败任务重试遵循新批次语义，前端清楚反馈重试结果；Phase 13 全部控制动作都能在同一后台页内完成。
</success_criteria>

<output>
After completion, create `.planning/phases/13-backend-monitoring-queue-control/13-04-SUMMARY.md`
</output>
