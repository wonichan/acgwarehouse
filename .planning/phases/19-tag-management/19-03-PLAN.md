---
phase: 19-tag-management
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - flutter_app/lib/app/fluent_screens.dart
  - flutter_app/lib/widgets/tag_management/tag_management_workspace.dart
  - flutter_app/lib/widgets/tag_management/tag_management_list.dart
  - flutter_app/lib/widgets/tag_management/tag_merge_panel.dart
  - flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart
  - flutter_app/lib/widgets/tag_management/tag_edit_dialog.dart
  - flutter_app/test/app/fluent_app_shell_test.dart
  - flutter_app/test/widgets/tag_management_workspace_test.dart
  - flutter_app/test/widgets/tag_merge_panel_test.dart
  - flutter_app/test/widgets/tag_bulk_action_bar_test.dart
autonomous: true
requirements: [DSK-04]

must_haves:
  truths:
    - "Admins can manage tags in a dedicated desktop tag-management destination without leaving the shell or opening the legacy UI"
    - "Admins can edit, merge, and safely delete tags from a list-first workspace with explicit merge-panel and delete-preview feedback"
    - "Admins can use multi-select bulk actions and then jump to affected images via a preset gallery filter"
  artifacts:
    - path: "flutter_app/lib/widgets/tag_management/tag_management_workspace.dart"
      provides: "Fluent desktop tag-governance workspace"
      exports: ["TagManagementWorkspace"]
    - path: "flutter_app/lib/widgets/tag_management/tag_merge_panel.dart"
      provides: "explicit merge target-selection and confirmation panel"
      exports: ["TagMergePanel"]
    - path: "flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart"
      provides: "multi-select governance action surface with confirmation/result feedback"
      exports: ["TagBulkActionBar"]
  key_links:
    - from: "flutter_app/lib/app/fluent_screens.dart"
      to: "flutter_app/lib/widgets/tag_management/tag_management_workspace.dart"
      via: "desktop shell tag-management destination"
      pattern: "TagManagementWorkspace"
    - from: "flutter_app/lib/widgets/tag_management/tag_management_workspace.dart"
      to: "flutter_app/lib/providers/tag_provider.dart"
      via: "provider-driven governance list and merge state"
      pattern: "loadGovernanceTags|selectedGovernanceIds"
    - from: "flutter_app/lib/widgets/tag_management/tag_management_workspace.dart"
      to: "flutter_app/lib/providers/image_provider.dart"
      via: "gallery drilldown using preset tag filter"
      pattern: "setTagFilter"
---

<objective>
Finish Phase 19 by replacing the baseline desktop tag-management body with a Fluent-native governance workspace that matches the locked list-first workflow.

Purpose: This plan realizes D-01 through D-13 in the actual desktop UI by giving admins a list-first governance surface, explicit merge panel, batch action bar, and gallery drilldown loop inside the Phase 17 shell.

Output: A test-backed desktop governance workspace wired into `FluentTagManagementPage` with row actions, merge panel, bulk governance, and preset gallery-filter handoff.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tag-management/19-CONTEXT.md
@.planning/phases/19-tag-management/19-RESEARCH.md
@.planning/phases/19-tag-management/19-VALIDATION.md
@.planning/phases/19-tag-management/19-02-PLAN.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md

<interfaces>
From `flutter_app/lib/app/fluent_screens.dart`:
```dart
class FluentTagManagementPage extends StatelessWidget {
  const FluentTagManagementPage({super.key});
}
```

From `flutter_app/lib/providers/navigation_provider.dart`:
```dart
static const int galleryIndex = 0;
static const int tagManagementIndex = 3;
void setSelectedIndex(int index)
```

From `flutter_app/lib/providers/image_provider.dart`:
```dart
Future<void> setTagFilter(List<int> tagIds) async
Future<void> setHasTagsFilter(bool? hasTags) async
```

Locked decisions from `19-CONTEXT.md`:
```text
D-01 dedicated navigation page, not legacy entry
D-02 list-first main view plus merge panel
D-03 row-level edit/delete/merge actions
D-05 delete confirmation shows affected image count explicitly
D-10/D-11 full batch governance with multi-select and failure feedback
D-12/D-13 drill down to filtered gallery/search instead of embedded preview
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the Fluent list-first governance workspace and row actions</name>
  <files>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/widgets/tag_management/tag_management_workspace.dart,
    flutter_app/lib/widgets/tag_management/tag_management_list.dart,
    flutter_app/lib/widgets/tag_management/tag_edit_dialog.dart,
    flutter_app/test/app/fluent_app_shell_test.dart,
    flutter_app/test/widgets/tag_management_workspace_test.dart
  </files>
  <read_first>
    flutter_app/lib/app/fluent_screens.dart,
    flutter_app/lib/app/fluent_app_shell.dart,
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/test/app/fluent_app_shell_test.dart,
    flutter_app/test/widgets/gallery_filter_panel_test.dart,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the desktop shell still exposes the tag-management destination, and that destination now renders a Fluent-native governance workspace instead of the old Material scaffold body
    - Test 2: the workspace shows summary stats, search/filter affordances, a row list with edit/delete/merge/view-images actions, and row-level delete affordances that respect `canDelete`
    - Test 3: delete confirmation copy includes the exact affected image count and blocks destructive confirmation for tags that are still in use
    - Test 4: clicking “View affected images” applies `ImageListProvider.setTagFilter([tagId])` and switches the shell to the gallery destination per D-12/D-13
  </behavior>
  <action>
    1. Add failing widget/app tests in `flutter_app/test/widgets/tag_management_workspace_test.dart` and `flutter_app/test/app/fluent_app_shell_test.dart` that assert a Fluent desktop workspace with these exact elements:
       - summary cards for usage/pending/AI/manual counts,
       - a search box,
       - a governance list/table,
       - row actions labeled `Edit`, `Merge`, `Delete`, and `View affected images`.
    2. Create `flutter_app/lib/widgets/tag_management/tag_management_workspace.dart` and `tag_management_list.dart` as the focused desktop workspace files. Keep the layout list-first per D-02; do not build a dashboard-first or wizard-first page.
    3. Create `flutter_app/lib/widgets/tag_management/tag_edit_dialog.dart` for rename/category/alias edits using lightweight Fluent dialogs. Alias editing should be visible in the row workflow per D-08.
    4. Update `flutter_app/lib/app/fluent_screens.dart` so `FluentTagManagementPage` hosts `TagManagementWorkspace` directly instead of wrapping the legacy `TagManagementScreen`.
    5. Wire “View affected images” to:
       - clear any `hasTags` filter conflicts,
       - call `ImageListProvider.setTagFilter([tagId])`,
       - switch `NavigationProvider` to `galleryIndex`.
        Do not create a new result page or embed preview content into the governance screen.
    6. Stage commits atomically as:
       - `test(19-03): add failing desktop tag workspace coverage`
       - `feat(19-03): add Fluent tag governance workspace and row actions`
       - `refactor(19-03): retire legacy desktop tag management wrapper path`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/widgets/tag_management_workspace_test.dart test/app/fluent_app_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/app/fluent_screens.dart` contains `TagManagementWorkspace`
    - `flutter_app/lib/widgets/tag_management/tag_management_workspace.dart` exists
    - `flutter_app/lib/widgets/tag_management/tag_management_list.dart` exists
    - `flutter_app/lib/widgets/tag_management/tag_management_workspace.dart` contains `View affected images`
    - `flutter_app/lib/widgets/tag_management/tag_management_workspace.dart` contains `affected image`
    - `cd flutter_app; flutter test test/widgets/tag_management_workspace_test.dart test/app/fluent_app_shell_test.dart` exits `0`
  </acceptance_criteria>
  <done>The desktop shell now presents a Fluent-native, list-first tag-governance workspace with safe row actions and gallery drilldown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green the merge panel and full batch-governance action bar</name>
  <files>
    flutter_app/lib/widgets/tag_management/tag_management_workspace.dart,
    flutter_app/lib/widgets/tag_management/tag_merge_panel.dart,
    flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart,
    flutter_app/test/widgets/tag_merge_panel_test.dart,
    flutter_app/test/widgets/tag_bulk_action_bar_test.dart,
    flutter_app/test/widgets/tag_management_workspace_test.dart
  </files>
  <read_first>
    flutter_app/lib/widgets/tag_management/tag_management_workspace.dart,
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/models/tag_governance.dart,
    flutter_app/test/widgets/tag_management_workspace_test.dart,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md,
    .planning/phases/19-tag-management/19-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: merge opens an explicit side panel or inline panel that excludes the source tag from target options and requires confirm before calling `mergeSelectionInto` or `mergeTagInto`
    - Test 2: multi-select rows reveal a bulk action bar for selected cleanup, bulk category, alias organization, and merge-candidate processing per D-10/D-11
    - Test 3: bulk actions show aggregated result feedback with separate success/failure counts instead of a single opaque snackbar string
    - Test 4: merge-panel and bulk-action flows leave the main list visible, preserving the list-first semantics from D-02
  </behavior>
  <action>
    1. Add failing widget tests in `flutter_app/test/widgets/tag_merge_panel_test.dart` and `flutter_app/test/widgets/tag_bulk_action_bar_test.dart` for:
       - explicit merge target selection,
       - confirm-required merge,
       - bulk cleanup/category/alias/merge controls when rows are selected,
       - partial-failure result messaging.
    2. Create `flutter_app/lib/widgets/tag_management/tag_merge_panel.dart` as the focused merge workflow surface. It must render source-tag context, searchable target choices, exact confirm copy, and a disabled confirm action until a valid target is selected.
    3. Create `flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart` with controls for selected cleanup, bulk category assignment, alias organization, and merge-candidate processing. Keep it selection-driven and visible only when `selectedGovernanceIds` is non-empty.
    4. Update `tag_management_workspace.dart` so the list remains the primary surface while the merge panel and batch action bar appear as adjunct tools. Do not replace the whole page with a wizard or full-screen admin console.
    5. Feed result copy from `TagGovernanceBatchResult` with exact counts (`X succeeded`, `Y failed`) and preserve failure details so a follow-up summary or info bar can render them.
    6. Stage commits atomically as:
       - `test(19-03): add failing merge panel and bulk action coverage`
       - `feat(19-03): add merge panel and batch governance action bar`
       - `refactor(19-03): keep tag workspace list-first while layering governance tools`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/widgets/tag_merge_panel_test.dart test/widgets/tag_bulk_action_bar_test.dart test/widgets/tag_management_workspace_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/widgets/tag_management/tag_merge_panel.dart` exists
    - `flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart` exists
    - `flutter_app/lib/widgets/tag_management/tag_merge_panel.dart` contains `targetTagId`
    - `flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart` contains `selectedGovernanceIds`
    - `flutter_app/lib/widgets/tag_management/tag_bulk_action_bar.dart` contains `succeeded`
    - `cd flutter_app; flutter test test/widgets/tag_merge_panel_test.dart test/widgets/tag_bulk_action_bar_test.dart test/widgets/tag_management_workspace_test.dart` exits `0`
  </acceptance_criteria>
  <done>The desktop tag-management workspace now supports explicit merge-panel flows and full batch governance while preserving list-first semantics.</done>
</task>

</tasks>

<verification>
- Run the targeted widget suites after each RED/GREEN slice.
- Confirm the desktop shell still renders the five-navigation-item contract from Phase 17.
- Perform one manual desktop smoke-check for gallery drilldown after widget tests are green.
</verification>

<success_criteria>
- Admins can complete Phase 19 governance work entirely inside the desktop shell.
- The shipped UI preserves list-first semantics while covering edit, alias/category, merge, safe delete, batch actions, and gallery drilldown.
</success_criteria>

<commit_strategy>
- Commit 1: `test(19-03): add failing desktop tag workspace coverage`
- Commit 2: `feat(19-03): add Fluent tag governance workspace and row actions`
- Commit 3: `refactor(19-03): retire legacy desktop tag management wrapper path`
- Commit 4: `test(19-03): add failing merge panel and bulk action coverage`
- Commit 5: `feat(19-03): add merge panel and batch governance action bar`
- Commit 6: `refactor(19-03): keep tag workspace list-first while layering governance tools`
</commit_strategy>

<output>
After completion, create `.planning/phases/19-tag-management/19-03-SUMMARY.md`
</output>
