---
phase: 19-tag-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/service/tag_admin_service.go
  - internal/service/tag_admin_service_test.go
  - internal/handler/tag_handler.go
  - internal/handler/tag_handler_test.go
  - internal/handler/routes.go
  - internal/repository/image_tag_repository.go
autonomous: true
requirements: [DSK-04]

must_haves:
  truths:
    - "Admins can load a dedicated governance list with per-tag usage statistics, aliases, category, and safe-delete eligibility"
    - "Admins can merge a duplicate source tag into an explicit target without fuzzy auto-selection or legacy-screen fallback"
    - "Admins cannot directly delete a tag that still affects images, and delete/cleanup responses always show affected image counts"
  artifacts:
    - path: "internal/service/tag_admin_service.go"
      provides: "transactional backend orchestration for governance rows, delete preview, merge, and selected cleanup"
      exports: ["TagAdminService"]
    - path: "internal/handler/tag_handler.go"
      provides: "HTTP contracts for enriched governance list, delete preview, merge, and safe cleanup"
      exports: ["GetGovernanceTags", "GetDeletePreview", "MergeTag", "CleanUnusedTags"]
    - path: "internal/handler/tag_handler_test.go"
      provides: "handler-level regression coverage for Phase 19 governance endpoints"
  key_links:
    - from: "internal/handler/routes.go"
      to: "internal/handler/tag_handler.go"
      via: "product-facing /api/v1 tag governance routes"
      pattern: "/tags/governance"
    - from: "internal/handler/tag_handler.go"
      to: "internal/service/tag_admin_service.go"
      via: "safe-governance orchestration"
      pattern: "TagAdminService"
    - from: "internal/service/tag_admin_service.go"
      to: "internal/repository/image_tag_repository.go"
      via: "merge/delete preview counts and image-tag reassignment"
      pattern: "GetTagStats|MergeImageTag|FindByTagID"
---

<objective>
Build the backend governance contracts that Phase 19 depends on before any desktop state or widget work begins.

Purpose: This plan enforces D-04/D-05/D-06 safe-delete rules, enables D-02 explicit merge workflow, and exposes the enriched tag-governance data needed for alias/category/list-first desktop management.

Output: A tested backend service/handler stack for governance rows, delete previews, selected unused cleanup, and explicit tag merge.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tag-management/19-CONTEXT.md
@.planning/phases/19-tag-management/19-RESEARCH.md
@.planning/phases/19-tag-management/19-VALIDATION.md
@.planning/phases/17-desktop-shell-foundation/17-03-SUMMARY.md

<interfaces>
From `internal/handler/tag_handler.go`:
```go
type TagHandler struct {
    tagRepo      repository.TagRepository
    aliasRepo    repository.TagAliasRepository
    imageTagRepo repository.ImageTagRepository
}
```

From `internal/repository/image_tag_repository.go`:
```go
type ImageTagRepository interface {
    FindByTagID(ctx context.Context, tagID int64, limit, offset int) ([]*domain.ImageTag, error)
    MergeImageTag(ctx context.Context, imageID, sourceTagID, targetTagID int64) error
    GetTagStats(ctx context.Context, tagID int64) (*TagStats, error)
    SyncFTSForTag(ctx context.Context, tagID int64) error
}
```

Locked decisions from `19-CONTEXT.md`:
```text
D-02 list-first workspace with explicit merge panel
D-04 only unused tags may be deleted
D-05 delete confirmation must show affected image count explicitly
D-06 no normal-path force delete that strips image associations
D-07/D-08 category + alias governance are in scope
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the enriched governance list and explicit merge contracts</name>
  <files>
    internal/service/tag_admin_service.go,
    internal/service/tag_admin_service_test.go,
    internal/handler/tag_handler.go,
    internal/handler/tag_handler_test.go,
    internal/handler/routes.go,
    internal/repository/image_tag_repository.go
  </files>
  <read_first>
    internal/handler/tag_handler.go,
    internal/handler/tag_handler_test.go,
    internal/handler/routes.go,
    internal/service/tag_governance_service.go,
    internal/service/tag_governance_service_test.go,
    internal/repository/tag_repository.go,
    internal/repository/tag_alias_repository.go,
    internal/repository/image_tag_repository.go,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/tags/governance` returns rows ordered by usage count and includes `tag_id`, `preferred_label`, `primary_category`, `aliases`, `usage_count`, `pending_count`, `confirmed_count`, `ai_count`, `manual_count`, `affected_image_count`, and `can_delete`
    - Test 2: `POST /api/v1/tags/:id/merge` requires an explicit `target_tag_id`, rejects `source == target`, and migrates image-tag associations and aliases onto the target tag
    - Test 3: merge keeps the exact-match-first rule from project decisions and never auto-picks a fuzzy candidate target
    - Test 4: merge preserves searchable target data by syncing affected FTS-backed image associations after reassignment
  </behavior>
  <action>
    1. Add failing service and handler tests in `internal/service/tag_admin_service_test.go` and `internal/handler/tag_handler_test.go` for two new product-facing contracts:
       - `GET /api/v1/tags/governance?search=&limit=&offset=`
       - `POST /api/v1/tags/:id/merge` with JSON body `{"target_tag_id": <int64>}`
       Seed one duplicate-tag pair, one unused tag, aliases on both tags, and at least one image-tag association on the source tag.
    2. Create `internal/service/tag_admin_service.go` with a focused orchestration type `TagAdminService`. Give it concrete methods:
       - `ListGovernanceTags(ctx context.Context, search string, limit, offset int) ([]TagGovernanceRow, int, error)`
       - `MergeTags(ctx context.Context, sourceTagID, targetTagID int64) (*TagMergeResult, error)`
       Use exact, user-selected targets only per D-02 and project “exact match first” rules; do not add fuzzy candidate selection in this plan.
    3. Define `TagGovernanceRow` with these fields exactly: `tag_id`, `preferred_label`, `primary_category`, `aliases`, `usage_count`, `pending_count`, `confirmed_count`, `rejected_count`, `ai_count`, `manual_count`, `affected_image_count`, `can_delete`.
    4. Implement merge as a transactional workflow that:
       - loads source and target tags,
       - rejects missing tags and `source == target`,
       - reassigns every source `image_tags` row through `imageTagRepo.MergeImageTag(...)`,
       - copies source aliases onto the target unless the normalized label already exists on the target or equals the target preferred label,
       - adds the source preferred label as a synonym alias on the target when it differs from the target preferred label,
       - deletes the source aliases and source tag after reassignment,
       - syncs FTS for the target tag after the merge finishes.
    5. Update `internal/handler/tag_handler.go` and `internal/handler/routes.go` to wire `GET /api/v1/tags/governance` and `POST /api/v1/tags/:id/merge` as product-facing API routes under `/api/v1`.
    6. Stage commits atomically as:
       - `test(19-01): add failing governance list and merge endpoint coverage`
       - `feat(19-01): add tag governance list and explicit merge contracts`
  </action>
  <verify>
    <automated>go test ./internal/service ./internal/handler -run "TestTag(AdminService|GetGovernance|Merge)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/handler/routes.go` contains `/tags/governance`
    - `internal/handler/routes.go` contains `/tags/:id/merge`
    - `internal/service/tag_admin_service.go` defines `type TagAdminService struct`
    - `internal/service/tag_admin_service.go` contains `ListGovernanceTags`
    - `internal/service/tag_admin_service.go` contains `MergeTags`
    - `go test ./internal/service ./internal/handler -run "TestTag(AdminService|GetGovernance|Merge)" -count=1` exits `0`
  </acceptance_criteria>
  <done>The backend exposes the enriched governance list and a safe explicit merge path that later plans can consume directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green safe delete preview and selected unused cleanup semantics</name>
  <files>
    internal/service/tag_admin_service.go,
    internal/service/tag_admin_service_test.go,
    internal/handler/tag_handler.go,
    internal/handler/tag_handler_test.go,
    internal/handler/routes.go
  </files>
  <read_first>
    internal/service/tag_admin_service.go,
    internal/service/tag_admin_service_test.go,
    internal/handler/tag_handler.go,
    internal/handler/tag_handler_test.go,
    internal/handler/routes.go,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md,
    .planning/phases/19-tag-management/19-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/tags/:id/delete-preview` returns `affected_image_count`, `can_delete`, and a blocking reason for used tags per D-04/D-05
    - Test 2: `DELETE /api/v1/tags/:id` succeeds only when `affected_image_count == 0` and returns that count explicitly as `0`
    - Test 3: `POST /api/v1/tags/batch/cleanup` deletes only the selected unused tags and reports `deleted`, `blocked`, and `failed` entries separately
    - Test 4: delete and cleanup never remove image associations as a hidden side effect per D-06
  </behavior>
  <action>
    1. Add failing coverage in `internal/service/tag_admin_service_test.go` and `internal/handler/tag_handler_test.go` for:
       - `GET /api/v1/tags/:id/delete-preview`
       - `DELETE /api/v1/tags/:id`
       - `POST /api/v1/tags/batch/cleanup` with JSON body `{"tag_ids": [1,2,3]}`
    2. Extend `TagAdminService` with:
       - `GetDeletePreview(ctx context.Context, tagID int64) (*TagDeletePreview, error)`
       - `CleanupUnusedTags(ctx context.Context, tagIDs []int64) (*TagCleanupResult, error)`
        `TagDeletePreview` must expose `tag_id`, `preferred_label`, `affected_image_count`, `can_delete`, and `blocking_reason`.
    3. Change `DELETE /api/v1/tags/:id` so it calls `GetDeletePreview(...)` first and returns `409` with JSON `{ "error": "tag is still in use", "affected_image_count": <n>, "blocking_reason": "merge_or_reclassify_required" }` when the tag still affects images. When deletion succeeds, return `{ "success": true, "deleted_tag_id": <id>, "affected_image_count": 0 }`.
    4. Replace the current global cleanup-only semantics with a selected cleanup endpoint `POST /api/v1/tags/batch/cleanup`. It must process only the submitted `tag_ids`, delete the unused subset, and return arrays named exactly `deleted`, `blocked`, and `failed`, each with `tag_id`, `preferred_label`, and any error/block metadata.
    5. Keep the legacy `DELETE /api/v1/tags/cleanup` route out of the plan; Phase 19 should move the desktop workflow to the selected cleanup endpoint that supports batch confirmation and batch failure feedback.
    6. Stage commits atomically as:
       - `test(19-01): add failing safe delete and cleanup coverage`
       - `feat(19-01): enforce safe delete preview and selected cleanup contracts`
       - `refactor(19-01): route tag cleanup through admin governance service`
  </action>
  <verify>
    <automated>go test ./internal/service ./internal/handler -run "TestTag(Delete|Cleanup|DeletePreview)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/handler/routes.go` contains `/tags/:id/delete-preview`
    - `internal/handler/routes.go` contains `/tags/batch/cleanup`
    - `internal/handler/tag_handler.go` contains `affected_image_count`
    - `internal/handler/tag_handler.go` contains `merge_or_reclassify_required`
    - `go test ./internal/service ./internal/handler -run "TestTag(Delete|Cleanup|DeletePreview)" -count=1` exits `0`
  </acceptance_criteria>
  <done>Unsafe force-delete behavior is removed from the normal product path, and both single delete and selected cleanup expose explicit impact counts and blocking reasons.</done>
</task>

</tasks>

<verification>
- Run the targeted backend governance tests after each RED/GREEN slice.
- Confirm no handler path silently strips image associations when delete is blocked.
- Confirm merge remains explicit-target-only and does not introduce fuzzy matching.
</verification>

<success_criteria>
- Backend contracts support the desktop list-first governance workspace without legacy fallback.
- Safe delete semantics match D-04, D-05, and D-06 exactly.
</success_criteria>

<commit_strategy>
- Commit 1: `test(19-01): add failing governance list and merge endpoint coverage`
- Commit 2: `feat(19-01): add tag governance list and explicit merge contracts`
- Commit 3: `test(19-01): add failing safe delete and cleanup coverage`
- Commit 4: `feat(19-01): enforce safe delete preview and selected cleanup contracts`
- Commit 5: `refactor(19-01): route tag cleanup through admin governance service`
</commit_strategy>

<output>
After completion, create `.planning/phases/19-tag-management/19-01-SUMMARY.md`
</output>
