---
phase: 19-tag-management
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - flutter_app/lib/models/tag_governance.dart
  - flutter_app/lib/services/tag_service.dart
  - flutter_app/lib/providers/tag_provider.dart
  - flutter_app/test/services/tag_service_test.dart
  - flutter_app/test/providers/tag_provider_test.dart
autonomous: true
requirements: [DSK-04]

must_haves:
  truths:
    - "Desktop state can load enriched governance rows, delete previews, alias/category data, and explicit merge contracts from the backend"
    - "Desktop governance state supports multi-select and reports partial failures for bulk cleanup, category, alias, and merge actions"
    - "The desktop flow can prepare a gallery drilldown using a chosen tag without inventing a separate result page"
  artifacts:
    - path: "flutter_app/lib/models/tag_governance.dart"
      provides: "typed governance-row, delete-preview, merge-request, and batch-result models"
      exports: ["TagGovernanceRow", "TagDeletePreview", "TagGovernanceBatchResult"]
    - path: "flutter_app/lib/services/tag_service.dart"
      provides: "typed Phase 19 tag-governance HTTP client methods"
      exports: ["TagService"]
    - path: "flutter_app/lib/providers/tag_provider.dart"
      provides: "provider-owned governance list, selection, merge state, and bulk action orchestration"
      exports: ["TagProvider"]
  key_links:
    - from: "flutter_app/lib/services/tag_service.dart"
      to: "/api/v1/tags/governance"
      via: "governance list fetch"
      pattern: "tags/governance"
    - from: "flutter_app/lib/providers/tag_provider.dart"
      to: "flutter_app/lib/models/tag_governance.dart"
      via: "typed desktop state"
      pattern: "TagGovernanceRow"
    - from: "flutter_app/lib/providers/tag_provider.dart"
      to: "flutter_app/lib/services/tag_service.dart"
      via: "bulk orchestration and state refresh"
      pattern: "fetchGovernanceTags|mergeTagInto|batchCleanupTags"
---

<objective>
Build the Flutter data contracts and state orchestration required to drive Phase 19’s desktop governance workspace.

Purpose: This plan translates the backend contracts from Plan 01 into typed Flutter models and provider-driven workflows that satisfy D-07 through D-13, especially full batch governance and gallery handoff readiness.

Output: Typed governance models, service methods for Phase 19 endpoints, and a `TagProvider` that can drive single-row and multi-select governance flows with clear failure aggregation.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tag-management/19-CONTEXT.md
@.planning/phases/19-tag-management/19-RESEARCH.md
@.planning/phases/19-tag-management/19-VALIDATION.md
@.planning/phases/19-tag-management/19-01-PLAN.md
@.planning/phases/17-desktop-shell-foundation/17-01-SUMMARY.md
@.planning/phases/17-desktop-shell-foundation/17-02-SUMMARY.md

<interfaces>
From `flutter_app/lib/providers/tag_provider.dart`:
```dart
class TagProvider extends ChangeNotifier {
  Future<void> loadStatistics() async
  Future<Map<String, dynamic>> cleanUnusedTags() async
  Future<void> updateTag(int tagId, {String? preferredLabel, String? primaryCategory, String? reviewState}) async
  Future<void> deleteTag(int tagId) async
}
```

From `flutter_app/lib/services/tag_service.dart`:
```dart
class TagService {
  Future<List<TagStatistics>> getTagStatistics() async
  Future<Tag> updateTag(int tagId, {String? preferredLabel, String? primaryCategory, String? reviewState}) async
  Future<void> deleteTag(int tagId) async
}
```

Locked decisions from `19-CONTEXT.md`:
```text
D-07 primaryCategory governance is in scope
D-08 alias governance is in scope
D-10/D-11 batch governance is required, including multi-select, confirmation, and failure feedback
D-12/D-13 gallery drilldown should reuse preset tag filtering rather than embedded preview
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green typed governance models and service contracts</name>
  <files>
    flutter_app/lib/models/tag_governance.dart,
    flutter_app/lib/services/tag_service.dart,
    flutter_app/test/services/tag_service_test.dart
  </files>
  <read_first>
    flutter_app/lib/models/tag.dart,
    flutter_app/lib/services/tag_service.dart,
    flutter_app/test/services/tag_service_test.dart,
    .planning/phases/19-tag-management/19-01-PLAN.md,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: service parses `GET /api/v1/tags/governance` into typed rows with aliases, category, delete eligibility, and affected-image counts
    - Test 2: service parses `GET /api/v1/tags/:id/delete-preview` into a typed delete-preview model
    - Test 3: service sends `POST /api/v1/tags/:id/merge` with exact JSON body `{ "target_tag_id": <id> }`
    - Test 4: service exposes alias CRUD wrappers and selected cleanup requests needed for Phase 19 desktop governance
  </behavior>
  <action>
    1. Add failing tests in `flutter_app/test/services/tag_service_test.dart` for these exact methods and endpoints:
       - `fetchGovernanceTags(...)` -> `GET /api/v1/tags/governance`
       - `fetchDeletePreview(tagId)` -> `GET /api/v1/tags/{id}/delete-preview`
       - `mergeTagInto(sourceTagId, targetTagId)` -> `POST /api/v1/tags/{id}/merge`
       - `getTagAliases(tagId)`, `addTagAlias(tagId, label, aliasType)`, `deleteTagAlias(tagId, aliasId)`
       - `batchCleanupTags(List<int> tagIds)` -> `POST /api/v1/tags/batch/cleanup`
    2. Create `flutter_app/lib/models/tag_governance.dart` with these types exactly:
       - `TagGovernanceRow`
       - `TagDeletePreview`
       - `TagGovernanceFailure`
       - `TagGovernanceBatchResult`
       - `TagMergeRequest`
        Model fields must include alias lists, `primaryCategory`, `affectedImageCount`, and `canDelete` so the UI never has to parse raw maps.
    3. Extend `flutter_app/lib/services/tag_service.dart` with typed Phase 19 methods listed above. Keep all URLs under `ApiConfig.baseUrl`; do not hardcode hostnames.
    4. Keep existing `updateTag(...)` and image-level methods intact; this plan extends the service instead of rewriting it.
    5. Stage commits atomically as:
       - `test(19-02): add failing tag governance service coverage`
       - `feat(19-02): add typed tag governance service contracts`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/services/tag_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/models/tag_governance.dart` exists
    - `flutter_app/lib/models/tag_governance.dart` contains `class TagGovernanceRow`
    - `flutter_app/lib/services/tag_service.dart` contains `fetchGovernanceTags`
    - `flutter_app/lib/services/tag_service.dart` contains `fetchDeletePreview`
    - `flutter_app/lib/services/tag_service.dart` contains `mergeTagInto`
    - `flutter_app/lib/services/tag_service.dart` contains `batchCleanupTags`
    - `cd flutter_app; flutter test test/services/tag_service_test.dart` exits `0`
  </acceptance_criteria>
  <done>The Flutter client layer can consume every backend contract needed for Phase 19 without raw-map parsing in widgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red-green provider-owned governance state, multi-select, and bulk orchestration</name>
  <files>
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/models/tag_governance.dart,
    flutter_app/lib/services/tag_service.dart,
    flutter_app/test/providers/tag_provider_test.dart
  </files>
  <read_first>
    flutter_app/lib/providers/tag_provider.dart,
    flutter_app/lib/providers/navigation_provider.dart,
    flutter_app/lib/providers/image_provider.dart,
    flutter_app/lib/models/tag_governance.dart,
    flutter_app/lib/services/tag_service.dart,
    flutter_app/test/providers/tag_provider_test.dart,
    .planning/phases/19-tag-management/19-CONTEXT.md,
    .planning/phases/19-tag-management/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: provider loads and refreshes governance rows independently from legacy statistics-only state
    - Test 2: provider tracks selected row ids, active merge source row, and delete preview state for list-first governance flows
    - Test 3: provider supports bulk category, bulk alias, selected cleanup, and multi-source merge operations with aggregated success/failure results
    - Test 4: provider exposes the selected tag ids and chosen source tag needed for gallery drilldown and merge-panel UI without embedding navigation logic in the service layer
  </behavior>
  <action>
    1. Add failing provider tests in `flutter_app/test/providers/tag_provider_test.dart` for these exact methods/state slices:
       - `loadGovernanceTags({String? search})`
       - `toggleGovernanceSelection(int tagId)`
       - `clearGovernanceSelection()`
       - `setActiveMergeSource(TagGovernanceRow row)` / `clearActiveMergeSource()`
       - `loadDeletePreview(int tagId)`
       - `applyPrimaryCategoryToSelection(String primaryCategory)`
       - `addAliasToSelection(String aliasLabel, {String aliasType = 'synonym'})`
       - `cleanupSelectedUnusedTags()`
       - `mergeSelectionInto(int targetTagId)`
    2. Extend `TagProvider` with dedicated governance state fields instead of overloading the existing image-tag review maps. Add typed properties for `governanceRows`, `selectedGovernanceIds`, `activeMergeSource`, `deletePreview`, `lastBatchResult`, `isRunningGovernanceAction`, and `governanceError`.
    3. Implement bulk actions as provider-level orchestration over service methods. Aggregate per-tag failures into `TagGovernanceBatchResult.failures` with exact fields `tagId`, `preferredLabel`, and `message` so the UI can render partial-failure feedback.
    4. Keep navigation/galleries decoupled: the provider should expose row ids and selected source rows, but not directly mutate `NavigationProvider` or `ImageListProvider`.
    5. After each successful mutation, refresh governance rows so the list view reflects alias/category/delete-eligibility changes immediately.
    6. Stage commits atomically as:
       - `test(19-02): add failing tag governance provider coverage`
       - `feat(19-02): add tag governance provider state and bulk actions`
       - `refactor(19-02): separate legacy statistics state from governance state`
  </action>
  <verify>
    <automated>cd flutter_app; flutter test test/providers/tag_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter_app/lib/providers/tag_provider.dart` contains `governanceRows`
    - `flutter_app/lib/providers/tag_provider.dart` contains `selectedGovernanceIds`
    - `flutter_app/lib/providers/tag_provider.dart` contains `applyPrimaryCategoryToSelection`
    - `flutter_app/lib/providers/tag_provider.dart` contains `addAliasToSelection`
    - `flutter_app/lib/providers/tag_provider.dart` contains `cleanupSelectedUnusedTags`
    - `flutter_app/lib/providers/tag_provider.dart` contains `mergeSelectionInto`
    - `cd flutter_app; flutter test test/providers/tag_provider_test.dart` exits `0`
  </acceptance_criteria>
  <done>Desktop governance flows have a test-backed provider seam for list rows, selection, merge state, and bulk failure reporting before widget work starts.</done>
</task>

</tasks>

<verification>
- Keep service tests and provider tests green independently; do not jump to widget work with unverified raw-map logic.
- Confirm bulk operations report partial failures instead of collapsing into a single opaque error string.
</verification>

<success_criteria>
- Phase 19 frontend state is typed, testable, and ready to drive a Fluent-native governance workspace.
- Multi-select and batch-governance behavior exist in provider state before UI composition begins.
</success_criteria>

<commit_strategy>
- Commit 1: `test(19-02): add failing tag governance service coverage`
- Commit 2: `feat(19-02): add typed tag governance service contracts`
- Commit 3: `test(19-02): add failing tag governance provider coverage`
- Commit 4: `feat(19-02): add tag governance provider state and bulk actions`
- Commit 5: `refactor(19-02): separate legacy statistics state from governance state`
</commit_strategy>

<output>
After completion, create `.planning/phases/19-tag-management/19-02-SUMMARY.md`
</output>
