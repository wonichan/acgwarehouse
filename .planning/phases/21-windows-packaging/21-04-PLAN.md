---
phase: 21-windows-packaging
plan: 04
type: execute
wave: 4
depends_on: [21-02, 21-03]
files_modified:
  - deploy/windows/package-smoke.ps1
  - .planning/phases/21-windows-packaging/21-HUMAN-UAT.md
autonomous: false
requirements: [OPS-03]

must_haves:
  truths:
    - "The packaged ZIP can be extracted and launched successfully as a complete Flutter + Go + Python bundle"
    - "The packaged app still performs duplicate detection without a system Python dependency"
    - "Phase 21 closes with explicit human verification of the real unzip-and-run experience"
  artifacts:
    - path: "deploy/windows/package-smoke.ps1"
      provides: "end-to-end packaged smoke verification"
      exports: ["package-smoke.ps1"]
    - path: ".planning/phases/21-windows-packaging/21-HUMAN-UAT.md"
      provides: "blocking human verification script for clean-machine packaging validation"
      exports: ["21-HUMAN-UAT.md"]
  key_links:
    - from: "deploy/windows/package-smoke.ps1"
      to: "dist/windows-zip/ACGWarehouse-windows-x64-portable.zip"
      via: "extract-and-run packaged artifact"
      pattern: "ACGWarehouse-windows-x64-portable.zip"
    - from: "deploy/windows/package-smoke.ps1"
      to: "runtime/python-sidecar/acgwarehouse-sidecar.exe"
      via: "PATH-independent packaged sidecar execution"
      pattern: "python-sidecar"
    - from: ".planning/phases/21-windows-packaging/21-HUMAN-UAT.md"
      to: "ACGWarehouse.exe"
      via: "clean-machine operator verification"
      pattern: "ACGWarehouse.exe"
---

<objective>
Close Phase 21 with automated packaged smoke checks and a blocking user verification checkpoint.

Purpose: This plan proves the packaged bundle actually launches and works as a Flutter + Go + Python product, then forces a final human confirmation of the real Windows unzip-and-run experience.

Output: Packaged smoke automation plus a human-verification checklist for final approval.
</objective>

<execution_context>
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/workflows/execute-plan.md
@D:/production/goworkspace/ACGWarehouse/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-windows-packaging/21-CONTEXT.md
@.planning/phases/21-windows-packaging/21-RESEARCH.md
@.planning/phases/21-windows-packaging/21-VALIDATION.md
@.planning/phases/21-windows-packaging/21-02-PLAN.md
@.planning/phases/21-windows-packaging/21-03-PLAN.md
@docs/windows-portable-package.md

<interfaces>
Smoke flow must validate these conditions from `OPS-03`:
```text
1. ZIP bundle contains Flutter + Go + Python together.
2. Startup follows the packaged Flutter -> Go -> Python readiness contract.
3. Duplicate detection still works without a separately installed Python runtime.
```
</interfaces>
</context>

<atomic_commit_strategy>
- Task 1: `test(21-04): add failing packaged smoke verification` → `feat(21-04): add packaged smoke verification flow`
- Task 2: `docs(21-04): add human packaging UAT checkpoint`
</atomic_commit_strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red-green the packaged extract-and-run smoke verification</name>
  <files>
    deploy/windows/package-smoke.ps1
  </files>
  <read_first>
    deploy/windows/package.ps1,
    docs/windows-portable-package.md,
    .planning/phases/21-windows-packaging/21-VALIDATION.md,
    .planning/phases/21-windows-packaging/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: The smoke script extracts `ACGWarehouse-windows-x64-portable.zip` to a temporary directory and verifies required bundle paths exist.
    - Test 2: The smoke script launches `ACGWarehouse.exe` from the extracted directory with a PATH scrubbed of any Python dependency.
    - Test 3: The smoke script waits for `runtime/runtime-manifest.json`, verifies log/diagnostic directories are created, and then probes the Go health endpoint from the manifest.
    - Test 4: The smoke script sends `POST /api/v1/duplicates/detect` with body `{"threshold":40}` and asserts the HTTP request succeeds while `runtime/logs/python-sidecar.log` changes after the request, proving the packaged sidecar handled the computation path.
  </behavior>
  <action>
    1. Extend `deploy/windows/package-smoke.ps1` into the authoritative packaged smoke runner.
    2. Implement these exact steps:
       - fail if `dist/windows-zip/ACGWarehouse-windows-x64-portable.zip` is missing,
       - extract the ZIP to a temporary directory,
       - verify `ACGWarehouse.exe`, `data/`, `runtime/bin/`, `runtime/python-sidecar/`, `config/`, `storage/`, and `library/` exist,
       - launch `ACGWarehouse.exe` with a controlled environment that does not depend on `python.exe` being available on PATH,
       - wait for `runtime/runtime-manifest.json`,
       - read the manifest base URL and probe `/health`,
       - assert `runtime/logs/` and `runtime/diagnostics/` exist,
       - record the pre-request size or timestamp of `runtime/logs/python-sidecar.log`,
       - call `POST /api/v1/duplicates/detect` with body `{"threshold":40}`,
       - assert the HTTP request returns a response from the packaged app and `runtime/logs/python-sidecar.log` changes after the request.
    3. Clean up launched processes and the extracted temp directory at the end of the smoke run.
    4. Stage commits as:
       - `test(21-04): add failing packaged smoke verification`
       - `feat(21-04): add packaged smoke verification flow`
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File deploy/windows/package-smoke.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/windows/package-smoke.ps1` contains `Expand-Archive`
    - `deploy/windows/package-smoke.ps1` contains `ACGWarehouse-windows-x64-portable.zip`
    - `deploy/windows/package-smoke.ps1` contains `runtime/runtime-manifest.json`
    - `deploy/windows/package-smoke.ps1` contains `/health`
    - `deploy/windows/package-smoke.ps1` contains `/api/v1/duplicates/detect`
    - `deploy/windows/package-smoke.ps1` contains `python-sidecar.log`
    - `powershell -ExecutionPolicy Bypass -File deploy/windows/package-smoke.ps1` exits `0`
  </acceptance_criteria>
  <done>The packaged ZIP is automatically extracted, launched, health-checked, and smoke-verified without relying on a system Python install.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of clean-machine unzip-and-run packaging flow</name>
  <files>
    .planning/phases/21-windows-packaging/21-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/21-windows-packaging/21-HUMAN-UAT.md,
    docs/windows-portable-package.md,
    .planning/phases/21-windows-packaging/21-CONTEXT.md
  </read_first>
  <what-built>The full Windows portable package, startup dialog path, and packaged smoke verification workflow.</what-built>
  <action>Pause execution and require a human to run the packaged ZIP on a Windows x64 machine without Python installed, following `21-HUMAN-UAT.md`. Treat any mismatch in launch behavior, duplicate detection availability, or startup dialog clarity as a blocking failure for Phase 21 sign-off.</action>
  <how-to-verify>
    1. Move `dist/windows-zip/ACGWarehouse-windows-x64-portable.zip` to a Windows machine that does not have Python installed.
    2. Extract the ZIP to a normal user-writable directory such as `C:\Tools\ACGWarehouse`.
    3. Run `ACGWarehouse.exe` and confirm the desktop shell opens without separate backend startup steps.
    4. Trigger duplicate detection and confirm the request completes through the packaged Python sidecar path.
    5. Close the app and confirm the runtime files remain inside the extracted directory.
    6. Optional failure-path check: temporarily rename `runtime/python-sidecar/acgwarehouse-sidecar.exe`, relaunch, and confirm the startup dialog names the failure class and shows log locations.
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification checkpoint; complete Task 1 automated smoke before pausing for clean-machine validation</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/21-windows-packaging/21-HUMAN-UAT.md` exists
    - Human verifier confirms unzip-and-run success on Windows x64 without Python installed
    - If failure-path test is executed, the startup dialog distinguishes `go`, `python`, or `startup_chain` and shows log locations
  </acceptance_criteria>
  <done>Phase 21 is not approved until a human confirms the portable ZIP works on a clean Windows machine.</done>
  <resume-signal>Type "approved" if the packaged bundle passed, or describe the failure observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged ZIP → extracted runtime | The smoke test must prove the extracted artifact is self-contained |
| clean machine → packaged runtime | Final user environment can differ from the dev box and may expose hidden dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | R | packaged smoke verification | mitigate | Make the smoke script extract, launch, probe, and clean up deterministically with machine-readable failures |
| T-21-12 | D | hidden external Python dependency | mitigate | Run smoke with PATH-independent packaged launch expectations and assert bundled sidecar paths exist before startup |
| T-21-13 | T | human release approval | mitigate | Require a blocking UAT checkpoint on a Windows machine without Python before phase sign-off |
</threat_model>

<verification>
- Run the packaged smoke script against the ZIP artifact.
- Pause for human verification on a clean Windows machine before claiming phase completion.
</verification>

<success_criteria>
- The ZIP artifact can be extracted and launched as a self-contained desktop bundle.
- Packaged duplicate detection still routes through the bundled Python sidecar.
- A human confirms the real unzip-and-run experience before final approval.
</success_criteria>

<output>
After completion, create `.planning/phases/21-windows-packaging/21-04-SUMMARY.md`
</output>
